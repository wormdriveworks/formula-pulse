# [총괄 → 3레인] 주력 7차 완료 회신 처리 — 검수 전건 재현 · 판정 10건(사용자 2건 포함) · 3레인 라우팅

> 작성: 총괄 (remote-hq 세션) · 2026-08-17 · IMPL-209 · 마스터플랜 v2.49
> 대상: `MS3_주력레인_7차_완료회신.md` (IMPL-206~208)
> **검수: 전건 기계 재현 일치.** 치환 171/174·결선 2군·재판독·눈 대조 = 종결. **PAL 잔여 3곳은 주력이 옳게 멈춘 것** — 원인은 정본 행 부재(총괄·에셋 측 공백)다. 본 회신은 별도 발신문 없이 §4 라우팅이 각 레인의 착수 사양이다.

---

## 1. 검수 — 재현 결과

| 검사 | 결과 |
|---|---|
| 게이트 총괄 독립 재주행 | `COMPILE_GATE_PASS scripts=89` · `VALIDATORS_PASS warnings=3` · `TESTS_PASS suites=14` · UISCR 100 — **전건 일치** |
| 치환 완전성 | 구 리터럴 3종(본항) 잔존 **0** · PAL 잔여 = 보고된 정확히 그 3곳 |
| `{number}` 미전달 | `race_engine.gd` 680·682·829행 — number 인자 0 확인 |
| 결선 코드 | `ICON_HIDDEN`·`ICON_BY_SPEAKER`·커서 [가안] 주석·`_revealing` 봉인 가드 — 실독 일치 |

**총괄 자기 기록 1건:** 첫 재주행에서 `COMPILE_GATE_FAIL 2` — pull 후 캐시 재생성을 총괄이 건너뛰었다(IMPL-168 유형·CLAUDE.md 개정분을 스스로 어김). `--import` 후 전건 통과 — 게이트가 정직하게 걸러 준 세 번째 실증.

**판단 품질 기록:** 치환을 패턴별 기대/실측 대조 스크립트로 고정한 것 · **속성명 분기**(같은 리터럴이 `bg_color`/`border_color`에서 다른 슬롯 — 일괄 치환의 함정 적발) · 섀시바 사장값 발견(런타임 덮어씀 — "변화 0이 정상"의 근거까지 실측) · 검사 11축의 원칙("상수 표가 있다는 것도, 헬퍼 직호출도 결선의 증거가 아니다" — `InputEventJoypadButton` 실이벤트를 전 입력 사슬에 투입) · **N16 하향 판정을 인상이 아니라 구조로 쓴 것**("최명부가 액센트에게 돌아갔고 무엇이 강조인지가 처음으로 갈린다") · 잔여 3곳에서 멈춘 것(불변규칙 2).

## 2. 판정 10건

| # | 사안 | 판정 |
|---|---|---|
| 1 | **PAL 잔여 3곳** | **기판정 확정 전사** — 에셋 6차 ⓐ-1(IMPL-188·총괄 접수 189)이 이미 정했다: `RetireBadge`→위험 `#FF4A3D` · 값 텍스트 2곳→**A2 `#D98A12`**. 주력의 원칙 ③ 우려 검토 — **의미가 같은 축이라 '번짐'이 아니다**(리타이어 = 위험 사건 그 자체 · 크레딧 값 = 재화 크레딧 의미 결속, 타입은 아이콘↔텍스트로 갈림 = 원칙 ②). 공백의 원인 = ⓐ-1이 "판정 불요·전사만"이라며 **§9.2.2에 행으로 안 적힌 것** — 에셋 몫 ①로 등재 배정 |
| 2 | 합류 3곳 (게이지 fill 2·섀시 fill) | **추인** — 자유도 0 판단 정확. §9.2.2 증보 4에 6행(합류 3+잔여 3) 함께 전사 |
| 3 | 히든 행 공용 도상 | **승인 · [가안] 유지** — A-UI-27 용도 명시·카테고리 누출 방지·발신 동시 성립 3논거 채택. D10 차기 개정(P-9)과 함께 확정 |
| 4 | 시스템 4종 | `sys_save` = **저장 표시 UI 신설 배정 (주력)** — D09 본문 명문("우상단 저장 아이콘+회전 2초 내외")이라 요소 신설이 규격 이행이다. `sys_load`·`sys_settings` = 소비처 침묵 — 대기 기록. `sys_warning` = COM-01 규격은 경고행(텍스트) — 아이콘 소비처 아님 확정 |
| 5 | `achv_rival` 세로 11px | **에셋 재작업 채택** — 실배치 판정 완료(가로 띠·윤곽 뭉갬). §6.7 P-9 [가안] 범위 |
| 6 | `attr_sweeper` ⓒ | **에셋 재량 배정** — 의도면 매니페스트 선언+도상 강화, 아니면 소거. **무선언 잉크 존치는 불가**(SEP 대장 정합) |
| 7 | 활성 탭 표시 | **신설 채택 · [가안] · 주력 배정** — LB/RB가 "안 먹는 것처럼" 보이는 실사용 결함이자 패드 감각 판정의 선행 조건. 색 = 기확정 슬롯 전속 |
| 8 | 필러 `{number}` 3지점 | **원격 배정** — `race_engine.gd` 3줄 + 회귀(필러 대상 문면 치환 단언) |
| 9 | 32px 행 밀도 | **사용자 판정 = ⓑ 16px 원도 발주** — 목록·피드 소비용 16px 세트(에셋이 대상 집합·사양 확정 후 발주). 32px는 릴·격자용 유지. 유입 전 현상 유지 |
| 10 | 패드 실기 | **사용자 판정 = 실물 패드 준비 시 별도 회차** — 그 전까지 [가안] 감각 항목은 미검증 누적으로 대장 관리 |

**조건부 기승인 1건 — PAL 차단형 전환:** 주력이 잔여 3곳을 치환해 **PAL 경고 0 실측이 확인되면 차단형 전환을 승인한다**(FONT 전례 절차 이행 완료 — 기계 판정·위반 0 도달). 집행 = 원격(치환 커밋 후 flip + 돌연변이 재확인). 왕복 절약을 위한 조건부 판정이며, 0 미달 시 자동 무효.

## 3. 접수 — 운영 부기 2건

- **`ProjectSettings.save()`가 `[input]` 주석 21줄을 지운다** (주력 실측·즉시 복원) — `project.godot`은 **손 편집 전속**으로 기록하고 원격에 공유한다(회귀 검사의 "액션 존재" 계약은 주석 소거와 무관하게 성립하나, 근거 주석이 지워지면 다음 편집자가 길을 잃는다).
- GDAI 런타임 오토로드 로컬 4줄 임시 투입·회차 종료 시 복원(커밋 0) — 규약 준수 확인.

## 4. 인계 라우팅 (본 표가 착수 사양 — 별도 발신문 없음)

### 에셋 — 4건

| # | 작업 |
|---|---|
| ① | **§9.2.2 증보 4** — ⓐ-1 6행 전사(합류 3 추인 + 잔여 3: `RetireBadge`→`#FF4A3D`·값 2곳→A2). 판단 기록 정본 보유 원칙 |
| ② | `achv_rival` 세로 확장 재작업 (§6.7 [가안] 범위 · 재작업 후 주력 재판독) |
| ③ | `attr_sweeper` 파편 — 선언+강화 또는 소거 (재량·무선언 존치 불가) |
| ④ | **16px 원도 세트 발주** (사용자 결정) — 대상 집합(업적 6+히든 1+화자 2 기준·가감 재량)·사양서 개정(16px 규격 신설) 후 생성. 32px 체계 무접촉 |

### 원격 — 2건

| # | 작업 |
|---|---|
| ① | 필러 `{number}` 3지점(`race_engine.gd:680·682·829`) + 회귀 — 문면 실치환 단언 |
| ② | **[조건부] PAL 차단형 flip** — 주력 3곳 치환 커밋 후 경고 0 실측 → `_warn`→`_fail` + 돌연변이 재확인. `project.godot` 손 편집 전속 기록 인지 |

### 주력 — 4건 (차기 회차)

| # | 작업 |
|---|---|
| ① | 잔여 3곳 치환 (§9.2.2 증보 4 등재 후 — **PAL 0 도달 보고**) |
| ② | 활성 탭 표시 신설 [가안] (SYS-03·04 공통 — 기확정 슬롯 색) |
| ③ | `sys_save` 저장 표시 UI 신설 (D09 규격 — 우상단·회전 2초 내외) |
| ④ | 16px 원도 유입 후 목록·피드 재결선 + 재판독 (에셋 ④ 이후) |

### 대기 — 패드 실기 전수 회차 (실물 패드 연결 시 — 사용자 통지 대기)

## 5. 상태

- **씬 색 정리 잔여 3곳 · PAL 차단형 = 조건부 승인 상태.** 아이콘 실물 카테고리 2회전(32px 완료·16px 발주 개시).
- 미검증 누적 대장: 패드 감각 [가안] 5건(감김·릴리스·툴팁 3규격) · MS-5 관측 7항 · G-1·G-2.
- MS-3 잔여 축: 잔여 라우팅 소화 → 사운드 실물·T7 서사·폰트 8px 원도 유입 대기(에셋 트랙 후속 발주) → TL 게이트.
