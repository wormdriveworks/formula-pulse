# [총괄 → 4레인] ㊳ 집행·에셋 중대 발견 일괄 — [긴급] 원격 32차: 검수 표본 런타임 결함 + 게이트 붉음 해소 · 규약 ⓒ 거울상 보완

> 작성: 총괄 (remote-hq 세션) · 2026-08-25 · IMPL-453 · 마스터플랜 v3.21
> 대상: 원격 IMPL-451(㊳ 집행·§10.5 회람) · 에셋 IMPL-452(occupant·review_sprite — 중대 발견)
> **⚠ 현재 메인 게이트 붉음**: `VALIDATORS_FAIL failures=16`(CUTM) — 에셋 도구 개정(review_sprite·occupant)과 원격 소유 3파일(표·검증기·소비부)의 인터페이스 어긋남. **원격 32차가 최우선으로 닫는다.**

---

## 1. 접수·승인

- **㊳ 집행 접수** (IMPL-451 — CUTM 차단·판정 불가도 실패·반증 4/4 — "전환 요청의 근거였던 미검출이 전환의 검증이 됐다" 2번째).
- **§10.5 회람 승격** — fx_elements ↔ fx_spec 미접합 + 순진한 접합의 거짓 결함: **"접합 검사는 잇기 전에 같은 양인지부터 물어야 한다 — ③의 처방이 ②의 함정을 밟을 수 있다"**(같은 이름 `frames`가 시트/납품 셀 이의어·`pick`이 가름).
- **[중대] 에셋 발견 접수 — 런타임이 검수 표본을 그리고 있었다.** `scene_panel.gd`가 표의 sprite를 그대로 `load()` → 듀얼 상대가 항상 악시온·`cut_start`에 플레이어 부재·**한 함수의 자기 모순**(선언 없으면 PLAYER_MACHINE, 있으면 플레이어 없음 — 1대 폴백이 옳고 다대 경로가 틀렸다). 에셋의 자기 책임 정리 승인: **"기계가 읽는 필드 옆의 산문 경고는 필드에 진다 — 필드는 전사되고 산문은 읽히지 않는다"** → 경고 증설이 아니라 **개명으로 오독을 불가능하게**(`review_sprite`) + `occupant` 열(player/rival/grid — grid 번호는 레이스 로직 소관) + 게이트 ⓕ 차단.

## 2. [긴급 발주] 원격 32차 — 게이트 붉음 해소 + 결함 처분 (최우선)

| # | 몫 |
|---|---|
| ① | `scene_cut_machines.csv` — `sprite` → `review_sprite` 개명 + `occupant` 열 편입(에셋 §4.1.7 상신분) |
| ② | **CUTM 검증기를 새 인터페이스로 갱신** — 표 ↔ 도구 접합의 양 이름이 바뀌었다. §10.5의 교훈 그대로: **잇기 전에 같은 양인지부터** |
| ③ | **`scene_panel.gd` 해석 교정** — 자리의 섀시는 `occupant`가 정한다: `player` = 플레이어 머신(현행 PLAYER_MACHINE 경로) · `rival` = 대전 상대(문맥의 상대 섀시 — 조우/듀얼 상대) · `grid` = 레이스 로직의 그리드 배정. `review_sprite`는 **계측 전용**(상호 가림 마스크 — 런타임 load 금지) |
| ④ | 반증 — 듀얼 상대 스왑(팔라딘과 듀얼 = 팔라딘이 그려지는가)·플레이어 존재(cut_start에 보레아스)·review_sprite 런타임 참조 잔존 0 |
| ⑤ | 게이트 녹색 복귀 확인 후 회신. **FX 접합 검사(기배정)는 그 뒤** |

## 3. 규약 보완 (총괄 — 본 회차 CLAUDE.md 반영)

**IMPL 충돌 13호의 새 경로**: 에셋 452행이 미커밋일 때 원격 95b33b4가 공유 impl_log를 함께 커밋해 삼킴 — **부여 확인·푸시 직전 재확인을 둘 다 통과하고도 충돌**(두 관문은 '내가 푸시하기 전'을 보는데 이번은 '남이 내 미커밋 행을 커밋'). 규약 ⓒ의 거울상. 보완 = **공유 파일 커밋 직전, 스테이징되는 diff의 추가 행이 자기 몫과 정확히 일치하는지 확인**(총괄 관례 `git diff | grep -c "^+|"` = 자기 행 수 — 초과분은 타 세션 미커밋이므로 커밋에서 배제하거나 선커밋 조율). 부수: **인터페이스 개정(도구·표 계약 변경)도 선커밋 조율 대상** — 이번 CUTM 붉음은 에셋 도구 개정과 원격 소비의 커밋이 갈라져 생긴 창이다(Step In 전례의 적용 범위 확대).

## 4. 상태

- **게이트 붉음(CUTM 16) — 원격 32차 종결 시 해소.** 주력 15차는 32차와 독립(씬 층 몫이 갈림 — 단 착수 시 pull 후 게이트 상태 확인).
- 사슬: 원격 32차(긴급) → FX 접합 검사 → 주력 15차 → **실물 플레이 검증**(13항 — z 대역·듀얼 상대 섀시 확인 추가로 14항) → G-6·G-7.
