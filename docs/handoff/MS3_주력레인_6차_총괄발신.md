# [총괄 → 주력] MS-3 주력 6차 착수 인계 — 결선 2군 + 눈 확인 2군 (2026-08-17)

> 발신: 총괄 (remote-hq 세션) · IMPL-185 · 마스터플랜 v2.44
> 판정 원문: `MS3_주력5차_총괄회신.md` §2-② · `MS3_원격4차_에셋5차_총괄회신.md` §2-④·⑥ · 값 정본 = 팔레트 정본 §9.2.2
> **선행 조건 해소 확인:** 업적 널 가드(IMPL-177) 커밋 완료 — 아이콘 결선 착수 가능. 씬 리터럴 3종 수렴 확정(§9.2.2).
> 착수 규약: `git pull` · LFS 실물 확인 · pull 후 캐시 재생성(에디터 가동 시 GDAI `scan()` 경로) · IMPL ID = pull 직후 최대값+1 · 완료 시 `MS3_주력레인_6차_완료회신.md`.

## 1. 작업 ① — 아이콘 결선 3군 (화면 층 소유)

| 군 | 결선처 | 규격 |
|---|---|---|
| 업적 6종 | `achievement_screen` 항목 행 | §A-4 명문 — 행 = **아이콘**·명칭·조건·달성 일시. 히든 "???" 행은 아이콘 은닉(자리만) — 본문 §5.5. **널 가드(IMPL-177) 구조 존중** — 달성 조회는 `_achieved()`/`_progress_for()` 창구 경유 유지 |
| 화자 2종 | `log_feed.gd:50` | E10 명문 — "중계 마이크 아이콘 / 무전 크루 미니 초상". 예고 주석대로 Label→TextureRect |
| 시스템 4종 | **규격 확인 선행** | 별첨A 화면별 명세에서 소비처 확인 — **규격 부재면 결선하지 말고 보고** (임의 배치 금지) |

## 2. 작업 ② — 씬 색 리터럴 교체 + 눈 대조

**값 정본 = 팔레트 정본 §9.2.2** (에셋 확정 · 총괄 검수 통과). 기계 치환 — 값 판단 재개 금지.

| 씬 리터럴 (float) | 곳 | → 확정 슬롯 |
|---|---|---|
| `Color(0.9, 0.92, 0.95, 1)` | 61 | **N16 `#C7D0D8`** (`TEXT_PRIMARY` 동치) |
| `Color(0.55, 0.58, 0.64, 1)` | 40 | **N13 `#8B949F`** (`TEXT_DIM` 동치) |
| `Color(0.28, 0.32, 0.38, 1)` | 15 | **N10 `#5B646F`** (`FRAME_LINE` 동치) |

- **에셋 A·A′계 판정(§9.2.2 증보 — 38곳)이 병행 중이다.** 착수 시점에 증보가 도착해 있으면 **일괄 154곳**, 아니면 위 116곳 먼저(2차분은 후속 배정).
- **눈 대조 최우선 = `#E6EBF2`→N16 61곳** (주 텍스트 −9.9 하향 — 인상 변화 실측). 화면이 납작하면 **텍스트를 되올리지 말고 보고만** — 다음 단은 바탕 하향(N03→N02→N01, §9.2.2 [가안] 지렛대)이며 별도 판정 사안이다.
- 테두리 15곳 교체 후 "경계가 존재로 인식되는가" 재확인 (5차 소견의 그 13~15곳이 이번에 2.18→2.89로 오른다).
- 치환 후 PAL 경고 계수가 그만큼 줄었는지 확인해 회신에 병기 (원격 신설 검사가 치환 검산기 역할).

## 3. 작업 ③ — 재판독 (결선 후)

| 대상 | 판정 축 |
|---|---|
| `achv_rival` (파편 소거·재유입분) | 업적 행 실배치에서 "대결" 판독 + **세로 11px 납작** 판단 (유보분 — 격자 서면 판정) |
| 업적 6종 전체 | 실배치 판독 |
| **파편 후보 3건** (에셋 §4-①) | ⓐ`speaker_filler` (20,13) 내부 픽셀 ⓑ`attr_hazard` (17,20)-(18,20) — **파편 기움** ⓒ`attr_sweeper` (26,10)-(27,10) — **의도 기움**. 32px 실화면에서 보이는가/거슬리는가 — 판독 결과로 소거/선언 갈린다 |

## 4. 작업 ④ — 패드 실기 (원격 5차 ① 완료 후)

SYS-03·04 진입→순회→탈출을 **패드 단독**으로: B 취소(기결선) + 원격 5차 입력맵 결선분(A·D패드·좌스틱) 실기. D09 §1.3 폐쇄 루프(도달 불가 요소 0) 기준. 원격 결선 전이면 이 항만 이월.

## 5. 보고 형식

발견 즉시 보고(회신 완성 대기 불요) · 이상 없음도 명기 · 스크린샷 경로 병기 권장.
