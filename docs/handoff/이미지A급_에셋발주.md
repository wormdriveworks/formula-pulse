# [총괄 → 에셋] 이미지 A급 발주 — RD 파이프라인 재개 (사용자 판정: A급 먼저)

> 작성: 총괄 (remote-hq 세션) · 2026-08-23 · 근거 = IMPL-303 (`MS3_완료조건_대조_총괄기안.md` §4~5)
> 성격: **MS-3 크리티컬 패스 재개.** 이미지 실물이 T6 잔여(화자 배지·L3 컷인)·스토어 준비물의 공통 전제다. **B급(씬 컷·배경·대형 CG — 수작업 주역·RD 384px 상한 밖)은 이 발주 밖**이며 A급 소화 후 별도 착수한다(사용자 판정).

---

## 정본 (전건 기존재 — 신설 0)

- 수량·규격·배치: `docs/assets/이미지에셋_제작사양서_v1_3.md` + `에셋_배치_대장_v1_2.md`
- 호출 규격: `retro_diffusion_운용지침_v1_0.md` (RD 사용 시 유일 정본 — §2.1 프리픽스 렌더링 어휘 제거·무료 후처리 대응표)
- 색: `팔레트_정본_v1_0.md` — **PAL 검사 차단형 발동 중**(색 리터럴 신설 = 빌드 실패)
- 에셋 ID = `A-<분류>-##` (D10 §8.1) · `.import` 커밋 · LFS

## 우선순위 축 (총괄 지정 — 배치 대장 안에서 이 순서로 소화)

| 순위 | 묶음 | 근거 |
|---|---|---|
| **1** | **로그 화자 도상 2종 — 크루 미니 초상·네임드 카 넘버 배지** (16px 세트 — `log_feed.gd` `MARK_SLOT=16` 계약) | **인게임 실 결손 해소** — 현재 4종 중 2종이 텍스트 폴백("실물이 서면 여기 한 줄이 붙는다" 명문). 수량이 적고 즉시 결선된다 |
| **2** | **RACE-01 화면에 걸리는 실물 전량** (머신·베인·릴 심볼·프레임 등 대장의 RACE-01 소비분) | **스토어 스크린샷 1순위 화면** (D15 §2.2 — "첫 노출 소재는 반드시 시스템 화면") — 이 화면이 실물로 서야 ⑥ 스토어 준비물이 착수 가능해진다 |
| **3** | 캐릭터·잔여 UI·글리프 등 A급 잔여 전량 | 화면 전반 실물 교체 완결 |

## 규율 리마인드

- 소유 경로 전속: `docs/assets/` + `godot/assets/` — 코드·씬 무접촉. 소비부 결선(코드 한 줄 포함)은 주력/원격 몫으로 회신에 명기만.
- 이 회차 `strings.csv` 무접촉 (내러티브 4차 배타 창구).
- 동일 경로·파일명 교체 규약 (플레이스홀더 → 실물). 매니페스트 갱신 + `sys_save.png` 보류 주석 전사(IMPL-294 ③ — 벽두 소건 겸무).
- 유료 호출 전 `estimate_inference_cost` 확인·`get_inference_result` 재청구 우선(중복 과금 금지) — 운용지침 기명문.
- 청취/검수 관례 동일: 총괄이 PNG 독립 디코드로 재현 검수한다 — 회신에 생성 파라미터·검증 실측(대칭·잉크·팔레트 준수) 기재.

## 회신 요구

에셋 회차 관례 (`docs/handoff/` 회신문) — ①우선순위 1 묶음부터 **묶음 단위 분할 회신 가**(1이 서면 주력/원격이 즉시 결선 가능하므로 완결 대기 불요) ②수량 대장 대조(발주분/유입분/잔여) ③검증 실측 ④사용자 눈 검수 요청 지점.
