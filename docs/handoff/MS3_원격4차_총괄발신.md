# [총괄 → 원격] MS-3 원격 4차 착수 인계 — 5건 (2026-08-17)

> 발신: 총괄 (remote-hq 세션) · IMPL-176 · 마스터플랜 v2.43
> 판정 원문: `MS3_주력5차_총괄회신.md` §2 (판정 ①⑧⑨⑩⑦) · 발견 원본 = `MS3_주력레인_5차_완료회신.md`
> **우선순위: ①이 차단급 최우선** — MS-3 EA 타이틀 첫 진입 경로 100% 재현 결함. ①만 먼저 커밋·푸시해도 좋다(주력 아이콘 결선이 ①에 걸려 있다).

## 0. 착수 규약

- `git pull` 먼저 · **pull 후 캐시 재생성 필수 판단**(신규 파일 유입 회차 — CLAUDE.md 개정분) · IMPL ID = pull 직후 최대값+1.
- 완료 시 `MS3_원격레인_4차_완료회신.md` 회신.

## 1. 작업 ① — 타이틀 → 업적(SYS-04) 널 역참조 가드 (차단급)

**결함:** `achievement_screen.gd:74` `_refresh_summary()`가 `session.outgame`을 무가드로 읽는다. `outgame`은 `run_session.gd` `begin_career()`에서만 생성되므로 타이틀 직행(SYS-01 → SYS-04, 커리어 미개시)에서 널 역참조 — `_build_row()`의 `achievement_progress()` 호출·`_build_tabs()`의 `add_child(null)`도 같은 축이다. 재현 절차·스택은 회신 §4-G.

**판정 (회신 §2-①):** 비활성 방향 기각 — D09 본문 145행·§A-1이 타이틀 진입을 규격으로 명문. **화면이 무커리어 문맥에서 성립해야 한다.**

**요구 결과 (구현 방식은 원격 재량):**
- 무커리어 시: 5탭·목록 전량·히든 "???" 행을 정적 데이터(`data.achievements`)로 성립시키고 **달성 표시 0** · 에러 0.
- 커리어 적재 후: 기존 거동 무변경 (달성 1/39 렌더 실측 경로).
- `# [가안]` 주석 + impl_log 등재: 달성 0 표시는 통산 업적 적재 층(Steam 연동 D12 §7.4 결선 시 확정) 전까지의 잠정 표시 규칙 — **이월 등재 필수.**
- **회귀 검사:** 무커리어 인스턴스화(기존 단독 인스턴스화 하네스 패턴) — 에러 0 · 요약 문면 세팅 · 행 전수 생성 단언. 돌연변이(가드 제거) 검출 확인.

## 2. 작업 ② — SYS-03·SYS-04 패드 순회 폐쇄 (D09 §1.3 저촉 시정)

- 두 화면에 초기 `grab_focus`(주 행동 버튼 — D09 89행 "화면별 주 행동 버튼") + `ui_cancel` → 닫기 처리.
- 입력맵: `ui_cancel` 패드 바인딩이 정본(D09 본문 입력 명세)에 규정돼 있으면 그대로, 침묵이면 엔진 관례(패드 B) `[가안]` 등재 후 `project.godot` `[input]` 반영.
- `ui/sys/` 밖 화면들도 같은 축이 의심되면 전수 grep 하여 **보고만** (수정은 배정 후 — 이번 몫은 SYS-03·04).
- 회귀: 포커스 존재 단언 검사 가능하면 추가, 실기 패드 확인은 주력 후속으로 넘긴다.

## 3. 작업 ③ — `race_screen` 단독 경로 O9 정합 (판정 = 경로 정합)

- `_boot()`에 `UiPalette.apply_options(session.options)` 결선 — "라우터가 하는 일을 그대로 한다"는 주석 계약에 코드를 맞춘다.
- `test_ui_options.gd:117` 사각 주석 갱신 + 단독 인스턴스화 경로에서 O9 관측 검사 1축(가능 범위).

## 4. 작업 ④ — `log_feed.gd:21` 초기값 리터럴 8 해소

- FONT-B 유형: `configure()` 미호출 경로가 생기면 D13 값(9) 밖의 8이 실렌더될 잠재 지점. D13 창구 경유 초기화 또는 미구성 상태 명시적 차단 — 방식 재량, 리터럴 8 잔존 금지.
- IMPL-159(로그 존 8px 원도 전환 이월)와 별개 — 그 이월은 상태 불변.

## 5. 작업 ⑤ — PAL 검사 신설 (경고형 — 차단형 전환은 별도 판정)

- 대상: `.tscn`·`.gd` 색 리터럴이 조달 대장 안인가 — 마스터 56(`master_56.gpl`) + 기능색 + 정본 §6 지정색 + 명시 허용 목록(주석 근거 필수).
- `.tscn`은 float 표기(`Color(0.9, 0.92, 0.95, 1)`) — 8bit 환산 오차 처리에 유의(총괄 재현: 0.9→229.5 같은 반올림 경계).
- **경고형 신설이 판정이다** — 기존 위반 116+곳(씬 리터럴 3종 포함)이 실재하므로 차단형은 정리 후 별도 판정. FONT 전례의 경고형 대기 절차 그대로. V7 무접촉.
- 신설 후 현행 위반 계수를 회신에 보고하라(주력 정리 작업의 규모 산정 자료).

## 6. 완료 기준

컴파일 게이트 · 검증기 · 테스트 전건 통과 + 신설 회귀 축 명세 + 돌연변이 반증 + impl_log 등재 + 회신.
