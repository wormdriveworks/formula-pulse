# [총괄 → 4레인] 원격 15차(선택지 편입) + 에셋 스피너 소회차 일괄 처리 — 판정 3건 · 함정 규약 등재

> 작성: 총괄 (remote-hq 세션) · 2026-08-20 · IMPL-269 · 마스터플랜 v2.67
> 대상: `MS3_원격레인_15차_완료회신.md` (IMPL-266~268) + `에셋트랙_스피너소회차_회신.md` (IMPL-265)
> **선택지 = 데이터·규칙·흐름 전량 결선(씬 노드만 주력 잔여). 스피너 = 대칭 기계 실증 유입. 다음 = 원격 16차(VN 소비부 — 차단급).**

---

## 1. 검수 — 전건 재현

| 검사 | 결과 |
|---|---|
| 게이트 총괄 독립 재주행 | `VALIDATORS_PASS warnings=0` · `COMPILE 91` · **`NARRATIVE 136` · `UISCR 218`** · `TESTS suites=15` · `ICON_CHECK 8353/0/0` · `SFX_GEN 68` — 일치 (에셋 §5가 관측한 타 레인 경고 20·ARCH 2는 원격 15차 커밋으로 해소 확인) |
| 스피너 PNG 독립 디코드 | 원도 20×20(origin 6,6) · **잉크 160 · 실루엣 90°/180°/좌우 반전 전건 픽셀 불변**(총괄 회전 대조 재현) · 색 `#3FE0F5` 120 + `#12A6C4` 40 = 3.00:1 — 회신 표와 완전 일치 |
| 선택지 폭 3중 검증 성립 | 내러티브 → 총괄 → 원격 편입 전 독립 재계산 — 3자 불일치 0 |

## 2. 판정 3건

| # | 사안 | 판정 |
|---|---|---|
| ① | `act_vn.json` `tone` — `enum` vs `enum_optional` | **ⓑ `enum_optional` 채택.** 기계는 정본의 이행이다 — D12 v1.4 §5.4가 "미지정 = `calm`"과 해석 우선순위 4단(인스턴스 공란 → 폴백 사슬)을 명문하므로, 인스턴스 공란을 빌드 실패로 만드는 것은 **기계가 정본보다 엄한 상태**(원격 규정 정확). "인스턴스는 항상 명시"는 작법 위생 권고로는 옳으나 기계 강제의 근거는 정본에 없다. 집행 = 원격 16차 벽두(스펙 타입 확장 + 공란 폴백 돌연변이) |
| ② | 선택 지점 위 진행 입력 = ⓐ(무소비 대기·스킵 생존) | **읽기 추인.** ⓑ(진행 = 건너뜀)는 연타 플레이어가 선택지를 한 번도 못 보게 되어 §3.3 도출("자동은 선택 지점에서 정지")과 정면 모순 — 원격 논증 그대로 채택. 조항의 취지 = 스킵 무페널티이며 ⓐ에서 스킵 생존으로 성립 |
| ③ | 에셋 부수 ⓑ — 대장 계수 체계 | **현행 유지 (총계 합산 개편 불채택).** "변형·치수 파생분은 별도 절"이 이미 3전례(16px §4.1.1·색각 2종·본 건 §4.1.2)로 선 관례이고, 본 세트 27종 총계는 D10 발주 계수와의 대조 창구라 흔들지 않는다 |

## 3. 접수·기록

**원격 15차** — ⓐ벽두 [가안] 해제(v1.4 정본 이행 확인) ⓑ자수 규칙 행 순서("더 좁은 규칙을 앞에" + 규칙이 무는지 값으로 확인 — 18.0폭 주입 FAIL) ⓒ**적재 층 가드 설계**("런타임은 관대하게, 게이트는 엄하게" — `_load_ok` 유지 + 생략 0 회귀 축·적재 순서 규격 M10 실측) ⓓ**합류 무장치 논증**("복귀 상태를 두면 그것이 곧 없어도 되는 분기 흔적") ⓔ자기 위반 ARCH 자가 적발·교정 ⓕ돌연변이 위생 3건(무효 주입 재주입·M18 미검출 → ⑬축 증설·**M22 등가 대조쌍** — "등가 돌연변이를 단독으로 두고 검출됨/안 됨을 세지 않는다") — 전건 기록.

**에셋 스피너** — ⓐ**설계 원리 "실루엣 대칭 + 음영 비대칭"**(의미 반전 없음 ∧ 회전 보임의 유일 해) + 중심 16.0 자기 교정(반 픽셀 — 회전 대조 기계 검사가 적발) ⓑ색 비 역전 근거(U-a 무음 표기 성격 — "움직임은 보이되 눈을 끌지 않는다") ⓒ**에셋 ID 신설 안 함**(A-UI-18의 두 표기 — 구현이 ID를 만들 자리가 아니다) ⓓ손 작화·RD 불가 판단(정확 회전 대칭 ≠ 확산 모델 산출) ⓔ**격리 워크트리 검증**(타 레인 미커밋 혼입 상태에서 자기 산출만 얹어 0 확인 — 다중 세션 규약 모범) ⓕ부수 ⓐ(대장은 이미 정합 — 총괄 지시의 과대 지목 자인 접수: 낡은 문면은 회신문 2곳뿐이었다) — 전건 기록.

**IMPL-265 재번호** — 에셋 자기 오기(최대값을 +1 없이 사용)를 **푸시 직전 재확인(규약 2항 보완분)이 첫 실전에서 잡았다** — 보완 문면의 즉효 실증 기록.

**함정 규약 등재(총괄 집행)** — 신규 CSV의 기본 임포터 = `csv_translation`(3번째 발생: IMPL-003·021 → 138 → 본 건). "파일을 만들기만 하면 조용히 성립하고 잘못된 임포터가 `.import` 커밋 규약을 타고 리포에 박힌다" → **CLAUDE.md 공통 규칙에 명문**.

## 4. 라우팅

| 레인 | 몫 |
|---|---|
| **원격 16차** (즉시 — 차단급) | 벽두: `_resolve_tone` 공란 슬롯 가드 + `tone` `enum_optional` 전환(판정 ①). 본몫: **막 VN 표시 소비부 결선**(NAR-01 페이로드 — `lines`·`tone` 필수 동반 · 선택 4지점은 자동 도달) + **1막 = 커리어 개시 최초 브리핑 슬롯 발화**(IMPL-263 ②) |
| **원격 17차** | 사운드 결선 6축 (`사운드결선_원격발주.md` · IMPL-264 — 순서 판정: 16차 후) |
| **주력 11차** (원격 16·17차 후) | ①선택지 오버레이 씬 노드(계약 = `ChoiceOverlay`(Control·초기 비표시) + `ChoiceList`(Container) — 이름만 맞으면 코드 무변경 발동 · 스킵 버튼 미가림) ②`sys_save_spin` 회전체 소비부(정지/회전 분리 여부 = 화면 층 판단) ③실기 청감(사운드 결선분) ④VN 6건 실기(소비부 결선분) |
| 에셋 | 배정 0 — 미유입 = 징글 5(즉시 가능)·BGM 18스템(기안 대기) |

## 5. 상태

- **T7 트랙**: 데이터·규칙·검사·선택 흐름 전량 결선 — 잔여 = 소비부 한 겹(16차) + 씬 노드(주력 11차). 그 둘이 닫히면 **4막·에필로그·기원·선택지가 게임에서 실재**한다.
- **오디오**: 68식 완결·귀 관문 통과·검사 전층 차단 — 잔여 = 재생기(17차)·주력 청감.
- 이월 변동 없음. 대기 = BGM·징글 조달 기안(사용자 지시).
