# [총괄 → 4레인] Windows 빌드 — **G-6·G-7 통과 판정 확정** · 판정 4건 · 제출물 잔여 3건 배정

> 작성: 총괄 (remote-hq 세션) · 2026-08-30 · IMPL-499 · 마스터플랜 v3.37
> 대상: `MS3_윈도우빌드_주력회신.md`(IMPL-497 — 9b49a42·보정 227d142) · 충돌 16호(총괄 498 양보) 수습 완료
> 총괄 재주행: `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=20`.

## 1. 판정 — **G-6·G-7 통과 확정** (MS-3 게이트 사슬 완료)

D14 §5.8 실측 기록 성립 — 실 산출물(exe 109MB·pck 889파일·빌드 식별 9b49a42) 기준:
- **G-6 = 통과** — 3방법 전건 노출 0(정적·SEAL-E 98·TC-C 2241·오디오 384). 팩 층(원격·총괄 재현) + 실 바이너리 층(주력) 완결.
- **G-7 = 통과(데스크탑 2검사)** — 심볼 0(pck + exe 별도 스캔)·권한 EXP 181. 모바일 ③ = 범위 밖 이월 확정.
- **재현성 실측 등재** — "커밋 + 프리셋 + 템플릿이 산출물을 결정한다"(원본 경로 재익스포트 md5 동일 — 우회 무영향 증명·워크트리 산출 규약의 실증).

## 2. 승인 — 전건

- **애드온 혼입 적발·교정** — 워크트리에서 뽑은 규율이 그 자리에서 결함을 꺼냈다(주력 트리 팩 895 vs 889 — 차이 6건 전부 MCP 런타임 + 선행 시도의 dll 복사 실사고 1건까지 소급). **"커밋을 막는 것과 팩에 담기는 것을 막는 것은 다른 일이다"** 등재 — .gitignore는 리포지토리를 지켰고 팩은 지키지 못했다. exclude_filter `addons/*` 합류(검증 프리셋 무접촉 — 존재 이유 정확) 승인.
- **원격 검침의 미계상 자인 구조** — "축의 결함이 아니라 대상이 미계상"(오염 팩이 PACK_PROBE_PASS 통과 실측) → §4-⑥ 발주.
- **환경 항목 해소** — RUNASADMIN 호환성 계층(error 740) 실측·우회·사용자 조치 복구·재현성으로 산출물 신뢰 절연까지. 재발 조건·증상·확인처가 회신문에 남았다(충분 — CLAUDE.md 추가 등재 불요 판정).
- 부수 실측 3건(ⓐⓑⓒ)의 **판단을 넘긴 형태 전건 승인** — 특히 ⓑ("끄면 '서명하지 않기로 했다'가 리포지토리 기록이 되므로 제 판단으로 넘길 자리가 아니다")·ⓒ("LegalTrademarks는 상표 규율의 면이라 임의 기입하지 않았다").

## 3. 사용자 판정 2건 (2026-08-30)

| # | 사안 | 판정 |
|---|---|---|
| ④ | 서명 정책 | **EA 단계 미서명 수용** — Steam 클라이언트 경유 실행은 SmartScreen 노출 제한적(인디 EA 관례). **정식 출시(MS-5+) 전 재론 이월 등재.** 프리셋 codesign/enable은 현행 유지(경고는 무해) |
| ③ | exe 메타데이터 | **확정**: ProductName = "Formula Pulse: Spin the Grid" · CompanyName = "Wormdrive Works" · LegalCopyright = "© 2026 Wormdrive Works" · **LegalTrademarks = 공란 유지**(운용 규율 5 정합 — KR 등록 후 재론) → 주력 결선 발주 |

## 4. 총괄 판정 2건 + 배정

| # | 사안 | 판정·배정 |
|---|---|---|
| ⑤ | embed_pck | **현행 분리(false) 유지** — Steam 디포 배포에 분리가 표준·패치 델타 유리·pck 직접 검침 가능(이번 회차가 그 실익의 실증). 단일 파일 전환 근거 없음 |
| ② | 부트 스플래시 | **결선한다** — 기본 Godot 스플래시로 제출 불가. 실재 `boot_splash` 실물을 project.godot에 결선 = **주력**(시각 확인 동반) |
| ① | Windows ICO 세트 | **에셋 발주** — 배치 대장 §6.1 선언 6파일 중 미생성분(ICO 16/32/48/256 · `icon_master_128` 원판 파생). `application/icon` 결선은 주력 겸행 |
| ⑥ⓐ | 검침 축·문면 | **원격 발주** — probe_pack에 "에디터 애드온 0" 축 1줄(필터 생존 감시 — "지금은 필터가 죽어도 아무도 붉지 않는다") + exe 경로 안내 문면 교정(embed_pck=false 전제 명문) |
| §6 | 릴리스 종료 누수 경고 | 관측 등재·이월(정상 종료 대조 후속 — 단정 없음 형태 승인) |

## 5. 상태 — **MS-3 마감 대조 직전**

- **게이트 전 사슬 완료**: V1~V8+구현 층 검사(차단 12종) · TL-1~5 · G-6·G-7(실 산출물). 잔여 실물 = **제출물 마감 3건**(①ICO[에셋] ②스플래시[주력] ③메타 기입[주력]) + ⑥ⓐ 소건(원격).
- 3건 소진 후 **총괄 = MS-3 마감 대조 회차**(`MS3_완료조건_대조_총괄기안.md` 재사용 — D15 §1.1 대조).
- 사용자 몫: KR 출원 · 외부 자문 발주서 발송 · (마감 대조 후) Steam 페이지 개설 트랙.
