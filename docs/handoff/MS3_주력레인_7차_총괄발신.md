# [총괄 → 주력] MS-3 주력 7차 일괄 착수 인계 — 결선 2군 · 치환 174 · 재판독 · 실기 (2026-08-17)

> 발신: 총괄 (remote-hq 세션) · IMPL-205 · 마스터플랜 v2.48
> **본 발신은 6차 발신(IMPL-185)을 대체한다** — 그 후 값 정본·입력 정본·치환 목록이 전부 갱신됐다(치환 116→174·D09 v1.3·자색 V 계열 신설·감광 등채널 등). 6차 문서는 읽지 말고 본 문서만 따르라.
> 판정 원문: `MS3_주력5차_총괄회신.md` §2-② · `MS3_원격7차_총괄회신.md` §3 · 값 정본 = **팔레트 정본 §9.2.2 (본항 + 증보 1·2·3 = 174곳 폐쇄 목록)** · 입력 정본 = **D09 v1.3 §1.3**
> 착수 규약: `git pull` · LFS 실물 확인 · **pull 후 캐시 재생성 필수**(신규 파일·팔레트 개명 다수 — 에디터 가동 시 GDAI `scan()` 경로) · IMPL ID = pull 직후 최대값+1 · **스테이징 = 파일 단위 명시 경로 전속(규약 5항)** · 완료 시 `MS3_주력레인_7차_완료회신.md`. 발견 즉시 보고·이상 없음도 명기.

---

## 1. 작업 ① — 씬 색 리터럴 치환 174곳 (기계 치환 — 값 판단 재개 금지)

**값 정본 = 팔레트 정본 §9.2.2 본항+증보 1·2·3.** 전량 판정 완료된 폐쇄 목록이다 — 정본 표의 (씬 리터럴 → 확정 슬롯)을 그대로 옮긴다. 주요 축(상세는 정본):

| 계 | 곳 | 대표 |
|---|---|---|
| 본항 3종 | 116 | `Color(0.9,0.92,0.95)`→N16 · `(0.55,0.58,0.64)`→N13 · `(0.28,0.32,0.38)`→N10 |
| 증보 1 (A·A′) | 37 | `#12141A`→N01 · `#1C1F26`→N03 · `#2E3038`→N05 |
| 증보 2 (ⓐ-1·ⓐ-2·스크림) | 19 | 게이지 fill 2·섀시 fill·시안 강조 4·앰버 3·`OpponentLabel`→M3·`DuelFrame`→C3·스크림 4(α 유지) |
| 증보 3 (자색) | 2 | `E02Resonance`·`BoostStack` → **V3 `#D08DFF`** |

- **검산기 = PAL.** 치환 후 검증기 재주행 — **PAL 경고 174 → 0 도달이 완료 조건**이다(잔여가 남으면 그 목록이 곧 누락 지점). 도달 시 회신에 명기하라 — **차단형 전환 판정의 성립 조건**이다.
- 치환은 `.tscn` float 표기 정밀도 주의 — 확정 hex 의 8bit 값을 그대로(예: N16 `#C7D0D8` = `Color(0.780392, 0.815686, 0.847059, 1)`). 손 반올림 금지, PAL ±1 허용 안에서.

## 2. 작업 ② — 결선 2군

**②-1 아이콘 결선 3군** (5차 회신 §1 미결선분):
- 업적 6종 → `achievement_screen` 항목 행(§A-4 — 아이콘·명칭·조건·달성 일시 · 히든 "???"는 아이콘 은닉). **달성 조회는 `_achieved()`/`_progress_for()` 창구 경유 유지**(IMPL-177 가드 구조).
- 화자 2종 → `log_feed.gd:50` Label→TextureRect (E10 — 중계 마이크 / 무전 미니 초상).
- 시스템 4종 → **규격 확인 선행, 별첨A 침묵이면 결선하지 말고 보고**(임의 배치 금지).

**②-2 릴 선택 커서 시각 표시 (신규 — 원격 7차 §3-라):** `X 홀드 + D패드 좌우`가 커서를 움직이나 화면 표시가 없다 — **표시가 없으면 패드 홀드 토글이 실사용 불가.** D09 §3 개입 창 명세에서 규격 확인, 침묵이면 [가안](예: 선택 릴 프레임 강조 — `FRAME_LINE`→C3 등 기확정 슬롯 색만 사용·신규 색 금지). **봉인(불변규칙 5) 주의: 커서는 선택 위치 표시 전속 — 릴 결과와 상관하는 어떤 신호도 싣지 않는다.**

## 3. 작업 ③ — 실배치 재판독 (결선 후)

| 대상 | 판정 축 |
|---|---|
| 업적 6종 + `achv_rival` | 격자 실배치 판독 · **세로 11px 납작 유보분 판정** |
| 파편 후보 3건 | ⓐ`speaker_filler`(20,13) ⓑ`attr_hazard`(17,20)-(18,20) — 파편 기움 ⓒ`attr_sweeper`(26,10)-(27,10) — 의도 기움. 32px 실화면에서 보이는가/거슬리는가 |
| 화자·시스템 아이콘 | 결선 화면 실배치 |

## 4. 작업 ④ — 대비·팔레트 눈 대조 (치환 후)

| 축 | 내용 |
|---|---|
| **`#E6EBF2`→N16 61곳 (최우선)** | 주 텍스트 −9.9 하향 — 화면이 납작하면 **되올리지 말고 보고**(다음 단 = 바탕 하향 N03→N02→N01, §9.2.2 [가안] 지렛대 — 별도 판정) |
| 색상 대변경 3건 | 게이지 후방 **주황→마젠타** · 섀시바 **녹→시안** · `OpponentLabel` **→M3**(대비 7.55→5.77 유일 하향 — 14px 굵은 글립 실독) |
| 자색 V3 2곳 | 공명·부스트 — 신설 슬롯 첫 실기. 찬스 마젠타(M계)와의 인접 인상 관측 |
| 앰버 A4 3곳 | 채도 하향(연한 금) 인상 |
| 홀드 감광 등채널 | 릴 홀드/비홀드 판별 인상 (0.78 게인 — IMPL-195) |
| 테두리 N10 15곳 | "경계가 존재로 인식되는가" 재확인 (2.18→2.89) |

## 5. 작업 ⑤ — 패드 실기 전수 (D09 v1.3 §1.3 기준)

| 층 | 확인 |
|---|---|
| 공통 층 | A 확인 · B 취소 · D패드/좌스틱 포커스 · LB/RB 탭(3화면 — 감김 [가안] 감각 포함) · Start 일시정지 · **LB 홀드+Y 상세**(툴팁 고정 — 위치·가독·해제 3규격 [가안] 판독) |
| 레이스 층 | **X 홀드+D패드→A 릴 토글**(②-2 커서와 함께) · **X 단독 리스핀**(릴리스 판정 감각 — 뗄 때 발화가 어색한지) · Y 차지 · LB 홀드+Y 상세 |
| 폐쇄 루프 | SYS-03·04 진입→순회→탈출 패드 단독 (H38 — 도달 불가 요소 0) |
| 무커리어 SYS-04 | 타이틀 직행 — 빈 껍데기 아닌 정상 화면(달성 0/39) 실기 확인 (IMPL-177 가드의 눈 검증) |

## 6. 권장 순서

①치환(PAL 0 도달) → ②결선·커서 → 검증기·게이트 → ③재판독 → ④눈 대조 → ⑤실기. ①과 ②는 커밋 분리 권장(치환 커밋이 커서 diff 리뷰가 갈리게).
