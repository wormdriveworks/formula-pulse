# [총괄 → 4레인] 키 비주얼 파일럿 성립 — 구도 확정(사용자) · 배경 3장 개방 · 아이콘 접수

> 작성: 총괄 (remote-hq 세션) · 2026-08-24 · IMPL-411 · 마스터플랜 v3.08
> 대상: IMPL-409(아이콘)·IMPL-410(키 비주얼 파일럿) — RD $0.065 · 잔고 $5.925

---

## 1. 접수·승인

- **치수 재량 변경(384→320) 승인** — 640/384 = 1.667 비정수·640/320 = 2.000 정수. **"파일럿의 질문이 화소 밀도였으므로 배율을 틀리게 잡으면 질문 자체가 성립하지 않는다"** — 승인 문면보다 승인 목적에 맞춘 재량이고, 지출도 낮았다($0.026). 이런 갈래의 재량은 계속 허용한다: **치수는 수단이고 판정 축이 계약이다.**
- **파일럿 성립 판정 승인** — 배경·머신·헬멧 전부 1× 원도의 ×2라 격자 동일("화소 밀도는 위험이 아니라 배수 선택의 종속 결과였다")·팔레트 순색 60 단일. 접지 그림자 = 선언형 디더 타원(웅덩이 1차를 눈으로 기각) 승인.
- **아이콘 접수** — 번호를 RD에 맡기지 않은 것(vh8이 `number 18`을 무시하고 13을 단 실측 — **"맞을 때도 우연이면 계약이 아니다"**)·크라운 시안 띠 = 번호판(실측이 답: 이마 띠 5px)·`digits.scale`(머신·아이콘 동일 자형 계약)·**`covers:false`("누락과 부재를 같은 모양으로 두면 깜빡한 것과 없는 것이 구분되지 않는다")** 전건 승인. 파생 배수 실측(48 = 16px 단순화 ×3·432 = 안전 영역 안 ×2) 접수.
- **맥락 기록 승인** — 전방 소실점 직선 주로: 무대 배경(타일)에서는 금지(소실점 중복 — IMPL-376), 키 비주얼(단일 구도)에서는 정답. **"같은 구도가 한 곳에서는 결함이고 다른 곳에서는 정답이다 — 규칙은 맥락에 매여 있다"** 문화 대장 등재.

## 2. 판정 3건

| # | 사안 | 판정 |
|---|---|---|
| ① | 키 비주얼 본제작 | **현행 구도 확정 — 사용자 판정 (2026-08-24 · 파일럿 합성본 눈 확인).** `kv_gen.js` 합성 선언 고정 → 재현 계약 편입 → **리프레임 9건(캡슐 7·세로 1·boot_splash — 지출 0)** 연쇄 착수 |
| ② | 배경 3장 동일 경로 | **개방 — IMPL-408 ② 예약분 집행.** 개러지 가로·VN 2 = 320×180 ×2 · 3호출 ≈ $0.078 · 1장씩 회당 상한 안 규율 유지 |
| ③ | 머신 크기 | **현행 유지 — ①의 구도 확정에 종속 종결.** 배경 ×3 재생성·전경 재배치 불착수 |

## 3. 상태

- **스토어 사슬 가동 중**: 아이콘 완결(파생 4종 지출 0) → 키 비주얼 확정 → 리프레임 9건 + 배경 3장 → 트레이딩 카드·배지·이모티콘 → 스크린샷 5매(주력 캡처 — 배경 종속 2매 해제됨). 남는 원격 밖 항목 = 캡슐 규격 콘솔 확인·스크린샷·트레일러 편집(전부 주력/사용자).
- RD 예상 잔여 지출 ≈ $0.078(배경 3장) · 잔고 $5.925.
- 잔여 지형: 원격 27차 회신 대기 · 주력 14차(6건) · 실물 플레이 검증 회차 · 전용 CG 6장 경로 기안(ⓒ 성립이 입력 — 착수 가능해짐) · 상표 법률(사용자) · G-6·G-7(직전).
