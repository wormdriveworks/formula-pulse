# [총괄 → 3레인] 주력 9차 완료 회신 처리 — 검수 전건 재현 · 판정 7건 · 16px 트랙 종결

> 작성: 총괄 (remote-hq 세션) · 2026-08-18 · IMPL-229 · 마스터플랜 v2.53
> 대상: `MS3_주력레인_9차_완료회신.md` (IMPL-226~228)
> **주력 9차 = 종결. 16px 트랙(발주→유입→결선→실기)·재정규화·저장 표시까지 전부 닫혔다 — 잔여는 소거 집행(에셋)·함정 조사(원격)·이월 대장뿐.**

---

## 1. 검수 — 전건 재현

| 검사 | 결과 |
|---|---|
| 게이트 총괄 독립 재주행 | `COMPILE 90` · `VALIDATORS_PASS warnings=0` · `TESTS suites=14` · **UISCR 138** — 일치 |
| **파편 ⓑ 색 분해** | **PNG 디코드 좌표까지 정확 재현** — `#2E6FA8` 6px = (17,16)/(21~23,20)/(19~20,23) 3덩이 · `#5B646F` = 23px 큰 성분. **대장 오기 지목이 맞다** |
| 창구 분리 | `_icon_texture(asset_id, variant="")` + `ATTR_VARIANT="_16"` · 릴 무인자 — 실독 일치 |
| 저장 표시 교정 | 오프셋 직접 기입 + preset 역산 함정 주석 — 실독 일치 |
| 정규화 커밋 | 단독 커밋(`9abe4af`) · 기계 등가 대조(노드 111·속성 9235·불일치 0) 방식 접수 |

**판단 품질 기록:** 치수 축을 창구 밖으로 뺀 것("창구 안에서 치수를 판단하면 두 소비부가 서로의 규격을 밟는다")·`_16` 부재 시 32 폴백 금지(비정수 축소의 조용한 부활 차단)·데이터에 치수를 안 새긴 것(표시 층 사정) · 스크린샷 도구 한계(2초 창)를 실측 방식 전환으로 우회하고 **그 과정에서 8차 자기 결함(좌상단 표시)을 발견·교정**한 것 · 회전 "감각"을 산술로 대신하지 않고 미판정 명기한 것.

## 2. 판정 7건

| # | 사안 | 판정 |
|---|---|---|
| 1 | 대장 §9-⑭ ⓑ 문면 오기 | **교정 배정 (에셋)** — 소거 대상 색 = `#2E6FA8` 6px (총괄 디코드 재현 확정). `#5B646F` 는 23px 음영 본체 — 그대로 지웠으면 도상 파손이었다. **집행 전 선행 순서 그대로** |
| 2 | 파편 ⓐⓑ 소거 | **승인 (에셋 집행)** — "렌더 소비부 0 = 기록 원본이므로 위생이 오히려 엄해진다(재파생 출발점)" 논거 채택. ⓐ = `speaker_filler` (20,13) 1px · ⓑ = `#2E6FA8` 6px 전량(흩어짐이 음영으로 설명 안 되고 16px 재작화가 이미 버린 톤). 소거 후 매니페스트·§9-⑭ 종결 |
| 3 | `achv_garage_16` | **추인 확정 — 종결** (8차 반대 → 9차 철회. 반대를 실물로 세우고 실물로 거둔 왕복이 규격대로다) |
| 4 | `attr_hazard_16` 의미 약함 | **비차단 접수 — 대장 관측 등재 (에셋)** — 즉시 재작화 불요(유일 미달이고 "요철"까지는 도달). MS-5 실기 관측 + 재작화 여부는 에셋 재량 |
| 5 | 저장 표시 회전 감각 | **실물 빌드 육안 이월** — 산술 정합(2회전) 확정·감각만 잔여. 패드 실기 회차 또는 TL 게이트에 합류 |
| 6 | `set_anchors_preset` 순서 함정 전수 | **원격 조사 배정** — 코드 생성 Control 전수. **총괄 선행 grep: `flow_screen.gd:176`(툴팁 패널 · PRESET_CENTER_BOTTOM)이 제1 후보** — 크기 설정 전 호출이면 같은 역산 함정. FULL_RECT 계열(오프셋 0 보존)은 무해 추정이나 확인 명기 |
| 7 | 이월 승계 | `speaker_filler_16`(발화원)·`CreditsValue`(투어 경계 — RUN-01 도달 경로 부기 접수)·패드 실기(실물)·스킬 소비부(TL-5) |

## 3. 인계 라우팅

| 레인 | 몫 |
|---|---|
| **에셋** | ①대장 §9-⑭ ⓑ 문면 교정(`#2E6FA8` 6px) — **소거보다 먼저** ②파편 ⓐⓑ 소거 집행(되읽기 대조·매니페스트 동기) ③`attr_hazard_16` 관측 등재 |
| **원격** | `set_anchors_preset` 순서 함정 전수 조사·보고 — `flow_screen.gd:176` 제1 후보. 발견 시 교정 + 위치 회귀 축(부모 폭 기준 실 rect — 주력 검사 방식 준용) |
| **주력** | **배정 잔여 0** — 이월 대장만 |

## 4. 상태 — MS-3 구현 잔여 지형

| 축 | 상태 |
|---|---|
| 씬 색 정리 | **종결** (v2.52) — PAL 차단형 상시 감시 |
| 아이콘 | 32px 29 + 16px 16 유입·결선 완료 — 잔여 = 파편 소거 2건·hazard_16 관측 |
| 입력·패드 | 배선 완료 — 잔여 = 실물 패드 감각 회차·스킬 소비부(TL-5) |
| 저장 표시 | 완료 — 잔여 = 회전 감각(실물 빌드) |
| **유입 대기 (에셋 발주 축)** | **사운드 실물(SFX 68식·BGM 13+1 — 디스패처·호출 지점 기결선) · T7 서사 데이터 · 폰트 8px 원도(G-1 트리거·IMPL-159 결선 대기)** |
| 게이트 | TL 게이트(D14) — 위 유입분 소화 후 |

**차기 총괄 안건 = 에셋 발주 순서 계획** (사운드/서사/폰트 3축의 착수 순서·의존 관계) — 사용자 지시 대기.
