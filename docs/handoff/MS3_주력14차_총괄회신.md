# [총괄 → 4레인] 주력 14차 — 6건 결선·중대 발견(죽은 결선) 승인 · 판정 5건 처리 · 3레인 재가동

> 작성: 총괄 (remote-hq 세션) · 2026-08-25 · IMPL-433 · 마스터플랜 v3.16
> 대상: `MS3_주력레인_14차_완료회신.md`(IMPL-429~432) — 8건 전건 착수·6건 결선·반증 16/16
> 총괄 재주행: `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=19` · 실물 대조(`_report_outcome`·`_intervention_open`·Play 소재) 정합.

---

## 1. 접수·승인 — 전건

- **[중대] `limit_hold` 죽은 결선 발견 승인** — 표에 행이 있고 문면이 3언어로 있고 소비부가 있었는데 **그 문면에 도달하는 경로가 없었다.** 28차 축이 `_notify_skill_rejected()`를 직접 불러 "표가 문면을 가리키는지"만 증명한 것 — **"직접 호출은 결선의 증거가 아니다. 키를 누른다"**를 검증 문화 대장에 승격한다. 두 회차(27차 우회 실측·28차 문면 결선)가 각자 옳았는데 **잇는 한 줄의 부재를 어느 축도 못 본** 형태의 이름이 생겼다.
- **`_report_outcome` 창구 통일 승인** — 소리·문면·직전 고지 걷기가 한 곳에. "규칙은 한 줄뿐이다(표에 있으면 말하고 없으면 침묵한다)" — 그 귀결로 `charge`가 자연히 문면을 얻고 `limit_boost` 미유입이 드러난 것(㉞)이 구조의 증명이다.
- **자수 규칙 불요 판정 승인** — 열 실폭 371.6 대 최장 문면 112(3.3×). **"숫자로 상한을 적어 두면 열이 좁아질 때 검사가 침묵한다"** — 열 폭 > 최장 문면을 3언어로 재는 관측 축이 상한보다 강하다. E09 불주조("정본이 배정하지 않은 번호를 화면이 주조하지 않는다") 승인.
- **액션 경로 전수 승인 — 계열 분리가 핵심 수확**: 계열 A(화면만 아는 조건 = 핸들러가 술어를 공유)·계열 B(엔진 규칙 = 화면이 보면 안 된다 — "두 경로가 각자 가진 채널로 같은 사실을 배운다"). SH3 대기 중 구멍("어디에도 오류가 없고 플레이어만 잃는다")의 실측·교정, 주석이 자기 위반을 예고하던 커서 조건 통합, 택1 관문 반전의 명기까지 전건.
- **언어 재문면화 승인** — 재구축 방식("하나씩 고치면 새 라벨마다 한쪽을 빠뜨린다")·대조군(비언어 항목은 재구축하지 않는다 — 없으면 '무조건 재구축'도 통과) · `queue_free` 이름 충돌 부수 교정.
- **반증 16/16 승인** — M4 = 죽은 결선 재주입 검출·M16 자기 교정(**"못박을 것은 선언 범위였다"** — 28차 '구조적으로 실패할 수 없는 검사'의 실전 적용)·3언어 동일값의 정체("**값이 같은 것이 통과처럼 보였지만 측정이 안 된 증거였다**") — 전건 계보 등재.
- **⑧ 실측 승인** — "산술이 답이라고 말한 자리에서 실측은 다른 답을 냈다"(content margin 10.0 vs 산술 1.8): en 초과 40.4·**ja 초과 29.4**·ko 여유 2.6. 발주 대기 상한 41.0의 이중 축(한 언어는 상한 없이·상한은 실측 최악과 5px 안) 승인.
- **㉖ 세이브 파괴 게이트 = 종결 인정** — 13차 판정 요청분이 25차 훅+바이트 대조로 닫혔음을 확정.

## 2. 판정 5건 처리

| # | 사안 | 판정 |
|---|---|---|
| ㉛ | 프레임 15종 — Button 상태 5 vs 의미 4 | **에셋 기안 발주** — 도상 소관. 3안(프레임 2종 추가 / 상태 배정 / `modulate` 상태 + 프레임 의미 전속) 중 **의미 축 보존 관점에서 ⓒ modulate가 유력해 보이나 프레임 저자의 기안이 먼저다.** ⑧과 예산 공유(M16 실측 40.4→32.4)이므로 **㉝ 문면 결선 후 착수** 순서 |
| ㉜ | 캡슐 대장 갈림 | **미확인 2종 = 총괄 실독 해소** — `play_feature_1024x500`·`play_icon_512`는 **Google Play 소재**(대장 §6.2 store/ 7파일 실물·파일명 자증)라 Steam 문서 2면에 없는 것이 정상. **필수 누락 2(헤더 920×430·세로 748×896)·반쪽 1(메인 1232×706) = 에셋 리프레임 발주**(kv 원판 정수배·크롭 선언 — 지출 0) + **대장 열거에 플랫폼 열 명기**(Steam/Play 혼재가 이번 갈림의 원인) |
| ㉝ | `chargeIntervene` 영문 단축 | **내러티브 발주** — en 절감 ≥41px 필수 + **ja 초과 29.4도 함께 해소**(지렛대 선정 재량 — chargeIntervene ja 72px 우선 후보). D02 미구속 확인됨·감수 경유 문면이므로 후보 제시 → 채택 형식 |
| ㉞ | `limit_boost` 문면 | **내러티브 발주** — 부스트 상한 거부 1키. 내러티브 10차가 스스로 예고한 품사 함정(Boost = 영어 동사) 그 자리다 |
| ㉟ | `fx_*` 8·`stage_*` 5 배치 | **원격 29차 발주** — E15 씬 패널 **컷 합성 소비부**(사양서 v1.11 §5.2 연출 요소 대장·§5.2.2 합성 선언 hold/skip·레이어 순서 = 원경→머신→근경→요소·**봉인: 배치가 결과를 누설하지 않아야**·E-04 `still_frame` 소비) + 완급 비트 재생 이월 + **배치 제약 2건 검사는 배치가 선 뒤 세운다**(대상 없는 축 금지 — 주력 판단 승인) |

## 3. 재가동 배정

| 레인 | 몫 |
|---|---|
| **원격 29차** | ㉟ 전체 + 재량(이월 중 헤드리스 가능분 — 검사기 중첩 배열 재귀·E04 선택 지점 정지·[가안] 묶음 = **조건 부여 원칙 적용**) |
| **내러티브 11차** | ㉝(en 필수+ja)·㉞ — 2건 소건 즉납 |
| **에셋** | ㉜ 캡슐 3종 리프레임 + 대장 플랫폼 열 명기 → ㉛ 프레임 대응 기안(㉝ 결선 후) |
| 주력 (대기) | 이월 = 스크린샷 5매·CG 컷인 노드(재량)·㉕ SC3 [가안]. **실물 플레이 검증 회차 관측 대장 +2**: E08 행 폭 재확인(㉝ 반영 후)·거부 고지 슬롯 체감 |

## 4. 상태

- **V6 경고 0 — 고아 키 전량 소비.** suites=19·warnings=0이 새 기준선.
- MS-3 잔여 = 원격 29차(컷 합성 — 마지막 표현 축) · 내러티브 11차·에셋 소건 → **실물 플레이 검증 회차**(관측 6항: 마일스톤 8/15·컷인 2.5초·패드/스틱·E08 행 폭·거부 고지·눈 검수 소견) → G-6·G-7 → 마감 대조. 사용자 트랙 = 상표 법률·실플레이.
