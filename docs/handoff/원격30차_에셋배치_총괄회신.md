# [총괄 → 4레인] 원격 30차 + 에셋 배치 6종 + 내러티브 정정 일괄 — 잠금 en = ―(사용자) · 섀시 = 오프셋 데이터 · 원격 31차 발주

> 작성: 총괄 (remote-hq 세션) · 2026-08-25 · IMPL-445 · 마스터플랜 v3.19
> 대상: 원격 30차(IMPL-442 — FXPL 차단·상한 철거) · 에셋 IMPL-443~444(danger_alt·페이지 배경·컷 배치 6종) · 내러티브 정정 2건
> 총괄 재주행: `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=20`.

---

## 1. 원격 30차 — 전건 승인

- **FXPL 차단 전환 승인** — 위반뿐 아니라 **판정 불가도 실패**("종료코드는 '위반 없음'과 '판정 없음'을 구분하지 못한다")·무대 표본 하한(검사가 헐거워지는 방향의 침묵 차단)·N15 재주입 검출 확인.
- **상한 철거 승인 — 보조 축 동반 철거의 원리 승격**: **"가드는 자기가 지키던 것이 사라지면 함께 내려간다."** 철거 실측이 내러티브 예보와 일치(ko −2.6·ja −6.6).
- **원본 대조 3축 승인** — "되돌림은 거동으로 잡히지 않는다"·대조 리터럴 쪼개기("붙여 두면 그 줄 자체가 원본에 남아 **검사가 자기를 잡는다**")·한계 명기(이름 변경 이탈)까지.
- **en 잠금 +2.4px을 판정으로 올리지 않은 절제 승인** — "남의 몫이 내 게이트를 붙들면 방금 철거한 상한이 다시 생긴다" — 관측+비공허성 차단의 형태가 맞다.

## 2. 판정 4건

| # | 사안 | 판정 |
|---|---|---|
| ① | 잠금 슬롯 en 라벨 | **`―`(U+2015) — 사용자 판정 (2026-08-25).** en 열만·ko/ja 무접촉(Step In 전례 — 설계 문서에 en 형태 부재·폭 구속 en 단독). 내러티브 정제 논거 채택: 글리프 4종 동폭(버튼 최소 폭 바닥) = **폭 계산에서 빠진다** — "숫자로 붙들어 둔 통과"(Closed 3.6px)를 남기지 않는 선택·`―`는 `consumableEmpty`가 이미 쥔 "여기엔 없다" 글리프(U+2014 산문 줄표 74회와 구분) |
| ② | 섀시 바닥 비정렬 (+11~+19 · 8px 산포) | **ⓑ 오프셋 데이터 — 소비부 계산 (총괄).** 노면선 형식(`셀 중심 = anchor_road_y − baseline_offset`)은 이미 양쪽 호환으로 제출됨. ⓐ재정렬은 이득이 열 하나 제거인데 비용이 보레아스 오버레이 6장+CG `at` 좌표 연쇄 — 비파괴·연쇄 0·실측값의 데이터화("고를 값을 남기지 않았다" 계열)로 ⓑ가 명백하다 |
| ③ | fx 대 머신 z 순서 | **원격 31차 편입** — 오라 뒤·불꽃 앞은 한 값이 아니라 요소별 선언이다(`scene_cut_layers` z 관계 열 등 — 형식 재량). 에셋의 "어느 순서든 읽히는 좌표" 처리로 배치는 순서 판정과 독립 |
| ④ | AUDIO-W 부하 민감 | **ⓐ 채택 — 관측을 표집에서 기록으로 (원격 31차).** 디스패처가 봉인 개폐 이력을 남기고 하네스가 이력을 본다 — "하네스가 프레임을 잡지 못한 것"을 구조적으로 소거. ⓑ(프레임 수 구동)보다 실제 거동을 재는 쪽. 오디오 축 = 원격 소관(IMPL-142~147 디스패처 저자) |

## 3. 에셋 배치 6종 — 전건 승인

- **섀시 비정렬 발견** — "1대 컷에서는 원리적으로 보이지 않던 결함이고 다대 발주가 드러냈다." 노면선 대역(상한 75 = 눈·하한 82 = 유도 — "폭 7px는 원근 축소 금지의 종속 결과·배치의 주된 단서가 세로가 아니라 겹침")·게이트 ⓒ 2회 오설정 자인(**"합성물을 재면서 한 가지 이름을 붙였다"** — 기준선 모순이 신호)·**"눈이 게이트를 두 번 이겼다"**(SC-05 융합 — 화소 잔존 87% 통과·"게이트는 화소를 셌고 눈은 차가 몇 대로 보이는가를 셌다") 전건 기록.
- CB-SCOPE 양방향(§6 색은 `_alt` 전속 + `_alt`는 §6 색 필수 — "빈 유예" 차단)·페이지 배경("물어서 얻은 게 소재 1건이 아니라 정본 해석 1건")·SC-08 2레인 도열(물리 불가 512>408 — 실제 그리드 홀짝과 동형)·fx 앵커가 좌표 절반을 선결정(cx 204 고정 계열) 승인. SC-03 판독 한계 명기 접수.
- `store/` 10파일 미보유 0 — **스토어 소재 실물 완결.**

## 4. 내러티브 정정 2건 — 접수·승격

- "한 구성에서 구한 여유 순위는 다른 구성의 순위가 아니다"(덱 구성 의존·부호 역전은 en 단독) + **"원문 폭으로 부호를 판단하지 않는다"**(`get_string_size` vs `get_combined_minimum_size` — 어긋남 2회: 패딩 10.0·반올림 0.7. 라벨당 1px 안의 부호 판정은 마운트해 재는 쪽 몫) — 측정 문화 대장 승격. 주력 15차 배치 편입은 보류 확정(문면 지렛대 존재).

## 5. 발주

| 레인 | 몫 |
|---|---|
| **내러티브** | `ui.race.locked` en = `―`(U+2015) 즉납 — 1행·ko/ja 무접촉 |
| **원격 31차** | ①컷 머신 배치 소비 — `cut_layout.js --table` 출력 전사(손 전사 금지)·오프셋 표 포함(판정 ②) ②fx-머신 z 순서 선언·소비(판정 ③) ③AUDIO-W 기록 관측 전환(판정 ④) ④`locked` en 유입 시 en 잠금 조합 관측 0 확인 |
| 에셋 | 대기 (실물 완결 — 플레이 검증 합류 대기) |
| 주력 15차 (대기열) | 프레임 15종 결선(ⓓ)·감광 후보 실기·스크린샷 5매·CG 컷인 노드(재량)·㉕ SC3 [가안] |

## 6. 상태

- **suites=20·warnings=0 유지 · 스토어 소재 실물 완결(10/10) · 컷 배치 좌표 확정.** 잔여 = 원격 31차(소비 3건) + 내러티브 1행 → 주력 15차 → **실물 플레이 검증**(관측 10항 + SC-03 판독·fx z 순서 확인 = 12항) → G-6·G-7 → MS-3 마감 대조. 사용자 트랙 = 상표 법률·실플레이.
