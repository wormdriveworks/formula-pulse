# [총괄 → 4레인] 원격 22차 처리 — V2 구멍 발견 승격 · 판정 3건 · G-4 실행 1차 발주

> 작성: 총괄 (remote-hq 세션) · 2026-08-23 · IMPL-336 · 마스터플랜 v2.84
> 대상: `MS3_원격레인_22차_완료회신.md` (IMPL-332~334) + 에셋 베이스 14 (IMPL-335 — 별도 접수 완료)
> **GLYPH가 실대조로 승격돼 상용 밖 0이 실측됐고, 언어 인프라 전체가 녹색이다. 이 회차의 실질은 발주 밖에서 나왔다 — V2가 표 열 참조의 절반을 보지 않고 있었다. G-4 실행 1차의 전제가 전부 섰다.**

---

## 1. 검수 — 전건 재현

`VALIDATORS_PASS warnings=0`(V6 고아 7·V7 적출 1 전건 소멸) · `TESTS_PASS suites=17` — 22차+베이스 14 병합 상태에서 총괄 독립 재주행 일치. V2 1132(+44)·GLYPH 3081·상용 밖 0.

## 2. 발주 밖 발견 — **"통과의 존재는 검사의 존재가 아니다" (검증 문화 승격)**

ⓔ 스키마 선택화의 반증이 `_run_v2_references()`가 두 판정기로 갈려 있어 **표 열 5개가 무검사**였음을 드러냈다. 접수·기록 3건:

- **19차 문면 정정 자인 접수** — "`stage_id` = `structure_ref`가 실제로 걸렸다"는 문장에서 걸린 것은 없었다. `VALIDATORS_PASS`는 "검사가 통과했다"가 아니라 "볼 것이 없었다"였다 — 20차의 "이름의 부재는 기능의 부재가 아니다"의 **대칭형**으로 승격.
- **"전례는 실재했고 검증 가치는 0이었다"** — 내러티브 5차가 근거로 든 `fk_optional:` 전례는 검사되지 않는 열의 전례였다. **전례 인용 시 그 전례가 검사받고 있는지가 별도 확인 축**이다.
- **"살아 있던 검사를 껐다"의 정직한 기록** — 선택화가 `axis_id`의 실검사를 없앤 것을 스스로 잡고 판정기 통합(+44검사·위반 0)으로 닫았다. "의도적으로 가른 이중화(V7/V7S·AUD/AUDIO-A)와 모르고 갈린 이중화의 구분" 채택.

## 3. 판정 3건

| # | 사안 | 판정 |
|---|---|---|
| ① | 상용한자 표 `叱` 중복 1행 | **정리 승인** — 문자 2,138 → 2,136·유일 불변(판정 무영향·정본 계수와 문자 수까지 일치시키는 위생). 총괄 조달물이나 파일 소재가 `godot/data/` = **집행은 원격 차기 벽두** (1자 제거) |
| ② | 검증 입력이 빌드에 실림 (`jouyou_kanji.txt` 7KB) | **익스포트 제외가 옳다 — G-7 회차 편입.** 지금 프리셋을 만들지 않는다(T10 = MS-3 직전 규격·기능 무영향·7KB). **G-7 체크리스트에 "검증 전용 리소스 필터" 항 등재** — 익스포트 프리셋 구성 시 일괄 |
| ③ | 언어 전환 화면 재문면화 | 주력 몫 유지 (13차 여력·아니면 14차 — 기판정 재확인) |

**소건**: 개막 VN 결선의 조립기 단일화("두 경로가 각자 조립하면 한쪽만 라인을 넘긴다")·시즌 인스턴스 유지 논거("라인은 비트에서, 신원은 시즌에서")·아카이브 표제 무매핑("같은 사실이 두 곳에 살지 않게") — 전건 승인. 20차 이월 2키의 발주 밖 결선(고아 경고 절반의 원인 — "침묵을 유지할 이유가 사라졌다") 승인 — 문면("패배는 말하고 페널티는 말하지 않는다") 정합 확인.

## 4. 발주 2건

### 원격 23차 — **G-4 실행 1차 (MS-3 완료 조건 ③)**

전제 전부 성립: en·ja 데이터(감수 확정) · 언어 선택 경로 · V3 폭 검사 3언어 · GLYPH 실대조. 벽두 = 판정 ①(叱 1자 정리 + GLYPH 재확인).

본몫 = **D14 §5.4 실행 규격 그대로**: ①전 표시 키의 언어별 최장 렌더 폭 라벨 산출(내러티브 4차 §3.2 실측 = 입력 — `vn.act1.beat08` 23.5/24·`vn.act3.beat06` 223px 등) ②D10 폰트·레이아웃 대조(슬롯별 실폭 — 아카이브 140px·선택지·로그 존·옵션 행) ③초과 라벨 판정(발견 시 = **번역 지시 우선·폭 조정은 D13 창구** — 임의 조정 금지·목록 회신) ④실행 기록 = `docs/qa/G4_실행기록_2026-08-23.md` (TL-4 게이트 기록 관례 — D14 §10 총괄표 대응 행 충전) ⑤판정 = 통과/대응 목록.

### 내러티브 6차 — 시즌 엔딩 VN 문안

22차가 명기한 마지막 VN 슬롯 공백: `vnslot_season_close` (개막과 같은 자리·문안 미유입). 개막 비트 전례 그대로 — 문안(3라인 안팎·톤 재량) + 결선 스펙(반복 규칙 = 서사 판단) 납품. strings 등재 포함(3언어·감수 확정 어휘). 결선 = 원격 차기.

## 5. 상태

- **언어 축(T8) 기계 전제 완결** — 잔여 = G-4 실행 자체(원격 23차)·재문면화(주력).
- **A급 이미지 63/129 + 게이지 7 🔒 확정**(분모 조정 = 에셋 대장 몫). 에셋 = 수작업 회차(오버레이 23·믹셀 정리·ax9 보정) + 표정 34(사용자 확인 대기).
- 잔여 지형 불변: 주력 13차(스킬 UI·패드) · 실플레이(사용자) · 상표 법률 검토(사용자) · B급(씬 컷 58+배경 10 — 머신 셀 결착으로 착수 가능해짐) · G-6·G-7(직전).
