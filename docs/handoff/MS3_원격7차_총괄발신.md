# [총괄 → 원격] MS-3 원격 7차 착수 인계 — 3건 (2026-08-17)

> 발신: 총괄 (remote-hq 세션) · IMPL-200 · 마스터플랜 v2.47
> 판정 원문: `MS3_에셋파생_총괄회신.md` §2-②·③ · **입력 정본 = D09 v1.3 §1.3 (본 회차 발행 — 층위 규칙·상세 재배치)**
> 착수 규약: `git pull` · 캐시 재생성 필수 판단 · IMPL ID = pull 직후 최대값+1 · 파일 단위 스테이징 · `.tscn` 무접촉 · 완료 시 `MS3_원격레인_7차_완료회신.md`.

## 1. 작업 ① — PAL 조달 경로 수정 + 소스별 적재 검증 (우선 — 기능 영향)

- `tools/validators/config.json:710` — `master_56.gpl` → **`master_60.gpl`** (죽은 경로 — 에셋 개명분).
- **소스별 적재 검증 신설** (에셋 발견 IMPL-198 §5 채택분): `palette_sources` 각 항목이 **최소 1색을 기여**했는지 단언, 0이면 경고. 현행은 없는 파일이 빈 기여로 조용히 지나가고 전체가 비었을 때만 경고한다 — 이번 개명이 실증한 무신호 통과를 막는다.
- 돌연변이: 소스 1건 경로 오타 주입 → 경고 검출 확인. 수정 후 재계수 174 불변 확인(대장 내용은 상위집합 관계라 동일해야 정상).

## 2. 작업 ② — 주석 꼬리 4곳 (문면 전속)

`ui_palette.gd` 3·5·11·15행(56색→60색·`master_60.gpl`·D10 v1.2) · `run_validators.gd:1039` PAL 주석. 에셋 인계 목록(IMPL-198 §4.1) 그대로.

## 3. 작업 ③ — 레이스 컨텍스트 층 패드 결선 (D09 v1.3 §1.3)

**정본이 갱신됐다 — 먼저 v1.3 §1.3을 읽어라.** 층위 우선 규칙이 명문화됐고(컨텍스트 층 우선·가려진 조작은 재배치 행 명시), 상세 정보가 재배치됐다(사용자 판정).

| 조작 | 키보드 | 패드 (v1.3 확정) |
|---|---|---|
| 릴 홀드 토글 (릴 1·2·3) | 1·2·3 | **X 홀드 + D패드 좌우 → A** (확정 기준값) |
| 리스핀 | R | **X** (단독 — 홀드 모디파이어와 판별 필요) |
| 스킬 슬롯 1~5 | F1~F5 | **RB 홀드 + D패드/A** (확정 기준값) |
| 차지 개입 | C | **Y** |
| 상세 정보(툴팁 고정) | T | **LB 홀드 + Y** (v1.3 재배치 행) |

- **조합 입력 처리 방식은 재량 · [가안] 등재** — 모디파이어 홀드 상태 기반 권고. 주의 2건: ⓐ **X 단독(리스핀) vs X 홀드(토글 모디파이어) 판별** — 릴리스 시점 판정 등 방식 자체가 [가안] 사안 ⓑ LB 홀드+Y 와 Y 단독(차지)의 판별 동형.
- 기존 키보드 경로(1·2·3·R·F1~F5·C·T) 거동 무변경 계약 — 6차 전환과 같은 원칙.
- 개입 창 봉인·10초 성립(D09 §1.4)과 간섭 금지 — 입력 판독 층만 추가.
- 회귀: 액션/조합 경로별 도달 단언 + 키보드 보존 + **X 단독·X 홀드 판별 축** + 돌연변이. 실기 감각(홀드 타이밍)은 주력 후속.

## 4. 완료 기준

게이트 전건 통과(PAL 174 불변) + 회귀 명세 + 돌연변이 반증 + [가안] impl_log 등재 + 회신.
