# [총괄 → 4레인] 주력 10차 + 에셋 SFX 소회차 일괄 처리 — 판정 8건 · 차단 발견 1건 · 규약 보완

> 작성: 총괄 (remote-hq 세션) · 2026-08-20 · IMPL-263 · 마스터플랜 v2.65
> 대상: `MS3_주력레인_10차_완료회신.md` (IMPL-258~259) + `에셋트랙_SFX소회차_회신.md` (IMPL-262)
> **주력 최대 발견 = 막 VN 표시 소비부 부재(차단 — T7 75라인이 게임 도달 불가) → 원격 16차 예약. SFX = 검사 전층 차단형 완결·일괄 청취만 잔여.**

---

## 1. 검수 — 전건 재현

| 검사 | 결과 |
|---|---|
| 게이트 총괄 독립 재주행 | `AUD PASS 821`(차단) · `VALIDATORS_PASS warnings=0` · `COMPILE 91` · `AUDIO_ASSET 350` · **`UI_SCREENS 190`**(⑯축) · `TESTS suites=15` · `SFX_GEN 68` — 양 회신 보고와 일치 |
| 에셋 실물 | `se_r04` = **5,668바이트 불변 · sha `6f3e9d2dcedf`**(회신 표기 그대로 — 피치 운동만 변경) · `_fail("AUD"` **15곳** · `deadParams` 실재 |
| 주력 실물 | `tutorial_overlay.gd` `_safe_area`·`update_minimum_size` 실독 — 예약 영역·캐시 무효화 교정 실재 |

## 2. 판정 — 주력 10차 6건

| # | 사안 | 판정 |
|---|---|---|
| ① | **막 VN 표시 소비부 부재 (차단)** | **원격 16차 배정 (15차 회신 후 즉시 — 최우선).** `pending_act_vn`·`entry["lines"]` production 소비 0 — T7 75라인이 게임 도달 불가. 결선 요건: NAR-01 페이로드에 lines(라인별 화자)·**tone 필수 동반**(§2 주의 — 미전달 시 tense 3건이 조용히 BGM-09) · 발화 위치 = D08 §8.1 공표 슬롯. 주력이 손대지 않은 것(다중 세션 규약 4 — run_session 소관) = 정확 |
| ② | 1·2막 동시 래치 | **1막 = 커리어 개시 최초 브리핑 슬롯 발화 (사용자 승인 2026-08-20).** 근거 = D04 §1.2 "1막 = 게임 시작" + D08 §8.1 공표 위치("~차기 투어 브리핑 슬롯" — 게임 시작 = 최초 브리핑 슬롯 실재). 래치 구조(투어 경계 전속)는 달성형 마일스톤(2~4막) 전용으로 유지 — 1막은 달성 개념이 없는 개시. **①과 한 벌로 원격 16차** |
| ③ | `attr_hazard_16` 재작화 | **추인 확정 — 이월 소멸.** "깨진 노면" 단일 해석 수렴·x3 실치수 유지. 부기 2건(유일 면 채움의 문법 이질감·`attr_straight_16` 삼각 실루엣의 '경고' 선독 여지) = **비차단 관측 등재**(MS-5 실플레이 관측 대장) |
| ④ | 저장 표시 회전 — 도상 의미 반전 | **회전 대칭 스피너 변형 발주 (사용자 승인 2026-08-20).** `sys_save` 회전용 변형 1종(고리/분절원 계열 — 형태 = 에셋 실물 소관) — 정본 문면("저장 아이콘 + 회전")은 무접촉: 아이콘 정지 표기·회전체 변형의 분리 또는 대칭화는 실물 재량. **에셋 차기 배정.** 시간 감각(2.0s)은 실물 빌드 이월 유지 |
| ⑤ | `_resolve_tone()` 공란 슬롯 가드 | **원격 몫**(원격 파일·한 줄) — 16차 벽두 |
| ⑥ | 툴팁 세로 상한 800자 | **조치 불요 확인** — 실 문안 최장 2자와 두 자릿수 차이. 상한 기록만(MS-5 관측 대장) |

## 3. 판정 — 에셋 소회차 2건

| # | 사안 | 판정 |
|---|---|---|
| ⑦ | `se_r04` 0.74 가감(발주 예시 0.80 대신) | **승인.** "계열 동일 = 값의 일치가 아니라 소리 안의 같은 자리"(r05 발화 지점 49.1% ↔ 49.3% 정합) — D11 §2.2 문면의 정확한 독해. 포락선 무접촉(길이·피크 불변 실측 확인) |
| ⑧ | `deadParams` 차단 전환 | **승인 — 조건 2건 명문 채택.** ⓐ **차단 대상 = 빌드가 아니라 생성**(생성기 층 차단 — 대장·검증기 열거에서 성격 분리 기재) ⓑ **벤더 갱신 시 발화 시점 공식 재확인 = 절차 조건**(공식이 `vendor/sfxr.js` 산술의 복제 — 갱신 시 어긋나면 정상 파라미터가 생성을 막는다). 대장 §7 전사 지시. 확장 5축 불채택("주체가 0건인 규칙을 지금 세우면 검사만 커지고 지키는 것이 없다") = 승인·기록 |

## 4. 접수·기록

- **주력 콜아웃 교정** — 결함의 재규정("IMPL-138 규칙은 지켜지고 있었다 — 깨진 것은 반대편을 어디로 잡는가")·예약 영역 좌표 무기입(실 rect 독취)·**최소 크기 캐시 함정**(`get_combined_minimum_size`가 폭 0 시점 값을 캐시 — `update_minimum_size()` 명시 무효화) 함정 등재 · UISCR ⑯축("앵커 단언이 아니라 실 rect 교집합 — 겹침 면적 0이 계약") · **M3 미검출의 정직한 기록**("검사의 눈이 아니라 가드의 정당성 — 등가 프로그램") — 전건 기록.
- **VN 75라인 통독 전건 통과** — 폭·줄·문면·지문 공란·화자 교대·큐음 오배정 0·tone 일치. 대사창 620px = 정본 기수용 판단(D09 504행 실독 — 표시 층 클램프 불가 판단 정확).
- **헤드리스 CLI × GDAI 오토로드 비양립이 `--script`에도 적용** — 신규 함정. **CLAUDE.md 주력 절 부기 집행**(본 회차 총괄).
- **IMPL 충돌 8호 회피** — "충돌은 부여 시점이 아니라 푸시 시점에 확정 — 긴 회차는 푸시 직전 재확인" = **규약 2항 보완 문면으로 채택·CLAUDE.md 집행**(7·8호 두 번 재현으로 문면화 기준 충족).
- 에셋 잔여 표의 "폰트 8px 원도 미유입" = **낡은 문면** — 폰트 축은 결정 #18(IMPL-241)로 신규 발주 없이 종결. 다음 에셋 회차에 대장·잔여 표 교정 지시.
- 도구 한계 명기 4건(홀드 액션 시뮬 불가 → 패드 이월 합류·모달 실기 미확보·저장 스크린샷 필름스트립 대체·오토로드 비양립) 접수.

## 5. 라우팅

| 레인 | 몫 |
|---|---|
| **원격 16차** (15차 회신 후 즉시) | ①막 VN 표시 소비부 결선(NAR-01 페이로드 — lines·tone 필수) ②1막 = 개시 슬롯 발화 ③`_resolve_tone` 공란 가드(벽두) |
| **에셋 차기** | ①`sys_save` 회전 대칭 스피너 변형 1종 발주(판정 ④) ②`deadParams` `_warn`→차단 flip + 조건 ⓑ 대장 §7 전사 ③잔여 표 폰트 문면 교정 |
| **사용자** | **68식 일괄 청취**(r04 0.74 수정본 포함 — 관측 2건: 루프 이음매 6식·`se_v01a` 노이즈 근사) — **T6 사운드 결선의 마지막 관문** |
| 총괄 (본 회차 집행) | CLAUDE.md 2건(규약 2항 푸시 직전 재확인·주력 절 `--script` 함정 부기) |

## 6. 상태

- **T7 잔여 = 소비부 한 겹**(원격 16차) + 선택지 편입(15차 진행 중) + 오버레이 씬(주력 11차 — 15·16차 후).
- **오디오 = 실물 68/68·검사 전층 차단(AUD 821·AUDIO-A 350·deadParams 전환 승인)** — 사용자 청취 관문만 잔여. 미유입 = 징글 5(즉시 가능)·BGM 18스템(도구 상이 — 기안 대기).
- 이월 유지: 패드 실기(홀드 축 합류)·저장 시간 감각·`speaker_filler_16`·CreditsValue·스킬 소비(TL-5)·[가안] 대장.
