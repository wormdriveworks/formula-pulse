# [총괄 → 4레인] 내러티브 1차(T7 집필 납품) 처리 — 검수 전건 재현 · 판정 4건 · 정본 2종 발행 · 편입 회차 발주

> 작성: 총괄 (remote-hq 세션) · 2026-08-19 · IMPL-249 · 마스터플랜 v2.60
> 대상: `MS3_내러티브레인_1차_완료회신.md` (IMPL-247~248) · 납품서 = `T7_서사납품서.md`
> **T7 집필 = 납품 성립. D13 v1.10(재회 임계 2/3/4 — 결정 #19)·D12 v1.3(tone 태그 — P-2 ㉛ 집행) 동시 발행. 편입 회차 = 원격 단일 발주.**

---

## 1. 검수 — 전건 재현

| 검사 | 결과 |
|---|---|
| 게이트 재주행 | `COMPILE 91` · `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=14` — 일치 |
| 편입분 실독 | diff 정확 6행(튜토리얼 5 값만 교체 + `resonance03` 인용 복원 — 값 무변경 확인) · 전 파일 **필드 초과 0 재현**(파이썬 csv 파서 독립 주사) · 폭 재계산 5키 전건 일치(23.0/19.0/20.5/20.0/21.5) |
| 납품서 VN 75행 | 표 셀 정위치 파싱 — **표기 폭 vs 총괄 재계산 불일치 0 · 24폭 초과 0** · 키 V8 정규식 fail 0 |
| 도출 정본 대조 | Q1: D04 §4.2 "회피→대면→응어리→화해/결착" + `achievements.csv:22` threshold=3 실독 — **양단 고정·대면=단계 1 성립** / Q3: D08 §8.1 "이층 처리 규칙 (결정 #6 — 확정)" 실독 — **규격 기확정 주장 정확** / 질문4 전제: D13 별첨A §5.2 = 3행(재회 부재)·계승 행 "비앙카/**셀린**" 실독 — **마로 행 즉시 편입 가능 정확** |

## 2. 판정 4건 (사용자 승인 2026-08-19 — 권고 전건 채택 포함)

| # | 사안 | 판정 |
|---|---|---|
| ① | 재회 임계 threshold2·3 (질문 4) | **안 A 채택 — 2 → 3 → 4 (결정 #19).** D13 별첨A §5.2 재회 행 신설·본문 판번 동기 → **D13 v1.10 발행**(대장 95항 불변 — 전수 대장 대상 외). threshold1=2 유일 해 도출·"임의 기입하지 않았다" 승인 |
| ② | 신설안 C — VN 라인별 화자 | **승인.** 정본 3개소(D04 §5.2 지문·D09 별첨A §A-19 좌/우 2인·D11 §2.10 베인 전용 큐음)가 복수 화자를 전제하므로 페이로드 단위 화자는 정본 미달 — 납품 6건 전부가 표현 불가라는 발동 조건 실증. 하위 호환 설계(문자열 항목 현행 해석)·`_speaker_is_vane` 라인 단위 이동(마르타 대사에 베인 3단계 큐음이 나는 현행 = D11 §2.10 위반의 교정) 포함 승인. 선택지 오버레이 범위 밖 유지 — 차기 발주 |
| ③ | `strings.csv` 필드 수 검사 (보고 ③) | **신설 승인 · 차단형 즉시 발동.** 기계 판정(필드 수 = 헤더 수 산술 비교)·오검출 여지 0·위반 0 실측(복원 완료) — PAL 선례(조건부 승인→위반 0 실측→발동)의 조건이 신설 시점에 전부 충족. 검사 실재(정의·등록) UISCR 차단 축 동반(ANCH 전례). 검사명 = 구현 재량(권고: STRF) |
| ④ | 신설안 B — `tone` 열 발효 | **발효 — D12 v1.3 발행 (P-2 ㉛ 동시 집행).** §5.4에 VN 정조 태그 명시(`tone ∈ {calm, tense}`·대상 = VN 발생 단위 표·미지정 calm·V1 enum). 값 도메인 주의(데이터 = calm/tense — sound_map 조립 결속) 문안 그대로 채택. 신설안 A(이벤트 앵커 2열)는 **발동 조건 미충족 유지**(본 납품에 앵커 요구 이벤트 0) |

**인지 처리 3건** — ⑤ 막 건너뜀 = **수용 확정**(사용자): `act` = 래치 마일스톤 막 번호 최대값·단조 증가, 건너뛴 막 전이 VN 미발생은 극단 상위 플레이의 예외 경로로 수용 — **MS-5 관측 대장 등재**(실플레이 도달 빈도 관측) / ④ 전제 낡음 연쇄 = **검증 문화 승격: "구현 주석의 [가안]·미결선 표기는 정본의 공백을 증명하지 않는다 — 주석이 인용한 조항을 직접 읽어야 한다. '구현이 미결선'과 '규격이 미결'은 다르다."** 3층 연쇄(발주→계약서→집필이 각자 앞 층의 낡은 전제를 적발)가 인계 사슬 자체의 검증 필요를 실증 / ⑥ tone 귀결 = ④로 해소.

## 3. 접수·기록

- **문안 설계 판단 4건**(화자 3자 고정·베인 1인칭 공백·4막 2건 분할·1줄+콤마 0) — 전건 구속의 귀결로 승인. 특히 ②(1인칭을 비워 단계 3중 집필 없이 D04 §5.4 성립)는 **분량 ×3을 규격 준수로 대체한 설계**로 기록.
- **`resonance03` 절단 결함** — 본 회차 이전 유일 위반의 발견·복원·되읽기 실증("런타임 되읽기만이 증거"의 CSV 층 적용 — IMPL-246 ④ 문화의 즉시 전파 사례).
- **편입 선행 금지 구속**(§6 — 관계 축 행을 트리거보다 먼저 넣으면 `pending_achievements()` 계수가 죽는다) — 납품이 행을 선행 편입하지 않은 판단 승인. 편입 회차 순서 구속으로 승계.
- CLAUDE.md 내러티브 레인 등재(레인 목록·소유 경로·차수 계열 분리) — **추인**.
- 에셋 IMPL-243→245 재번호 = 미집행 유지(에셋 차기 회차 벽두 몫).

## 4. 편입 회차 발주 — 원격 레인 단일 (내러티브 §6 순서 구속 승계)

| 순 | 작업 | 근거 |
|---|---|---|
| 1 | `strings.csv`에 VN 75키 + 화자 3키 전사 (납품서 값 그대로 — 콤마 0·1줄) | 즉시 가능 |
| 2 | `vn_screen.gd` 페이로드 계약 확장(`{speaker_key, text_key}` 사전 허용·하위 호환) + **큐음 라인 단위화** | 판정 ② |
| 3 | 막 VN 6건 발생 결선 — 막 전이 래치(투어 경계·D08 §8.1) → VN 호출 · `run_session.gd:90` `act` 조회를 래치 최대값으로 교체 | 규격 기확정·수용 확정 ⑤ |
| 4 | `tone` 열 유입(`vn_slots`·`milestone_vn` — calm/tense·V1 enum) + `vn_screen.gd` 표 조회 결선 | D12 v1.3 |
| 5 | `relation_axes.csv` 마로 행 + `ui.relation.successionMaro` + **트리거 결선 동일 회차**(선착·듀얼 +1 = 기존 2단 구조 · 보조 = `event_number_two_check` 발생 +1) | §6 구속 |
| 6 | 재회 행(**2,3,4**) + `ui.relation.reunion` + 트리거 결선(알타 리지 투어 내 체인 비트 발생 +1·발생=도달·재열람 불계수·투어당 상한 2). **비트 발생 단위의 표 설계는 착수 전 판단이 갈리면 보고** — `milestone_vn` 4행의 성격(슬롯 종류 축)을 바꾸는 설계는 총괄 경유 | 결정 #19·§3-Q2 |
| 7 | CG-02 갈래 — `l3_reunion_for()` 신설(순수 static·듀얼 결판 ∧ 카이 ∧ `relation_stage>=1`) + `_l3_encounter_id()` 폴백 사슬 편입 · 반환 `cg_02_reunion` | Q1 확정 |
| 8 | `strings.csv` 필드 수 검사 **차단형 신설** + 검사 실재 UISCR 축 + 돌연변이 | 판정 ③ |

전 단계 완료 후 게이트 전량 + 돌연변이. 튜토리얼 콜아웃 상단 스트립 겹침 재배치(주력 3차 접수분 — 문안 유입 시 동시 처리 예고)는 **주력 몫으로 이월 유지**(씬 소유권).

## 5. 상태

- **T7 축: 집필 완료 → 편입 회차 1개 잔여.** 유입 대기 축 중 서사가 데이터로 내려왔다 — 남은 유입 대기 = 사운드 잔여 60식(파일럿 귀 판정 대기)·BGM/징글(후속 기안).
- 정본 현행: D12 **v1.3** · D13 **v1.10**(+별첨A) · P-2 잔여 **22건**(㉛ 집행).
- 차기 발주 예고(내러티브): **선택지 문안 + 오버레이 골격 활성화** 한 벌 — 편입 회차 완료 후.
