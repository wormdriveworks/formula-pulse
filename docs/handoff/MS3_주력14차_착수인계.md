# [총괄 → 주력] MS-3 주력 14차 착수 인계 — 8건 대장 (MS-3 마지막 씬 층 회차)

> 작성: 총괄 (remote-hq 세션) · 2026-08-25 · IMPL-428 · 마스터플랜 v3.15
> 전제: **원격 28차·내러티브 10차·에셋 전 트랙 대기 전환 완료** — 잔여가 전부 주력 몫이다.
> 시작 전: `git pull` → LFS 확인 → 캐시 재생성(에디터 켜져 있으면 GDAI `scan()` — CLAUDE.md 규약). **원격 26~28차에 신규 class_name 다수**(CgCutIn 등) — 캐시 미재생성 시 파스 에러 위장(IMPL-168 형태).

---

## 대장 8건

| # | 몫 | 내용 · 근거 |
|---|---|---|
| ① | **거부 고지 슬롯 신설** | 로그 존 잠정 수용분(고지 8종 — `race_screen.gd _notify_skill_rejected`)을 전용 슬롯으로. 최소 100px(ja 실측 최장 112px — 내러티브 10차 §2.2 표 참조) → 슬롯 폭 확정 시 자수 규칙 판정을 총괄에 회신 |
| ② | **씬 uid 일괄 정규화** | 13차 승인분(㉗) — 17개 씬. 에디터 재정규화 커밋 |
| ③ | **언어 전환 재문면화** | 옵션 화면 언어 항목 — 13차 이월 |
| ④ | **SH3 신 후보 전용 버튼** | `ui.race.snapshotKeepNew` 소비(현재 유일 V6 경고 — 총괄 판정: 대기 유지·버튼이 서면 라벨이 된다). "신 후보 유지 = 그냥 확정"의 병치 구조는 유지하되 라벨을 걸 노드를 세움. 무산 판단이 서면 사유와 함께 회신(그때 키 삭제 판정) |
| ⑤ | **화면 층 제원표 반영** | `에셋트랙_화면층결선_제원표.md` — 9패치 `texture_margin_*` 16종·**예외 2건 주의**(`tab_9p` 아래 변 개방·`focus_ring_9p` 코너 꺾쇠 = 결함 아님)·연출 배치 제약 2건·배경 레이어 순서 |
| ⑥ | **캡슐 규격 콘솔 확인** | Steamworks 콘솔에서 스토어 소재 요구 목록 대조 — `460×215` 헤더 캡슐 존재 여부(에셋의 미확인 기억). 정본(D15·대장 7종)과 갈리면 총괄 판정으로 |
| ⑦ | **액션 경로 전수** | `_on_respin` 형태(키·패드 액션이 버튼 `disabled`를 우회) — 다른 액션 전부에 같은 형태가 있는지 화면 층 전수(원격 27차 보고 ③·28차 부기 "limit_hold 고지로 표면화가 늦춰졌을 뿐 우회는 남아 있다") |
| ⑧ | **E08 액션 행 폭 실기 확인** | 내러티브 10차 관측 4.1 — **en 여유 29px(배지 2 포함)·버튼 8개 측당 1.8px.** 기본 테마 Button 좌우 content margin 실측 1회로 판정. 넘치면: 최저가 지렛대 = `ui.race.chargeIntervene` 영문(97px·D02 미구속 재량 번역) 후보를 내러티브에 발주(임의 수정 금지 — 감수 경유 문면). 부수 산술: 잠금 3 + 배지 2 조합 여유 14px — 도달성 미확인이니 실기에서 함께 볼 것 |

## 부수 (재량·예고)

- **CG 컷인 노드**: 컷인·장면 CG는 코드 생성 Control(씬 무접촉 — 원격 28차 §7). 전용 노드로 옮기려면 호출부(`_show_cg_cutin`·`_mount_cg`) 하나씩 — 재량.
- **스크린샷 5매 캡처**(스토어 — RACE-01 ×2·정산듀얼·VN·개러지)는 14차 뒤 별도 처리 가능. `store/screenshots/` 자유 배치.
- **실물 플레이 검증 회차 관측 대장 예고**: 마일스톤 시즌 상한 소비(8/15) · 컷인 2.5초 체감(진행 가림 여부 — 입력은 `MOUSE_FILTER_IGNORE`로 열림) · 패드 실기 체감(스틱 L/R 표식 포함).

## 규약 리마인더

IMPL ID = 매 커밋 직전 최대값+1 재확인(현재 428) · 명시 경로 스테이징 전속 · 공유 파일 `git checkout` 금지(**28차 실사고 — 자기 미커밋 편집도 함께 지운다**) · 기계 검증은 GDAI 오토로드 제거 상태에서(IMPL-259).
