# [원격 → 주력·총괄] MS-3 T4 회신 — 업적 데이터·마일스톤·통산 지표 결선 (원격 몫)

> 발신: 원격 헤드리스 레인 · 2026-08-16
> 근거: `prompts/MS3_kickoff.md` T4 (D09 §8.1 · D04) · 결정 로그 = **IMPL-113**
> 게이트: `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=10` (TC-C **1949** · TC-O **353**)

---

## 1. 결선된 것 (원격 = 데이터·판정 층)

| # | 내용 | 정본 |
|---|---|---|
| ① | **업적 39종** `achievements.csv` — 카테고리 6 / 조건 3형식 / 비노출(H) 9종 / 판정 소스 5계. **무보상 명예형 = 보상 열 자체 부재** | D08 §8.11 · D07 §7.1 |
| ② | **마일스톤 15종** `milestones.csv` + **기록 경로 신설** — GP·투어·시즌 결산에서 데이터 조건 판정 | D08 §8.2 |
| ③ | **통산 지표** — D07 §6.2 확정 6종 + 업적 소재(무트러블 GP·무개입 우승·역전 우승·서킷 제패 집합·네임드 선착 누계) | D07 §6.2 |
| ④ | **엔진 GP 요약 계수 5종** — duel_wins·trouble_turns·hold_uses·chance_three_matches·final_lap_entry_rank (엔진은 계수만, 업적 규칙은 아웃게임 전속) | D12 §2.1 혼입 0 |
| ⑤ | 세션 결선 — 경계마다 기록·판정 + `newly_achieved` 노출 (표시 층 고지용) | D09 §5.5 |

## 2. ⚠ 선재 결함 1건 해소 — 스킬 티어 2·3 영구 잠김

`milestones` 딕셔너리에 **소비부(`skill_tier_open`)만 있고 기록 주체가 0건**이었습니다. 즉 첫 포디움·첫 우승이 아무 데서도 기록되지 않아 **스킬 티어 2·3이 구조적으로 열리지 않는 상태**였습니다(HUB-05 스킬 화면에서 티어 2 이상이 영구 잠김). T4의 마일스톤 기록 경로가 이 구멍을 닫습니다 — 주력 레인에서 **HUB-05 티어 개방 실기 확인**을 권합니다(첫 포디움 후 티어 2 카드가 열리는지).

## 3. 주력 레인 몫 (T4 잔여)

1. **SYS-04 업적 화면** — 코어 API 준비 완료:
   - 목록 = `session.data.achievements` (행: category·condition_type·hidden·name_key)
   - 달성 여부 = `session.outgame.achievements.has(id)` · 신규 달성 = `session.newly_achieved`
   - 히든 규격 = `hidden == 1`이면 달성 전 명칭·조건 비노출 (D07 §7.1 · D09 §5.5 Steam 히든 1:1)
   - 5카테고리 탭 + 발견형 처리는 §4 보고 판정 후 확정 권장
2. **TUT-01 튜토리얼** — 원격 미착수 (D04 트랙 의존).
3. **L3 조우 3종 통지 결선** — 발견형 업적은 `outgame.record_discovery("cg_01_throne" | "cg_02_reunion" | "cg_03_kinship")` 호출로 달성됩니다. L3 컷인 재생(T6)에서 1줄 연결하면 됩니다.

## 4. 총괄 판정 대기 (비차단)

1. **D08 §8.11 계수 불일치** — 헤딩 "총 35종" vs 표 열거 **39종**(5카테고리 35 + 발견형 4). 발견형 4종이 5카테고리 어디에도 들어갈 자리가 없습니다(각 괄호 수치와 행 내용이 정확히 일치). **열거를 실체로 보아 39종 전량 등재**했습니다.
2. **발견형의 카테고리 귀속 [가안]** — D07 §7.1은 발견형을 '조건 유형'으로 규정하는데 D08 표는 카테고리 열에 별도 행으로 열거합니다. 6번째 카테고리로 다뤘습니다.

## 5. 판정 소스 미결선 2종 (T7 의존 — 보고)

관계 축 5종(D07 §5.5) 중 **재회(카이)·계승 2번째 축(셀린)이 `relation_axes.csv`에 부재**(현행 3축)라 해당 업적 2종이 판정 불가입니다. 재회 축은 형식 A(VN 체인)라 D04 서사 유입(T7) 의존입니다. 조용한 영구 미달성을 막기 위해 `pending_achievements()`가 이 2종을 계수하고 검사가 수치를 못박습니다 — 축이 유입되면 자동으로 판정 대상이 됩니다.

## 6. 검증

돌연변이 8종 전건 검출 (순위 임계 완화·리타이어 완주 인정·서킷 제패 중복 계수·업적 1회 발화 가드 제거·직렬화 누락·라이벌 파일 축 중복 계수·히든 플래그 변조·엔진 홀드 계수 미기록).

## 7. 원격 레인 잔여

T1~T4(데이터 몫) 종결. 다음 = **T9 TL-5 러너**(모델 유효성 ±15% — 무대 2~5 유입으로 전제 성립).
