# [총괄 → 4레인] VN 결선 사슬 일괄 — 감광 종결·17장면 바탕·사샤 장소/개작 판정·스탠딩 발주

> 작성: 총괄 (remote-hq 세션) · 2026-08-26 · IMPL-473 · 마스터플랜 v3.26
> 대상: 원격 IMPL-469(감광)·472(바탕 결선) · 내러티브 IMPL-470(매핑)·471(개작 기입) · 충돌 15호
> 총괄 재주행: `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=20`(SCENE 389·UISCR 736).

---

## 1. 승인 — 전건

- **㊲ 감광 종결(469)** — **"없는 값을 채우지 않은 것이 틀린 값을 막았다"**: 29차가 빌리지 않은 0.5가 실기 실측에서 가독 바닥 미달 오답으로 판명 — 불변규칙 2의 실증 회수. W4 이중 교정(**"갈아 끼울 대상은 소비부가 실제로 쥔 것이어야 한다"** — 다른 인스턴스를 간 픽스처의 무동작이 통과로 위장 = 3형 ①의 변종) 등재.
- **매핑 스펙(470)** — 17장면 전건·무배경 0("무배경을 쓸 자리가 없었던 것은 우연이 아니다" — D04 지문 규정의 귀결·"집필 시점에 문면에 적혀 있던 것을 표로 옮겼다")·`vn_id` 비안정 함정 선명기.
- **바탕 결선(472)** — 두 겹 방어(페이로드 실체 id + **표 선언 접두** — "코드에 적으면 검사가 보지 못한다")·층 단언의 리터럴→관계 전환("층이 늘 때마다 거짓이 되는 형태")·계수 핀 산식 명기(9+1)·관측 3건 수용(감광 전용 거부 — "옮기면 근거 없는 전용·필요하면 값 부재 사안이라 중단·보고").
- **사샤 개작 기입(471)** — 개작 전 같은 VN 9행 재독의 발견("9행은 개러지에 서 있었고 beat01만 다른 곳을 가리켰다 — **충돌은 한 VN 안에도 있었다**")·A안 논거(CG '따뜻한 작업등'을 문면이 같은 말로 받음 — "고치는 자리에서 둘을 붙여 둔다"·오스카 평행-변주·B안 상한 동률 자기 비권고).

## 2. 사용자 판정 2건 (2026-08-26)

| # | 사안 | 판정 |
|---|---|---|
| ① | 사샤 복귀 장소 (문면 "피트 앞" vs A-CG-05 "개러지" — 정본 무근거 충돌·불변규칙 9) | **ⓐ 문면을 CG에** — crew_sasha 매핑 = `garage_h`(개러지 10·패독 5·트랙사이드 2 = 17 폐합) |
| ② | 개작안 | **A안** — "개러지의 작업등 아래. 낯익은 얼굴이 서 있다."(3언어·신규 한자 業 1·533·여유 31) |

## 3. 충돌 15호 · 발주

- **충돌 15호(471 중복 → 원격 472 양보)** — 신형 기록: **"같은 판정에서 나온 양쪽이 동시에 번호를 잡았다"**(문면과 소비부가 한 판정의 두 몫 — 시각 겹침이 구조적). 로그 순서 판정·자체 발견·회신문 참조 0 확인.
- **화자 스탠딩 발주 = 내러티브** — `vn_speakers.csv` 스펙: `speaker_key → char_id`(대장 §127 전사·crew/ai 접두·speakerNarration = 행 없음 — "없는 것이 정상" 계약) + **`side` = 새 판단**(화자 고정/장면별·근거 명기). 원격 소비 계약 기전달(씬 무접촉 — StandingLeft/Right 기존재·텍스처만).

## 4. 상태

- **MS-3 능동 잔여 = 스탠딩(내러티브 스펙 → 원격 소비) + 주력 스크린샷 4번.** 그 뒤 **실물 플레이 검증**(관측 15항 + 정산 캡처 + [가안] 3건 눈 판정) → G-6·G-7 → 마감 대조.
- 사용자 트랙 = 상표 1군 법률 검토 발주 · 실플레이 2~4막.
