# [원격 → 주력] MS-3 T2 완료 회신 — 소모품 사용 경로 결선

> 발신: 원격 헤드리스 레인 · 2026-08-16
> 근거: `prompts/MS3_kickoff.md` T2 (D06 §3.5) · 결정 로그 = **IMPL-111**
> 게이트 스냅샷: 커밋 시점 로그 참조 — `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=10` (TC-C **1910** · TC-P **139**)

---

## 1. 결선된 것 (원격 몫)

| # | 내용 | 정본 |
|---|---|---|
| ① | **엔진 사용 API** `RaceEngine.use_consumable(id) -> Array` — T1 섹터 개시 전속(R-B), 듀얼 턴 제외, 거부 = 빈 배열 + 상태 무변경. 성공 시 `raceLog.consumableUse01` 이벤트 반환 | D06 §3.5 · D05 §2.3 무개정 |
| ② | **P1~P3 효과** — P1 +15 즉시(최대치 절단) · P2 +8 + 다음 트러블 1회 소모 반감(−50%) · P3 잔여 구간 섀시 소모 −20% (해저드 턴당·트러블·듀얼 실패 페널티 3경로) | D07 §3.1 · D13 §3.6 |
| ③ | **R5 반입/이월** — begin_gp 주입 · close_gp 잔여 회수. 인벤토리 정본 = `OutgameState.consumables` (섀시 이월과 동형 구조) | D06 §3.5 R5 |
| ④ | 엔진 직렬화 3필드 편입 (구스냅샷 = 빈 인벤토리·무효과) + 세션 왕복 검사 | D12 §7 |

## 2. 주력 레인으로 넘어가는 것 (T2 협업분)

1. **RACE-01 E13 사용 UI 결선** — 별첨A §A-4 E13 "T1에만 활성". 엔진 쪽 준비 완료:
   - 재고 조회 = `engine.consumables_held` (id → 수량) · 사용 = `engine.use_consumable(id)` (반환 이벤트를 `_push_events`로 흘리면 로그 결선)
   - 활성 판정 = 반환값이 아니라 사전 가드로 하려면: T1 여부는 `engine.turn_phase == T1_SECTOR_OPEN and not engine.current_turn_is_duel`, 재고는 `consumables_held`
   - 지속 효과 표시 소재: `engine.trouble_shield_charges`(P2 잔여) · `engine.wear_reduction`(P3 활성 = 0.2)
2. **RUN-01 E03 소모품 보충 UI** (T5 묶음) — 구매 코어는 기존 `outgame.buy_consumable()` (상한 2 가드 내장). TL-2 잔여 "RUN-01 E03/E04" 중 E03의 코어 측 선행 조건이 이것으로 전부 성립.

## 3. 해석·[가안] (IMPL-111 등재 — 총괄 확인 대상)

1. 듀얼 턴의 T1 사용 제외 — R-B 문면 "T1 **섹터 개시**" 준거 (듀얼 턴 = 전용 스핀).
2. P2 실드 = 사용 횟수만큼 적립 [가안] (각 사용이 각 1회 트러블을 보호).
3. P3 중복 사용 = 허용·비누적 [가안] — UI 층에서 중복 안내 권장.
4. P3 경감 대상 = 인레이스 섀시 감소 전 경로 [가안] (문면이 소모원을 한정하지 않음).

## 4. 검증

돌연변이 7종 전건 검출 (T1 가드 제거·실드 소진 제거·해저드 경감 미적용·재고 감산 제거·직렬화 누락·세션 회수 누락·반감 계수 변조). 하한 상향: TC-C 1862→1910 · TC-P 136→139.

## 5. 다음

원격 레인은 T3 (HUB-08 오버홀 후보 추첨 — 시즌 결산 층·추첨 스트림, D07 §7 · D12 §6)로 진행.
