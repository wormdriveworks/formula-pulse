# [총괄 → 4레인] 주력 16차 — 스토어 13/13 승인 · **실물 플레이 검증 종결(전항 적합)** · 판정 4건 · 잔여 = ㊸ 에셋 + ㊹ 원격 → G-6·G-7

> 작성: 총괄 (remote-hq 세션) · 2026-08-28 · IMPL-481 · 마스터플랜 v3.28
> 대상: `MS3_주력레인_16차_완료회신.md`(IMPL-478~480) + 실물 플레이 검증(사용자·주력 동반)
> 총괄 재주행: `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=20`(UISCR 751·SCENE 577) ·
> 실물 확인: vn3(얼굴 4개 실측)·스크린샷 4번(마르타 좌·테오 우)·5번(P3·+6·900Cr — 하단 여백 실측)·거울상 교정 diff.

---

## 1. 승인 — 전건

- **① VN 컷(478)** — *"막힌 것은 소비부가 아니라 하네스였다"* 자인 접수: 15차 상신 *"소비부 0건"* 의 절반은 표에 없는 이름(`vn_act1_open`)과 화자를 잃는 문자열 라인이었다. **검은 화면은 "부재의 그림"과 "잘못된 열쇠의 그림"을 구분해 주지 않는다** — 생산 경로 동형(`_act_vn_payload` 형태) 교정이 옳은 자리다. 조회처 합치기 한 함수("컷 계획이 데이터 구조를 따라 낡는다" 방지)까지 승인.
- **② ㊶ 정산 컷(478)** — 총괄 배정("실플레이 회차 겸행")을 **넘어선 것이 아니라 앞당긴 것**: 엔진 있는 문맥을 TL-5 러너 전사로 그 자리에서 만들었다(불변규칙 8의 귀결 — 전건 동기 호출). **"고정할 수 없는 값을 고정한 척하는 것이 더 나쁘다" — 값이 아니라 조건을 고정한다**(첫 시상대 정산·미달 = `STORESHOT_WARN` 고지) 등재. *"경로를 줄이면 실플레이가 아니다"*(GP만 이으면 20판 전건 미도달 — 빠진 것은 정책이 아니라 실플레이가 반드시 지나는 경로) 등재. 부수 관측(개막 GP = P12~P15 — 진행도 문제) 접수.
- **③ VN 4층 판독 8매(479)** — *"한 장으로는 판정할 수 없었다"*(한 원도의 우연한 겹침과 규칙 공백은 그림 한 장에서 같아 보인다 — 3장면 대조로 원도 1장의 규약 이탈로 확정) · *"라인을 뛰지 않고 첫 줄부터 흘린다"*(빈 좌측은 "규칙이 일하지 않았다"와 구분되지 않는다) 승인. 에셋 실물 무접촉(소유 경로 규약) 확인.
- **④ 거울상 교정(480)** — **"검사는 자기가 멈춘 지점 뒤를 보지 못한다"** 등재(35차 축은 베인 라인에서 `break` — 그 뒤에 무엇이 서는지 볼 수 없었다). 축을 먼저 세워 사전 FAIL을 받고 교정·반증 3/3. **자기 정정**(*"틀린 근거를 남기는 것이 근거를 안 남기는 것보다 나쁘다"* — queue_free 주석 교정)은 원격 475 ④의 같은 사실을 반대 방향에서 다시 잰 것으로, 두 레인의 결론이 합치한다(어느 해제든 슬롯에서 이미 빠졌으므로 그려지지 않는다).

## 2. 사용자 판정 4건 (2026-08-28 — 등재)

| # | 사안 | 판정 |
|---|---|---|
| ① | **실물 플레이 검증**(관측 대장 16항 + [가안] 눈 판정 3건) | **전항 적합 — 교정 없음. 검증 회차 종결.** [가안] 3건(hover 색·위험 배정·rival 결속) = **눈 폐문** — 폐문 문면(주석·대장)은 각 소유 레인이 다음 능동 회차에 겸행해 닫는다(IMPL-424 "문면은 손으로 닫아야 한다") |
| ② | ㊸ `cg05_sasha_return` 인물 중복(얼굴 4개) | **ⓑ 원도에서 초상 제거 — 에셋 트랙 배정.** 형제 규약("CG = 장소·사물, 인물은 스탠딩")과 판정 ①(해빙 차분 열기)을 양쪽 다 지킨다 |
| ③ | 베인 파형 VN 판독(밝은 바탕 앞 "스친 선") | **현행 유지** — 이월 대장에서도 내린다(사용자 실물 확인 경유) |
| ④ | 정산 화면 하단 여백(1080p 아래 60%) | **현행 유지** — D09 §5.1 규격 내·관측 기록만 남기고 종결 |

## 3. 배정 — 잔여 2건

| 레인 | 몫 |
|---|---|
| **에셋** | **㊸ 집행**: `cg05_sasha_return` 원도에서 사샤·마르타 초상 제거(개러지 장소·사물 구도로 — cg04·cg06 형제 규약 준수). 치수·팔레트·`.import` 규약 현행. 교체 후 판독 재확인은 표 대조 + 주력 재캡처(vn3 동일 컷) — 주력 대기 중이므로 재캡처는 다음 주력 가동에 겸행 |
| **원격** | **㊹ 집행**: 아카이브 재열람 문면 공백 — 고칠 자리는 `run_session.gd` 조립기(주력 소견 동의: 접미 규칙을 상수로 승격해 역방향 성립 + 재열람 페이로드 창구 하나 — "페이로드 조립기는 하나다"). **같은 결함의 두 번째 경로**(`season_open_payload` 전례)이므로 세 번째 경로가 없는지 페이로드 발신처 전수 확인 포함. rival [가안] 폐문 문면 겸행 |
| 주력 | **대기** — 에셋 교체 후 vn3 재캡처 1건 예약 |
| 내러티브 | 대기 유지 |

## 4. 상태 — **MS-3 실물 전량 + 검증 종결**

- **스토어 소재 13/13** · 실기 관측 대장 16항 **전항 적합** · [가안] 눈 판정 3건 폐문 · 게이트 녹색(suites=20·warnings=0).
- **잔여 사슬 = ㊸(에셋) + ㊹(원격) → G-6·G-7 실행 → MS-3 마감 대조.** 사용자 트랙 = 상표 1군 법률 검토 발주(EA 진입 조건).
- 이월 대장 정리: 베인 파형 판독·정산 여백 **내림**(본 회신 §2) · SC3 폐문 = 대상 지정 UI 회차 · AUDIO-W 표집 기록화 = 오디오 정비 회차 · ㊷ PAL `.tres` = 조건부 — 현행 유지.
