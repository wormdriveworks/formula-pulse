# [총괄 → 4레인] 원격 25차 + 에셋 표정 파일럿·전환 일괄 — 세이브 파괴 종결 · 마일스톤 VN = MS-3 편입

> 작성: 총괄 (remote-hq 세션) · 2026-08-23 · IMPL-366 · 마스터플랜 v2.94
> 대상: `MS3_원격레인_25차_완료회신.md`(IMPL-364~365) · `에셋트랙_표정차분_파일럿_회신.md`(IMPL-358) + 파생 전환 집행(IMPL-362) · `마일스톤VN_발화경로_실독보고.md`
> **차단급(세이브 파괴)이 세 겹으로 닫혔고, 마일스톤 VN 콘텐츠 공백이 MS-3 마지막 집필 축으로 편입됐다.**

---

## 1. 원격 25차 — 전건 승인

- **저장 격리 훅 = 차단급 종결.** 설계 핵("기본값을 두지 않았다" — 미설정 = 소리내어 실패) 승인 · **훅을 만든 뒤 실기 루트가 격리 루트를 덮는 누출을 스스로 발견**(J7 거동 축 — "소스 검사는 언제 효과가 나는지를 보지 못한다") · 세 겹(안 = 기본값 부재 / 중 = UISCR ㉑ 하네스 전수 / **밖 = 게이트 전후 실 파일 md5** — 피해 자체를 본다). 게이트가 "실 프로필 무접촉 (바이트 불변)"을 스스로 보고하는 형태가 완성형이다. **격리 관례(GODOT_USER_DIR)는 해제** — 훅이 대체한다.
- **좁은 슬롯 방법 교정 승인** — `custom_minimum_size`는 하한이지 상한이 아니다(초과해도 컨테이너가 안 자르면 절단 없음) → 판정에서 관측으로 강등. **"느슨한 판정으로 통과시키는 것보다 정직한 관측이 낫다"** 기록.
- **증보 처리 승인**: `JA_KANJI_EXPECTED` = 하한 전환("정확 핀은 표가 없던 시절의 트립와이어 — 지금은 공유 파일 마찰만 남긴다") · `already` 2분할 집행 · **침묵 3종 기계화**(`SEAL_SILENT_ERRORS` + 문면 키 부재 단언 — "문면이 유입되면 축이 실패해 판정을 다시 받는다"의 양방향 대장) · 스킬 문면 이월 판단("키가 다 모인 뒤 한 번에" — 19종 매핑 설계 선행) 승인.

## 2. 에셋 표정 — 파일럿 규율 접수·전환 확인

- **RD 기각 파일럿 접수** — $6.12 승인 아래에서 $1.08에 멈춘 판단("전량 집행 전에 멈춘 것이 이 파일럿의 값" — 채택률 1/2 × 34장 = $12.24 > 잔고 검산). **"RD는 새 도상에 강하고 계약에 약하다"의 4번째 확인**(9패치·글리프·오버레이·얼굴 레이어) — "레퍼런스와 비슷한 프레이밍과 픽셀 정합은 다른 명제" 기록.
- **파생+좌표 전환 = 사용자 기승인 집행 확인**(IMPL-362 — 로렌츠 4 유입·`shift` op "그리지 않고 옮긴다"·합성 이음선 0). 잔여 30(12인) 계속.
- **마젠타 번호(`ai_lorentz_base`) 교정 승인** — VH-8 전례·`pixel_patch` 색 치환·표정 회차와 병행.

## 3. 사용자 판정 3건 (2026-08-23)

| # | 사안 | 판정 |
|---|---|---|
| ① | 마일스톤 VN 문안 | **MS-3 편입 — 내러티브 발주** (권고 채택). 크루 합류 3인×2~3비트 + 기원 단서 2종 + 일반 성취 — 정본 명문 공백의 EA 전 충전 (스킬 소비부 전례) |
| ② | 막 전이 슬롯 | **A안 — 브리핑 유지** (IMPL-263 기승인 존치). `mvn_act_transition` 분류 = 투어 종료에 타 분류가 몰릴 때의 **서열 전용**으로 명문 — 결선 회차에 전사 |
| ③ | B급 경로·RD 잔고 | **수작업 유지** (RD 파일럿 불발동 — 잔고 $6.93 보존). B급 착수 시점·순서는 표정 30 완주 후 재론 |

## 4. 발주·배정

| 레인 | 몫 |
|---|---|
| **내러티브 8차** | **마일스톤 VN 집필** — ⓐ크루 합류 3인(마르타·테오·나디아? — D07 §5.2 대상 실독으로 확정) × 2~3비트 ⓑ기원 단서 잔여 2종(D04 §7 — 기존 `vn_origin`과 사슬 정합) ⓒ일반 성취(D04 §3 표준형~중량형 — 수량 재량·근거 명기) ⓓ**`already` 분할 2키 즉납**(`ui.race.skillRejectedAlreadyHold`/`AlreadyMod`) ⓔ친애 축(`relation_kinship`) 전이가 크루 합류 문안에 걸려 있음 — 전이 서사 포함. 전 키 3언어·한자 계수 명기·봉인. 표·결선 스펙은 재회/개막 전례(문안+스펙 회신·표 행은 원격) |
| **원격 26차** (문안 유입 후) | 마일스톤 VN 결선 — 발화 경로(투어 종료 슬롯)·우선순위 4단·이월(D08 §8.4 — 이제 실행 대상이 생긴다)·친애 축 전이 소비·형식 B 카운터 소비부 + **스킬 문면 소비부**(19종 매핑 — 25차 §6 입력 대장) |
| **주력 14차** (대기열) | 거부 고지 슬롯 신설(최소 100px·ja 99px) → 자수 규칙 판정 · 씬 uid 정규화(㉗ 승인분) · 언어 전환 재문면화 |
| **에셋** | 표정 30 계속 + 마젠타 번호 교정 편입. B급 = 수작업 유지(판정 ③ — RD 잔고 보존) |

## 5. 상태

- **차단급 잔여 0** — 세이브 파괴 종결로 실기·게이트 병존이 안전해졌다.
- **MS-3 잔여 지형**: 마일스톤 VN(집필→결선 — 마지막 콘텐츠 축) · 표정 30 · 스킬 문면 결선 · 주력 14차 소건 · 실물 플레이 검증 회차 · 스토어 준비물(착수 가능) · 상표 법률(사용자) · G-6·G-7(직전) · B급(수작업 — 시점 재론).
