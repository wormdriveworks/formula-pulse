# [총괄 → 4레인] 주력 17차 — 재검수 승인·차분 합성 교정 승인·`face_offset_y` 거처 추인 · **다음 = G-6·G-7 집행 준비 발주**

> 작성: 총괄 (remote-hq 세션) · 2026-08-30 · IMPL-494 · 마스터플랜 v3.34
> 대상: `MS3_주력레인_17차_완료회신.md`(IMPL-491~493 — a138bff)
> 총괄 재주행: `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=20`(UISCR 812) · vn3 갱신본 실물 육안(얼굴 2·차분 합성 정상·의자 무저촉) · `vn_speakers.csv` face_offset_y 열 실물 대조.

## 1. 승인 — 전건

- **① 재검수 3항 적합 + 1항 부적합의 가치** — 부적합을 돌려준 것이 이 재캡처의 존재 이유였다. **"결함이 결함을 가리고 있었다"**(구 cg05의 얼굴 4개 소음이 턱 잘린 차분을 차폐 소음으로 위장 — 원도를 고치자 남은 하나가 드러남) 등재. 부수 확인 2건(바탕 = 폴백 성격·**개작 문면과 그림의 붙음이 실물에서 성립** — A안 논거 "고치는 자리에서 둘을 붙여 둔다"의 실물 회수) 접수.
- **② 차분 = 얼굴 레이어 합성 교정** — D10 §3.1("얼굴 영역 레이어 교체·전신 재작화 금지")이 금지하던 방식을 초판이 쓰고 있었고, 35차 축이 접미 문자열만 봐서 못 본 사유("문자열은 무엇이 그려졌는지 말하지 않는다" — 측정 동일성 계보)까지 완결. **값을 만들지 않은 처리 승인**: x = 두 실물 폭에서 유도·y = 대장 실측값 전사(D10 §3.1의 "조정 창구 = 원화 실작업" 위임 — 팔레트 실색상과 같은 자리·불변규칙 2 무저촉). 실패 = 기본 폴백 + 시끄럽게(push_error + 접미 메타 축) · 반증 3/3.
- **③ 하네스 자기 교정** — "컷은 생산 경로를 지나야 한다"의 두 번째 자리(하네스가 자기 옛 입력을 증언). ㊹ 창구 호출 전환·vn8 실문안 실물 확인.
- **④ [가안] 폐문 2건** — 주력분 잔여 0. 근거를 폐문 조건에서 사용자 실물 판정으로 갈아 끼운 형태·판독 전제 정정 전사 확인.
- **⑤ 관측 등재** — "검사 수 하한은 `tests/` 디렉토리 내용의 함수다"(임시 스크립트 2개가 계수를 813으로 올림 — 하한 확정 전 임시 산출 제거·UISCR 하한 = 812 확정). 절차 주의로 전 레인 공유.

## 2. 추인 — `face_offset_y` 거처 = `vn_speakers.csv`

**추인.** 값이 인물 art의 성질이지만 ⓐ소비부가 VN 스탠딩 하나뿐이고 ⓑ화자 표가 13인을 함께 담는 유일한 인물 키 표다. **승격 조건 명문 승인**: 두 번째 소비부가 생기는 회차에 인물 표로 승격 — 그때까지 이 거처가 옳다("예외는 예외가 서는 자리에"의 표 판).

## 3. 발주 — G-6·G-7 집행 준비 (원격)

MS-3 잔여 = G-6·G-7 → 마감 대조. 양 게이트의 전제가 **빌드**이므로(D14 §5.6 "출시 후보 빌드"·§5.7 "스토어 제출물 성립") 집행 준비를 원격에 발주한다:

1. **익스포트 파이프라인**: Windows 데스크탑 export preset 성립(D12 §2.3 — 데스크탑 빌드에 광고 모듈 컴파일 자체 제외·Compatibility 렌더러). 헤드리스 export 가능성 실측(export template 유무 포함 — 불가 시 주력 몫으로 회신).
2. **G-6 준비**: ①정적 검사(디버그 표시 코드의 출시 빌드 제거 — 컴파일 플래그·심볼 스캔·TL-1 결속) 축 성립 ②TC-C11 하네스의 출시 후보 빌드 재실행 경로 ③D11 층위 원칙(정지 전 결과 암시음 0) 이벤트 발화 로그 대조 축. **판정 기준 = 3방법 전건 노출 0 · 위반 = SV-1 등록**(발신 경로 아님 — D14 명문).
3. **G-7 준비**: ①IAP·결제 SDK 심볼 0 스캔(데스크탑 빌드) ②스토어 결제 권한 선언 0 ③(모바일은 MS-3 범위 밖 — Steam 제출물 한정). **jouyou 익스포트 필터 = G-7 체크리스트 결속**(기존 이월 항목 소진).
4. 집행 결과는 D14 §5.8 실측 기록 양식(공란 내장)으로 회신.

## 4. 상태

- 스토어 소재 13/13(캡처 갱신 2매 — 위상 변동 비회귀 판단 승인) · [가안] 전 레인 잔여 = 내러티브 이월 1건뿐.
- **MS-3 사슬: G-6·G-7 집행(원격 준비 → 판정) → 마감 대조.** 사용자 몫 = KR 출원·외부 자문 발주서 발송(불변).
