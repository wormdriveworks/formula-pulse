# 눈 검수 대장 v1.0 — 에셋 트랙

> 작성: 에셋 트랙 (원격 세션) · 개설 2026-08-24 · **IMPL-394**
> 근거: 총괄 배정(IMPL-391) — *"눈 검수 백로그 착수. 플레이 화면에 가장 많이 노출되는 층(머신·프레임·연출)을 앞에 두는 것을 권고"*
> 도구: `tools/assets/review_sheet.js` (검수 시트 — 배율·배경·순서를 고정해 **회차 간 비교**가 성립하게 한다)

## 0. 이 대장이 세는 것

| 층 | 수량 | 상태 |
|---|---|---|
| 머신 | 37 | ✅ **1회차 완료** (§1) |
| 프레임 9패치 + 커서 | 16 | ✅ **2회차 완료** (§2) |
| 연출 요소 | 8 | ✅ **2회차 완료** (§3) |
| 무대 배경 | 10 | ✅ **3회차 완료** (§4)|
| 글리프 | 20 | ✅ **4회차 완료 · 5회차 교정** (§5·§8) |
| 캐릭터 스탠딩 | 13 | ✅ **4회차 완료 · 5회차 교정** (§6·§8) |
| 표정 차분 | 34 | ✅ **4회차 완료 · 파생 교정 동기** (§7·§8) |

> **검수 배경을 고정한 이유.** 투명 에셋은 **배경에 따라 다르게 보인다** — 밝은 배경에서 사라지는 획이 어두운 배경에서는 보인다. 시트 배경은 `#141822` 로 고정하고, 바꾸면 대장에 적는다.

---

## 1. 머신 37 — 1회차 (2026-08-24 · IMPL-394)

### 1.1 본 것

| 시트 | 대상 |
|---|---|
| 측면 베이스 | ax9 · gm12 · vh8 · boreas s1~s3 · priv_common (7) |
| 측면 리버리 합성 | 세컨드 드라이버 3(마로·비앙카·볼코바) + 팀 2(코멧웍스·실버트레일) |
| 추격 베이스 | 4팀 + boreas s1~s3 (7) |
| 보레아스 오버레이 | 리버리 3 · 윙 3단(전·후 동시) · 리버리+윙 동시 |
| **실기 합성** | 메트로 나이트 · 펄스 돔 배경 위에 4대 배치 |

> **오버레이는 단독으로 보지 않았다.** 투명 레이어를 그대로 보면 번호만 떠 있어 **판정이 서지 않는다.** 시트 도구에 `base.png+overlay.png` 합성을 넣은 이유다.

### 1.2 고친 것 — 자색 3건 · 원인이 둘로 갈렸다

| 파일 | 증상 | 원본색 | 처리 |
|---|---|---|---|
| `ax9_theorem_base_chase` | 뒷바퀴 안쪽 **좌우 대칭** 자두색 28px | `#A90E34`·`#98011F` **진홍** | → `AMBER` |
| `priv_common_base_chase` | 후면 정중앙 하단 자두색 9px | `#601926` **암적** | → `AMBER` |
| `gm12_paladin_base_side` | 사이드포드 윗선 자색 7px | `#9960CA` **원판부터 자색** | → `N13` |

**앞의 둘과 뒤의 하나는 원인이 다르다.** 앞의 둘은 **붉은색이 갈 곳이 없어 밀려간 것**이고(60색 대장에 주황 `#E2620F` 와 갈색 사이가 비어 있다 — §4.6.2.3 과 같은 빈 자리), 뒤의 하나는 **원판에 있던 색**이다.

> **⚠ 예고했던 재발이 아니라 이미 일어나 있었다.** 붉은색 빈 자리는 연출 요소(IMPL-386)에서 처음 발견했는데, **머신 층에는 그전부터 들어 있었다.** 눈 검수가 아니었으면 계속 남았을 것이다 — **기계 검사는 "대장 안의 색인가"를 묻지 "옳은 색인가"를 묻지 않는다.**

**치환색을 고른 방식도 갈랐다.** 후미등은 **눈으로 골랐다** — 4안을 나란히 렌더해 보니 자색 = 얼룩 · 자동 제외값 갈색(`#6B4430`) = 진흙 · 주황(`#E2620F`) = 차체와 경합 · **호박(`#D98A12`)만 등불로 읽혔다.** 팀색(청·백)과 겹치지 않아 팀 정체성도 흐리지 않는다. 반면 gm12 는 **양자화가 자색 제외 시 스스로 고르는 값**(`#8B949F`)을 그대로 썼다 — **손으로 고를 이유가 없는 자리는 손으로 고르지 않는다.**

gm12 의 판정 축은 `crew_theo` 주황 전례와 같다(IMPL-337) — **색이 소속을 밝힌다.** 그리폰은 녹색·주황 팀이므로 자색 하이라이트는 의도로 볼 수 없다.

### 1.3 확인하고 넘어간 것

| 건 | 판정 |
|---|---|
| `priv_common_livery_team_silvertrail` 이 코멧웍스보다 **면적이 1/3** (155px vs 453px) | **정본 부합 — 결함 아님.** 사양서 A-MC-12 = *"**은빛 궤적 라인** 리버리"*. **면적이 작은 것이 설계다.** 부수로 대비를 재보니 실버트레일이 **오히려 더 높았다**(평균 ΔL 0.444 · 94% 가 문턱 위 · 코멧웍스 0.177 · 42%) — 눈에 약해 보인 것은 대비가 아니라 면적이었다 |
| 세컨드 드라이버 번호 3건 — 옛 번호 잔존 | **없음.** 2·51·81 전건 단독 판독 |
| 보레아스 윙 3단·리버리 3종 | **계단이 보인다.** 01→03 으로 갈수록 면적·돌출이 커지고, 리버리+윙 동시 합성에서도 서로 가리지 않는다 |

### 1.4 기각한 우려 — 평균 휘도가 오도했다

보레아스가 어두워 보여 **차체 평균 상대휘도**를 재니 s1 0.036 · s2 0.060 · s3 0.075 로 **메트로 나이트 근경(0.074)과 거의 같았다.** 명암비로는 **1.0~1.4:1**, 즉 "야간 무대에서 사라진다"는 결론이 나온다.

**그런데 실기 합성에서는 네 대 모두 또렷이 읽힌다.** 평균이 **검은 타이어를 함께 세었기** 때문이고, 실제로 눈이 잡는 것은 **차체 상부가 노면 띠보다 밝다는 것**이다.

> **이 세션에서 같은 실수가 여섯 번째다** — 참이지만 **다른 명제를 잰** 것이다(형태 연속성 · 8-이웃 접촉 · 양자화 전 이음 · 비네트 대역 평균 · 팔레트 사용량 · 이번 휘도 평균). **규율은 그대로다: 게이트는 눈이 무엇을 보는가를 재야 한다.** 그리고 이번에는 규율이 값을 했다 — **재고 나서 얹어 봤기 때문에 멀쩡한 에셋 7장을 고치지 않았다.**

### 1.5 실측

| 축 | 결과 |
|---|---|
| `quantize --check` (머신) | **PASS files=14 palette=60 patches=4 derived=6** |
| `quantize --check` (캐릭터) | **PASS files=47 palette=60 patches=6 derived=34** — 무접촉 확인 |
| `overlay_gen` | **OK overlays=23** (베이스 갱신분 재산출) |
| 자색·분홍 잔존 | **0 / 37 파일** |
| 파생 기하 | 실루엣 IoU **100%** (측면 3 · 추격 3) |
| 변경 파일 | **3** (오버레이 23 은 바이트 불변 — 보레아스 베이스가 안 바뀌었다) |


---

## 2. 프레임 16 — 2회차 (2026-08-24 · IMPL-396) · **결함 0**

### 2.1 배율을 갈라 봤다 — 프레임은 1× 가 실기다

머신은 확대해서 봐도 되지만 **UI 프레임은 1px 획이 전부**라, 확대하면 보이고 실기에서는 사라진다. 그래서 **9패치를 실제 위젯 크기로 늘려** 정본 화면 바탕(**N03 `#151B22`** — 팔레트 정본 §1) 위에 얹고 **1×·3× 두 배율**로 봤다.

**결과: 1× 에서 16종 전부 위치를 찾을 수 있고, 위계도 의도대로다** — 모달·릴 글로우가 가장 강하고 버튼이 중간이며 비활성·카드가 가장 여리다.

### 2.2 여기서도 처음엔 다른 명제를 잴 뻔했다

테두리 대비를 재려고 **바깥 1px 링**을 잡았더니 15종 중 13종이 `#080B11` 로 나왔다 — 전부 같은 값이라 이상해서 보니 **그건 테두리가 아니라 드롭 섀도**였다. 한 칸 안쪽(inset 1)이 실제 획이다.

| 프레임 | 획 | 획 대 내부 |
|---|---|---|
| `button_primary` | `#12A6C4` | **5.01:1** |
| `dialog_modal` | `#8B949F` | **6.02:1** |
| `button_danger` | `#FF4A3D` | **4.33:1** |
| `tooltip` | `#5B646F` | 3.08:1 |
| `button_default` · `panel_large` | `#4E5661`·`#424A54` | 1.94·1.93:1 |
| `tab` · `input_field` · `panel_medium` | — | 1.61·1.47·1.38:1 |
| `button_disabled` · `card_small` | — | **1.11:1** (의도 — 비활성·채움형) |
| `list_row` | 획 = 채움 | **1.00:1** (의도 — 테두리 없는 행 채움) |

### 2.3 색 감사 — 마스터 60 밖 5종, 전부 기능 부속

프레임 16 + 글리프 20 + 아이콘 27 전량을 훑어 **마스터 60 밖 색을 5종** 찾았고 **전부 기능 컬러 부속이며 쓰인 자리도 정본과 일치**한다: `#FF4A3D`(위험 버튼·트러블 심볼) · `#7BE04A`(라인 심볼·광고 면제) · `#7A5CFF`·`#E8EEF5`(색각 대체 2종) · `#FFD86B`(경고). **미상 색 0.**

> **⚠ 그리고 여기서 팔레트 붉은색 사안의 근거가 뒤집혔다 — 정본이 이미 알고 있었다.** 팔레트 정본 §5 명문: *"**적(`#FF4A3D`)과 라임(`#7BE04A`)은 네온 3계열(시안·마젠타·앰버)에 없다 — 면제 조항이 없으면 이 두 색은 갈 곳이 없다.** 이것이 부속을 56 밖에 두는 근거다."*
>
> 즉 **붉은색 빈 자리는 §4.6.2.3 이 발견한 것이 아니라 정본이 명문화해 둔 것**이고, 부속 13 은 그 구멍을 **기능·접근성 용도로만** 메운다(D10 §2.3 면제 = 심볼 이중 부호화·색각 대체·게이지 상태·경고색). **아트 층에는 면제가 적용되지 않으므로** 브레이크 등(§1.2)과 잉걸(§4.6.2.3)이 자색으로 밀린 것이다.
>
> **보류된 팔레트 판정의 근거가 이로써 단단해졌다** — 추후 재상신 시 *"아트에도 같은 면제가 필요한가"* 로 질문을 좁힐 수 있다.

---

## 3. 연출 요소 8 — 2회차 (2026-08-24 · IMPL-396) · **에셋 결함 0 · 배치 규약 2건 신설**

낱장은 제작 회차에 이미 봤으므로 이번엔 **실제 쓰이는 방식**으로 봤다 — 5개 컷을 무대 배경 + 머신 + 연출 요소로 합성. 부수로 **§5.2.2 합성 선언이 성립하는지**도 함께 검증된다.

| 컷 | 무대 | 판정 |
|---|---|---|
| SC-01 스피드 라인 | 메트로 나이트 | ✅ 야간 도심 위에서 획이 또렷하다 |
| SC-05 접촉 불꽃 | 펄스 돔 | ✅ 두 머신 사이에서 강하게 읽힌다 |
| SC-06 연기·파편 | 알타 리지 | ✅ 연기는 창백한 하늘 위에서도 읽힌다 |
| SC-07 듀얼 오라 | 미라지 플랫 | ✅ 주황 사막 위 시안이라 대비가 크다 |
| SC-09 섬광 | 아주르 코스트 | ⚠ **아래 ②** |

### 3.1 두 건 다 에셋이 아니라 **배치 규약의 공백**이었다

| # | 실측 | 규약 |
|---|---|---|
| ① | **`fx_warning_light` 가 밝은 무대에서 사라진다** — 알타 리지 능선 위에서 거의 보이지 않았다. 중립 회백으로 만든 것은 §5.2.1 이 **색 치환 대상**으로 지정했기 때문인데, 정본은 *"치환 대상"* 이라고만 적고 **"치환 없이 쓰면 안 된다"** 를 적지 않았다 | **색 치환 없이 쓰지 않는다 (강제)** — `fx_spec.json` `_합성제약` 에 명문화 |
| ② | **`fx_flash_burst` 를 하늘에 두면 태양이 된다** — 아주르 코스트(주간 해안) 합성에서 대칭 방사 도상이 **곧바로 태양으로 읽혔다** | **앵커는 머신 높이에 둔다** — 〃 |

> **②는 도상이 아니라 위치가 의미를 바꾼 사례**이고, 경고광 3작(§4.6.3.2)·오라 3작(§4.6.4.1)과 같은 축이다 — **형태는 맥락에서 읽힌다.** 낱장 검수로는 절대 나오지 않았을 발견이며, **"실제 쓰이는 방식으로 본다"가 검수 방법론으로 값을 했다.**


---

## 4. 무대 배경 10 — 3회차 (2026-08-24 · IMPL-399) · **결함 1건 교정 · 게이트 신설**

### 4.1 이음은 눈으로도 안 보인다 — 게이트가 일을 했다

**랩 지점을 뷰포트 정중앙에 놓고** 5식을 데스크탑 실기 폭(384)으로 잘라 봤다. **5식 전건 이음이 보이지 않는다.** 보이는 것은 이음이 아니라 이미 기록된 **아치 대칭**(§4.6.1.1)이며 새 사안이 아니다.

> 부수 관측 — **원도 408 · 데스크탑 뷰 384 이므로 스크롤 여유가 24px 뿐**이다. 배경은 사실상 정지이고 §5.1 도 *"패럴랙스 없어도 **컷 합성용**"* 이라 적었으므로 용도와 맞다. 다만 **중앙 아치가 항상 화면에 있다**는 뜻이기도 하다.

### 4.2 ⚠ 메트로 나이트가 머신을 97% 삼켰다 — 분할 기준이 틀린 명제를 재고 있었다

| 무대 | `split_y` | 근경 행 | **머신 가림(실루엣 실측)** |
|---|---|---|---|
| `stage_metro_night` | **53** | 47 | **97%** ✗ |
| `stage_mirage_flat` | 71 | 29 | 18% |
| `stage_azure_coast` | 80 | 20 | 0% |
| `stage_pulse_dome` | 85 | 15 | 0% |
| `stage_alta_ridge` | 88 | 12 | 0% |

`split_y` 는 **"행 변화량 최대점"** 으로 골랐는데, 2레이어의 목적은 §5.1 이 적은 대로 **"컷 합성용"** — **머신이 원경과 근경 사이에 들어가는 것**이다. 그러므로 물어야 할 것은 *"그림이 가장 크게 바뀌는 곳"* 이 아니라 **"머신 앞에 무엇이 오는가"** 였다. 메트로는 변화 최대점이 하필 **차가 달리는 노면의 윗선**이어서, **노면이 통째로 차 앞으로 갔다.**

**교정: 53 → 88** (실측 가림 97% → 0%). 88·92·95 를 나란히 렌더해 **88** 을 골랐다 — 92 이상은 바퀴가 반사 띠에 파묻힌다. 근경은 여전히 가장 가까운 노면 띠 4896px 를 유지한다.

> **이 세션에서 같은 종류의 오독이 일곱 번째다** — 형태 연속성 · 8-이웃 접촉 · 양자화 전 이음 · 비네트 대역 평균 · 팔레트 사용량 · 휘도 평균 · **분할 기준**.

### 4.3 그래서 이번엔 기준을 고치는 데서 그치지 않고 게이트로 받았다

**고친 값은 다음 회차에 누군가 다시 옮길 수 있고, 그때 이 실패는 조용히 돌아온다.** `bg_gen.js` 에 `machine_box` 게이트를 신설했다 — 머신 표준 자리(408×100 의 `[140,30,128,64]`)를 근경이 얼마나 덮는지 재고 **40% 초과면 차단**한다. 돌연변이 실측: `split_y` 를 53 으로 되돌리자 **64% 로 검출**했다.

> **게이트가 대리 지표라는 것도 적어 뒀다.** 머신 실루엣이 아니라 **구획 안에서 분할선 아래 행의 비율**을 센다 — 실루엣으로 재면 메트로 97%·미라지 18% 인데 대리로는 64%·36% 다. **크기는 다르고 순위는 같다.** 머신 실물을 읽으면 정확해지지만 **`bg_gen` 이 `machines/` 에 묶여 배경 생성이 머신 회차에 깨진다.** 정확도보다 **결합을 피하는 쪽**을 골랐고, 대신 값이 무엇인지 숨기지 않는다.

### 4.4 확인하고 넘어간 것

`stage_mirage_flat` 의 18% 가림은 **근경 모래 띠가 바퀴 아래를 덮는 것**이고 **차가 모래에 앉은 것으로 읽혀 의도에 맞다** — 허용으로 둔다.


---

## 5. 글리프 20 — 4회차 (2026-08-24 · IMPL-400) · **결함 0 · 판정 요청 2건**

도상은 6배로 봤고(16px 원도의 내용을 그대로 보는 배율이다) 전건 판독된다. 위험은 도상이 아니라 **혼동 쌍이 몇 픽셀로 갈리는가**다 — 표식이 2~3px 이면 실기에서 방향을 못 읽는다.

| 쌍 | 차이 / 잉크 | 비율 |
|---|---|---|
| `trigger_l` ↔ `trigger_r` | 30 / 65 | **46%** |
| `shoulder_l` ↔ `shoulder_r` | 24 / 56 | **42%** |
| dpad 4종 상호 | 12 / 46 | **26%** |
| face 4종 상호 | 8 / 44 | 18% |
| `touch_tap` ↔ `touch_long_press` | 29 / 128 | 22% |
| `touch_hold` ↔ `touch_long_press` | 21 / 120 | 17% |
| **`shoulder_l` ↔ `trigger_l`** | 9 / 56 | **16%** ⚠ |
| **`stick_l` ↔ `stick_r`** | 8 / 56 | **14%** ⚠ |
| **`touch_tap` ↔ `touch_hold`** | **8 / 128** | **6%** ⚠ |

> **판정 요청 근거는 외부 기준이 아니라 자기 기준이다.** 이 세트가 스스로 "반드시 구분돼야 한다"고 정한 방향 세트가 dpad **26%** · face **18%** 인데, `touch_tap`↔`touch_hold` 는 **6%** 로 그 1/3~1/4 다. **세트 안에서 이상치**다. `shoulder_l`↔`trigger_l` 은 **다른 입력(L1/L2)인데 9px 로 갈린다.**
>
> **고치지 않고 올린다** — 글리프는 *"공유 base + 표식 + 미러"* 라는 family 계약으로 채택된 세트이고(IMPL-314), 표식을 키우는 것은 **도상 판단**이다.

---

## 6. 캐릭터 스탠딩 13 — 4회차 (2026-08-24 · IMPL-400) · **⚠ 4인 얼굴 냉색화 · 판정 요청**

### 6.1 층 간 정합은 섰다

번호가 **머신 리버리와 일치**한다 — 마로 2 · 비앙카 51 · 볼코바 81 · 주드 77 · 셔우드 24. 화풍도 13인이 일관되고 개체 구분이 뚜렷하다.

### 6.2 ⚠ 그런데 4인의 얼굴이 회색으로 굳었다 — 붉은색 빈 자리의 세 번째 발현

얼굴 구획의 **냉색 우세 화소 비율**을 산출물과 RD 원판에서 각각 쟀다.

| 인물 | 산출 | 원판 | 차 |
|---|---|---|---|
| **`ai_lorentz`** | **83%** | 9% | **+74** ⚠ |
| **`crew_sasha`** | **76%** | 23% | **+53** ⚠ |
| **`ai_maro`** | **56%** | 20% | **+36** ⚠ |
| **`ai_sherwood`** | **41%** | 14% | **+27** ⚠ |
| `ai_holloway` | 8% | 0% | +8 |
| 나머지 8인 | ≤11% | — | −11 ~ +1 |

**원판을 보면 이건 화풍이 아니라 우리 파이프라인의 산물이다.** RD 원판의 얼굴색은 `#A16B7E`·`#8E546A`·`#673354` 같은 **자주빛 피부**(따뜻한 조명 아래의 정상 피부)인데, **60색 대장에 그 대역이 없어** 감청 `#0B2A5E` 과 청회색으로 밀렸다. 대조군 `crew_theo` 는 원판이 `#E08C73`(따뜻한 복숭아)이라 `#C08A5E` 로 곱게 떨어진다 — **정상 양자화가 어떤 모습인지 보여 준다.**

> **§1.2(브레이크 등)·§4.6.2.3(잉걸)에 이은 세 번째 발현이고, 이번엔 사람 얼굴이다.** 로렌츠·사샤는 얼굴 전체가 회색 돌이 되고, 마로는 파란 얼룩이 멍처럼 읽히며, 셔우드는 얼굴 절반만 회색이다.

### 6.3 교정안을 만들어 봤다 — 성립한다

마로로 시제품을 만들었다: 얼굴 구획에서 **냉색 팔레트 엔트리를 피부 램프로 치환**하되 **눈 두 칸을 보존**한다(2441px). 결과는 따뜻한 피부 + 파란 눈 + 얼룩 소거이며 이음도 생기지 않는다.

세 번 만에 나왔고 실패가 각각 무엇을 가르쳤다 — ⓐ**구획 전체 치환**은 홍채까지 먹었다 ⓑ**채도로 가르기**는 실패했다(진청이 **홍채이자 뺨 얼룩**이라 색으로는 안 갈린다) ⓒ**눈 띠 보존**은 가로 줄무늬를 남겼다(띠 안 그늘이 청회색으로 남는다) → **눈 두 칸만 보존**이 답이었다.

> **그러나 집행하지 않았다.** 주요 인물의 얼굴을 2441px 다시 칠하는 일이고, **사용자가 "전체 아트 품질 관리" 회차를 예고**했다(2026-08-24). **이 사안이 바로 그 회차의 것**이므로 판정으로 올린다.

---

## 7. 표정 차분 34 — 4회차 (2026-08-24 · IMPL-400) · **결함 0 (§6 파생분 제외)**

4상태(`confident`·`anger`·`shaken`·`special`)와 2상태(`positive`·`negative`)가 **눈썹 각도·눈꺼풀·입꼬리로 구분된다** — 3배에서 전건 판독. 파생이므로 베이스의 기하가 그대로 보존되고 합성 이음도 없다.

> **§6 의 냉색화는 표정 층에도 그대로 내려온다** — 파생이기 때문이다. 베이스를 고치면 **표정 층은 재산출로 따라온다**(별도 작업 없음). 이것이 파생 구조의 이득이다.


---

## 8. 5회차 — 판정 2건 집행 (2026-08-24 · IMPL-401 · 사용자 승인)

### 8.1 얼굴 냉색화 — `warm_lock` 신설 · 4인 교정

| 인물 | 교정 전 | 교정 후 |
|---|---|---|
| `ai_lorentz` | 83% | **8%** |
| `crew_sasha` | 76% | **12%** |
| `ai_maro` | 56% | **3%** |
| `ai_sherwood` | 41% | **8%** |
| `crew_theo`(대조) | 0% | 0% |

**규칙 하나로 눈 구획 없이 풀었다.** 조건이 *산출색*이 아니라 **원판색**이다 — 선언 구획 안에서 **원판이 파랗지 않은 화소**만 온색 후보로 양자화한다. **눈은 원판부터 파랗기 때문에 저절로 빠진다.**

> **눈 구획 자동 검출은 이번에도 실패했다** — 상자가 이마 광택·안경·머리카락에 앉았다(IMPL-372 의 기각과 같은 결과). **검출을 고치는 대신 검출이 필요 없는 규칙을 찾았다.**

### 8.2 다섯 번의 실패가 각각 무엇을 가르쳤다

| # | 시도 | 결과 |
|---|---|---|
| ① | 구획 전체를 피부 램프로 치환 | **홍채까지 먹었다** |
| ② | 채도로 가르기 | **실패** — 진청이 *홍채이자 뺨 얼룩*이라 색으로는 안 갈린다 |
| ③ | 눈 띠 보존 | **가로 줄무늬** — 띠 안 그늘이 청회색으로 남는다 |
| ④ | 원판 문턱을 넓히며 후보 필터도 같이 넓힘 | **그늘이 분홍으로 갔다** — **원판 판정 문턱과 후보 팔레트 필터는 다른 것**이고 한 값으로 묶은 것이 원인 |
| ⑤ | 상자 윗변 y54 | **이마를 가로지르는 띠** — 표정 크롭에서 드러났다. y46 으로 올려 해소(머리색은 유지) |

> **⚠ 그리고 회차 중간에 내가 죽은 실행의 숫자를 믿었다.** 문턱을 훑을 때 도구 출력을 `/dev/null` 로 버렸는데, 그 사이 **양자화가 마로에서 즉사**하고 있었다(옛 패치가 대상 0). 알파벳 순서상 로렌츠까지만 새로 쓰이고 사샤는 옛 파일이 남아, **두 측정이 서로 모순되는 값을 냈다.** **도구는 제대로 막고 있었고 내가 눈을 가렸다** — 이후 전 실행을 출력과 함께 돌렸다.

### 8.3 부수 발견 — 마로의 파란 얼룩은 사고가 아니라 **이전 회차의 선택**이었다

옛 패치가 자홍으로 앉은 얼굴 음영을 **악시온 팀 블루**(`AX_SHADOW`·`AX_BLUE`)로 치환하고 있었다. 근본 원인은 같지만(60색에 장미 대역 부재) **선택이 한 번 더 얹혀 뺨이 멍처럼 읽혔다.** `warm_lock` 이 자홍을 애초에 만들지 않으므로 그 패치는 대상 0이 되어 **도구가 즉사시켰다 — 낡은 선언을 막는 검사가 낡은 판단도 함께 드러냈다.**

### 8.4 파생 상속 — 같은 사고를 두 번 막았다

`warm_lock` 을 파생 잡에도 물려주도록 했다. 하지 않으면 **베이스는 따뜻하고 표정 34 는 회색**으로 남는다 — **같은 인물이 표정에 따라 다른 색**이 되는 조용한 사고이며, 이 파일의 파생 패치 상속(IMPL-372)이 막으려던 것과 **같은 사고다.** 실측으로 표정 14장이 함께 갱신됐다(로렌츠 4·사샤 4·셔우드 4·마로 2).

### 8.5 글리프 표식 강화

| 쌍 | 전 | 후 |
|---|---|---|
| `touch_tap` ↔ `touch_hold` | 6% | **18%** (face 세트와 동급) |
| `shoulder_l` ↔ `trigger_l` | 16% | **26%** (dpad 세트와 동급) |
| `shoulder_r` ↔ `trigger_r` | 16% | **26%** |

**`mirror` 는 좌표를 뒤집을 뿐 표식 목록을 물려받지 않는다** — L 만 키웠더니 R 이 16% 로 남아 **한쪽만 고쳐진 상태**가 됐다. 미러 쌍은 표식도 함께 옮겨야 한다.

> **탭의 대각 파문을 링에 붙여 뒀더니 또 톱니처럼 읽혀 한 칸 더 뗐다** — 경고광 3작·오라 3작과 **같은 실패**다: **규칙적 경계가 링에 닿으면 기계가 된다.**

**잔여:** `pad_stick_l`↔`pad_stick_r` 14% — 이번 승인 범위(탭/홀드·L1/L2) 밖이라 손대지 않았다.


---

## 9. 윤곽 정리 (§2.3 ④ 잔여) — 종결 (2026-08-24 · IMPL-403) · **정리 대상 없음**

`파편 소거`는 IMPL-337 에 끝났고 **윤곽 정리는 "기계로 특정되지 않는다"며 눈 소관으로 남아 있었다.** 후보는 기계로 뽑고 **판정은 눈으로** 했다.

### 9.1 후보 303 → 분류

| 분류 | 수 | 판정 |
|---|---|---|
| **직교 이웃 0** (대각 전속) | **2** | 볼코바 2px — 실측하니 **머리카락 가닥의 끝**(대각으로 이어지는 사슬)이었다. 구조 |
| **선의 끝** (이웃이 얇다) | 147 | 머리카락·윙 끝·안테나. 구조 |
| **덩어리 옆 1px 혹** | **154** | 아래 §9.2 |

### 9.2 154 혹의 성격이 층마다 달랐다

| 층 | 수 | 확대 판정 |
|---|---|---|
| 캐릭터 13 | **125** | **전부 필치다.** 오스카(28)·테오(13)·볼코바(12) 등 곱슬·헝클어진 머리를 가진 인물에 몰려 있고, 확대하면 **머리카락 가닥의 끝**이다. **손대면 그림이 상한다** |
| 머신 14 | **29** | 대부분 **구조** — 보레아스 3px 은 **롤후프/미러 스탤크**, vh8 은 **윙 끝과 노즈 팁**, gm12 는 리어윙 가장자리. 남는 소수(gm12 `(63,43)`·프라이비티어 측면 4)는 **차체에 붙은 1px 요철**이고 **어두운 영역이라 1× 에서 읽히지 않는다** |

> **떠 있어 보이던 것이 붙어 있었다.** 프라이비티어 뒤쪽 4점은 확대 렌더에서 허공에 뜬 점처럼 보였는데, 화소 지도를 찍으니 **주변이 전부 불투명한 어두운 차체**였다 — **어두운 배경 위의 어두운 화소를 눈이 못 본 것**이고 기계 분류(이웃 밀도)가 옳았다.

### 9.3 세 번째 축 — 반투명 잔재 **0**

윤곽의 다른 축인 안티에일리어싱 잔재를 전수 검사했다: **193파일 · 반투명 화소 0.** 알파가 전건 이진이다.

### 9.4 결론

**§2.3 ④ 의 윤곽 정리에 남은 기계 특정분은 없다.** 이는 이 항목을 처음 눈 소관으로 남긴 판단(IMPL-337)과 **일치한다** — 그때 *"기계로 특정되지 않는다"* 고 적은 것이 맞았고, 이번 회차는 **그 사실을 실측으로 확인하고 닫는 것**이다.

> **154건을 '정리'했다고 편집하지 않았다.** 눈에 보이지도 않는 화소를 154개 옮기는 것은 **작업의 외양**이지 작업이 아니고, 캐릭터 쪽은 **실제로 그림을 상하게 한다.** 후속으로 큰 확대 용도(대형 초상 등)가 생기면 캐릭터 가장자리는 그때 다시 볼 값이 있다.

---

## 10. 글리프 `pad_stick` 강화 (2026-08-24 · IMPL-403 · 총괄 승인)

L↔R **14% → 20%** — face 세트 기준(18%)을 넘겼다. 세로 4px 막대를 **링에서 안쪽으로 뻗는 6px 스포크**로 바꿨다.

> **20% 가 표식으로 얻을 수 있는 상한이다.** 실측하면 링이 x=2, 디스크가 x=6 이라 **여백을 양쪽에 두고 쓸 수 있는 칸이 x=4 하나뿐**이고, 키우면 반드시 링이나 디스크에 붙는다. 1차로 바깥 쐐기를 냈더니 23% 였으나 **혹처럼 튀어나와 도상이 상했다** — **붙는 것을 결함이 아니라 도상으로 쓰는 편**이 나아 스포크로 갔다(돌출 없이 한쪽이 두꺼워진다). **더 벌리려면 base 자체를 바꿔야 하고 그것은 도상 개정이므로 하지 않았다.**
