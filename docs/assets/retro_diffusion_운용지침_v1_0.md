# Retro Diffusion 운용 지침 v1.0 (구현 문서)

> **지위:** 에셋 트랙의 **생성 도구 운용 계약서**. 무엇을 그리는가는 [제작 사양서](이미지에셋_제작사양서_v1_3.md), 무슨 색인가는 [팔레트 정본](팔레트_정본_v1_0.md), 어디에 두는가는 [배치 대장](에셋_배치_대장_v1_2.md)이 소유한다. **본 문서는 "어떻게 호출하는가"만 소유한다.**
> **작성:** 2026-08-17 · 근거: MCP 무료 조회 도구 실측(`get_service_status`·`list_available_models`·`list_available_styles`·`list_edit_tools`·`get_style_usage`·`estimate_inference_cost` 14회) · MCP 서버 README · 도구 스키마 실물
> **작성 방법 주의:** `retrodiffusion.ai/app/guide` 와 `/guide/api` 는 **JS 렌더링이라 본문을 가져올 수 없었다**(제목만 반환). 그래서 본 문서의 수치·이름은 전부 **MCP 응답 실측**이며 웹 문서 전사가 아니다. 서비스가 값을 바꾸면 **실측이 정본이고 본 문서가 낡는다** — §8 재검 절차를 둔 이유다.

> **v1.0 개정 (2026-08-17 · IMPL-198 — D10 v1.2 자색 슬롯 신설 반영):** 양자화 기준이 **마스터 56색 → 60색**으로 늘었다. 파일명 `master_56.*` → **`master_60.*`** 개명 · 스트립 **56×1 → 60×1** · 스와치 색 수 재산출(`master_60.png` **64색** · `colorblind_alt.png` **67색**). **§4.3 양자화 절차 자체는 무변경** — 입력 파일만 갈린다.

---

## 0. 비용 규칙 — **모든 호출에 우선한다** (사용자 지시 2026-08-17)

| # | 규칙 |
|---|---|
| ① | **기본 모델 = `rd_fast`.** 다른 모델(`rd_pro`·`rd_plus`·`rd_mini`)은 승인 없이 쓰지 않는다 |
| ② | **1회 생성 = `balance_cost` $0.05 미만.** 초과하면 호출하지 않는다 |
| ③ | **①②를 벗어나야 한다고 판단되면 호출 전에 멈추고, 작업 내용과 사유를 사용자에게 제시해 승인을 받는다** |

**단위 해석 (실측 근거):** 본 계정의 생성 호출은 `credit_cost: 0` · `balance_cost: $0.0xx` 로 청구된다 — 즉 **크레딧이 아니라 잔액(USD)이 소비된다.** 따라서 ②의 "0.05"는 **`balance_cost` $0.05**로 읽는다. 크레딧 표기가 병존하는 편집 도구 쪽 환산은 일정하지 않으므로(배경 제거 $0.01 = 2 크레딧 → 1크레딧 $0.005 / 이미지 편집 $0.18 = 20 크레딧 → 1크레딧 $0.009) **고정 환율을 가정하지 않는다.** 판단 기준은 항상 `balance_cost`다.

> **실행 규칙:** `estimate_inference_cost` · `estimate_edit_tool_cost` 는 **무료**다. **금액이 붙는 호출 전에는 예외 없이 먼저 추정한다.** 추정 없이 생성하지 않는다.

**현재 잔고 (2026-08-17 실측):** `credits: 50` · `balance: $10.04`. 32×32 아이콘 1장이 $0.017이므로 **UI 아이콘 27종 1회차 = 약 $0.46** — 예산은 병목이 아니다. 병목은 규칙 ②(회당 상한)이며 이는 **배치 크기 제한**으로 나타난다(§3).

---

## 1. 서비스 형태 — 무엇을 하는 도구인가

Retro Diffusion 은 **도트 픽셀 아트 전용 생성기**다. 범용 이미지 생성기를 픽셀아트처럼 보이게 만든 것이 아니라, **요청한 해상도에서 네이티브 픽셀 그리드를 직접 산출**한다. 이것이 본 프로젝트에 중요한 이유는 §4에서 다룬다.

- **모델 4종:** `rd_fast` · `rd_mini` · `rd_plus` · `rd_pro`. 상태 조회 = `get_service_status`(무인증·무료).
- **스타일이 렌더링을 전담한다.** 호출자는 `prompt_style` 을 고르고 **프롬프트에는 소재만 적는다.**
- **스타일은 탭으로 갈린다:** `tab:image`(정지 이미지) · `tab:tileset` · `tab:animation` · `tab:advanced-animation`. 본 프로젝트가 쓰는 것은 **`tab:image` 전속**이다 — 애니메이션·타일셋은 D10 계상에 없다.
- 접속·인증은 이미 구성돼 있다. **API 키를 다루지 않는다** — 세션에서 키를 묻거나 노출하지 않는다.

---

## 2. MCP 도구 — 무료/유료 구분

**무료 (마음껏 호출한다)**

| 도구 | 용도 |
|---|---|
| `get_service_status` | 하위 시스템 상태(무인증) |
| `get_balance` | 잔고 — **잔고의 정본** |
| `list_available_models` / `list_available_styles` / `get_style_usage` | 스타일 카탈로그·크기 한계·참조 이미지 지원 여부 |
| `estimate_inference_cost` / `estimate_edit_tool_cost` / `list_edit_tools` | 비용 사전 확인 |
| `get_inference_result` / `get_inference_job` / `list_inference_jobs` | **이미 낸 돈의 결과 회수** (재과금 없음) |
| `fix_pixel_art` | 확대·뭉개진 이미지에서 네이티브 픽셀 그리드 복원 (10 req/min 제한) |
| `run_edit_tool` 중 `color_reducer` · `palette_converter` · `pixel_correction` · `k_centroid_downscale` · `rotate` | 후처리 |

**유료 (규칙 §0 적용)**

| 도구 / 대상 | 비용 | 판단 |
|---|---|---|
| `create_inference` — `rd_fast` | $0.017 ~ $0.041 (크기별 §3) | ✅ 규칙 내 |
| `create_inference` — `rd_pro` | **$0.18 고정** (64~256 전 크기 동일 실측) | ❌ **승인 필요** |
| `run_edit_tool` `background_remover` | $0.01 (2 크레딧) | ✅ |
| `run_edit_tool` `color_style_transfer` | $0.01 (2 크레딧) | ✅ |
| `run_edit_tool` `image_edit` · `inpainting` · `outpainting` · `seam_tiling` | **$0.18 (20 크레딧)** | ❌ **승인 필요** |

> **`rd_pro` 가 왜 매력적으로 보이는가, 그런데 왜 기본이 아닌가:** `rd_pro__*` 만이 **`reference_images` 를 지원**한다(최대 9장 — 캐릭터·스타일 일관성). 캐릭터 13인의 표정 차분 34장처럼 **동일 인물 유지가 생명인 묶음**에서는 값어치가 있을 수 있다. 그러나 장당 $0.18 은 규칙 ②의 3.6배이고, `rd_pro` 는 **최대 256px** 이라 180×240 스탠딩은 되지만 384가 필요한 곳은 오히려 못 쓴다. **필요하다고 판단되면 §7 승인 요청서를 쓴다 — 임의로 쓰지 않는다.**

---

## 3. 실측 비용표 — `rd_fast` (2026-08-17 · `estimate_inference_cost`)

| 크기 | 1장 | 2장 | 3장 | 4장 |
|---|---|---|---|---|
| 32×32 | **$0.017** | $0.034 | ~$0.051 ❌ | — |
| 64×64 | **$0.017** | $0.035 | **$0.052 ❌** | $0.069 ❌ |
| 128×128 | **$0.019** | $0.039 | ~$0.058 ❌ | — |
| 192×256 | **$0.025** | ~$0.050 ❌ | — | — |
| 256×256 | **$0.028** | **$0.055 ❌** | — | — |
| 384×384 | **$0.041** | ❌ | — | — |

비용은 **면적 × 장수에 거의 선형**이다. 여기서 규칙 ②가 곧바로 배치 상한을 정한다.

### 3.1 배치 상한 — 외우고 쓸 것

| 원도 크기 | `num_images` 상한 | 회당 |
|---|---|---|
| ~128px 이하 (아이콘·글리프·9패치·머신 셀) | **2** | ≤ $0.039 |
| 129~256px (캐릭터 스탠딩 180×240) | **1** | ≤ $0.028 |
| 257~384px (씬 컷·대형) | **1** | ≤ $0.041 |

> **시안을 여러 장 보고 고르고 싶으면 호출을 나눈다.** 한 호출에 3장을 담으면 규칙 ②에 걸리지만, **1~2장짜리 호출을 두 번** 하면 걸리지 않는다(회당 상한이지 총액 상한이 아니다). 다만 총액이 커지면 §7로 보고한다. `seed` 를 바꿔 가며 나눠 부르는 편이 회수·재현에도 유리하다.

---

## 4. 프로젝트 파이프라인과의 접합 — **여기가 이 문서의 핵심**

### 4.1 사양서 §2.3 5단계에서 RD가 대체하는 구간

사양서의 생성 파이프라인은 **①생성(원도의 4~8배) → ②정수 다운스케일 → ③마스터 **60색** 양자화 → ④믹셀 제거 수작업 → ⑤배치·`.import`** 다. 이는 *픽셀 그리드를 못 만드는* 범용 생성기를 전제로 쓰였다.

**RD는 네이티브 픽셀 그리드를 직접 낸다. 따라서 ①②가 한 단계로 접힌다.**

| 사양서 단계 | RD 운용 |
|---|---|
| ① 생성 (4~8배) + ② 정수 다운스케일 | **`create_inference` 를 원도 크기 그대로 호출한다.** 4~8배로 뽑아 줄이지 않는다 — RD에서는 오히려 그리드가 흐려진다 |
| ③ 마스터 **60색** 양자화 | **`run_edit_tool` `palette_converter` (무료)** · 또는 `create_inference` 의 `input_palette` 파라미터. §4.3 |
| ④ 믹셀 제거·윤곽 정리 | `pixel_correction`(무료)으로 고립 픽셀 1차 정리 후 **수작업**. 자동화 아님 |
| ⑤ 배치·`.import` | 배치 대장 §7 절차 그대로 |

> **[가안] 표기:** 위 ①②의 접힘은 본 문서의 판단이다. 사양서 §2.3 문면 자체는 범용 파이프라인으로 유지하고, RD 사용 시의 대체는 본 문서가 소유한다. 실기에서 "원도 크기 직접 생성"이 열등하다고 드러나면 4~8배+`k_centroid_downscale`(무료) 경로로 되돌린다 — **두 경로 모두 비용이 같으므로 실측으로 정하면 된다.**

### 4.2 프롬프트 — **사양서 §2.1 공통 프리픽스를 그대로 쓰면 안 된다**

RD 도구 스키마의 명문: *프롬프트에는 **소재만** 적고 **"pixel art"라고 쓰지 말 것** — 렌더링은 선택한 스타일이 전담한다.*

사양서 §2.1 공통 프리픽스는 렌더링 어휘로 시작한다(`pixel art, dense dithering, mid-90s console 2D, ...`). **그대로 넣으면 스타일과 경합해 품질이 떨어진다.** 그래서 프리픽스를 **렌더링 어휘 / 소재 어휘**로 가르고, 렌더링 쪽은 `prompt_style` 선택으로 옮긴다.

| 사양서 §2.1 토큰 | RD 처리 |
|---|---|
| `pixel art` · `dense dithering` · `mid-90s console 2D` · `crisp silhouette` · `limited palette` | **프롬프트에서 뺀다** → `prompt_style` 선택 + `palette_converter` 가 담당 |
| `night neon mood` · `telemetry glow` · `cyan magenta amber accents` | **남긴다** — 소재·분위기·색 내용이다 |

> **RD 공통 프리픽스 (본 문서 확정 · 모든 호출 앞에 결합):**
> **국문** — `야간 네온 무드, 텔레메트리 발광, 시안·마젠타·앰버 액센트`
> **영문** — `night neon mood, telemetry glow, cyan magenta amber accents`

> **⚠ 프리픽스 개정 (2026-08-23 · IMPL-316 — §9-③ [가안] 발동분).** 세 번째 토큰이 **팀 컬러를 덮는다.** 로렌츠(악시온 = **백청** 확정 · 사양서 §1.4)를 정본 프롬프트대로 뽑았더니 **분홍 어깨**가 나왔다 — 항목 프롬프트가 `white and blue racing suit` 라고 명시했는데도 프리픽스의 `magenta` 가 이겼다. 캐릭터·머신처럼 **소재 자체가 색을 지정하는 계열에서는 액센트 토큰이 지정을 무효화**한다.
>
> | 계열 | 프리픽스 |
> |---|---|
> | UI(아이콘·프레임·글리프)·씬·배경 | `night neon mood, telemetry glow, cyan magenta amber accents` (기존 유지 — 지정 색이 없어 액센트가 톤을 만든다) |
> | **캐릭터·머신** | **`night neon mood, telemetry glow`** — 액센트 3색 제거. 색은 항목 프롬프트의 팀 컬러가 지정하고, 그 위에 `<팀색> only` 를 덧붙인다 |
>
> 실측: 제거 후 백청 회복 · 고유색 **59 → 43**(마젠타 계열이 통째로 빠졌다).

**⚠ 네거티브 프롬프트가 없다.** `create_inference` 스키마에 부정 프롬프트 필드가 **존재하지 않는다**(실측 — `prompt`·`extra_prompt` 뿐). 따라서 **사양서 §2.2 공통 네거티브(사진·3D·벡터·양피지 톤·판타지 장식…)는 기계로 강제할 수 없다.** 방어는 세 겹뿐이다:

1. **스타일 선택** — 도트 전용 모델이라 사진·3D·벡터는 구조적으로 거의 안 나온다.
2. **긍정 표현으로 대체** — "양피지 톤 금지" 대신 `sunlit stone, desaturated`처럼 원하는 쪽을 적는다.
3. **육안 검수** — 사양서 §2.4 구속 조항(베인 인체 금지 · 주인공 맨얼굴 금지 · 실존 브랜드 금지 · 대치 구도 금지)은 **사람이 본다.** 기계 게이트가 없으므로 검수를 건너뛰지 않는다.

**`bypass_prompt_expansion` — 확정 규칙 (§9-⑥ · IMPL-311 종결 · IMPL-349 개정).** RD는 기본적으로 프롬프트를 자동 확장하고, 확장은 **문면의 구속을 소재 묘사로 희석한다.** 그래서 **프롬프트가 문면 그대로 살아남아야 하는 구속을 담고 있으면 확장을 끈다.**

| 구속의 종류 | 처리 | 실측 |
|---|---|---|
| **기하·배치·여백** (프레임·9패치·스트립·정렬물) | `bypass_prompt_expansion=true` | 외곽 링 불투명률 0.0% → **100.0%** (IMPL-311) |
| **정체성·배제** ("오픈휠"·"윙 없음"·"번호 없음") | `bypass_prompt_expansion=true` | 확장 켜면 오픈휠 포뮬러가 **로드카로 무너진다**(4장 전건 지붕·창·리어윙 · IMPL-349) |
| **순수 묘사** (구속 없는 소재·분위기) | 확장을 켠 채로 둔다 | — |

> **⚠ IMPL-349 가 이 규칙의 예외를 뒤집었다.** IMPL-311 판은 *"소재 표현이 주인 호출(캐릭터·머신·씬)은 확장을 켠 채로 둔다"* 였다. 그런데 머신 섀시 원판(A-MC-00)은 **소재 표현이면서 동시에 강한 구속을 담고 있었고**(오픈휠·무윙·무번호) 확장이 그 구속을 지웠다. **판정의 축은 "소재냐 기하냐"가 아니라 "문면에 지켜져야 할 구속이 있느냐"다** — 계열이 아니라 프롬프트의 내용이 정한다.

### 4.3 팔레트 양자화 — 마스터 **60색** 결속

두 경로가 있고 **둘 다 무료**다.

| 경로 | 방법 |
|---|---|
| 생성 시 | `create_inference(input_palette=<팔레트 이미지 base64>)` |
| 생성 후 | `run_edit_tool(tool_id="palette_converter", input_image=…, input_palette=…, dither_mode="none")` |

> **⚠ 구현 위치 개정 (2026-08-23 · IMPL-316):** 생성 후 경로의 **권고는 유지하되 구현을 국소로 옮겼다** — `tools/assets/quantize.js`. 이유 둘. ⓐ`run_edit_tool` 은 결과를 **base64 로 인라인 반환**한다(스키마에 `upload_outputs` 가 없다) — 캐릭터 47·머신 37 규모에서 180×240 PNG 를 왕복마다 base64 로 받으면 **회차가 못 굴러간다** ⓑ국소 구현은 `--check` 가 **원본+팔레트 → 산출물 재현 대조**를 돌 수 있다(API 양자화는 결과 PNG 만 남고 *어떻게* 가 남지 않는다). **팔레트 출처·디더 끔·되돌릴 수 있음은 그대로다** — 원본을 `tools/assets/rd_raw/` 에 남기며, 버리면 `--check` 자체가 성립하지 않는다.
>
> **부수 판단: 지각 거리로 고른다.** 단순 RGB 거리는 **피부와 회색을 바꿔치기한다**(sRGB 는 비선형이고 인간 시각은 명도에 민감하다). 명도차에 강한 가중을 준 거리로 골라 형태·음영 구조가 살아남게 했다 — 실측 고유색 43 → 25 에서 초상이 유지된다.

**권고 = 생성 후 경로.** 이유는 되돌릴 수 있어서다 — 원본을 남긴 채 팔레트를 갈아 볼 수 있고(`return_pre_palette` 파라미터도 같은 취지), 양자화가 형태를 뭉갰을 때 원인을 분리할 수 있다.

**디더는 끈다** (`dither_mode="none"`). 사양서 §1.2 의 고밀도 디더링은 **손으로** 넣는 규격이고, 자동 디더는 도트 그리드를 깨서 ④단계 수작업량을 늘린다.

### 4.3.1 어느 이미지를 팔레트로 먹이는가 — **실측 확정**

`palette_converter` 는 팔레트를 **이미지로** 받고 그 이미지의 **고유색**을 팔레트로 삼는다. 현행 스와치 PNG 를 디코드해 실제 고유색을 셌다(2026-08-17).

| 파일 | 고유색 | 내역 |
|---|---|---|
| `master_60.png` | **64** | 마스터 60 + 마스터 밖 4 = `#7BE04A` · `#FF4A3D` · `#FFD86B` · `#6B7686` |
| `colorblind_alt.png` | **67** | 마스터 60 + 마스터 밖 7 |

기능 부속 13 중 **9색이 이미 마스터 색과 같은 값**이라(심볼 슬립=C3 · 브레이킹=A3 · 찬스=M3 · 펄스=NW4 · 베인 평상=C2 …) 실제로 늘어나는 것은 4색뿐이다. 시트의 바탕·글자·테두리도 전부 마스터 색이라 오염이 없다.

| 대상 | 팔레트 입력 | 근거 |
|---|---|---|
| **UI 아이콘 27종** | ✅ **`godot/assets/palettes/master_60.png` 그대로** (**64색**) | 심볼 6종은 이중 부호화의 색 축을 **원도에 실제로 칠한다.** 순색 **60**만 먹이면 라인의 라임·트러블의 적이 마스터 색으로 끌려가 **이중 부호화가 무너진다** |
| **비아이콘 164장** (캐릭터 47·머신 37·씬 컷 68·배경 4·대형 8) | ✅ **`tools/palette/master_60_strip.png`** (**60×1** 순색) | 기능 컬러는 D10 §2.3 이 **접근성 요건**으로 격리한 색군이지 작화 팔레트가 아니다. 스와치 시트를 쓰면 `#FF4A3D` 등 4색이 피부·배경에 샌다. 스트립은 `swatch_gen.js` 가 `.gpl` 에서 산출하며 **되읽기 검산(고유색 정확히 **60**·`.gpl` 1:1)** 을 통과해야 나온다 (총괄 판정 IMPL-161 §1) |
| 색각 대체 | ❌ **`colorblind_alt.png` 를 팔레트 입력으로 쓰지 않는다** | 63색 안에 **기본색과 대체색이 함께** 들어 있다(`#FF4A3D` 와 `#7A5CFF` 둘 다). `REPLACED 4` 구획이 분할 칩으로 양쪽을 보여 주기 때문이며 **사람 대조용으로는 옳고 기계 팔레트로는 무의미**하다 — 교체의 요점 자체를 무효화한다 |

### 4.3.2 색각 대체 — **두 줄 규칙** (총괄 판정 IMPL-161 §2)

> **ⓐ `colorblind_alt.png` = 팔레트 입력 금지.** 위 표의 근거대로 기계 팔레트로 쓰면 교체가 무의미해진다. 사람 대조용으로만 쓴다.
> **ⓑ 대체 심볼 2종(`symbol_line_alt.png`·`symbol_trouble_alt.png`) = 기본 도상의 색 치환 전속. 재생성·재양자화 금지.**

색각 대체는 원칙적으로 **런타임 옵션**(O9 — IMPL-155~156 기결선)이고 교체 4색이 전부 기능 컬러라 **표시 층 틴트**가 처리한다. 따라서 **색각용 원도 양자화는 존재하지 않는다.** 단 하나의 예외가 심볼 도상 2종인데(색이 PNG 에 구워져 있어 틴트가 불가 — `race_screen.gd:_icon_texture()` 가 `<id>_alt.png` 를 우선 조회), **이것도 양자화로 만들지 않는다:**

- D10 §2.4 의 규정이 **"색상만 교체·도상 불변"** 이다. AI 재생성·재양자화는 도상 변형 위험을 안는다.
- 옳은 경로 = **기본 도상의 결정적 색 치환.** 교체쌍이 exact 값이므로 치환도 exact이고, **도상 불변이 도구 수준에서 보장**된다(집행 실측: 알파 채널 차이 0px). 값 출처는 여전히 정본 §6 단일.
- 따라서 **대체 스트립도 색각 양자화도 영구히 불요**하다.

**집행 완료 (2026-08-17 · IMPL-163):** `symbol_line` `#7BE04A`→`#E8EEF5` 42px · `symbol_trouble` `#FF4A3D`→`#7A5CFF` 118px, 양쪽 알파 차이 0px.

---

## 5. 카테고리별 표준 레시피

**공통:** `prompt` = §4.2 RD 프리픽스 + 사양서 해당 항목의 영문 프롬프트. 전 호출 **`rd_fast`**.

### 5.1 A급 — RD가 원도를 그대로 낼 수 있다

| 에셋 | 원도 | `prompt_style` | `num_images` | 회당 |
|---|---|---|---|---|
| **UI 아이콘 27종** (사양서 §6) | 32×32 | `rd_fast__low_res` | 2 | $0.034 |
| 글리프 패드·키캡 (§8) | 16×16 | `rd_fast__low_res` | — | **❌ 기각 (IMPL-314)** |
| 글리프 터치 (§8) | 24×24 | `rd_fast__low_res` | — | **❌ 기각 (IMPL-314)** |
| 9패치 버튼·툴팁 (§7) | 24×24 | `ui_element:rd_flux`* | 2 | ~$0.034 |
| 9패치 패널 (§7) | 36×36 | `ui_element:rd_flux`* | 2 | ~$0.034 |
| 릴 프레임 (§7) | 48×48 | `ui_element:rd_flux`* | 2 | ~$0.035 |
| 머신 셀 측면 (§4) | 96×48 → **128×64** | `game_asset:rd_flux` | 1 | **✅ 성립 (IMPL-330)** — 규격 개정 확정 후 |
| 머신 셀 추격 (§4) | 64×64 → **80×80** | `game_asset:rd_flux` | 1 | **✅ 성립 (IMPL-330)** — 〃 |
| 얼굴 차분 레이어 (§3) | 112×112 | `portrait:rd_flux` | 2 | ~$0.038 |
| **캐릭터 스탠딩 13인** (§3) | 180×240 | `portrait:rd_flux` | **1** | ~$0.024 |
| 창 아이콘 마스터 (§11) | 128×128 | `game_asset:rd_flux` | 2 | $0.039 |

\* `ui_element:rd_flux` 는 **최소 64px** 이다. 24·36·48 원도는 **하한 미달**이므로 두 선택지 중 하나를 쓴다: ⓐ `rd_fast__low_res`(최소 16)로 원도 크기 직접 생성 ⓑ `ui_element:rd_flux` 로 64~128에 생성 후 **무료 `k_centroid_downscale`** 로 정수 축소(64→32, 128→32 등 **정수배만**). 9패치는 코너 규격이 걸려 있어 ⓑ의 축소가 코너를 뭉갤 수 있다 — **실기 대조 후 확정**([가안]).

> **⚠ 글리프 20종 = RD 기각 (2026-08-23 · IMPL-314 실측 · $0.033).** `rd_fast__low_res` 16×16 ×2 를 뽑았더니 **같은 호출·같은 프롬프트의 2장이 서로 아무 관계가 없었다**(고유색 20 vs 8 · 실루엣 불일치 · 한 장은 dpad 가 아니라 흩어진 마름모 4개). 부수로 **잉크 36px 에 고유색 20 = 안티에일리어싱**이라 16px 에서 도트 그리드가 서지 않고 색도 전부 순회색이었다.
>
> **판정 축은 품질이 아니라 family 다.** 패드 16종은 *"같은 버튼인데 하나만 다르다"* 로 정의된 세트고 **10종이 미러 또는 반복**인데, `rd_fast` 는 `reference_images` 를 지원하지 않으므로 *"이것과 같게, 하나만 다르게"* 를 표현할 방법이 **구조적으로 없다**(§2 · §8 재검에서 세 번 확인된 사실). 9패치 기각(§9-④)이 *기하 계약* 때문이었다면 이쪽은 *family 계약* 때문이고, **둘 다 "정확히 같아야 한다"는 요구**다.
>
> **채택 = `frame_gen.js` 의 `glyph` 타입** — 공유 base + 표식 + 미러. 상세 = 배치 대장 §4.3.1.
>
> **이 기각이 확대 적용되는 범위와 안 되는 범위를 갈라 둔다:** ⓐ**확대되는 곳 = 머신 37**(오버레이가 베이스와 **동일 셀·동일 좌표계**여야 한다 — 대장 §4.5 · 같은 family 계약) · **캐릭터 표정 차분 34**(같은 인물 유지 — §7 이 이미 `rd_pro` 승인 후보로 명기) ⓑ**확대되지 않는 곳 = 캐릭터 스탠딩 13**(서로 다른 13인이라 인물 간 동일성 요구가 없다 — 각 시트가 독립 도상이므로 RD 가 강한 영역 그대로다).

> **⚠ 머신 셀 37장 = RD 기각 (2026-08-23 · IMPL-319 실측 · $0.092 · 4경로 대조).** 규격 치수가 **RD 의 실사용 하한 아래**다.
>
> | 경로 | 실측 | 판정 |
> |---|---|---|
> | 96×48 직접 (`rd_fast__low_res`) | 잉크율 **17.9%** · 고유색 19 · **차를 알아볼 수 없다**(어두운 얼룩) | ❌ |
> | 64×64 직접 (`game_asset:rd_flux`) | 잉크율 22~26% · 2장 모두 **형체 불명** | ❌ |
> | **192×96** (`game_asset:rd_flux`) | ✅ **성립** — 오픈휠 측면 실루엣·스틸그레이×아이스시안 리버리·**13번 판독** | 참고: 규격 밖 치수 |
> | 192×96 → **2:1 정수 축소** | **13번·바퀴 소멸**, 실루엣 뭉개짐 | ❌ (§9-④ 각주 ⓑ 경로 기각) |
>
> **일반화 — RD 의 하한은 절대 픽셀 수가 아니라 "대상 복잡도 대비 픽셀 수"다.** 32×32 아이콘 27종은 RD 산출로 성립했다(IMPL-160) — 단일 기호이기 때문이다. **64×64 머신은 그보다 4배 넓은데 실패한다** — 오픈휠 차량은 바퀴 4·윙 2·보디·번호를 한 칸에 담아야 한다. 이 회차의 모든 기각(9패치 24~48px · 글리프 16px · 머신 48~64px)과 성립(아이콘 32px · 스탠딩 180×240)이 이 한 축으로 정리된다.
>
> **부수 — 머신 셀에서는 야간 프리픽스도 빼야 한다.** `night neon mood` 를 넣으면 셀이 통째로 어두워져(상위색 `#010213`·`#00010A`) 리버리와 번호가 사라진다. 머신 셀은 **씬 컷 위에 합성**되므로 야간 정조는 컨테이너가 공급하고 셀 자체는 읽혀야 한다 — `brightly lit` 계열로 갈아야 리버리가 실린다(§4.2 캐릭터·머신 행의 연장).
>
> **남는 경로 = 수작업 · `rd_pro` 참조 이미지 · 규격 재검(정본 사안).** 상세·판정 요청 = 배치 대장 §9-㉙ ⓒ.

**아이콘 배경:** 사양서 §6 공통 토큰이 `투명 배경`을 요구한다. `create_inference(remove_bg=true)` 또는 생성 후 `background_remover`($0.01). **`remove_bg` 가 추가 과금인지 미확인** — 호출 전 `estimate_inference_cost` 로 확인한다(무료). 또는 `rd_fast__mc_item`(자동 투명·16~128)을 쓴다.

### 5.2 B급 — **384px 상한을 넘어 RD 단독으로 최종 원도를 못 낸다**

`rd_fast` 최대 = **384×384**. (`rd_pro` 는 256이라 더 작다 — 큰 그림에서 `rd_pro` 는 대안이 아니다.)

| 에셋 | 원도 | 초과분 |
|---|---|---|
| 씬 컷 58프레임 · 무대 배경 10장 | **408**×100 | 폭 +24 |
| 배경 개러지 가로 · VN 2종 | **640**×**360** | 폭 +256 |
| 개러지 세로 | **408**×**780** | 폭 +24 · 높이 +396 |
| 키 비주얼 · 전용 CG 6장 | **640**×**360** | 폭 +256 |

**대응 (권고 순):**

1. **씬 컷·무대 배경 (408×100)** — **384×100 으로 생성**하고 좌우 블리드 24px를 **수작업 연장**한다. 블리드는 가장자리 연속이라 손으로 싸다. 사양서도 "데스크탑은 384 크롭"이라 했으니 **핵심 영역이 정확히 384**다 — 손실이 없다. ✅ 실질 해결
2. **배경·키 비주얼·CG (640×360)** — RD로 **384×216 구도 시안**을 뽑고(같은 3:5.3 비율) **최종 640×360 원도는 수작업 확대·재작화**한다. 또는 320×360 두 장을 같은 `seed`·프롬프트로 뽑아 접합 후 이음매 보정. **어느 쪽도 RD가 최종물을 주지 않는다** — 시안 도구로 쓴다.
3. `outpainting`($0.18)으로 확장하는 길이 있으나 **승인 대상**이고, 도트 정합을 보장하지 않아 권하지 않는다.

> **이것이 발주 계획에 주는 함의:** 사양서 총계 중 **씬 컷 68 + 배경 4 + 대형 8 = 80장이 B급**이다. RD로 자동화되는 것은 A급(아이콘 27·글리프 20·프레임 25·캐릭터 47·머신 37)이고 **B급은 사람 손이 주역**이다. 발주 순서를 A급 우선으로 잡아야 하는 실제 이유이며, 총괄이 승인한 `ui/icons/` 16종 우선과 방향이 같다.

### 5.3 C급 — 생성 대상이 아니다

게이지 바(전방·후방 **90×6** · 섀시 **70×8** · 펄스 **64×12**)는 **높이가 RD 최소 16px 미만**이라 호출 자체가 불가하다. 사양서 §7이 이 계열을 "일부 런타임 렌더"로 둔 것과 정합한다 — **수작업 또는 런타임 렌더**로 간다. RD에 넣으려 하지 않는다.

> **✅ 2026-08-23 보강 (IMPL-311) — 게이지는 "수작업" 쪽도 성립하지 않는다.** 본 절은 *"수작업 또는 런타임 렌더"* 로 두 길을 열어 두었으나, 프레임 회차에서 소비부를 실독한 결과 **원도를 만들면 오히려 깨지는 것이 3건**이다. 상세·판정 요청은 배치 대장 §4.2.1 · §9-㉘. 요지: ⓐ`_fill` 에 색을 구우면 섀시 경고 상태 전환(`gauge_danger()`)과 색각 대체(O9)가 함께 죽는다 ⓑ펄스 세그먼트 수는 `param_charge_cap`(D13 = 10)인데 **64px 폭에 10칸 균일 정수 분할이 없다** ⓒD13 값을 이미지에 굽는 것 자체가 불변규칙 2 저촉이다. **RD 불가는 이 계열의 결론이 아니라 첫 근거였다.**

---

> **⚠ 캐릭터 계열 육안 검수 항목 3건 (2026-08-23 · IMPL-316 실측 — 전건 기계 게이트 없음).**
>
> ⓐ **가상 스폰서 문자가 구워진다 — 긍정 표현으로 못 막는다.** `plain unmarked panels, clean fabric with no lettering and no logos` 를 넣고도 가슴·어깨에 의미 없는 글자열이 계속 나왔다. §9-⑦(네거티브 프롬프트 필드 부재)의 실증이며, **레이싱 슈트라는 소재 자체가 스폰서 마크의 사전 확률을 갖는다.** 카 넘버는 정본 요소(D03 결정 #13)이므로 남기고, **그 밖의 문자열은 눈으로 세어 판정한다.**
>
> ⓑ **실존 브랜드 유사 형상이 실제로 나왔다 — 소재 어휘가 불렀다.** `comet-marked racing suit`(사양서 §3.2 정본 어휘)가 **굽은 스우시**를 만들었다. 사양서 §2.4 구속 *"실존 브랜드 금지"* 직접 저촉이고 **우리가 브랜드를 지시하지 않아도 발생한다.** 교정 = 형상을 직접 규정(`straight diagonal light streak` + `no curved swoosh shapes`). **정본 어휘를 그대로 넣는 것이 안전을 보장하지 않는다.**
>
> ⓒ **외형 정본 문면만으로는 미달한다 — 수치화가 필요하다.** `veteran team boss, stern lined face` 로 **30대**가 나왔다(마르타 = 백전노장 확정). `in her sixties, deeply wrinkled weathered face` 로 해소. 연령·체격처럼 **정도(degree)를 갖는 형용은 모델에 수치로 줘야 한다.**

> **✅ `remove_bg` 는 크기에 의존한다 (2026-08-23 실측).** 48×48 9패치에서는 도상을 통째로 먹었으나(IMPL-311 — 잉크율 6%), **180×240 초상 13/13 은 본체가 온전**했다(성분 1~4 · ≤4px 파편 합 5). 작은 캔버스에서는 배경 제거를 신뢰하지 않고, 큰 캔버스에서는 써도 된다.

## 6. 호출 절차 (매 생성 고정)

```
① estimate_inference_cost 로 금액 확인          ← 무료. 생략 금지
② balance_cost < $0.05 인가?
     아니오 → 중단하고 §7 승인 요청
③ create_inference 실행 (rd_fast · seed 기록)
④ 결과 URL 회수. 비었으면 get_inference_result(request_id)  ← 재생성 금지
⑤ 후처리(무료): pixel_correction → palette_converter(dither none)
⑥ 육안 검수 — 사양서 §2.4 구속 조항 (기계 게이트 없음)
⑦ 배치 대장 §7 절차 (LFS · .import · 에디터 scan)
```

**절대 규칙 3가지**

1. **실패해 보인다고 재생성하지 않는다.** `create_inference` 는 **전달 실패와 무관하게 과금**된다. URL이 없거나 만료되면 **`get_inference_result(request_id)`** 로 회수한다 — 무료다. `request_id` 를 매번 남긴다.
2. **`seed` 를 기록한다.** 구도를 유지한 채 변형하려면 같은 시드가 필요하고, 재현이 안 되면 재과금이다.
3. **비용을 보고한다.** 한 묶음 작업이 끝나면 `get_balance` 로 실제 차감을 확인하고 사용자에게 알린다(추정과 실청구가 다를 수 있다).

---

> **⚠ 추정은 유효성 검사가 아니다 (2026-08-23 · IMPL-330 실측).** 위 절차 ①이 *"금액이 붙는 호출 전에는 예외 없이 먼저 추정한다"* 로 추정을 전제 단계에 두는데, **추정이 통과한 조합이 실호출에서 거부됐다.**
>
> | 단계 | 결과 |
> |---|---|
> | `get_style_usage('portrait:rd_flux')` | `input_image` 사용을 guidance 에 **명시** · `limitations` **비어 있음** |
> | `estimate_inference_cost` (`input_image` 112×112 + `strength` 0.45) | **통과** — `balance_cost 0.019` · `used_input_image: true` · `used_strength: true` |
> | `create_inference` (동일 입력) | ❌ **400 `inference_failed`** · `remove_bg` 제거 후 재시도도 400 |
>
> **즉 `estimate` 는 가격만 계산하는 드라이런이고 스타일·파라미터 조합의 성립을 보증하지 않는다.** `get_style_usage` 의 `limitations` 도 비어 있었으므로 **사전에 알 방법이 없었다.** 절차 ①은 그대로 지키되 **"추정 통과 = 호출 성립"으로 읽지 않는다** — 새 파라미터 조합(특히 `input_image`·`strength`)은 첫 호출을 파일럿으로 삼는다. 실패는 과금되지 않았다(3회 전건 잔액 불변) — **이것이 파일럿의 비용이 0인 이유**다.
>
> **부수 판정: `rd_fast` img2img 경로는 닫혔다.** `reference_images` 미지원(4회 확인)에 더해 `input_image` 경로도 실호출이 거부되므로, **동일성이 필요한 묶음(표정 차분 34 · 머신 오버레이 23)에 `rd_fast` 를 쓸 방법은 없다.**

## 7. 승인 요청 형식 (규칙 §0-③)

규칙을 벗어나야 할 때 **호출하지 말고** 아래를 제시한다.

```
[RD 승인 요청]
· 무엇을: <에셋 id·이름·수량>
· 왜 규칙을 벗어나야 하는가: <rd_fast/한도로 안 되는 이유 — 실측 근거>
· 요청 호출: 모델 <…> · 스타일 <…> · 크기 <…> · 장수 <…>
· 예상 비용: $<estimate 실측값> (규칙 상한 $0.05 대비 <n>배)
· 대안과 그 한계: <더 싼 경로를 왜 못 쓰는지>
```

**전형적인 승인 후보 3가지**

| 상황 | 요청 |
|---|---|
| 캐릭터 13인의 **표정 차분 34장**에서 인물 동일성이 무너짐 | `rd_pro__default` + `reference_images` ($0.18/장) — `rd_fast` 는 참조 이미지를 지원하지 않는다(실측) |
| 생성물의 **일부만** 고치면 되는데 재생성이 아까움 | `inpainting` ($0.18) |
| 씬 컷 블리드를 손으로 못 늘리겠음 | `outpainting` ($0.18) — 단 §5.2 권고는 수작업이다 |

---

## 8. 이 문서가 낡는 지점 (재검 절차)

> **✅ 재검 실시 (2026-08-23 · IMPL-311 — 이미지 A급 묶음 2 착수 시점 · §8 "묶음 발주를 새로 시작할 때" 조항 이행).** 무료 조회 4건 + 유료 3회차 실측.
>
> | 축 | 재검 결과 |
> |---|---|
> | `get_service_status` | `rd_fast`·`rd_pro`·`rd_plus`·`animations`·`background_removal` 전건 `ok` |
> | 잔고 (`get_balance`) | 착수 시 **$9.225** — 직전 재검(IMPL-245) 이후 지출 0 확인 |
> | `list_available_styles(rd_fast, tab:image)` | 15종. `ui_element:rd_flux` **min 64 · max 384** · `rd_fast__low_res` **min 16 · max 128** · `supports_reference_images` **전 스타일 false** — §3·§5.1·§2 전건 유효 |
> | 비용 (`estimate_inference_cost`) | 48×48 ×2 = **$0.034** · 96×96 ×2 = **$0.036** — §3 표와 정합(면적×장수 선형) |
> | **추정 대 실청구** | 3회차 전건 **추정 = 실청구** (0.034 / 0.036 / 0.036). 잔고 9.225 → **9.119 = $0.106** · 절대 규칙 3("비용을 보고한다") 이행 |
>
> **본문 갱신 3건** — §9-④⑤⑥ 전건 종결(9패치 RD 기각·`remove_bg` 무과금·`bypass_prompt_expansion` 규칙 확정). §9-⑤ 는 **본 문서의 지시 자체가 틀렸던 건**이다(추정 도구에 `remove_bg` 필드가 없다).

> **✅ 재검 실시 (2026-08-19 · IMPL-245 — 총괄 발주 §7.1 부수분 · 무료 조회 전속).** 이미지 A급 재개 대비 데이터 신선화. **§3 비용표·§5.1 스타일·§2 참조 이미지 미지원 전건 유효 — 본 문서 갱신 불요.**
>
> | 축 | 재검 결과 |
> |---|---|
> | 비용 (`estimate_inference_cost` · 32×32 ×2) | **$0.034 — §3 표와 일치** |
> | 잔고 (`get_balance`) | `credits 50` · `balance $9.225` — 직전 회차(IMPL-215) 이후 지출 0 |
> | `rd_fast__low_res` | **min 16 · max 128** · `max_number_of_images` 16 — §5.1 의 16/24/32/64 원도 전건 사정권 |
> | `ui_element:rd_flux` | **min 64** — §5.1 각주("최소 64px")와 일치, 9패치 24·36·48 하한 미달 판단 유효 |
> | `rd_flux` 계열 전반 | min 64 · **max 384** — §5.2 "rd_fast 상한 384" 유효(B급 80장 판단 불변) |
> | **`supports_reference_images`** | **rd_fast 전 스타일 `false`** — §2 의 구조적 제약 재확인. IMPL-214·215 가 이 사실 위에서 "RD 는 새 도상에 강하고 축약에는 못 쓴다"를 세웠고 그 전제가 유효하다 |
>
> **신규 관측 2건 (본문 미반영 — 발주 시 참조):** ⓐ `low_res` 상한이 **128** 이라는 것이 목록에 처음 잡혔다(§5.1 은 최소값만 각주로 다뤘다) — 32px 아이콘·16px 세트·글리프는 전부 안쪽이라 영향 0 ⓑ 카탈로그에 §5.1 미등재 스타일이 있다(`item_sheet`·`character_turnaround`·`1_bit`·`texture`·`simple`·`detailed`·`retro`·`no_style`·`mc_texture`) — 현 발주 계획에 대응 항목이 없어 채택하지 않았다. **스타일 id 접두 규칙이 여전히 불규칙**(`rd_fast__low_res` ↔ `game_asset:rd_flux` 공존)해 §8 의 *"추측하지 말고 목록에서 집는다"* 는 그대로 유효하다.


수치·이름은 전부 서비스 실측이므로 **서비스가 바뀌면 본 문서가 틀린다.** 다음 시점에 무료 조회로 재검한다.

- **에셋 묶음 발주를 새로 시작할 때** — `list_available_styles(model="rd_fast")` 로 스타일 id·크기 한계 확인, 대표 크기 1건 `estimate_inference_cost`.
- **비용이 추정과 어긋날 때** — `get_balance` 전후 대조.
- **스타일 id 가 거부될 때** — id는 접두 규칙이 일정하지 않다(`rd_fast__low_res` 와 `game_asset:rd_flux` 가 공존한다). **추측하지 말고 목록에서 집는다.**

---

## 9. [가안]·보고 대장

| # | 항목 | 내용 | 판정 창구 |
|---|---|---|---|
| ① | **순색 60 팔레트 스트립 미확보 (비아이콘 164장 대상)** | 실측 결과 `master_60.png` = **64색(마스터 60 + 기능 4)** 으로 **아이콘 27종에는 정확**하다 — 발주 1순위는 차단되지 않는다(§4.3.1). 다만 비아이콘 164장은 순색 60만 써야 하는데 그 입력이 없다. 생성기에 출력 1종 추가로 끝나나(**구현 비용 사실상 0**) **파일의 귀속**이 판정 사안이다 — `godot/assets/palettes/` 에 두면 대장 §4.9 "4파일 = 2식" 계약을 바꾸고 `res://` 빌드에도 실린다. **권고 = `tools/palette/`**(계약 무변경·빌드 밖·IMPL-097 전례 정합) | **총괄 판정 대기** — `docs/handoff/에셋트랙_팔레트스트립_총괄발신.md` |
| ⑨ | **[보고] `colorblind_alt.png` 는 팔레트 입력 불가** | 63색 안에 기본색과 대체색이 함께 있다(`REPLACED 4` 분할 칩이 양쪽을 그린다). 사람 대조용으로는 옳고 기계 팔레트로는 무의미하다. **색각 대체는 런타임 옵션(O9)이고 교체 4색이 전부 기능 컬러라 원도가 아니라 표시 층에서 적용된다** — 원도 양자화 대상이 아니라는 해석이며 맞다면 조치 불요 | **총괄 확인** (같은 발신문 §3) |
| ② | 사양서 §2.3 ①② 접힘 | RD는 네이티브 그리드를 내므로 "4~8배 생성 후 정수 축소"가 불필요하다는 판단. 열등하면 `k_centroid_downscale`(무료) 경로로 되돌린다 — 비용 동일 | 실기 |
| ③ | RD 공통 프리픽스 3토큰 | 사양서 §2.1에서 렌더링 어휘를 걷어낸 잔여. 실기에서 톤이 안 나오면 조정 | 실기 |
| ④ | **9패치 생성 경로 — ✅ 종결 (2026-08-23 · IMPL-311): RD 기각·파라메트릭 채택** | 첫 배치 대조를 실측했다(3회차 6장 · $0.106). **두 경로 모두 9패치가 못 된다.** ⓐ`rd_fast__low_res` 48×48 = 프레임이 아예 안 나온다(잉크율 6.0%·21.2% — 시안 막대와 무정형 덩이) ⓑ`ui_element:rd_flux` 96×96 = 의장은 나온다(계기반 베젤·앰버 눈금·시안 림). 그러나 **외곽 1px 링 불투명률 0.0%** — 프레임이 캔버스 끝에 닿지 않고 여백 안에 떠 있다 ⓒ프롬프트를 "edge to edge·hollow centre"로 고치고 `bypass_prompt_expansion=true` 를 켜니 **링 100%** 로 붙었다(= 프레이밍은 프롬프트 문제였다) **그런데도 9패치 3축이 전건 미달이다:** 좌우 불일치 **1584px** · 상하 **2250px**(코너 4개가 같은 규격이 아니다) · 변 가운데 늘림축 불균일 **106~114px / 1024 = 10~11%**(늘리면 변화가 드러난다). 원인은 품질이 아니라 표현력이다 — **확산 모델에는 "정확히 16px" 이나 "이 열은 저 열과 같다" 를 표현하는 층위가 없다.** 아이콘 축약 기각(사양서 §6.8.3)과 같은 축이며 RD 가 **새 도상에 강하고 기하 계약에 약하다**는 판단이 3번째로 확인됐다. **채택 = `tools/assets/frame_gen.js` 파라메트릭 생성** (동심 링 + 코너 4방 미러 → 대칭·코너 정확·변 균일이 **구성으로** 보장) · **RD 산출물은 의장 시안으로 승계**(`docs/assets/시안/rd_frame_시안{1,2}.png`) | **종결 — IMPL-311** |
| ⑤ | **`remove_bg` 추가 과금 — ✅ 종결 (2026-08-23 · IMPL-311): 무과금** | `remove_bg=true` 로 48×48 ×2 를 호출해 잔액 델타를 쟀다: **9.225 → 9.191 = $0.034**, 즉 `remove_bg` 없는 추정치와 **완전히 같다.** **부수로 본 문서의 지시 자체가 교정된다** — 초판은 *"호출 전 `estimate_inference_cost` 로 확인"* 이라고 적었으나 **추정 도구 스키마에 `remove_bg` 필드가 없다**(실측). 그 축은 추정으로 확인할 수 없고 **잔액 전후 대조가 유일한 창구**다. 배경 제거를 별 편집 도구로 부르면 $0.01 이므로 생성 시 옵션으로 켜는 편이 항상 싸다 | **종결 — IMPL-311** |
| ㉚ | **부정 표현 금지 — ✅ 종결 (2026-08-23 · IMPL-349): 사양서 v1.9 §4.2 로 명문화** | §4.2 는 창설부터 *"`create_inference` 스키마에 부정 프롬프트 필드가 **존재하지 않는다**"* 를 적어 두었다. 그런데 내가 쓴 v1.8 초안이 `no wings`·`no numbers` 를 넣었고 **리어 윙과 번호 `3` 이 그대로 나왔다.** 문서가 맞았고 문서를 쓴 쪽이 그것을 다시 읽지 않았다. 긍정으로 바꾸니 통했다(`wingless prototype chassis, flat bare rear deck` — 2장 중 1장 무윙 채택). **총괄이 v1.9 §4.2 에 금지 명문을 신설**해 이제 사양서 층에서도 강제된다. **규율: 배제는 긍정 표현으로 적는다.** 그리고 **파츠 배제 문면이 대상의 정체성을 지우지 않는지 확인한다** — `chassis only` 는 오픈휠 정체성을 함께 지웠다(*"무엇을 빼는가"만 적으면 "무엇인가"가 사라진다*) | **종결 — IMPL-349 · v1.9 명문** |
| ㉛ | **보레아스 섀시 무채 확인 — 상시 축 (2026-08-23 · IMPL-349 · 총괄 승인 IMPL-350 · 트랙 내부)** | 섀시 원판이 순색 60 으로 양자화되며 `#2E6FA8`·`#0B2A5E`·`#6FA8FF` — **악시온 팀 4색 계열**에 앉았다. **보레아스 섀시가 타 팀 색을 달고 있었다**(VH-8 번호 IMPL-347 과 같은 부류의 충돌). 원인은 구조적이다 — 프리픽스 `night neon mood, telemetry glow` 가 **무채 소재에 청 기미를 얹는다**(정본 §1 코어 뉴트럴도 청 기미를 갖고 있어 최근접 양자화가 팀 블루로 떨어진다). **규율: 보레아스 베이스는 양자화 후 고유색 전건이 무채(뉴트럴 16)인지 확인한다.** 팩토리 8장은 팀 컬러가 정본이라 대상이 아니다 — **보레아스 섀시 전속**이다(v1.9 이후 리버리가 오버레이 층 소관이므로 베이스에 팀 컬러가 있으면 안 된다) | **상시 — 트랙 내부 게이트** |
| ㉜ | **표정 차분 34 = RD 기각 (2026-08-23 · IMPL-358): `rd_pro` 는 동일성을 주고 정합을 주지 않는다** | 사용자 착수 지시 후 **파일럿 6장 · 4호출 · $1.080** 실측. 운용지침 §7 이 이 건을 *"전형적 승인 후보 1순위"* 로 두었고 `supports_reference_images: true` 도 확인됐다 — **동일성은 실제로 지켜졌다**(4회 전건 같은 인물로 읽힌다). **깨진 것은 정합이다.** ⓐ`reference_images` 1차: 인물 유지·**프레이밍 이탈**(얼굴이 셀을 채우지 않고 어깨까지 물러난다) ⓑ`input_image`+`strength=0.35`: **구도가 전신으로 재구성**됐다(정합 보존 도구로 쓸 수 없다 — 추정은 `used_input_image: true` 로 통과했다) ⓒ프레이밍 토큰 명시(`extreme close-up, face fills the whole frame, chin near the bottom edge`): **프레이밍 해결**(2/2) — 그러나 팀색 접미를 빠뜨려 마젠타·주황 림라이트 ⓓ`desaturated steel grey and ice blue only` 추가: **2장 중 1장 채택 가능**(동일성·프레이밍·무채·표정·투명 전부 통과) · 1장은 **배경에 글자 베이크**(`LAT-5`·`BRAKEPRESS`) — §9-⑥ 이 등재해 둔 실패 양태이고 D12 §8.1 위반이라 기각. **그런데 채택안을 실제로 합성하니 못 쓴다** — 기본 시트 위 (34,0) 에 얹으면 **레이어 사각 경계가 보이고 턱이 두 개**가 된다. 머리 폭·턱 위치가 몇 px 다르고, 사양서 §3.1 의 *"얼굴 영역 레이어 교체"* 는 **눈·코·입이 같은 좌표에 올 것**을 요구한다. **"레퍼런스와 비슷한 프레이밍"과 "픽셀 정합"은 다른 명제다** — 확산 모델은 전자를 하고 후자를 못 한다(9패치 기하 계약·글리프 family 계약·머신 오버레이 좌표 정합과 **네 번째 같은 축**). **경제성도 성립하지 않았다:** 채택률 1/2 로 34장이면 ~$12 이고 잔고는 $6.933 이다. **전량 집행 전에 파일럿에서 멈춘 것이 이 항목의 값이다.** **대체 경로 = 파생 + 좌표 편집.** 기본 시트 자기 얼굴 구획을 원판으로 삼고 눈썹·눈·입 픽셀만 좌표로 고치면 **정합이 구성으로 보장**된다(보레아스 3단계에서 실증한 알파 잠금 — IMPL-349). 표정은 30~80px 규모이므로 바퀴 작화가 아니라 패치 사정권이다 | **기각 — IMPL-358 · 대체 경로 상신** |
| ⑥ | **`bypass_prompt_expansion` — ✅ 종결 (2026-08-23 · IMPL-311): 기하 지시가 있는 호출에서 켠다** | 같은 소재를 확장 켜고/끄고 뽑아 대조했다. **끈 쪽만 "캔버스 끝까지"가 지켜졌다**(외곽 링 불투명률 0.0% → **100.0%**). 확장은 프롬프트를 그림 설명으로 부풀리면서 **배치 지시를 소재 묘사로 희석한다** — "edge to edge" 같은 문면은 확장을 통과하면 사라진다. **규칙:** 기하·배치·여백을 지시하는 호출(프레임·9패치·스트립·정렬물)은 `bypass_prompt_expansion=true`. 소재 표현이 주인 호출(캐릭터·머신·씬)은 확장을 켠 채로 둔다. **부작용 1건 관측:** 확장을 끈 96×96 2장 중 1장이 **숫자 글립("40.98" 형상)을 그려 넣었다** — 계기반 소재를 직설하면 모델이 계기 표시까지 그린다. 텍스트가 원도에 구워지면 D12 §8.1(전 표시 문자열 = 키 참조)과 어긋나므로 **육안 검수의 배제 항목에 "베이크된 글자·숫자"를 추가**한다. **⚠ 개정 (2026-08-23 · IMPL-349) — 이 행의 예외가 뒤집혔다.** 초판 규칙은 *"소재 표현이 주인 호출(캐릭터·머신·씬)은 확장을 켠 채로 둔다"* 였으나, 머신 섀시 원판(A-MC-00)이 **소재 표현이면서 동시에 강한 구속**(오픈휠·무윙·무번호)을 담고 있었고 확장이 그 구속을 지웠다 — 발행 문면 그대로 뽑은 **4장 전건이 지붕·창·리어윙 있는 로드카**였고, 확장을 끄자 포뮬러 정체성이 유지됐다. **판정의 축은 "소재냐 기하냐"가 아니라 "문면에 지켜져야 할 구속이 있느냐"다** — 계열이 아니라 프롬프트의 내용이 정한다(§4.2 표로 승격). **부수 자기 정정:** IMPL-349 회신에서 이 건을 *"[가안] 해소·첫 실효 사례"* 로 보고했으나 **틀렸다** — [가안]은 IMPL-311 에서 이미 종결됐고 실효 사례도 그때가 처음이다. IMPL-349 가 보탠 것은 **예외의 반증**이다. 본 문서를 다시 읽지 않고 보고한 것이 원인이며, 같은 회차에 *"운용지침 §4.2 를 다시 읽지 않아 부정 표현을 썼다"* 와 **원인이 동일하다** | **종결 — IMPL-311 · 개정 IMPL-349** |
| ⑦ | **[보고] 네거티브 프롬프트 필드 부재** | 사양서 §2.2 공통 네거티브를 **기계로 강제할 수 없다**. 방어는 스타일 선택·긍정 표현·육안 검수 3겹뿐이며 **기계 게이트가 없다** | 기록 (검수 절차로 흡수) |
| ⑧ | **[보고] B급 80장은 RD 단독 불가** | 씬 컷 68 + 배경 4 + 대형 8 이 384px 상한 초과. 씬 컷은 384 생성 + 블리드 수작업으로 실질 해결되나 **배경·CG 12장은 RD가 시안까지만** 낸다 | 기록 (발주 계획 반영) |
