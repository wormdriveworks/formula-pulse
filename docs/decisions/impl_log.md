# 구현 결정 로그 (impl_log)

> 설계 문서가 답하지 않는 구현 세부의 결정·[가안]을 기록한다.
> 설계 층위 판단이 필요한 사안은 여기 기록하지 말고 사용자에게 질문한다.

| 날짜 | ID | 결정 | 근거 | 관련 문서/규칙 | 상태 |
|---|---|---|---|---|---|
| 2026-08-18 | IMPL-226 | **섹터 속성 `_16` 결선 + 저장 표시 위치 결함 교정 (총괄 회신 §4-①⑤).** **①창구 공유 처리** — `_icon_texture()` 를 릴 심볼(32 무배율)과 섹터 속성(16)이 함께 쓰므로 **치수 축을 소비 지점으로 뺐다**(`variant` 인자 · 기본값 = 32). 창구 안에서 치수를 판단하면 두 소비부가 서로의 규격을 밟는다. 두 축의 이름 순서를 `<id>[_alt][_16]` 로 고정 — O9 대체는 도상 축, 치수는 치수 축이라 독립인데 순서를 안 정하면 같은 파일을 두 이름으로 찾는다. **`_16` 부재 시 32 로 되돌리지 않는다** — 되돌리면 비정수 축소가 조용히 되살아난다(Zone A 가 정확히 그 상태였다). 데이터는 접미 없는 base id 를 그대로 유지 — 치수는 표시 층 사정이므로 데이터에 새기지 않았다. **②저장 표시 위치 결함 교정 (8차 IMPL-218 자산분)** — `_ready()` 에서 `set_anchors_preset(PRESET_TOP_RIGHT)` 를 크기 설정 **전에** 불러, 기본값 `keep_offsets=false` 가 **당시 rect(0,0,0,0)를 보존하도록 오프셋을 역산**해 `offset_left = -부모폭` 이 됐다. 결과 = 아이콘이 **좌상단**에 떴다(실측 640 캔버스에서 rect `P=(0,0)` · 우측 여백 **608**). D09 §181 은 우상단 명문이다. 앵커만 세우고 오프셋을 도상 크기로 직접 적어 순서 의존을 없앴다. **8차 회차에는 못 잡았다** — 2.0s 표시 창이 스크린샷 왕복(2~5s)보다 짧아 실기로는 한 번도 보이지 않았고, 이번에 화면을 트리에 세워 rect 를 재는 방식으로 드러났다 | 총괄 회신 §4-①⑤ · 대장 §4.1.1(16px 16종) · D09 §181 · D10 §2.2 | `race_screen.gd` · `save_indicator.gd` · `test_ui_screens.gd` · `run_tests.sh` | 확정 |
| 2026-08-18 | IMPL-228 | **검사 2군 신설 + 돌연변이 5건 (IMPL-226 부대).** **⑫ 도상 치수 2규격 공존** — 섹터 속성 6종이 `_16` 파일·16×16 인지, 릴 심볼 6종이 접미 없는 32 를 **유지**하는지, A존 실배치 슬롯이 등배(`scale≈1.0`)인지, 캐시 키가 치수로 갈리는지. **파일명과 실 rect 를 함께 본다** — 슬롯 크기만 보면 두 규격이 우연히 맞는 경우를 통과시키고, 상수만 보면 창구가 그것을 쓰는지 알 수 없다. **저장 표시 위치 3축 추가** — 우측 여백 0 · 상단 여백 0 · 회전 피벗 = 도상 중심(피벗이 0 이면 좌상단 축으로 휘돈다). 앵커가 아니라 **부모 폭 기준 실 rect** 로 보는 것이 핵심이다: 앵커만 보면 결함이 통과한다. 검사 수 130 → **138**. **돌연변이 5건 전건 검출** — ①창구가 치수를 스스로 정함(릴까지 `_16`) → 릴 6건 + 캐시 키 FAIL ②속성 호출부가 접미 미전달 → `scale=0.5000` FAIL(Zone A 회귀를 정확한 수치로) ③`_16` 부재 시 32 폴백 → 3축 FAIL ④위치 결함 되돌림(preset 역산) → `우측여백=608.0` FAIL ⑤피벗 0 → 피벗 FAIL | 불변규칙 7 · [[check-evidence-rule]] | `test_ui_screens.gd` · `run_tests.sh` | 확정 |
| 2026-08-18 | IMPL-225 | **[총괄 접수·판정] 원격 9차 + 에셋 9차 일괄 처리 — 씬 색 리터럴 정리 트랙 종결 선언** (`docs/handoff/MS3_원격9차_에셋9차_총괄회신.md`). **검수** = 게이트 재현(90·**warnings=0 — PAL 차단형·PASS**·suites=14·UISCR 130·ICON_CHECK 8211·ICON_DRAW 17) · `_fail("PAL")` 5곳/`_warn` 0곳 · csv 2행(D13 v1.8 전사) · attr `_16` 6종+garage_16 실물. **판정** — ①PAL 5곳 전부 차단형 승인("검사 무결성 경고 = 검사를 끄는 우회로" — 불변규칙 7 대칭 적용·IMPL-198 실증 근거) + CLAUDE.md 불변규칙 7 편입 추인(차기부터 문면 제안 관례 부기) ②8차 주석 사실 오류 교정 접수 + **M5 항진명제 원칙 승격**("같은 창구의 두 값 비교는 창구가 무너지면 함께 무너진다") ③에셋 — garage_16 세로 렌치·attr 6종 접수 · **"축소는 파편을 지우는 게 아니라 만든다"**(주력 가설 방향 반전 실측 — 파편 재판정 축을 화면 가시성→원도 위생으로 갱신 승인) · 검사 한계 기록(방향 반전 = 기계 밖·눈 소관) 승인 · 서술 오류 자기 진단 기록. **트랙 종결** — 발견(주력 5차 눈)→검사(PAL 경고형→제외 축→소스 검증)→판정(§9.2.2 본항+증보 1~4·D10 v1.2·D09 v1.3·D13 v1.8 경유)→치환(7~8차·PAL 0)→게이트화(차단형). 라우팅 = 주력 9차 5건(attr 결선·파편 재판정·garage 재판독·정규화 커밋·저장 표시 실기) 단일 잔여 | 잔여 큰 축 = 사운드 실물·T7 서사·폰트 8px 원도(에셋 발주)·TL 게이트·패드 회차 | `MS3_원격레인_9차_완료회신.md` · `에셋트랙_9차배정_회신.md` · IMPL-219~224 | 확정 — 트랙 종결 |
| 2026-08-18 | IMPL-224 | **섹터 속성 6종 `_16` 발주·유입 — 16px 세트 10 → 16종** (총괄 판정 `MS3_주력8차_총괄회신.md` §3 에셋 ② · §2-4 채택). **직전 회차에 내가 발주를 보류한 건이고, 보류 전제가 주력 실측으로 뒤집혔다.** IMPL-215 는 대장 §9-⑱ 에 세 근거로 보류했다 — ①선언 1곳뿐 ②주력이 Zone A 밀도를 문제로 올리지 않았다 ③릴 심볼이 32 고정이라 속성만 16 으로 내리면 크기 위계가 갈린다. 주력이 화면을 실제 트리에 세워 레이아웃을 돌린 결과 **③이 성립하지 않는다**: `ATTR box=(16,16) tex=(32,32) KEEP_ASPECT_CENTERED → ATTR_RENDER_SCALE=0.5000` — **속성은 지금도 16 논리px 로 표시**되므로 위계는 **이미 갈려 있고** 16px 원도를 넣어도 표시 크기는 바뀌지 않는다. 바뀌는 것은 **그 16px 이 작화된 것이냐 2:1 니어리스트가 버린 절반이냐**다. ①②는 유효하나 **판단 축이 위계에서 정보 손실로 옮겨가며 무력해졌다** — 보류가 근거를 잃었으므로 채택이 맞다. **집행:** 6종 손 작화(§6.8 규격·32px 계열 조달·신규 색 유입 0) · 잉크 42~109 · 성분은 32px 판과 정합(`battle_zone` 2 · `pulse_section` 3 · 나머지 1, **단 `hazard` 는 6** — 낙하 점열을 버리고 갈라진 슬래브 6장으로 재구성했으므로 32px 판의 5와 다르며 의도다) · `.import` 6건 · 사양서 §6.8.1 대상 집합 10→16 + §6.8.3 축소 표본 2건 보강 + §6.8.4 버린 것 4행 · 대장 §4.1.1 6행 + **§9-⑱ 종결** | **부수 실측 1건이 7차 파편 판정의 전제를 바꾼다.** 주력이 *"2:1 축소는 홀수 좌표를 버린다 — 파편 판정이 '작아서 안 보인다'가 아니라 '축소가 지웠다' 였을 가능성"* 을 올렸는데, 재현해 보니 **축소는 파편을 지우는 것이 아니라 만든다**: `attr_sweeper` 의 호 끝단 테이퍼가 2:1 에서 **끊겨 고립 점 1개가 된다**(IMPL-214 가 32px 원도에서 바로 그 끝단을 연결해 1성분으로 만든 자리다). `attr_straight` 는 **중앙 파선이 소멸**한다. 그래서 16px 판에서는 두 요소를 **의도적으로 살려 그렸다** — 주력의 파편 재판정이 겨눌 대상이 실재해야 판정이 성립한다. **대장 §9-⑭ 판정 축을 갱신했다**: 속성 결선이 `_16` 으로 옮겨가면 32px 판의 ⓑ 는 화면에 뜨지 않고 ⓐ 는 16px 판에 좌표 대응이 없으므로, **두 건은 화면 가시성 문제에서 원도 위생 문제로 성격이 바뀐다.** **검사 한계 1건을 실측으로 명기했다** — `attr_battle_zone_16` 우측 화살표 방향을 반전(정의 `mirror` 제거 + 재렌더)했더니 `icon_draw --check`·`icon_check` **둘 다 통과했다.** 정의와 실물이 함께 바뀌면 대조가 성립하고 기하 축은 성분 수·치수·팔레트만 본다 — **D10 §5.3 "마주 미는" 방향은 기계 밖·눈 소관**이다(IMPL-162 가 32px 에서 같은 결함을 눈으로 잡았다). 사양서 §6.8.2 에 등재 | 총괄 회신 §3 에셋 ② · §2-4·5 · 주력 8차 회신 §5-③ · 사양서 §6.8.1~6.8.4 · 대장 §4.1.1·§9-⑭·§9-⑱ · D10 §5.3 · IMPL-162·214·215 | `icon_art.json` · `icon_manifest.json` · `attr_*_16.png` 6 + `.import` 6 · 사양서 · 대장 | 확정 — 검사 한계 1건 등재 |
| 2026-08-18 | IMPL-223 | **`achv_garage_16` 재작업 — 보고문과 실물이 어긋난 것을 주력 실배치가 잡았다** (총괄 판정 §3 에셋 ① · §2-2 반대 권고 채택). **IMPL-215 는 이 아이콘을 *"렌치+리프트 2요소 → 렌치 1요소"* 로 보고했다. 실물은 그렇지 않았다** — 32px 판이 이미 렌치 2개 교차이고 16px 는 **그 교차 형태만 남기고 공구 머리를 잃어** "공구"가 아니라 **얇은 X 표시**로 읽혔다(주력: 같은 행 `achv_driving_16` 과 나란히 두면 차이가 확연). **내가 도상을 잘못 서술한 것이고, 서술이 틀렸으니 §6.8.4 의 "버린 것" 기록도 틀려 있었다.** **원인 진단:** 16px 에서 대각선은 계단으로 부서지고, 그 계단이 먹는 것이 하필 **머리 형태**다 — 공구를 공구로 읽히게 하는 것이 대각선이 아니라 머리이므로 배치를 바꿔야 한다. **재작화:** 세로로 세운 **콤비네이션 렌치 1개** — 오픈엔드 조(위·4px 열린 홈) + 샤프트 + 박스엔드 링(아래·투명 구멍). 잉크 58 → **68** · 성분 1 유지 · 색 계열 불변(`#12A6C4` + 조 하이라이트 `#3FE0F5`) | **총괄 판정이 "제안대로 렌치 1개 + 머리 형태 보존" 이었고 그 두 조건을 기하로 환산했다** — 렌치 1개 = 교차 폐기 · 머리 보존 = 조와 링을 각각 4행 이상 확보. 세로 배치를 택한 것은 **대각선 회피가 목적**이며, 이것이 16px 축약의 일반 교훈이라 사양서 §6.8.4 에 *"16px 에서 공구를 읽히게 하는 것은 대각선이 아니라 머리 형태다"* 로 남겼다. **돌연변이 2종 검출:** 정의 1칸 변조(렌더 미실행) → `ICON_DRAW` 바이트 4개 어긋남 · `attr_hazard_16` 슬래브 1장 소거 → `SEP` 성분 5 != 선언 6. 부수로 **선행 회차 재번호 잔여 2건 교정**(사양서·대장 판올림 주석의 `IMPL-214·212` → `214·215` — IMPL ID 선점 재번호 시 `211·212` 형태의 병기 문면에서 뒤 번호가 남았다) | 총괄 회신 §3 에셋 ① · §2-2 · 주력 8차 회신 §5-② · 사양서 §6.7·§6.8.4 · 대장 §9-⑰ ⓑ · IMPL-215 | `achv_garage_16.png` · `icon_art.json` · `icon_manifest.json` · 사양서 · 대장 | 확정 (돌연변이 2종 실측) |
| 2026-08-18 | IMPL-220 | **PAL 차단형 전환 집행 — 조건부 기승인(IMPL-209) 발동 (총괄 판정 IMPL-219 ①).** 발동 조건 = "위반 0 실측"이 주력 치환(IMPL-206·217)과 총괄 독립 재실측으로 성립했고, 본 레인 실측도 `VALIDATORS_PASS warnings=0` 로 일치했다. FONT 전례(IMPL-147 경고형 신설 → 148 판정 → 168 집행)와 같은 절차다. **`_warn`→`_fail` 은 대장 밖 색 1곳만이 아니라 PAL 의 경고 5곳 전부에 적용했다** — 대장 밖 색(위반 본체) 외에 ①조달 대장 적재 실패 ②소스가 한 색도 기여 못 함 ③기능색 출처가 한 색도 기여 못 함 ④허용 목록 항목에 근거(reason) 부재. **검사 자신이 서지 못한 상태를 경고로 남기면 "조달 경로를 망가뜨리는 것"이 곧 검사를 끄는 우회로가 된다** — 실제로 `master_56`→`master_60` 개명이 그 형태로 대장 절반을 죽인 채 통과했고(IMPL-198 발견) 그 구멍을 IMPL-201 이 신호로 바꿔 놨는데, 신호가 경고면 차단형 게이트에서 여전히 통과한다. 불변규칙 7의 "검증기를 우회·완화·비활성화하지 않는다"를 검사 무결성 축에도 대칭 적용한 것이다. **V7 무접촉**(작법 판단 = D04 소관). `CLAUDE.md` 불변규칙 7·`run_validators.gd` 머리 주석의 차단형 목록에 PAL 편입 | 총괄 판정 IMPL-219 ①(발동) · 조건부 기승인 IMPL-209 · 불변규칙 7 · FONT 전례 IMPL-148·168 · IMPL-198 죽은 조달 전례 | `tools/validators/run_validators.gd` · `CLAUDE.md` | 확정 |
| 2026-08-18 | IMPL-221 | **저장 표시 결선 — D13 v1.8 전사 + `configure()` 창구 결선 (총괄 판정 IMPL-219 ②).** D13 v1.8 별첨A §8.1 신설 2행(사용자 승인 2026-08-18 · 결정 #17)을 `core_params.csv` 에 전사: **`param_fx_save_hold_sec,2.0`**(표시 시간 — D09 §181 "2초 내외"의 값 충전) · **`param_fx_save_spin_sec,1.0`**(회전 주기 — 원천 확정). 키 이름은 기존 `param_fx_*` 문법 준수이며 `param_fx_duel_result_hold_sec`(표기 유지 = hold) 의 조어를 그대로 승계했다. 8차에 주력이 값 부재를 이유로 보류해 둔 `app_root._mount_save_indicator()` 의 `configure()` 호출을 결선했다(`data` 를 인자로 받아 `param()` 창구 경유 — 리터럴 0). **부수 교정 1건 — 8차 주석의 사실 오류를 실측으로 정정했다.** 주석은 "없는 키를 부르면 상단 가드가 부팅을 거부한다"고 적었으나, 그 `is_ok()` 가드는 `load_all()` 직후에 서 있고 이 조회는 그 **뒤**라 **행이 사라져도 부팅은 거부되지 않는다**(돌연변이 M5 실측 — 표시가 0.0 으로 "구성됐는데 안 도는" 상태가 된다). 보류 판단 자체는 옳았고(불변규칙 2) 근거 문면만 틀렸다 — 주석을 실측대로 고치고, 보증은 주석이 아니라 회귀 축(표 행 실재)으로 옮겼다 | 총괄 판정 IMPL-219 ②·판정 1(D13 v1.8 발행) · D13 별첨A §8.1 v1.8 +2행 · D09 본문 §181 · 불변규칙 2(값 창구) · 주력 8차 보류 판단(IMPL-218) | `godot/data/tables/core_params.csv` · `godot/ui/flow/app_root.gd` | 확정 |
| 2026-08-18 | IMPL-222 | **UISCR ⑪축 신설 + 돌연변이 7건 (IMPL-220·221 부대).** 저장 표시는 화면이 아니라 **라우터**가 들고(저장 지점 3곳의 화면이 정해져 있지 않다 — D09 §2.4) 있으므로 실화면이 아니라 `app_root.tscn` 을 세워 본다. 축 4종 = ⓐ값 창구(표 행 실재 + 값 일치) ⓑ성공 저장 = 표시 ⓒ실패 저장 = 미표시(§181 용도 = 저장 중 종료 경고의 근거라, 저장 안 된 회차에 회전을 보이면 근거가 거짓이 된다) ⓓ화면 전환 생존. 검사 수 118 → **130**(하한 동반 상향). **설계 함정 ③(항진명제) 자기 적발 1건 — 돌연변이가 내 단언의 결함을 잡았다.** "값이 D13 창구에서 왔는가"를 `indicator._hold_sec == data.param(...)` 로만 보면, **행이 사라진 경우 양쪽이 똑같이 0.0 을 돌려주어 `0.0 == 0.0` 으로 조용히 통과**한다(M5 첫 시행에서 회전 축이 우연히 걸려 겨우 드러났다). 비교와 독립된 사실인 **표 행의 실재**(`data.params.has(...)`)를 먼저 세우는 축을 추가해 닫았고, 재시행에서 3축 동시 실패로 잡힌다. **돌연변이 7건 전건 검출** — M1 대장 밖 색 주입(도색 문맥) · M2 조달 소스 경로 오타 · M3 `configure()` 결선 제거 · M4 실패 저장에도 표시 · M5 D13 행 2개 삭제 · M6 회전 주기를 표시 시간으로 오배선 · M7 표시가 화면 전환에 딸려 나감. **M1 은 첫 주입이 무효였다** — `self_modulate` 자리에 넣어 PAL 의 modulate 문맥 제외(IMPL-184)에 걸렸다(프로토콜 함정 #1). 도색 문맥으로 옮겨 재주입해 검출 확인 | `docs/decisions/verification_protocol.md` 함정 #1(무효 돌연변이)·설계 함정 ③(항진명제) · 불변규칙 7 · IMPL-211 동형 전례(돌연변이가 검사 결함을 잡은 회차) | `godot/tests/test_ui_screens.gd` · `tools/validators/run_tests.sh` | 확정 |
| 2026-08-18 | IMPL-219 | **[총괄 접수·판정·발행] 주력 8차 완료 회신 — PAL 0 총괄 재실측 확정·flip 발동 + 판정 9건 + D13 v1.8 발행 (결정 #17)** (`docs/handoff/MS3_주력8차_총괄회신.md`). **검수** = 게이트 재현(90·**warnings=0**·suites=14) — PAL 경고형 신설 이래 첫 0, 조건부 승인(IMPL-209) 발동 조건 성립. 파일 축 분기(#F2D973 → A2/N16)·16px scale=1.0000·씬 선언 14→16 동반 상향·저장 표시 구조(라우터 소유·세션 관문·없는 param = 부팅 거부 실측 → configure 보류 = 불변규칙 2 이행) 전건 확인. **D13 v1.8 발행** — 사용자 승인 2026-08-18: 별첨A §8.1 +2행(저장 표시 2.0초 = D09 §181 값 충전 · 회전 주기 1.0초 = 원천 확정)·대장 95항 불변(결정 #16 동형)·본문 판번 동기·CLAUDE.md v1.8 기준. **판정** — garage_16 재작업 채택(주력 반대 권고 채택 — 실물 = "얇은 X") · filler_16 추인 확정 · **Zone A 속성 6종 _16 발주 채택**(실측이 전제 뒤집음 — 위계 불변·쟁점 = 2:1 축소 정보 손실) · 7차 파편 재판정 채택("축소가 지웠다" 가설) · CreditsValue 투어 경계 이월 · **재정규화 = ⓐ 정규화 전용 커밋 1회 채택**(등가 검증 동반·원격 .tscn 무접촉으로 재오염 경로 없음) · 활성 탭 [가안] 추인 · 패드 승계. **라우팅** — 원격 2(flip + 저장 표시 결선: csv 2행·configure·회귀) · 에셋 2(garage_16 재작업·attr 6종 발주) · 주력 대기(attr 결선·재판정·정규화 커밋·저장 표시 실기) | 씬 색 리터럴 정리 트랙(5차 발견 → 8차) 판정·치환 전량 종결 — 잔여 = flip 1줄 | `MS3_주력레인_8차_완료회신.md` · IMPL-216~218 · D09 §181 · D13 v1.8 결정 #17 | 확정 — flip·결선 대기 |
| 2026-08-18 | IMPL-218 | **16px 세트 결선 + 활성 탭 표시 + 저장 표시 UI (총괄 회신 §4-②③④).** **②16px 결선** — `ICON_SLOT` 32→16 · `MARK_SLOT` 32→16 · 도상 표 8종 `_16` 전환 · 재화 적재 6지점 `_16` · **씬 17곳 선언 14→16**. 선언을 함께 올린 이유: 14 로 두면 텍스처 최소치(16)가 이겨 16 이 뜨는데 선언만 14 로 남아 **대장 §9-⑱ 이 지적한 "선언과 실물이 어긋난 자리"를 새로 만든다.** 실측 검산 = 재화·표지 슬롯 전건 `scale=1.0000`(box 16 = tex 16). **축소가 아니라 원도 교체다** — 32px 를 줄이면 비정수 배율이 되므로(D10 §2.2) 16px 원도 유입이 전제였다. 7차 §6-③ 밀도 저하 **해소 확인**: SYS-04 커리어 탭이 6.5행+스크롤 → **10행 무스크롤**. **③활성 탭 표시 [가안] 신설** — SYS-03·04 공통 `_mark_active_tab()`. 활성 = `ACCENT_ACTIVE` C3 · 비활성 = `TEXT_PRIMARY` N16(신규 색 0). **비활성을 감광하지 않는다** — 탭 5종이 전부 도달 가능하므로 흐리게 두면 잠긴 것으로 오독된다. `font_color`·`hover`·`focus` 3상태를 같은 값으로 고정해 **포커스가 옮겨 가도 활성 표시가 유지**된다 — 실기 확인: LB/RB 로 돌리면 포커스 링은 커리어에 남고 활성 표시만 라이벌로 이동(7차 §6-① 종결). **④저장 표시 UI 신설** — `SaveIndicator`(우상단·회전·`sys_save`)를 **라우터가 든다**(화면마다 두면 저장이 화면 전환과 겹치는 회차에 함께 사라진다). 관측 지점 = `RunSession.progress_saved` 신설 — `SaveManager` 는 static 이라 시그널 자리가 없고 코어에 신설하면 표시 층 사정이 코어로 올라간다(혼입 0). 실패 저장에는 띄우지 않는다(§181 용도가 "종료 경고의 근거"라 거짓 근거가 된다). **⚠ `configure()` 미호출 — D13 값 부재로 잠들어 있다**(§4-④ 보고분) | 총괄 회신 §4-②③④ · 대장 §4.1.1(16px 세트)·§9-⑱ · D09 §181·§1.3 · 정본 증보 2(C3 활성 강조) · 불변규칙 2 | `achievement_screen.gd` · `options_screen.gd` · `log_feed.gd` · `save_indicator.gd`(신설) · `app_root.gd` · `run_session.gd` · 허브 8씬 + `event_node_screen` · 재화 적재 4파일 | 확정 — 저장 표시는 D13 행 대기 |
| 2026-08-18 | IMPL-217 | **씬 색 리터럴 잔여 3곳 치환 — PAL 174 → 0 도달** (총괄 회신 §4-① · 값 정본 = 팔레트 정본 §9.2.2 증보 4 ④⑤⑥). `RetireBadge` → 위험 `#FF4A3D` · `CreditsValue` → A2 `#D98A12` · `RewardValue` → **N16 `#C7D0D8`**(증보 4 ⑥ 정정 추인분 — A2 아님). **파일 단위로 치환했다** — ⑤와 ⑥이 같은 리터럴 `#F2D973` 인데 슬롯이 갈리므로(크레딧 전속 라벨 vs credit·dp·chassis 3종 공용 라벨) 리터럴 일괄 치환은 여기서 틀린다. 7차의 속성명 분기(M2/M3·C2/C3)와 같은 함정이 파일 축으로 한 번 더 나온 것이다. 치환 후 검증기 재주행 = **`PAL color literal ledger: PASS (181 checks)` · `VALIDATORS_PASS warnings=0`** — 경고형 신설(IMPL-176 ⑤) 이래 처음 0. **원격 차단형 flip 의 성립 조건 충족** | 총괄 회신 §4-① · 팔레트 정본 §9.2.2 증보 4 · 불변규칙 2 단서 | `gp_result_screen.tscn` · `run_recap_screen.tscn` · `event_node_screen.tscn` | 확정 — flip 대기 |
| 2026-08-18 | IMPL-216 | **[총괄 접수·판정] 원격 8차 + 에셋 8차 일괄 처리 — 검수 전건 재현 · 추인 3건** (`docs/handoff/MS3_원격8차_에셋8차_총괄회신.md`). **검수** = 게이트 재현(89·warnings=3·suites=14·UISCR 118·ICON_CHECK 7747/0/4·ICON_DRAW 11) · `_entrant_params` 6지점·`:693` 포함 실독 · `RewardValue` 정정 근거 소비부 실독(reward_type 3종·타입별 아이콘·rare_dp) · 재작업 실물(26×19·394·성분 2 / 스위퍼 성분 1 / 16px 10종). **원격** — 4지점(:693 = 주력·총괄 양쪽 누락분 적발)·창구화("짝이 손 규약이 아니라 구조")·M3 은닉 의존 제거(사전 삽입 순서의 우연 제거) = 기록 · PAL flip 보류 = 승인 문면 정확 이행(조건부 승인 유효 존속). **에셋 추인 3건** — ①`RewardValue` A2→N16 정정 추인(**총괄 원판정 IMPL-209 §2-1 전제 오류 인정** — 크레딧 전속은 CreditsValue뿐·dp 보상 오표기는 원칙 ③ 위반) ②`speaker_filler_16` 시점 변경(판별 위한 규격 위반 공시) ③`achv_garage_16` 요소 삭감 — 최종 = 주력 눈. **승인** — 스위퍼 연결(제3의 답·성분 1)·16px 가감(히든 −1·재화 +2 씬 17곳 선언 근거)·경로 3종 실측 후 손 작화 + RD 경계선 명문·icon_draw 신설·DIM 매니페스트화·Zone A 발주 보류. **주력 8차 = 마지막 수렴점** — 잔여 3곳 치환(PAL 0 보고)·16px 결선·활성 탭·저장 표시·재판독 → 원격 flip → 씬 색 정리 트랙 종결 | 라우팅 = 주력 5건 전속(회신 §4) · 원격 = flip 트리거 대기 · 에셋 = 배정 0 | `MS3_원격레인_8차_완료회신.md` · `에셋트랙_8차배정_회신.md` · IMPL-209~215 | 확정 |
| 2026-08-18 | IMPL-215 | **16px 원도 세트 10종 신설 — 대상 집합 판정 + 사양서 §6.8 규격 신설 + 손 작화 집행** (총괄 판정 `MS3_주력7차_총괄회신.md` §4 에셋 ④ · 사용자 결정 IMPL-209 ⑨). **①대상 집합 = 10종**(업적 6 · 화자 2 · **재화 2**). 총괄 기준선은 "업적 6+히든 1+화자 2"였고 가감 재량이 붙었다 — **업적 6 은 이미 카테고리 5 + 히든 1** 이므로 히든을 따로 더하면 중복이며(대장 §4.1 A-UI-22~27), 대신 **재화 2종을 편입**했다: 씬 저작자가 `custom_minimum_size = Vector2(14, 14)` 를 **씬 17곳**(허브 8화면 공통 바 ×2 + 이벤트 노드 1) + 동적 2지점(`records_screen.gd:112` 16×16 · `facility_screen.gd:32`)에 선언했는데 32px 가 뜬다 — **주력이 지목한 두 군보다 선언 근거가 더 강하다**. **②제작 경로 = 손 작화 전속. 기계 2경로를 실물로 시험하고 기각했다** — ⓐ**정수 2:1 축소**(최빈색): 3표본 실측에서 `achv_career` **월계관이 통째로 소멸**(체커만 남아 상자로 읽힘) · `currency_credit` 테두리 눈금 소멸·실루엣 붕괴 · `speaker_relay` 삼각대가 1px 로 흩어짐 → 대장 §4.1 의 *"절반 정보 손실"* 예고가 그대로 실증 ⓑ**RD 16×16 재생성**($0.033·2시안): 코인 형태는 **나오지만** 요청 없는 통화 기호 글립이 얹히고 32px 와 **다른 코인**이 된다. `rd_fast` 는 `reference_images` 미지원이라(지침 §2) *"같은 코인의 작은 판"* 을 표현할 방법이 **구조적으로 없다** — **RD 는 새 도상에 강하고 축약에는 못 쓴다**가 결론이다. **③`tools/assets/icon_art.json` + `icon_draw.js` 로 집행**(IMPL-214 신설분). 10종 전건 16×16 · 잉크 56~176 · 신규 색 유입 0(32px 원도 계열에서만 조달). **④사양서 §6.8 신설**(규격·대상 집합 근거·제작 경로 실측 표·축약 시 버린 것 5건) · 대장 **§4.1.1 신설**(10파일 표 · 파일명 `<32px>_16.png` — 32px 원도는 남긴다) · `icon_check.js` **DIM 축을 파일별 `size` 선언으로 확장**(미등재 = 32 단정이라 임의 치수가 조용히 못 들어온다) + FRG 간격 하한을 캔버스 비례로 | **축약은 규격을 두 곳에서 스스로 어겼고 그것을 감추지 않았다**(대장 §9-⑰ 추인 요청): ⓐ`speaker_filler_16` **시점 변경(측면→정면)** — 16px 측면 헬멧은 `achv_rival_16` 과 같은 실루엣 언어가 되어 혼동된다 ⓑ`achv_garage_16` **요소 삭감(렌치+리프트 → 렌치)** — 16px 에서 두 요소가 서로를 먹는다(IMPL-165 가 `achv_rival` 에서 실증한 형태). **발주하지 않은 후보 1건도 남겼다**(§9-⑱): 섹터 속성 6종이 `race_screen.tscn:196` 에서 16 선언·32 렌더로 **같은 종류의 근거**를 갖지만 ①선언 1곳뿐 ②주력이 Zone A 밀도를 문제로 올리지 않았다 ③같은 화면 릴 심볼이 32 고정이라 위계가 갈리는 것이 이득인지 손실인지는 눈 소관 — 재판독 요청으로 올린다. **32px 체계 무접촉**(릴 심볼 6종 `KEEP_CENTERED` 무배율 계약 불변). 결선은 주력 몫 | 총괄 회신 §4 에셋 ④ · 사용자 결정 IMPL-209 ⑨ · 주력 7차 회신 §6-③ · 사양서 §6.8·§6.1 · 대장 §4.1·§4.1.1·§7·§9-⑰⑱ · RD 지침 §2·§5.1 · D10 §2.2·§5.2 · D12 §9.1 | `icon_art.json` · `icon_draw.js` · `icon_check.js` · `icon_manifest.json` · 신규 PNG 10 + `.import` 10 · 사양서 · 대장 | 확정 — 추인 요청 2건·2차 후보 1건 |
| 2026-08-18 | IMPL-214 | **`achv_rival` 세로 확장 재작업 + `attr_sweeper` 파편 종결 + 손 작화 도구 신설** (총괄 판정 §4 에셋 ②③). **①`achv_rival` — RD 재생성을 먼저 시도하고 실패를 실측으로 확인한 뒤 손으로 그렸다.** RD 2시안($0.034)의 모티프 세로 점유는 **15행·12행**으로, 요구(현행 11행 초과·32px 슬롯 충전)에 미달했고 헬멧으로도 안 읽혔다 — **RD 가 이 도상에 실패한 두 번째 회차**다(1차 = IMPL-162 *"교차하는 두 헬멧이 32px 에서 안 읽힌다"*). 손 작화로 **측면 실루엣(돔+바이저+턱) 세로 19행**을 세우고 두 바이저가 안쪽을 향하게 해 대결 축을 배치로도 지게 했다. 잉크 193→394 · bbox 24×11 → **26×19**(업적 6종 세로 대역 14~26 안으로 복귀 — 종전 11 은 유일 이탈값이었다) · 성분 2 불변 · 색 계열 불변(좌 시안·우 주황). **②`attr_sweeper` — 소거가 아니라 연결을 택했다.** 총괄이 *"선언+강화 또는 소거, 무선언 존치 불가"* 로 재량 배정했고, 주력 재판독이 *"의도라면 더 살려야 보이고 현 상태는 있으나 없으나 같다"* 로 돌려주었다. 끝단 2px `#6FA8FF` @(26,10)-(27,10) 은 호 테이퍼를 그대로 잇는 자리인데 **(25,10)(25,11) 구멍 때문에 끊겨 있었을 뿐**이라 구멍을 채웠다 — **파편이 파편이 아니게 되고** 매니페스트 선언이 2→1 로 내려간다. 잉크 108→110 · 되읽기 전수 대조로 지정 밖 변동 0 확인. **③`tools/assets/icon_draw.js` + `icon_art.json` 신설** — 손 작화 도상을 ASCII 맵으로 두고 PNG 를 파생시킨다. `--check` 가 정의⇔실물 대조를 돌려 **한쪽만 고쳐지는 것**을 막는다 | **왜 도구를 만들었는가:** 손 작화를 PNG 바이트로만 남기면 다음 편집자가 도상을 읽을 수 없고 재현도 못 한다 — 3px 파편 하나를 고치려 해도 전체를 다시 디코드해야 한다. 대장 §9-⑬ 의 *"대조는 값이 두 곳에 있을 때만 성립한다"* 를 **뒤집어 적용**한 것이다(두 곳에 있으니 대조한다). **한계를 명기했다** — RD 산출물은 도상 정의가 없어 대조 대상이 아니고, 색이 팔레트 안인지도 보지 않는다(그 축은 `icon_check.js` PAL 소관). **돌연변이 5종 전건 검출:** 정의 맵 1칸 변조(3바이트) · PNG 1픽셀 변조(1바이트) · 스위퍼 연결 되돌림(SEP 선언 1 위반) · 16px `size` 선언 삭제(32 로 단정되어 DIM 위반) · 원도 15×15 변조. **`--check` 가 실제로 일을 했다** — 32px 도상을 두 번째로 다듬을 때 정의만 고치고 렌더를 안 돌린 상태를 즉시 잡았다 | 총괄 회신 §4 에셋 ②③ · 주력 7차 회신 §3-①② · 사양서 §6.7 v1.4 조정 · 대장 §7-④·§9-⑭ · IMPL-162·165·182 · RD 지침 §5.1 | `achv_rival.png` · `attr_sweeper.png` · `icon_draw.js` · `icon_art.json` · `icon_manifest.json` · 사양서 · 대장 | 확정 (돌연변이 5종 실측) |
| 2026-08-18 | IMPL-213 | **팔레트 정본 §9.2.2 증보 4 — ⓐ-1 6곳 등재(합류 3 추인 + 잔여 3 확정) · 기판정 1건 정정** (총괄 판정 §2-1·2). **①공백의 원인은 에셋 측이다** — ⓐ-1(IMPL-188)이 *"정본 기확정 슬롯이므로 판정 불요·전사만"* 이라며 6곳을 **회신문에만 적고 §9.2.2 에 행으로 남기지 않았다.** 그래서 주력이 치환 회차에 정본을 읽었을 때 잔여 3곳의 슬롯이 어디에도 없었고 주력은 불변규칙 2 대로 옳게 멈췄다(PAL 174→3). **판단이 회신문에만 있고 정본에 없으면 그 판단은 다음 독자에게 존재하지 않는다.** ②**합류 3곳 추인** — 게이지 전방 fill→C2 · 후방 fill→M2 · 섀시바 fill→C3. 부수로 **섀시바 리터럴이 사장값**이었다는 주력 실측(`race_screen.gd:788` 런타임 덮어씀)을 정본에 남겼다 — 이 행은 색을 바꾼 것이 아니라 **죽은 값을 정본 값으로 맞춘 것**이라 눈 대조에서 변화 0 이 정상이다. ③**`RetireBadge` → 위험 `#FF4A3D`** (ΔL\* 1.7 · 4.91→5.20:1). 원칙 ③ 검토 = 저촉 없음 — 색이 진 의미가 *위험*이고 리타이어는 위험 사건 그 자체라 의미 번짐이 아니라 같은 축이다. ④**`CreditsValue` → A2** (`ui.recap.creditsFormat` 전속이라 언제나 크레딧). **대비 12.34→6.27:1 은 6곳 중 유일한 큰 폭 하향이고 9px 텍스트라 눈 대조 대상으로 명시**했다(`OpponentLabel` 과 같은 취급). ⑤**`RewardValue` → N16 — 기판정(A2)을 정정했다** | **정정 근거는 소비부 실독이다.** ⓐ-1 은 `` `#F2D973` ×2 | `CreditsValue`·`RewardValue` `` 를 한 행으로 묶었다 — **hex 가 같다는 이유로 역할을 같다고 본 것**이고 이 트랙이 반복해 온 오류다(§9.2.2 본항이 세운 *"근접이 아니라 역할"*). 실독: `event_node_screen.gd:47~64` 의 `reward_type` 이 **credit/dp/chassis 3종**으로 갈리고(`event_categories.csv` — `category_c1`=chassis 10~20 · `category_c2`=credit 120~250 · 희귀 분기=**`rare_dp` 12**) 셋 다 이 라벨에 찍힌다. **아이콘은 이미 타입으로 갈리는데**(`currency_credit.png`/`currency_data.png`) 값 텍스트를 A2 로 고정하면 **데이터 포인트 보상이 크레딧 앰버로 찍히고 옆의 시안 카트리지 아이콘과 어긋난다** — 원칙 ③ 저촉. **희귀일수록 어긋남이 커진다**(희귀 = 항상 dp = 항상 오표기 + 옆 `RareBadge` A4 점등). 앰버 4단은 전부 배정(A1 딥·A2 크레딧·A3 경고·A4 강조)이라 중립 자리가 없고, A4 는 같은 행의 `RareBadge` 와 같아져 배지가 서지 않는다. **N16 = 타입을 다시 주장하지 않는 선택** — D10 §5.2 가 재화를 *실루엣 1차·색 보조* 로 규정했고 이 행은 그대로 서 있다. 대비 12.34→11.09 로 사실상 유지. **총괄 추인 요청 1건** — 총괄 전제(*"크레딧 값 = 재화 크레딧 의미 결속"*)는 ⑤에는 성립하고 ⑥에는 성립하지 않으며, 그 전제를 만든 것이 에셋 ⓐ-1 이므로 정정도 에셋이 진다. A2 유지로 되돌리면 1줄 | 총괄 회신 §2-1·2 · 주력 7차 회신 §1-③④ · 팔레트 정본 §9.2·§9.2.2 증보 2 원칙 ①②③ · IMPL-188·189 · D10 §5.2 · 불변규칙 2 | 팔레트 정본 §9.2.2 증보 4 | 확정 — 추인 요청 1건 |
| 2026-08-17 | IMPL-210 | **필러 대상 로그 `{number}` 결선 — 지시 3지점 + 미보고 1지점, 창구로 구조 고정 (총괄 판정 IMPL-209 ⑧).** 필러의 `name_key` 는 이름이 아니라 **문면**(`ui.race.fillerName` = `No.{number} 머신`)이라, 표기 층이 그것을 번역할 때 `number` 를 **같은 params 에서** 찾는다. 발행 층이 짝을 안 넘기면 화면에 `No.{number} 머신 리타이어.` 가 그대로 뜬다(주력 실기 관측). **인계는 3지점(680·682·829)이었으나 실측 결과 4지점이다** — `{target}` 을 싣는 `raceLog` 문면 6종을 전수 대조해 `:693 defendFail01`(방어 실패 = 자리 내줌)이 같은 결함으로 빠져 있음을 확인했다. 주력 조사표·총괄 검수 양쪽이 이 1행을 놓쳤고, 방어 듀얼 **패배** 분기라 표본에 덜 걸렸을 뿐 결함은 동형이다. **호출부마다 손으로 적는 규약을 없애고 `_entrant_params(id, extra)` 창구 하나로 짝을 구조에 고정**했다 — 5지점(정상 1 포함) 전량 경유. 손 규약을 남기면 다음 발행 지점이 또 한쪽을 빠뜨리는데, **네임드 문면에는 `{number}` 자리가 아예 없어 그 누락이 화면에 드러나지도 않는다**(그래서 여태 안 잡혔다) | 총괄 판정 IMPL-209 ⑧(원격 배정) · 주력 7차 회신 §6-② · 불변규칙 6(표시 문자열 = 키 참조) · `race_engine.gd:570` 기존 규약 주석 | `godot/core/state/race_engine.gd` · `godot/data/strings/strings.csv`(무변경·대조만) | 확정 |
| 2026-08-17 | IMPL-211 | **UISCR ⑩축 신설 — 필러 문면 실치환 회귀 + 매개 순서의 은닉 의존 제거 (IMPL-210 부대).** **관측 지점 = 실제로 렌더된 로그 줄의 문자열.** 엔진이 넘긴 params 만 보면 표기 층이 치환을 건너뛰어도 통과하므로(죽은 쪽 단언), 엔진이 발행한 이벤트를 **실화면 `_push_events()`** 에 흘려 넣고 `LogFeed` 슬롯의 본문 라벨을 읽는다. 대상 문면 4종 전수 — 듀얼 3분기(승패는 `resonance_duel_bonus` 로 강제·릴 결과 미모사)와 AI 리타이어(확률 분기·시행 한정 후 **못 뽑았으면 실패**로 본다). 전제 단언 2건(필러 실재·문면에 치환 자리 실재)으로 빈 통과를 막는다. 검사 수 100 → **118**(하한 동반 상향). **부수 발견 — 돌연변이가 코드의 은닉 의존을 드러냈다.** 표기 층의 중첩 키 선해결(`_push_events` 1줄)을 지우는 M3 이 처음에 **미검출**이었다: `StringTable.text()` 가 params 를 **삽입 순서대로 1회** 훑으므로 `target` 이 먼저면 `{target}` 자리에 들어온 `No.{number} 머신` 의 `{number}` 가 **같은 훑기에서 우연히 뒤이어 치환**된다. 즉 그 1줄은 있으나 마나였고 지워도 아무 검사도 울리지 않았다. **검사를 고치는 대신 의존을 없앴다** — `_entrant_params` 에서 `number` 를 `target` 앞에 두어 **선해결만이 유일한 성립 경로**가 되게 했고, 그러자 M3 이 8축 동시 실패로 검출된다. 돌연변이 4건(M1 창구에서 number 제거 · M2 창구 우회 손 기입 복귀 · M3 선해결 제거 · M4 number 에 엉뚱한 값) **전건 검출** | `docs/decisions/verification_protocol.md` 함정 #1(무효 돌연변이)·설계 함정 ②(죽은 쪽 단언) · 불변규칙 7(검사 = 빌드 게이트) · IMPL-208 전례(실이벤트 사슬 검사) | `godot/tests/test_ui_screens.gd` · `tools/validators/run_tests.sh` · `godot/core/state/race_engine.gd` | 확정 |
| 2026-08-17 | IMPL-212 | **[조건부] PAL 차단형 전환 — 선행 조건 미성립 실측, 집행 보류.** 총괄 조건부 기승인(IMPL-209)의 발동 조건은 "주력이 잔여 3곳을 치환해 **PAL 경고 0 실측**"이며, 0 미달 시 자동 무효다. **본 회차 실측 = `VALIDATORS_PASS warnings=3`** — 잔여 3곳(`gp_result_screen.tscn:49` `Color(0.95,0.3,0.3,1)` · `run_recap_screen.tscn:61` · `event_node_screen.tscn:76` `Color(0.95,0.85,0.45,1)`)이 그대로다. 라우팅상 주력 ①(치환)은 차기 회차이고 그마저 에셋 ①(§9.2.2 증보 4 등재) 뒤이므로 **조건은 아직 오지 않았다**. `_warn`→`_fail` 을 지금 넣으면 그 순간 빌드 3건 실패가 되어, 승인 문면의 "0 미달 시 무효"를 구현이 어기는 것이 된다. **조건 성립 시 flip 1줄 + 돌연변이 재확인만 남으며 그 외 선행 작업은 없다**(FONT 전례 절차와 동형) | 총괄 판정 IMPL-209 조건부 기승인 · 불변규칙 7(검사 우회·완화 금지의 대칭 — 성립하지 않은 차단형도 넣지 않는다) · FONT 차단형 전환 전례 IMPL-148 | `tools/validators/run_validators.gd`(무변경) · 주력 라우팅 ① 대기 | 보류 — 주력 치환 후 재개 |
| 2026-08-17 | IMPL-209 | **[총괄 접수·판정] 주력 7차 완료 회신 — 검수 전건 재현 · 판정 10건(사용자 2건 포함) · 조건부 차단형 승인** (`docs/handoff/MS3_주력7차_총괄회신.md`). **검수** = 게이트 재현(89·warnings=3·suites=14·UISCR 100 — 총괄 첫 재주행 실패는 캐시 미갱신 자기 실수·`--import` 후 통과) · 구 리터럴 잔존 0·잔여 3곳 정확 일치·`{number}` 3지점·결선 코드 실독. **판정** ①잔여 3곳 = 기판정 전사(RetireBadge→`#FF4A3D`·값 2곳→A2 — 원칙 ③ 검토: 의미 동일 축이라 번짐 아님·공백 원인 = ⓐ-1 미등재) ②합류 3곳 추인 ③히든 공용 도상 승인 [가안](A-UI-27 명시·누출 방지) ④`sys_save` 표시 UI = 주력 신설 배정(D09 명문)·load/settings 대기·warning 아이콘 소비처 아님 ⑤`achv_rival` 세로 확장 = 에셋 재작업 ⑥`attr_sweeper` = 에셋 재량(무선언 존치 불가) ⑦활성 탭 표시 신설 [가안] = 주력 ⑧`{number}` 3지점 = 원격 ⑨**[사용자] 16px 원도 발주 채택**(목록·피드용 — 32px는 릴·격자 유지) ⑩**[사용자] 패드 실기 = 실물 준비 시 별도 회차**. **조건부 기승인** — 주력 3곳 치환 후 PAL 0 실측 시 차단형 전환 승인(집행 원격·0 미달 시 무효). **운영 기록** — `ProjectSettings.save()`가 `[input]` 주석 21줄 소거(주력 실측) → `project.godot` 손 편집 전속. 판단 품질 = 치환 기대/실측 대조·속성명 분기 적발·섀시바 사장값 발견·실이벤트 사슬 검사·N16 하향의 구조 판정 | 라우팅 = 에셋 4(증보 4·재작업 2·16px 발주)·원격 2(`{number}`·조건부 flip)·주력 4(3곳 치환·활성 탭·저장 표시·16px 재결선)·패드 회차 대기 | `MS3_주력레인_7차_완료회신.md` · IMPL-205~208 · 에셋 6차 ⓐ-1(IMPL-188·189) · D09 §5.5·본문 저장 표시 규격 | 확정 — 조건부 1건 대기 |
| 2026-08-17 | IMPL-207 | **결선 2군 — 아이콘 3군 + 릴 선택 커서 신설 (인계 IMPL-205 ②).** **①업적 6종 → `achievement_screen` 항목 행** (§A-4 "항목 행 = 아이콘·명칭·조건·달성 일시"). 카테고리 1:1 매핑(대장 A-UI-22~26 이 카테고리 이름으로 배정 = 아이콘은 업적의 정체가 아니라 소속 축의 표지). 히든 미달성 행은 **공용 도상 `achv_hidden`**(A-UI-27 "미달성 공용") — **[가안]**: §5.5 문면은 "아이콘 전부 비노출"이라 비우는 독법도 가능하나, ⓐ대장이 이 도상에 용도를 명시했고 ⓑ카테고리 도상을 그대로 쓰면 탭 위치와 합쳐 후보가 좁혀져 오히려 누출이며 ⓒ발신이 "6종 결선"과 "히든 아이콘 은닉"을 함께 지시해 둘이 동시에 성립하는 독법이 이것뿐이다. 총괄 판정으로 1줄 뒤집을 수 있다. 원도 32×32 **등배**(축소 = 비정수 배율 = 믹셀, D10 §2.2·D12 §9.1). 달성/미달성 감광 없음(상태는 표기 열·명칭 색 소관 — 감광 신설은 총괄 경유). **②화자 → `log_feed` Label→TextureRect.** `speaker_relay` 실결선. **`speaker_filler` 는 결선 못 했다** — D09 §3.3 화자 4종 중 도상 축은 2종뿐이고(크루 = 미니 초상 · 라이벌 = 카 넘버 배지 = 아이콘 27종 밖), 게다가 **필러 발화를 코어가 발행하지 않아 호출 지점 자체가 없다**(strings 에 `ui.race.speakerRelay` 1종만 실재). 도상 없는 화자는 텍스트 표지로 되돌아가고 표지 폭(32)은 공통 — 갈라 두면 본문 시작 x 가 어긋난다. **③시스템 4종 미결선 — 규격 확인 결과 보고 (발신 §2 지시대로 임의 배치 금지).** 별첨A 아이콘 언급 전수(9곳)에 SYS-02·SYS-03·COM-01 행 없음. `sys_save` 만 본문 §181 "저장 표시 = 우상단 저장 아이콘 + 회전"에 규격이 있으나 **그 UI 자체가 미구현**이라 아이콘 결선 범위 밖이다. **④릴 선택 커서 신설** — 선택 릴 프레임 테두리 `FRAME_LINE` N10 → **`ACCENT_ACTIVE` C3**(정본 증보 2 가 `DuelFrame` 활성 테두리로 확정한 슬롯·역할 동일 = 신규 색 아님). 표시 조건 = **X 홀드 중 + 개입 창 열림 + 정지 연출 아님** — 상시 표시하면 키보드 사용자에게 뜻 없는 영구 강조가 되고, 가드를 `_toggle_hold()` 와 같게 두지 않으면 "커서는 보이는데 A 가 안 먹는" 상태가 생긴다. 감광(modulate)과 테두리는 **다른 채널**이라 홀드 표시와 커서가 서로를 지우지 않는다. **봉인(불변규칙 5): 커서는 선택 위치 전속** — 심볼·결과·매치 무참조, `_revealing` 중 소등. 프레임 스타일박스는 릴별 복제(씬 리소스 직접 변경 시 다음 인스턴스·에디터까지 오염 — 섀시바 전례) | 인계 IMPL-205 ② · D09 별첨A §A-4·§A-6 · D09 본문 §3.3·§5.5·§181 · D10 §2.2 · 대장 A-UI-16~27 · IMPL-177 가드 창구 유지 · IMPL-195 감광 전례 | `achievement_screen.gd` · `log_feed.gd` · `race_screen.gd` · `ui_palette.gd` · `test_ui_screens.gd` · `run_tests.sh` | 확정 — 히든 도상 독법·시스템 4종 판정 대기 |
| 2026-08-17 | IMPL-208 | **결선 검사 11축 신설 + 돌연변이 5건 실측 (IMPL-207 부대).** [[check-evidence-rule]] 대로 **상수 표·직접 호출은 결선의 증거로 인정하지 않았다** — 실배치된 행 노드를 훑어 **텍스처 파일명까지** 대조하고, 커서는 **`InputEventJoypadButton` 을 실제로 만들어 전 입력 사슬**(`_input` → `_shortcut_input` → `_unhandled_input` → `_handle_pad_context`)에 흘려 넣는다. 액션 시뮬레이션으로는 이 경로가 안 열린다(판독기가 이벤트 **형**을 본다) — 그래서 실물 패드 부재 환경에서도 OS→엔진 전달 구간만 빼고 전부 검사 안에 든다. 검사 수 82 → **100**(하한 동반 상향). **돌연변이 5건 전건 검출**: ①히든 행에 카테고리 도상 주입 → `wrong=9` ②화자 도상 경로 차단 → 2축 FAIL ③D패드 이동 후 재도색 제거 → 이동 미검출 FAIL ④봉인 조건 `_revealing` 제거 → 봉인 FAIL ⑤X 뗌 소등 제거 → 소등 FAIL. **부수: 검사가 내 오해 1건을 잡았다** — 개입 창이 턴 시작에 열린다고 보고 전제를 세웠는데 실제로는 **스핀 → 정지 연출 완료 후**에 열린다(`race_screen.gd:708`). 전제 단언이 FAIL 로 그것을 드러냈고, 국면을 명시적으로 세우는 형태로 교정했다 | 불변규칙 7(검사 = 빌드 게이트) · [[check-evidence-rule]] · IMPL-182 전례("검사는 커밋한다") | `test_ui_screens.gd` · `run_tests.sh` | 확정 |
| 2026-08-17 | IMPL-206 | **씬 색 리터럴 171곳 기계 치환 — 인계 IMPL-205 ① 집행 (PAL 174 → 3).** 값 정본 = 팔레트 정본 §9.2.2 본항+증보1·2·3. 값 판단 재개 없이 (리터럴 → 확정 슬롯) 전사만 했고, **치환 표를 스크립트로 고정해 패턴별 기대 건수와 실측 건수를 대조**했다(17패턴 전건 일치·불일치 0 — 손 치환의 누락·과잉을 기계가 막는다). float 는 확정 hex 의 8bit 값을 6자리로 그대로(손 반올림 0). diff = 19파일 171+/171− (구조 변경 0). **역할로 갈라야 하는 리터럴 2종을 속성명으로 분기**했다 — `Color(1,0.55,0.35,1)` 은 `font_color`(OpponentLabel)→**M3**·`bg_color`(후방 게이지 fill)→**M2**, `Color(0.4,0.8,1,1)` 은 `border_color`(DuelFrame)→**C3**·`bg_color`(전방 게이지 fill)→**C2**. 같은 문자열이라 일괄 치환하면 게이지 방향색이 텍스트 단으로 올라가 C/M 베이스 짝이 깨진다. **정본 표에 행이 없어 기존 역할 상수로 합류시킨 3곳**(회신 §1-③에 명시·판정 요청): 전방 게이지 fill→`GAUGE_FRONT` C2 · 후방 게이지 fill→`GAUGE_REAR` M2 · 섀시바 fill→`CHASSIS_OK` C3. 근거 3중 = 발신 §1 이 "게이지 fill 2·섀시 fill"을 범위로 명시 · §4 가 방향 지정("후방 주황→마젠타"·"섀시바 녹→시안") · `ui_palette.gd` 에 역할 일치 상수가 이미 있고 정본 조달 규칙이 "게이지 방향색 = 베이스 2단"을 못박음(자유도 0). **부수 실측: 섀시바 fill 리터럴은 사장값이었다** — `race_screen.gd:788` 이 런타임에 `CHASSIS_OK`/`gauge_danger()` 로 덮으므로 녹색은 화면에 뜬 적이 없다(§4 "섀시바 녹→시안" 눈 대조는 변화 0이 정상). **잔여 3곳 = 정본·발신 양쪽에 슬롯 부재로 미치환**(`RetireBadge` `#F24D4D` · `RewardValue`·`CreditsValue` `#F2D973`) — 임의 기입 금지(불변규칙 2)라 손대지 않았다 | 인계 IMPL-205 ① · 팔레트 정본 §9.2.2 본항·증보1(IMPL-188)·증보2(IMPL-196)·증보3(IMPL-198) · 불변규칙 2 단서(색값 = 에셋 소관) | `godot/ui/**/*.tscn` 19파일 · `run_validators.gd` PAL | 확정 — 잔여 3곳 판정 대기 |
| 2026-08-17 | IMPL-205 | **[총괄 발신] 주력 7차 일괄 착수 인계 — 6차 발신(IMPL-185) 대체** (`docs/handoff/MS3_주력레인_7차_총괄발신.md`). ①씬 치환 174곳(값 정본 = §9.2.2 폐쇄 목록 — **PAL 174→0 도달이 완료 조건·차단형 판정 성립 조건**) ②결선 2군(아이콘 3군 + **릴 선택 커서 시각 표시 신설** — 봉인 주의 명문·기확정 슬롯 색 전속) ③재판독(업적 격자·납작 유보분·파편 후보 3) ④눈 대조(#E6EBF2 61곳 최우선·색상 대변경 3건·V3 첫 실기) ⑤패드 실기 전수(D09 v1.3 — 릴리스 판정 감각·무커리어 SYS-04 눈 검증 포함) | 선행 조건 전건 성립(원격 7차 종결·174 전량 판정·D09 v1.3). 치환/결선 커밋 분리 권장 | IMPL-204 · 팔레트 정본 §9.2.2 · D09 v1.3 §1.3 | 발신 — 주력 회신 대기 |
| 2026-08-17 | IMPL-204 | **[총괄 접수·판정] 원격 7차 완료 회신 — 검수 전건 통과 · 주력 일괄 회차 개시** (`docs/handoff/MS3_원격7차_총괄회신.md`). **검수** = 게이트 재현(174·suites=14·UISCR 82) · `config.json:710` `master_60` · 소스별 기여 단언 2지점 · 모디파이어 장부 `_input`(무소비)·컨텍스트 층 `ui_accept` 선행·`_shortcut_input`+`super`·`_toggle_detail` 분리 · 주석 꼬리 잔여 0 — 전건 실독 일치. **접수·기록** — "경로는 증상·결함은 구조"(소스별 단언+UISCR 이중화) · [가안] 릴리스 시점 판정 + 총괄 "동형" 오기 정정 승인(모디파이어 위치가 판별 시점을 가른다) · 스킬 조합 가로채기("아무 일도 안 하는 것이 스핀보다 옳다")·소비부 부재 = TL-5 이월 확인 · **M6′ = 돌연변이가 검사의 결함을 적발**(포커스·툴팁 없는 상태의 항진명제 축 — 검사 수리 후 재시행)·M2′ 죽은 경로 위 재시행 — **검증 문화 기록 승격**. **판정** — 릴 선택 커서 시각 표시 = 주력 배정(실사용 성립 조건·봉인 주의) | 원격 7회차 누적 종결 — 원격·에셋 대기 배정 0·주력 선행 조건 전건 성립 | `MS3_원격레인_7차_완료회신.md` · IMPL-200~203 · D09 v1.3 | 확정 |
| 2026-08-17 | IMPL-203 | **레이스 컨텍스트 층 패드 결선 — 총괄 인계 IMPL-200 ③ 집행 (D09 v1.3 §1.3).** ①**모디파이어 장부는 `_input` 에 둔다** — 전 입력을 가장 먼저 보고 **아무것도 소비하지 않으므로**, 이후 어느 층에서 판정하든 눌림 상태가 항상 최신이다 ②**[가안] 조합 판별 = 릴리스 시점 판정** — `X` 는 단독이면 리스핀·홀드면 릴 선택 모디파이어인데 누르는 순간에는 알 수 없다. **뗄 때** 판정한다: 홀드 중 조합이 한 번이라도 성립했으면 모디파이어였고 아니면 단독이었다. `LB + Y`(상세) 대 `Y`(차지)는 모디파이어가 LB 라 Y 누름 시점에 판별이 서므로 릴리스 대기가 불요다 ③**층위 우선 이행** — `_handle_pad_context()` 를 `ui_accept` 보다 **먼저** 본다(X·RB 홀드 중의 A 는 스핀이 아니라 조합의 일부다) ④**Y 재배치** — `_shortcut_input` 을 재정의해 패드 Y 만 가로채고 나머지는 `super` 로 넘긴다(키보드 T 는 공통 층 그대로 — 무언의 소실 금지). 이를 위해 `FlowScreen._toggle_detail()` 을 분리. **⚠ 스킬 슬롯(RB 홀드 + D패드/A)은 결선 불가 — 소비부 부재:** 스킬 버튼에 `pressed` 핸들러가 없고 키보드 F1~F5 도 미결선이다(인게임 스킬 소비부 = 별도 이월). 조합을 **가로채기만** 했다 — 놓아두면 RB 홀드 중의 A 가 공통 층으로 흘러 **스핀으로 오발화**한다. 부수: **릴 선택 커서의 시각 표시가 없다**(입력 판독 층만 결선 — 주력 몫). 회귀 = X 누름 무발화 / X 단독 뗌 도달 / X+A 토글 / 조합 후 X 는 뗌 무발화 / 커서 좌우 / Y 단독 = 차지 · 상세 미표출 / LB+Y = 상세 / 키보드 T 보존 | 총괄 판정 IMPL-200 ③ · D09 v1.3 §1.3 층위 우선 규칙 | `race_screen.gd` · `flow_screen.gd` · `test_ui_screens.gd` | 확정 ([가안] 1건 · 스킬 행 이월) |
| 2026-08-17 | IMPL-202 | **주석 꼬리 4곳 정정 — 총괄 인계 IMPL-200 ② 집행.** `ui_palette.gd` 3·5·11·15행(56색 → **60색** · `master_56.gpl` → **`master_60.gpl`** · D10 v1.2 부기) · `run_validators.gd` PAL 주석 대장 행. 에셋 인계 목록(IMPL-198 §4.1) 그대로. 11행에는 **개명이 죽은 조달을 만들었던 전례**를 함께 남겼다 — 다음 개명 때 같은 자리를 보게 하려는 것이다 | 총괄 인계 IMPL-200 ② · D10 v1.2 | `ui_palette.gd` · `run_validators.gd` | 확정 |
| 2026-08-17 | IMPL-201 | **PAL 조달 경로 수정 + 소스별 적재 검증 신설 — 총괄 인계 IMPL-200 ① 집행.** `config.json` `master_56.gpl` → `master_60.gpl`(에셋 개명분 · 죽은 경로). **구조 결함이 본체다** — 이전 구조는 **전체 대장이 비었을 때만** 경고해서, 소스 하나가 죽어도 나머지가 채워 주면 **무신호로 지나갔다**. 실제로 개명이 그 경로로 통과했고 대장 절반이 빈 채 174건이 "정상"으로 보였다. 이제 `palette_sources` **항목마다 기여 ≥ 1** 을 단언하고 기능색 출처도 같은 축으로 본다 — 죽은 경로가 그 자체로 신호가 된다. **재계수 174 불변 확인** (대장이 상위집합 관계라 동일한 것이 정상 — `colorblind_alt.gpl` 이 마스터 60색을 이미 포함한다. 즉 개명 전에도 색 판정은 옳았고 **깨진 것은 신호 경로뿐**이었다). 검사 자신이 죽었을 때 스스로 신고할 수 없으므로 **UISCR 에도 설정 경로 실재 단언**을 이중으로 뒀다. 반증: 경로 오타 주입 → 경고 검출 / 단언 제거는 **죽은 경로 위에서** 재시행해 관측 가능 조건을 만든 뒤 누락 확인(프로토콜 함정 ④ 회피) | 총괄 판정 IMPL-200 ① · 에셋 발견 IMPL-198 §5 | `config.json` · `run_validators.gd` · `test_ui_screens.gd` | 확정 |
| 2026-08-17 | IMPL-200 | **[총괄 발신] 원격 7차 착수 인계 — 3건** (`docs/handoff/MS3_원격7차_총괄발신.md`) — ①`config.json:710` 경로 수정 + **PAL 소스별 적재 검증 신설**(항목별 기여 ≥1 단언 — 무신호 통과 차단) ②주석 꼬리 4곳 ③레이스 컨텍스트 층 패드 결선 — **D09 v1.3 기준**(조합 입력 [가안] 재량 — X 단독/홀드 판별·LB 홀드+Y/Y 판별 주의 명시·키보드 보존·봉인/10초 간섭 금지) | IMPL-199 §2-②·③ 집행 전개 | D09 v1.3 §1.3 · IMPL-198 §4·§5 | 발신 — 원격 회신 대기 |
| 2026-08-17 | IMPL-199 | **[총괄 접수·판정] 에셋 파생 집행 접수 + 판정 3건 + D09 v1.3 발행** (`docs/handoff/MS3_에셋파생_총괄회신.md`). **검수** = `master_60.gpl` 60색(V1~V4 위치 실독)·CB 64·생성기/아이콘 검사 총괄 재실행 전건 재현(288×916/288×972·67색 대장)·게이트 174·`config.json:710` 죽은 경로 실독. **판정** ①정본 §2 네온 교차 검사(에셋 자발 추가) = 승인 — 부분 커버리지 명기 포함·잔여 3표 파서는 이월 ②PAL 소스별 적재 검증 권고 = 채택·원격 배정("경로 1건이 죽었는데 무신호" — 개명이 실증 표본) ③**Y 층위 충돌 = 사용자 판정 '재배치' → D09 v1.2→v1.3 발행**: 총괄이 원격 7차 준비 중 발견한 §1.3 내부 중복(공통 층 상세 Y ↔ 레이스 층 차지 Y·층위 우선 명문 부재)을 불변규칙 9 상신 — §1.3 층위 우선 규칙 명문("컨텍스트 층 우선·가려진 조작은 재배치 행 명시 — 무언의 소실 금지") + 레이스 층 상세 행 신설(T / **LB 홀드+Y**·차지 확정 기준값 무변경) + 별첨A v1.3 무변경 판번 동기. **자색 파이프라인 완주**(188→191→196→197→198→본 건) — 씬 리터럴 174곳 전건 슬롯 확정·PAL 판정 잔여 0 | 이번 회차 구조 발견 2건(정본 미대조·PAL 소스 무검증)이 전부 "값 이동 작업이 검사 사각을 실증"하는 형태 — 각각 검사 신설·강화로 환원 | `에셋트랙_파생집행_회신.md` · D09 v1.3 · IMPL-191·197·198 · 불변규칙 9 | 확정 — D09 v1.3 발행 완료 |
| 2026-08-17 | IMPL-198 | **자색 슬롯 파생 연쇄 집행 — 기안 §6 전건 + §9.2.2 증보 3 + §9-⑫ 관측 2항** (D10 v1.2 · D13 별첨A v1.7 발행 후 · 총괄 회신 `MS3_원격6차_에셋7차_총괄회신.md` §4). **①`.gpl` 2종 전사** — V1 `#490C8A`·V2 `#8B31ED`·V3 `#D08DFF`·V4 `#EAC7FF` 를 **A4 뒤·AX1 앞**에 삽입(`master_60.gpl` 56→**60** · `colorblind_alt.gpl` 60→**64**). **②개명** — `master_56.{gpl,png,png.import}` → `master_60.*` · `tools/palette/master_56_strip.png` → `master_60_strip.png` (`git mv` · `.import` 경로 3줄 갱신 후 재임포트로 해시 재산출). **③`swatch_gen.js` 개정 15곳** — BLOCKS 네온 `n:12→16`·`/^[CMA]\d$/`→`/^[CMAV]\d$/` · 개수 단언 56→60 · CB 64 · `slice(56)`→`slice(60)` · 스트립 60 · 시트 제목·푸터 · 구성 문면을 **`BLOCKS.map(b=>b.n).join('+')` 로 파생화**(다음 개정에 문면이 저절로 따라온다). **④정본 §2 네온 교차 검사 신설 — 배정 밖 자발 추가.** **⑤재생성·되읽기 검산** — 스트립 60×1 고유색 60 · `master_60.png` **288×916** · `colorblind_alt.png` **288×972**. **⑥에셋 문서 5종** — 정본(§0·§2·헤더·§7 자기 대조 3행·구속 조건 명문) · 대장(§1·§4.9 6곳·개정 노트) · 사양서(§2.3·§1.4 경로·개정 노트) · RD 지침(§4.3 · 스와치 색 수 **64/67** · 개정 노트) · `tools/palette/README.md`. **⑦`icon_check` 대장 63 → 67 자동 증가 확인**(경로·주석만 수정 — `.gpl` 을 읽으므로 값은 저절로). **⑧§9.2.2 증보 3** — 씬 자색 2곳 `#D999FF`(`E02Resonance`)·`#CC8CFF`(`BoostStack`) → **V3 `#D08DFF`**. **PAL 판정 잔여 0 도달.** **⑨대장 §9-⑫ 에 MS-5 관측 2항 병기** — ⑤색각 구속 조건(V2 ↔ CB 위험) ⑥V 램프 4단 실기 판독. **⑩구현 꼬리 3파일 무접촉**(원격 배정 — 목록 회신) | **④가 이번 회차의 자발 추가이고 근거가 분명하다.** 자색 4단을 `.gpl` 과 정본 §2 **양쪽에** 적는 순간 그것이 곧 두 벌이며, **마스터 색은 종전까지 정본 표와 대조가 없었다** — `.gpl` ↔ `colorblind_alt.gpl` 앞부분 대조만 있어서 **정본 §2 hex 를 오타로 고쳐도 아무것도 안 걸렸다**(대장 §9-⑬ 이 경고한 형태). 네온 블록만 거는 것은 실용 판단이다 — §2 는 4열 표라 파서가 안정적이고 §1(2단 병렬)·§3(산문 혼재)·§4(군/ID/HEX)는 파서가 셋 더 필요하다. **부분 커버리지임을 README·주석에 명기**했다(뉴트럴 16·팀 16·환경 12 미대조 잔존). **돌연변이 3종 전건 검출:** 정본 §2 자색 hex 1자리 변조 → `정본 §2 네온 15번째 != .gpl V3` · `.gpl` V3 RGB 1바이트 변조 → CB 대조에서 선검출 · 정본 §2 자색 행 삭제 → `12색 != 16색`. **신설 검사가 실제로 일을 했다** — 정본 §2 를 갱신하기 전에 재생성했더니 즉시 중단시켰다(설계 의도 그대로). **⑧의 V3 선택 근거:** 두 리터럴이 전부 브라이트 대역(L\* 68.6·72.7)이고 V3 는 69.2 다. 베이스 V2(3.10:1)는 텍스트로 얇고 **글로우 V4(84.8)는 주 텍스트 N16(83.0)보다 밝아 위계가 뒤집힌다** — 액센트가 본문보다 밝으면 강조가 아니라 소음이다. 둘이 한 슬롯을 쓰는 것은 원칙 ②(글리프·위치가 1차 단서)에 들고, 애초에 현행 두 값이 ΔL\* 4.1·색상 2° 차로 사실상 같은 색이었다. **부수 발견 1건 (원격 인계) — 개명이 우연히 PAL 돌연변이가 됐고 PAL 이 그것을 통과시켰다.** `config.json` `palette_sources` 의 `master_56.gpl` 이 사라졌는데 **경고 수 174 불변**이다. 나머지 항목 `colorblind_alt.gpl` 이 마스터 60 전량을 담는 **상위집합**이라 대장이 그대로 채워졌기 때문이다. 더 큰 문제는 `_palette_allowed_set()` 이 **없는 파일을 조용히 건너뛴다**는 것 — `_read_text()` 가 빈 문자열을 주고 기여 0 으로 지나가며, **전체가 비었을 때만** 경고한다. 경로 수정과 함께 **소스별 적재 검증**(각 항목이 최소 1색 기여) 권고 | 총괄 회신 §2(사용자 승인 3항)·§4 · 기안 `D10_자색슬롯_개정기안_v1_0.md` §6 · **D10 v1.2 §2.3(60색·네온 16)** · **D13 별첨A v1.7:584** · 팔레트 정본 §2·§6·§7·§9.2.2 증보 3·§9.4 · 대장 §4.9·§9-⑫⑬ · D03 §1.5 · IMPL-191·196·197 | `master_60.gpl` · `colorblind_alt.gpl` · PNG 3종 · `swatch_gen.js` · `icon_check.js` · `README.md` · 팔레트 정본 · 배치 대장 · 사양서 · RD 지침 | 확정 (돌연변이 3종 실측 · 되읽기 검산) — **PAL 판정 잔여 0** · 구현 꼬리 3파일 원격 대기 |
| 2026-08-17 | IMPL-197 | **[총괄 접수·발행] 원격 6차 + 에셋 7차 일괄 처리 + 사용자 승인 3항 + D10 v1.2 / D13 v1.7 발행** (`docs/handoff/MS3_원격6차_에셋7차_총괄회신.md`). **검수** = 게이트 재현(174·suites=14·UISCR 67) · 원격 ①~④(액션 경유 `_unhandled_input` 이동 — "키 핸들러에는 패드가 도달하지 않는다" 적발 · 탭 3화면 · `_shortcut_input` 툴팁 — 상속 가림 회피 · 감광 등채널) · 에셋 13곳(§9.2.2 증보 2) + 색 공유 원칙 3항 문면화(A3 기각 실작동) + "소비부 0" 3칸 전수 정정 수용 — 전건 코드·정본 실독 일치. **기안 검수 통과** — D13 별첨A:584 동시 개정 대상 적발(두 정본 갈림 방지)·`master_56` 런타임 참조 0 확인. **사용자 승인** (스와치 시안 제시 후·3항 전건 기안 권고 채택) — ①네온 4계열·60색·V1~V4(312°) ②`master_60` 개명 ③색각 충돌 = 현행 유지+MS-5 관측+구속 조건("V2는 색각 모드에서 위험 신호와 동시 표시 자리에 단독 색 단서 금지"). **발행 집행** — D10 v1.2(68행+이력 #9) · D13 본문 v1.7(무변경 판번 동기 K1-B) · 별첨A v1.7(584행 60색+인용 §2.4→§2.3 교정) · CLAUDE.md v1.7 기준(대장 95항 불변). **라우팅** — 에셋 = 파생 연쇄 집행+증보 3(자색 2곳→V3) · 원격 = 대기 큐(레이스 층 패드 — **총괄 관찰: 차지 Y ↔ 상세 Y 층위 충돌 판정 필요** + 파생 꼬리 3파일) · 주력 = 순차 대기(치환 174 전량+아이콘+재판독+실기 일괄 권고) | 발행 = D13 v1.5·v1.6 전례(사용자 승인 경유 총괄 집행). 코드 주석의 "D13 v1.6 §8.1" 인용 2건은 시점 기록이라 무접촉 | `MS3_원격레인_6차_완료회신.md` · `에셋트랙_7차배정_회신.md` · `D10_자색슬롯_개정기안_v1_0.md` · IMPL-190~196 · 불변규칙 9 | 확정 — 발행 완료 |
| 2026-08-17 | IMPL-196 | **배정 3건 — ⓐ-2 9곳 + 스크림 4곳 수렴 확정(§9.2.2 증보 2) · 색 공유 원칙 명문 · D10 자색 슬롯 개정 기안** (총괄 회신 `MS3_원격5차_에셋6차_총괄회신.md` §3-④⑤ + 사용자 결정 IMPL-191). **①ⓐ-2 9곳 확정:** 시안 강조 텍스트 4곳(`SubtitleLabel`·`SpeakerLabel`·`CountLabel`·`StepLabel`) → **C3 `#3FE0F5`** · 최명부 강조 3곳(`ResultLabel`·`EpilogueLabel`·`RareBadge`) → **A4 `#FFE4A3`** · `OpponentLabel` → **M3 `#FF4FB0`** · `DuelFrame` `border_color` → **C3**. **②스크림 4곳 확정:** `TutorialOverlay` `DimTop/Bottom` 등 `#0A0D0F`@α0.72 → **N01 `#080B11`**(α 유지). **③색 공유(역할 중복) 원칙 명문** — 총괄 §3-⑤ 요청분. **④D10 §2.3 자색 슬롯 개정 기안 작성** — `docs/assets/D10_자색슬롯_개정기안_v1_0.md`. 결정 요청 4항(슬롯 수·실색값·파일명 계약·색각 충돌) + 파생 연쇄 전수. **⑤계수 정정 — 총괄 검수 편차 접수.** §9.2.2 의 *"소비부 0"* 표가 세 칸 전부 틀렸다: `GAUGE_TRACK` **1곳**(`ghost_gauge.gd:28` — 총괄 적발) · `BG_DEEP` **1곳**(`tests/gen_icon_sheet.gd:39` — production 0) · `BG_PANEL` **3곳**(`flow_screen.gd:152` 누락). 원인은 조회 범위 — 앞 둘은 `godot/ui/` 만 훑어 `godot/tests/` 를 빠뜨렸고 `GAUGE_TRACK` 은 **두 파일만 지정 조회**했다. 정본 문면에 정정 부기 삽입 | **③의 원칙은 신설이 아니라 정본이 이미 적용하고 있는 것의 문면화다.** ①같은 판별 집합 안에서는 공유하지 않는다(심볼 6색·게이지 상태 3색 — 그 집합에서 색은 판별 그 자체) ②집합이 다르고 색 아닌 1차 단서(도상·실루엣·위치·타입·채움량)가 있으면 공유한다 — **§9.2 본표가 `CURRENCY_DATA` 를 심볼 슬립스트림과 같은 `#3FE0F5` 로 확정한 근거가 정확히 이것이다**(*"D10 §5.2가 실루엣을 1차 구분으로 두고 색은 보조로 규정"*) ③**이미 의미를 진 색은 공유하지 않는다 — 의미가 번진다.** ②와 ③이 부딪히면 ③이 이긴다. **③은 이번 배치에서 실제로 후보를 기각하며 작동했다** — `OpponentLabel`·강조 텍스트 양쪽에서 앰버 **A3 `#FFBF3D`** 를 배제한 것이 그것이다(A3 = 심볼 브레이킹·게이지 주의 = **경고 의미** 보유 → 상대 이름·희귀 보상이 경고로 읽힌다). 그래서 강조는 미배정 슬롯 **A4** 를 쓴다. **`OpponentLabel` → M3 의 근거는 대치 쌍의 짝이다** — `PlayerLabel` 이 `#E6EBF2` → **N16 무채**이므로 유채는 상대만 진다. 팔레트에서 '상대편' 계열은 이미 마젠타로 확정돼 있고(`GAUGE_REAR` M2 = 후방·피추월, 듀얼은 추월 경합) 텍스트라 베이스 단 M2(3.16:1)로는 얇아 **브라이트 M3**(5.77:1)를 쓴다. **최근접 VU4 `#FF9A3C`(ΔL\* 2.7)를 버린 것은 불카 팀색이라 모든 상대가 불카로 보이기 때문**(AX4 배제와 같은 논거). M3 = `SYMBOL_CHANCE` 동시표시 우려는 실측으로 낮다 — **찬스 도상의 지배색은 M2 `#C41E7E`(×56)이고 명목 M3 가 아니다**(ΔL\* 17.2 · §9-⑫ ④ '명목색 vs 실물 지배색'과 같은 축). **대비가 7.55→5.77:1 로 내려가는 유일한 건이라 눈 대조 대상으로 명시**했다. **A4 의 대가는 채도다**(채도폭 153→92 · ΔL\* 0.3 로 밝기는 동일) — 감추지 않고 적었다. **스크림 논거는 C계 판정의 정밀화다** — *배율(`modulate`)은 색이 아니지만 **스크림의 RGB 는 색이다. 알파만 색이 아니다.*** RGB 가 대장 안이면 알파 무관 통과이므로 **제외 축을 늘리지 않고** 4곳이 닫힌다. **④기안의 핵심 판단 = 네온 계열 확장(3→4계열·각 4단·마스터 56→60)이 가장 작은 개정이라는 것.** 근거는 정본 §2 가 4단의 이유를 스스로 명문화했다는 점(*발광 = 어두운 테두리 + 밝은 심 · 딥은 그림자 몫*)이고, 2단만 넣으면 발광 표현이 붙는 순간 재개정을 예약한다. **램프는 마젠타와 같은 명도 배치**(22.0/43.9/69.2/84.8)로 잡았다 — 자색은 마젠타처럼 최대 휘도가 낮은 색역이라 시안·앰버 배치를 강요하면 브라이트 단에서 색이 빠진다. **색상 312° 는 308~320° 탐색에서 충돌 집합 전체 최소 ΔE 를 최대화하는 지점**(18.4)이고 씬이 이미 쓰는 313~315° 에 인접하다. **기안이 스스로 드러낸 위험 2건:** ⓐ**D13 별첨A line 584 도 개정 대상**이다(팔레트 개수를 확정 기준값 대장에 채택 등재 — D10 만 고치면 두 정본이 갈린다) ⓑ**자색 신설이 색각 대체 §6 의 전제를 건드린다** — §6 가 위험을 청자 `#7A5CFF`(304°)로 옮긴 것은 그 색역이 비어 있었기 때문이고, V2(312°)와 **ΔE 19.1·색상 8° 차**로 근접한다. **판정하지 않고 선택지 3종으로 올렸다.** 코드 측 위험은 실측으로 좁혀 뒀다 — 개수 하드코딩은 `swatch_gen.js` 1파일 전속이고 `icon_check` 대장은 자동 증가(63→67), 테스트 2건은 ID 대조·플래그 조회라 개수·인덱스 단언 0건 | 총괄 회신 §3-④⑤ · 사용자 결정 IMPL-191 · 팔레트 정본 §2·§5·§6·§7-5·§9.2 본표·§9.2.2·§9.4 · D10 §2.3(line 68)·§2.4 · D13 별첨A(line 584) · D03 §1.5 · 대장 §4.9·§9-⑫ · 불변규칙 2·9 · IMPL-188·189 | 팔레트 정본 §9.2.2 증보 2·§9.4 · **`D10_자색슬롯_개정기안_v1_0.md`(신설)** | 확정 (ⓐ-2 9곳 + 스크림 4곳 = **13곳**) · **기안 = 총괄 검수 → 사용자 문안 승인 대기** |
| 2026-08-17 | IMPL-195 | **홀드 감광 등채널 정규화 — 총괄 인계 IMPL-190 ④ 집행.** `race_screen.gd` 비홀드 릴 프레임 `Color(0.75, 0.78, 0.82)` → **`Color(0.78, 0.78, 0.78)`**. **비의도로 판정**했다 — git 이력상 RACE-01 초판(`2fef8e9`)에서 근거 없이 들어왔고, D09 §3.2 는 홀드 이중 표시만 규정할 뿐 감광 자체를 규정하지 않으며 한색 편이의 근거는 설계 어디에도 없다(인계 §4 "근거 없으면 비의도"). 감광은 **상태 표시이지 색 정보가 아니므로** 색상 이동은 심볼 판독(D10 §5.1 색+도상 이중 부호화)에 잡음이 된다. 감광률은 구 값의 중앙 채널(0.78)을 취해 실화면 인상을 보존했다 — 실기 눈 대조는 주력 후속. 회귀 = UISCR 등채널 단언(`r≈g≈b`) | 총괄 판정 IMPL-190 ④ · D09 §3.2 · D10 §5.1 | `race_screen.gd` · `test_ui_screens.gd` | 확정 |
| 2026-08-17 | IMPL-194 | **`detail_info` 툴팁 고정 최소 기구 — 총괄 인계 IMPL-190 ③ 집행.** 엔진 기본 툴팁은 마우스 호버 전속이라 매핑표에 행이 있는데 소비부가 0 이었다. 포커스 대상의 `tooltip_text` 를 **코드 생성 오버레이 1장**으로 띄운다 — 씬·라우트 신설 0(`.tscn` 무접촉). **`FlowScreen._shortcut_input` 에 둔 것이 설계 요점** — 화면들이 저마다 `_unhandled_input` 을 정의하는데 GDScript 는 자동 super 가 없어, 베이스에 같은 이름을 두면 **정의한 화면에서만 조용히 죽는다**(전 화면 공통 기구가 일부에서만 안 먹는 형태). `_shortcut_input` 은 아무 화면도 쓰지 않는 층이라 상속이 성립한다. 색은 `UiPalette` 상수 경유(PAL 무접촉)·폰트는 `_body_font_size` 창구 경유(FONT). **[가안] 3건 — D09 침묵분:** 위치 = 하단 중앙(대상 옆에 붙이면 대상을 가린다) · 해제 = 재입력·포커스 이동·화면 이탈 · **빈 툴팁이면 상자 자체를 띄우지 않는다**. 실기 판독은 주력 후속. 회귀 4축(미표출·표출·D13 폰트 크기 보유·재입력 해제) | 총괄 판정 IMPL-190 ③ · D09 §1.3 | `flow_screen.gd` · `test_ui_screens.gd` | 확정 ([가안] 3건) |
| 2026-08-17 | IMPL-193 | **탭 3화면 `tab_prev`/`tab_next` 결선 — 총괄 인계 IMPL-190 ② 집행.** `options_screen`(5탭)·`achievement_screen`(5탭)·`records_screen`(3탭)에 액션 청취 **추가**(`button.pressed` 경로는 불변 — 마우스·포커스 조작 무변경). 인덱스형 2화면은 `_active_tab` 추적, `records_screen` 은 `_tabs` 삽입 순서 = 화면 탭 배치 순서를 그대로 순회 축으로 쓴다(별도 순서 배열을 두지 않는다 — 두면 배치와 갈릴 수 있다). **[가안] 경계 감김(wrap)** — D09 는 순환 방향·경계에 침묵. 탭이 3~5개로 적어 끝에서 막히면 반대 방향 키를 다시 찾아야 하므로 감는 편이 조작 비용이 낮다고 봤다. 회귀 = 액션 발화 → `_active_tab` 전환 + **패널 가시성 동반 확인**(인덱스만 바뀌고 화면이 안 따라오는 결함 차단) + 경계 감김 | 총괄 판정 IMPL-190 ② · D09 §1.3 | `options_screen.gd` · `achievement_screen.gd` · `records_screen.gd` · `test_ui_screens.gd` | 확정 ([가안] 1건) |
| 2026-08-17 | IMPL-192 | **RACE-01 원시 키코드 직독 → 액션 경유 전환 — 총괄 인계 IMPL-190 ① 집행.** `_unhandled_key_input` 의 `KEY_ESCAPE` → `pause_menu` · `KEY_SPACE/ENTER/KP_ENTER` → `ui_accept`. **핸들러를 `_unhandled_input` 으로 옮긴 것이 핵심** — `_unhandled_key_input` 은 `InputEventKey` 만 받으므로 그 자리에 두면 액션으로 바꿔도 **패드 이벤트가 애초에 도달하지 못한다**. 키보드 거동은 액션이 매핑표의 키보드 열을 담고 있어(IMPL-186) 보존되며, 전환은 동작 집합을 **넓히기만** 한다. 레이스 컨텍스트 층 전속 조작(리스핀 R·차지 C·홀드 1/2/3)은 키 경로에 남겼다 — 패드 열이 조합 입력이라 별도 배정 대상이다. **부수 이득: 두 핸들러의 키 집합이 disjoint 가 되어 호출 순서 의존이 사라졌다.** 회귀 = 스핀 커밋·일시정지 각각 **키보드·패드·액션 3경로 동일 결과** 단언(결과는 보지 않고 국면 이탈만 본다 — 불변규칙 5) | 총괄 판정 IMPL-190 ① · D09 §1.2·§1.3 | `race_screen.gd` · `test_ui_screens.gd` | 확정 |
| 2026-08-17 | IMPL-191 | **[사용자 결정] 자색 = 슬롯 신설 방향 확정 — D10 §2.3 정본 개정 절차 개시** (IMPL-189 §3-⑥ 상신 회신). 공명·부스트의 '증폭' 의미 축은 M4 분홍 수렴(구성 해석)이 아니라 **팔레트에 자색 슬롯을 신설**해 담는다. 개정 절차: **에셋 기안**(슬롯 수·단 구성·실색값·파생 연쇄 — `master_56.gpl`/스트립/스와치 생성기/`icon_check` 대장/D13 별첨A 언급 전수) → 총괄 검수 → **사용자 문안 최종 승인** → D10 v1.2 발행. 그 전까지 씬 자색 2곳(#D999FF·#CC8CFF)은 현행 유지 — PAL 경고 잔존이 정상 상태다(치환 대상이 아니라 신설 대기) | 사용자 판정 2026-08-17 (AskUserQuestion — 선택지 3종 중 신설 채택). 56색 구성 계약이 열리는 사안이므로 개수·명명·검사 하한 연쇄를 기안이 전수 적시해야 한다 | D10 §2.3 · 불변규칙 9 · IMPL-188 §3-ⓑ·189 | **진행 — 에셋 기안 대기** |
| 2026-08-17 | IMPL-190 | **[총괄 발신] 원격 6차 착수 인계 — 4건** (`docs/handoff/MS3_원격6차_총괄발신.md`) — ①RACE-01 원시 키코드 직독→액션 경유 전환(pause_menu·ui_accept — D09 §1.2 패드 풀 지원 직결·키보드 거동 보존 계약) ②탭 소비 3화면 tab_prev/next 결선(감김 [가안]) ③detail_info 툴팁 고정 최소 기구(코드 생성 — 씬 무접촉·FONT 창구 준수) ④홀드 감광 쿨 시프트 확인·정규화. 전건 `.gd` 전속 — 주력 씬 치환 병행 대비 `.tscn` 무접촉 원칙 명시 | IMPL-189 §3-②·⑦ 집행 전개 | `MS3_원격레인_5차_완료회신.md` §1 실태표 · D09 §1.2·§1.3 | 발신 — 원격 회신 대기 |
| 2026-08-17 | IMPL-189 | **[총괄 접수·판정] 원격 5차 + 에셋 6차 일괄 처리 — 검수 통과·사고 종결·판정 7건·사용자 상신 1건** (`docs/handoff/MS3_원격5차_에셋6차_총괄회신.md`). **검수** = 게이트 재현(warnings=174·suites=14·UISCR 38) · 입력맵 4액션·원시 키코드 직독·PAL 제외 2축·§9.2.2 증보 37곳·ⓐ-1 슬롯 5종 전건 실독 일치 · 편차 1건("GAUGE_TRACK 소비부 0" — 실재 1곳 `ghost_gauge.gd:28`, 판정 무영향). **사고 종결** — c1357f1 add -A 혼입: 원격 미복원 판단 승인(작업 소실 > 귀속 오류)·에셋 IMPL-188 재번호 접수·**규약 문면화 채택 = CLAUDE.md 다중 세션 규약 5항 신설**(파일 단위 명시 경로 전속·광역 스테이징 금지)·pre-commit 훅 기각(세션 식별 불가 — 정당한 교차 커밋 오차단·재발 시 재검토). **판정** — 원격 액션 4종·방향 4행 미기입·과잉 보고 자기 정정 = 승인 / A·A′ 37곳 §9.2.2 접수(역할>근접 3연속·ΔL* 0.4 공짜 수렴 거부 = 판단 우수 기록) / ⓐ-1 6곳 주력 치환 합류(게이지 주황→마젠타·섀시 녹→시안 = 색상 대변경 눈 대조 필수) / **스크림 4곳 N01 채택**("배율은 색이 아니지만 스크림 RGB는 색이다" — C계 정밀화) / ⓐ-2 9곳 에셋 판정 배정(역할 중복 원칙 포함) / **ⓑ 자색 2곳 = 사용자 상신**(D10 §2.3 구성 해석 or 슬롯 신설 — 불변규칙 9) / 배율 쿨 시프트 원격 배정. 주력 치환 목록 116→**159곳** 갱신 | 검수 = 양 회신 주장 기계 재현 + 코드·정본 실독. PAL 174 = 159 판정 완료 + 13 배정 + 2 상신 | `MS3_원격레인_5차_완료회신.md` · `에셋트랙_6차배정_회신.md` · IMPL-184~188 · CLAUDE.md 다중 세션 규약 | 확정 — ⓑ 사용자 판정 대기 |
| 2026-08-17 | IMPL-188 | **PAL 잔여 A·A′계 37곳 수렴 슬롯 확정(정본 §9.2.2 증보) + B계 17곳 소비부 조사·2분류 보고** (총괄 회신 `MS3_원격4차_에셋5차_총괄회신.md` §2-⑥). **①A·A′ 확정 3종:** `#12141A` 26곳 → **N01 `#080B11`**(= `BG_DEEP`) · `#1C1F26` 8곳 → **N03 `#151B22`**(= `BG_PANEL`) · `#2E3038` 3곳 → **N05 `#242A33`**(= `GAUGE_TRACK`). **②계수 정정 — `#BFC7D1` 1곳은 A계가 아니라 C계다.** `race_screen.gd:627` 실독 결과 `_reel_panels[i].modulate = Color(1,1,1) if held else Color(0.75,0.78,0.82)` — **홀드 표시의 감광 배율**이고 색이 아니다(D09 §3.2 이중 표시). 총괄 갱신 전제의 A계 35 → **34**(판정 대상 = 34+A′3 = **37곳**). IMPL-182 가 hex 근접으로 N16 후보에 넣은 것이 오분류였다 — 같은 회차에 세운 *"근접이 아니라 역할"* 규칙을 스스로 어긴 자리다. **원격 IMPL-187 의 `modulate` 문맥 제외가 같은 결론에 독립 도달**했다(재계수 181→174 · 제외 7). **③B계 17곳 2분류(판정 없음 — 지시대로 분류만):** ⓐ-1 **정본 기확정 슬롯 6곳**(전사만) · ⓐ-2 **기존 슬롯 수렴 가능하나 역할→슬롯 대응이 신규 판정 9곳** · ⓑ **조달처 없음 2곳**(자색 `#D999FF` E02Resonance · `#CC8CFF` BoostStack). 대장 §9-⑯ 등재 | **①의 세 건 전부 근접과 역할이 갈리고 역할을 택했다.** `#12141A` 최근접은 N02(ΔL\* **0.4**)인데 N01(3.4)을 택했고, `#1C1F26` 은 N04(1.3) 대신 N03(2.4), `#2E3038` 은 최근접이 **NW1 `#2B3239`(노스윈드 팀색 · ΔRGB 3.7)** 라 근접으로 정하면 **팀색이 게이지 크롬이 된다**. **판단을 굳힌 사실 = 세 슬롯의 코드 상수가 소비부 0 인 채 방치돼 있었다** — `BG_DEEP`(N01) **0곳** · `GAUGE_TRACK`(N05) **0곳** · `BG_PANEL`(N03) 2곳(`confirm_dialog`·`garage_screen`). 즉 이 37곳은 *"팔레트 밖 색이 씬에 있다"* 이기 전에 **"확정된 슬롯이 결선되지 않아 씬이 눈대중으로 대신 그리고 있다"** 이며, 최근접 양자화로 N02/N04/NW1 에 맞추면 두 상수가 영구히 소비부 0 으로 남고 씬은 이름 없는 단에 고정된다 — 정리의 목적에 반한다. **역할은 전수 실독으로 확정했다:** `#12141A` = `ColorRect[Background]` 19 + `BoardMask` 1(일시정지 전면 차폐) + `Frame` 3(릴 프레임 안쪽) + `Cell0/1/2` 3 = **전건 최심면** / `#1C1F26` = `PanelContainer` 8곳(ZoneA·ZoneD·CalloutPanel·E15ScenePanel·DuelFrame·Panel×2·DialoguePanel·CalendarPanel) = **전건 패널면** / `#2E3038` = `ProgressBar` `background` 3곳 = **전건 게이지 바탕**. **깊이 위계는 오히려 고르게 펴진다** — 6.4/11.7/20.0(간격 5.3·8.3) → 3.0/9.3/17.0(간격 **6.3·7.7**). 배경↔패널 분리가 넓어지므로 깊이감이 약해지지 않는다. 최심 웰 6곳이 배경과 동값인 것은 현행 설계 그대로이고 분리는 테두리(`#475261`→N10)가 지므로 **2.18→2.89:1 로 이번 정리에서 오히려 강해진다.** **③ⓐ-1 은 판정이 아니라 사실 확인이다** — 게이지 fill 2종·섀시 fill·재화 값 2곳·리타이어 배지가 전건 §9.2 본표 기확정 슬롯(`GAUGE_FRONT` C2·`GAUGE_REAR` M2·`CHASSIS_OK`·`CURRENCY_CREDIT` A2·위험 `#FF4A3D`)에 대응한다. **부수로 IMPL-182 진단 1건을 정정한다** — *"팔레트에 녹이 없다"*(`#8CD98C`)는 문제 규정이 틀렸다: 조달처 부재가 아니라 **정본이 `CHASSIS_OK` 를 시안으로 확정한 슬롯을 씬이 녹으로 그린 것**이다. 소비부를 읽지 않고 근접만 본 대가다. **ⓑ 는 판정하지 않았다**(D10 §2.3 구성 개정 사안 · 불변규칙 9). 선택지만 데이터로 남겼다 — 네온 3계열 중 최근접은 마젠타이고 **M4 `#FFB0DC` 가 미배정 슬롯으로 비어 있으나**(M3 는 `SYMBOL_CHANCE` — 릴과 동시 표시되므로 의미 충돌) 자↔분홍 색상 이동을 감수해야 한다. **배정 밖 제안 1건:** PAL 잔여 유채+알파 4곳(`TutorialOverlay` `DimTop/Bottom` 등 스포트라이트 감광 스크림 `#0A0D0F`@0.72)은 **N01 `#080B11` 로 수렴 가능**하다(ΔL\* 0.5) — 알파는 색이 아니지만 **스크림의 RGB 는 색이다**. 배정 밖이라 확정하지 않았다 | 총괄 회신 §2-⑥ · 팔레트 정본 §1 사다리·§9.2 본표·§9.2.1·§9.2.2 · `ui_palette.gd:29·56·57` · `race_screen.gd:627` · D09 §3.2 · D10 §2.3 · 불변규칙 2·9 · IMPL-182·187 | 팔레트 정본 §9.2.2 증보 · 배치 대장 §9-⑯ | 확정 (A·A′ 37곳 — 전수 실독) · **B계 분류 보고 — ⓐ-2 9곳·ⓑ 2곳 판정 대기** |
| 2026-08-17 | IMPL-187 | **PAL 문맥 제외 2축 신설 — 총괄 판정 IMPL-184 ② 집행.** ①**프로퍼티 문맥** — `modulate`/`self_modulate` 는 곱해지는 게인이지 칠하는 색이 아니다(씬 `modulate =`·코드 `.modulate =`·트윈 `"modulate"` 인자 전부 덮는 줄 단위 판정 — 한 줄이 게인과 도색을 함께 쓰는 형태는 실측상 부재하고, 표현식 파싱까지 가면 검사가 스스로 오검출원이 된다) ②**순 무채 + 알파** — R=G=B 가 **정확히** 같고 알파 < 1. **제외를 허용 목록으로 처리하지 않았다** — 허용 목록은 "대장 밖이지만 봐준다"는 판단 기록이고 이 둘은 "애초에 대상이 아니다"라는 검사 정의라, 섞으면 목록이 판단인지 정의인지 알 수 없게 된다. **재계수 181 → 174** (제외 7 · 고유 색 24 → 17 · `.gd` 4 → **0** — `.gd` 위반은 전부 게인·스크림이었다). **유채+알파 `Color(0.04,0.05,0.06,0.72)` ×4 는 잔존 확인**(판정 §2 명문 — 감광 층으로 위장한 도색 차단). 반증: 양성/음성 probe 7건 전건 기대 일치 + 제외 축 자체를 느슨하게 만드는 돌연변이 3건(무채 조건 완화·알파 조건 제거·modulate 전면 제외) 전건에서 **정상=검출 / 돌연변이=누락** 확인 | 총괄 판정 IMPL-184 ② · 에셋 §4-③ C계 | `run_validators.gd` | 확정 (차단형 전환 판정 대기) |
| 2026-08-17 | IMPL-186 | **패드 입력맵 잔여 결선 — 총괄 판정 IMPL-184 ① 집행.** D09 §1.3 공통 층 매핑표 기계 전사: `ui_accept` +A(0) · 신설 `tab_prev`(Q/LB 9)·`tab_next`(E/RB 10)·`pause_menu`(Esc/Start 6)·`detail_info`(T/Y 3). 버튼 인덱스는 **엔진에서 실측**해 확정(A=0·B=1·Y=3·Start=6·LB=9·RB=10·D패드 11~14). **인계 문면과 2건 차이 — 판단해 집행하고 보고한다:** ⓐ**신설 액션은 3종이 아니라 4종** — '탭 전환' 1행이 Q·E / LB·RB 로 **방향 2종**이라 액션 1개로 표현할 수 없다(행 3 = 액션 4) ⓑ**`ui_up/down/left/right` 는 손대지 않았다** — 엔진 기본이 **이미 D패드 + 좌스틱을 담고 있음을 실측**했다(4차 회신의 '바인딩 부재' 보고는 `ui_cancel`·`ui_accept` 만 실측하고 방향 4행을 미실측 추정한 **과잉 보고였다 — 정정**). `[input]` 항목은 엔진 기본을 **대체**하므로 부분 전사가 오히려 잃을 위험이 크다. 계약 고정은 회귀 검사가 맡는다. 회귀 = UISCR 입력맵 계약 20검사(액션 10종의 패드 버튼 인덱스 단언 + 방향 4행 좌스틱 축 + 키보드 열 보존 6축 — `[input]` 대체 성질 때문에 키보드가 조용히 죽는 경로가 실재한다). 하한 18 → 38 | 총괄 판정 IMPL-184 ① · D09 §1.3 공통 층 매핑표 | `project.godot` · `test_ui_screens.gd` | 확정 (소비부 결선은 배정 대기) |
| 2026-08-17 | IMPL-185 | **[총괄 발신] 주력 6차 착수 인계** (`docs/handoff/MS3_주력레인_6차_총괄발신.md`) — ①아이콘 결선 3군(업적 6 = §A-4 행 규격·창구 경유 유지 / 화자 2 = E10 / 시스템 4 = 규격 확인 선행·부재 시 보고) ②씬 리터럴 116곳 기계 치환(§9.2.2 — 에셋 A·A′ 증보 38곳 도착 시 일괄 154) + `#E6EBF2`→N16 61곳 눈 대조 최우선(납작하면 되올리지 말고 보고 — 지렛대 = 바탕 하향 별도 판정) ③재판독(achv_rival 납작 유보분 + 파편 후보 3건) ④패드 실기(원격 5차 ① 후) | 선행 조건 전건 해소 — 가드 IMPL-177 커밋·§9.2.2 확정. PAL 계수를 치환 검산기로 활용 지시 | IMPL-183 §3 · 팔레트 정본 §9.2.2 | 발신 — 주력 회신 대기 |
| 2026-08-17 | IMPL-184 | **[총괄 발신] 원격 5차 착수 인계 — 2건** (`docs/handoff/MS3_원격5차_총괄발신.md`) — ①패드 입력맵 잔여 5행 결선(D09 §1.3 매핑표 기계 전사 — `ui_accept` A·방향 4행 D패드/좌스틱·신설 3액션 LB/RB·Start·Y) + 신설 액션 소비부는 조사·보고만 + 액션 존재 단언 회귀 ②PAL 문맥 제외 2축(`modulate`/`self_modulate` 문맥 · 순 무채+알파 — **유채+알파는 제외 금지**) + 제외 사유 검사 주석 명문 + 재계수·돌연변이 보고 | IMPL-183 §2-①·⑤ 판정 집행 전개. 소유 충돌 없음(리매핑 탭은 재바인딩 UI — 액션 존재가 전제 골격) | `MS3_원격4차_에셋5차_총괄회신.md` §2 · D09 §1.3 | 발신 — 원격 회신 대기 |
| 2026-08-17 | IMPL-183 | **[총괄 접수·판정] 원격 4차 + 에셋 5차 일괄 처리 — 검수 전건 재현·판정 8건** (`docs/handoff/MS3_원격4차_에셋5차_총괄회신.md`). **검수** = 게이트 독립 재주행 전건 일치(`COMPILE 89`·`VALIDATORS warnings=181` 전량 PAL·`TESTS suites=14`·UISCR 18) · 원격 ①~⑤ 코드 실독 일치 · D09 §1.3 매핑표 원문 대조 일치 · 파편 소거 2건 PNG 재디코드 확인(193/168·지정 밖 변동 0) · `icon_check.js` 재실행 정확 재현(6203/0/5) · PAL 3계 분류 계수 합치(116+35+3+17+10=181). **판정** ①입력맵 5행 = 원격 배정·소유 충돌 없음 ②PAL 차단형 = 대기 유지·전제 갱신(181곳 전 계 정리 후) ③[가안] 2건 접수 ④파편 후보 3건 = 주력 재판독 합류 ⑤C계 10곳 = **검사 층 분리 승인**(배율·감광은 색이 아니다 — 허용 목록 오염 방지) ⑥A·A′ 38곳 = 에셋 수렴 판정 배정 · B계 17곳 = 에셋 2분류 조사 배정(**조달처 없는 분은 D10 §2.3 정본 개정 사안 — 사용자 승인 경유 예고**) ⑦IMPL-165 "고립 픽셀 0" 불성립 정정 접수 — **"산문 검사는 재현을 보증하지 못한다" 전 트랙 운영 원칙 승격** ⑧`tools/assets/` 경로 승인 · **훅 4건 종결**(총괄 ea2b0ab 비의도 포함 자인·내용 적법 사후 승인 — 교훈: 공유 트리 `git add -A` 금지·명시 경로 스테이징). **총괄 자기 정정 1건**: 포인터부기 회신문 "미커밋" 관찰은 오류 — 356655c에 실재(파일 목록 미확인 단정) | 검수 방식 = 양 회신 주장 전수 기계 재현. 차단급(업적 널 역참조) 폐쇄 확인 — 주력 대기 전건 해소 | `MS3_원격레인_4차_완료회신.md` · `에셋트랙_5차배정_회신.md` · IMPL-176~182 | 확정 |
| 2026-08-17 | IMPL-182 | **에셋 배정 3건 집행 — 아이콘 파편 소거 + 검사 커밋 · 씬 리터럴 수렴 슬롯 3건 확정 · 대장 §9-⑫ 관측 병기** (총괄 회신 `MS3_주력5차_총괄회신.md` §2-③④⑤⑥). **①`achv_rival` 파편 3px 소거** — x=31 열 (15·16·17) `#2A211A` → 알파 0. 도상 본체 무접촉을 되읽기 전수 대조로 검산(지정 밖 픽셀 변동 0 · 불투명 196→193). **동반 소거 1건 — `achv_garage` (0,31) 1px `#373E48`**(불투명 169→168): 캔버스 최좌하 모서리로 잉크장(행 7~24·열 6~25) 밖 gap 8 에 떨어진 단독 픽셀이라 **어떤 독법으로도 도상이 아니다.** 배정 밖이지만 총괄 §2-③ 이 세운 규칙(*파편만 소거·도상 본체 무변경*)의 같은 부류이므로 적용했고 좌표를 남겨 되돌릴 수 있게 했다. **판단이 갈릴 수 있는 3건은 소거하지 않고 올렸다**(§9-⑭ — `speaker_filler` 내부 1px · `attr_hazard` 2px · `attr_sweeper` 2px). **②검사를 커밋했다 — `tools/assets/icon_check.js` + `icon_manifest.json` 신설.** 5축: DIM(32×32) · PAL(조달 대장 63색) · ISO(단독 1px) · **SEP(분리 성분 수 = 선언값 — 신설 축)** 차단 / FRG(파편 후보) 경고. **SEP 를 "성분 1개"로 못 박지 않은 것이 설계의 핵심** — 실측하니 29파일 중 9파일이 다성분이고 대부분 의도된 도상(슬립스트림 겹꺾쇠 2단·세이브 화살표·펄스 막대 3·배틀존 대칭 꺾쇠)이라 차단 규격으로 두면 정상 도상이 대량 오검출된다. 그래서 성분 수를 사유와 함께 매니페스트에 선언하고 실측과 대조한다(대장 §9-⑬ 의 *"대조는 값이 두 곳에 있을 때만 성립한다"* 를 그대로 적용 — 새 파편은 선언에 없으므로 즉시 차단). **③씬 리터럴 3종 수렴 슬롯 확정** — `#475261`(테두리 15곳)→**N10** · `#8C94A3`(부텍스트 40곳)→**N13** · `#E6EBF2`(주텍스트 61곳)→**N16**. 값 출처 = 팔레트 정본 **§9.2.2 신설**, 적용은 주력(씬 소유권). **④대장 §9-⑫ 에 MS-5 관측 2항 병기** + §7 배치 후 절차 3→4단계(기계 검사) + §9-⑭⑮ 신설 | **②의 ISO 축 정의를 4-이웃 → 8-이웃으로 교정했다.** IMPL-165 의 정의(*상하좌우 4이웃이 모두 투명*)를 그대로 구현하니 `symbol_chance` 의 **대각 광선 끝 4픽셀**(11,11)(20,11)(11,20)(20,20)이 걸린다 — 완전 대칭이고 4갈래 반짝임 도상의 뾰족한 끝이다. 대각으로만 붙는 1픽셀은 도트 관용이며, 잡으려는 결함은 *"아무것에도 붙지 않은 잉크"* 이므로 정의는 8-이웃 단독 성분이 맞다. **③ `#475261`→N10 은 근접이 아니라 역할로 정했다** — 최근접 단은 N08(Δ3.4)이고 N10 은 Δ7.5 로 더 먼데, **N08 은 §9.2.1 이 `FRAME_LINE` 에서 방금 탈락시킨 값**이라 최근접 양자화로 정하면 §9.2.1 이 고친 결함을 씬 15곳에 되돌린다. 소비 성격이 `FRAME_LINE` 과 같으므로 같은 슬롯을 쓴다. **`#E6EBF2`→N16 은 두 대안을 실측으로 닫고 −9.9 L\* 하향을 받아들인 것이다**: ⓐ신설 단은 정본 개정 사안이기 **전에 규격 부적합** — N16(83.0)→`#E6EBF2`(92.9) **ΔL\* 9.8** 인데 코어 뉴트럴 16 사다리의 전 구간 간격은 **3.1~7.7** 이다(v0.2 가 승인 기준 ①로 내걸고 전량 재분포해 얻은 범위) ⓑ**밝기를 지키면서 팔레트 안에 있는 선택지가 없다** — L\* 83 위 9색 전수가 팀(AX4·GR4)·네온(C4·A4)·환경(EV6)·피부(SK1)·머티리얼(MT3)·색각대체(§6 `#E8EEF5`)·심볼 펄스(NW4)로 이미 의미를 지고 있고, 무채 근접 둘도 각각 팀 정체성 오염(AX4)·**색상 방향 역행**(MT3 `#E4E0D8` R>G>B 웜 ↔ N16 `#C7D0D8` B>G>R 쿨)으로 막힌다 ⓒ11.09:1 은 §7-5 가 스스로 세운 "여유" 안이라 판독이 아니라 인상 문제다 ⓓ주력이 적발한 **"한 화면에 주 텍스트 두 밝기"** 가 이 수렴으로 닫힌다. **되돌림 지렛대는 [가안]으로만 제시** — 납작하면 텍스트를 올리는 대신 바탕을 내린다(N03 11.09 → N02 11.84 → N01 12.61). **돌연변이 6종 전건 검출:** 원 파편 재주입(SEP+FRG) · 단독 1px 주입(ISO+SEP) · 대장 밖 색 `#FF00FF`(PAL) · 유예 해소 위장(빈 `iso_open` 차단) · 치수 31×32(DIM) · 유령 선언(실물 부재). **`--headless --import` 2건 재임포트 · `swatch_gen.js` PNG 3종 바이트 동일 재현 · VALIDATORS_PASS warnings=181**(전량 PAL — IMPL-181 신설분) | 총괄 회신 §2-③④⑤⑥·§3(에셋 3건) · 팔레트 정본 §9.2.1·§9.2.2·§7-5 · 대장 §4.1·§7·§9-⑫⑬⑭⑮ · D10 §2.3·§2.4·§5.1 · IMPL-139(검사 커밋 전례)·162·165·167·172·181 · 불변규칙 2·9 | `icon_check.js` · `icon_manifest.json` · `achv_rival.png` · `achv_garage.png` · 팔레트 정본 · 배치 대장 | 확정 (돌연변이 6종 실측) — **보고 3건 총괄 판정 대기** |
| 2026-08-17 | IMPL-181 | **PAL 색 조달 대장 검사 신설 (경고형) — 총괄 인계 IMPL-176 ⑤ 집행.** 대장 = 마스터 56(`master_56.gpl`) + 정본 §6 색각 대체(`colorblind_alt.gpl` CB 4행) + **기능색 부속(= `ui_palette.gd` 전사분)** + 명시 허용 목록. 기능색은 `.gpl` 실물이 없어 `ui_palette.gd` 를 출처로 삼았다 — IMPL-144·167 로 정본 대조가 선 유일한 기계 출처이며, 그래서 그 파일 자신은 스캔 면제다(UIOPT `ROUTE_EXEMPT` 와 같은 성격). **씬도 스캔한다** — 위반의 98%(177/181)가 `.tscn` 이라 코드만 훑으면 사각이 통째로 남는다. **8bit 환산은 채널당 ±1 허용** (`0.9×255=229.5` 반올림 경계 — 인계 §5 지적분. 마스터 인접 색은 그보다 훨씬 멀어 허용이 다른 색을 삼키지 않는다). **허용 목록은 비운 채 신설했다** — 무엇을 허용할지는 정리 작업의 판단이고 검사 신설이 대신 정할 사안이 아니다(항목마다 `reason` 필수 · 없으면 그 자체를 경고). **대장 적재 자체를 양성/음성 대조로 검증**했다: 기능색 hex·마스터 float 표기는 통과, 임의색·구 팔레트값은 경고 — 전량 경고가 "적재 실패"가 아님을 확인. **성격 = 경고형이며 차단형 전환은 별도 판정** (FONT IMPL-147 → 148 절차 그대로). **V7 무접촉** | 총괄 인계 IMPL-176 ⑤ · 팔레트 정본 §5·§6 · FONT 전례 | `run_validators.gd` · `config.json` | 확정 (차단형 전환 판정 대기) |
| 2026-08-17 | IMPL-180 | **`log_feed` 미구성 폰트 크기 차단 — 총괄 인계 IMPL-176 ④ 집행.** `var _font_size := 8` 리터럴 제거. 방식은 **미구성 상태의 명시적 차단**을 택했다 — D13 값으로 초기화하면 `configure()` 미호출이 여전히 "그럴듯하게" 그려지므로, 센티넬(`UNCONFIGURED = -1`) + 소비 지점(`_build_slot`) 보고로 두어 미구성이 기본값이 아니라 **오류 상태**가 되게 했다. **FONT-B 사정거리 밖임을 확인하고 검사를 별도로 세웠다** — FONT-B 는 `add_theme_font_size_override` 의 **리터럴 인자**를 보는데 여기서는 멤버 변수를 넘기므로 정적 검사가 원리적으로 못 잡는다. UISCR 3검사(센티넬 보유 / 센티넬이 렌더 가능 범위 밖 / 구성 후 D13 값 보유). **부수 실측 1건:** 최초 구현의 `push_error` 한글 문면을 **V4 가 즉시 차단**했다(hangul literal) — 영문으로 교체. 검증기가 자기 일을 한 사례라 그대로 기록한다 | 총괄 판정 IMPL-176 ④ · 불변규칙 2·6 | `log_feed.gd` · `test_ui_screens.gd` | 확정 |
| 2026-08-17 | IMPL-179 | **`race_screen` 단독 경로 O9 정합 — 총괄 인계 IMPL-176 ③ 집행.** `_boot()` 에 `UiPalette.apply_options(session.options)` 결선. 팔레트가 정적 클래스라 옵션은 밀어 넣는 구조인데 그 밀어 넣기가 `FlowScreen.bind()` 에만 있어, **세션 없이 스스로 세션을 세우는 단독 경로**(디버그 씬·캡처 하네스)에서만 옛 색으로 떴다. 검사는 UIOPT 가 아니라 **UISCR 로 배치** — UIOPT 는 `_init()` 동기 실행이라 실화면 인스턴스화(프레임 필요)를 못 하고, 스크립트만 `new()` 해 `_boot()` 을 부르면 `@onready` 노드가 Nil 이라 함수가 중간에 끊긴 채 단언이 서는 **거짓 통과**가 된다(실측). UIOPT §117 사각 주석은 그 이관을 명기하도록 갱신 | 총괄 판정 IMPL-176 ③ · D09 §6.1 | `race_screen.gd` · `test_ui_screens.gd` · `test_ui_options.gd` | 확정 |
| 2026-08-17 | IMPL-178 | **SYS-03·04 패드 순회 폐쇄 — 총괄 인계 IMPL-176 ② 집행.** 두 화면에 초기 `grab_focus` + `ui_cancel` → 닫기. 초기 포커스 = SYS-03 **첫 탭 첫 항목**(§A-3 명시 — 라벨은 포커스를 안 받으므로 실물은 단계 감소 버튼) / SYS-04 **첫 탭 버튼 [가안]**(§A-4 는 초기 포커스 미명시 — 본문 §1.3 89행 일반 규칙 적용, 닫기를 첫 포커스로 두면 "열자마자 나가기"가 된다). **입력맵: [가안] 불요 — D09 §1.3 공통 층 매핑표가 '취소/뒤로 = Esc·우클릭·패드 B' 로 명시**했다. 엔진 기본 `ui_cancel` 은 Escape 단독임을 실측 확인하고 `project.godot [input]` 에 패드 버튼 1 을 얹었다(재실측 = Escape + Joypad Button 1). **⚠ 보고: 같은 매핑표의 나머지 5행도 패드 바인딩이 없다** — `ui_accept`(A)·방향 4행 전부 미결선(실측). 즉 **패드 순회는 이번 커밋으로 완결되지 않는다**(포커스는 서지만 A 로 누를 수 없다). 배정 대기 | 총괄 판정 IMPL-176 ② · D09 §1.3 89행·공통 층 매핑표 · §A-3 | `achievement_screen.gd` · `options_screen.gd` · `project.godot` · `test_ui_screens.gd` | 확정 (전수 결선은 배정 대기) |
| 2026-08-17 | IMPL-177 | **SYS-04 무커리어 널 역참조 가드 — 총괄 인계 IMPL-176 ① 집행 (차단급).** `outgame` 은 `begin_career()` 에서만 생기므로 **커리어 미개시가 정상 상태**이고 널은 예외가 아니다. 그래서 호출부마다 널 검사를 흩뿌리지 않고 **조달 창구 2개로 닫았다** — `_achieved()` · `_progress_for()`. 창구가 하나면 새 소비부가 생겨도 같은 자리를 지나므로 가드를 "기억해서 붙이는" 구조가 되지 않는다. `_progress_for()` 는 무커리어에서도 `threshold` 를 데이터에서 그대로 읽는다: 0 으로 뭉개면 '임계 2 이상만 진척 표기' 분기가 무커리어에서만 달리 갈려 목록 모양이 커리어 유무로 바뀐다. **[가안] 무커리어 = 달성 0 표시** — 통산 업적 적재 층(플랫폼 도전과제 역방향 조회 · D12 §7.4 Steam 연동 결선 시 확정) 전까지의 잠정 규칙. **이월 등재.** 신설 UISCR 스위트가 무커리어 인스턴스화를 회귀로 고정(행 전수 39 · 요약 문면 · 탭 5) + 커리어 적재 시 거동 무변경 대조 | 총괄 판정 IMPL-176 ① · D09 본문 145행·§A-1·§5.5 | `achievement_screen.gd` · `test_ui_screens.gd` | 확정 ([가안] 1건 이월) |
| 2026-08-17 | IMPL-176 | **[총괄 발신] 원격 4차 착수 인계 — 5건** (`docs/handoff/MS3_원격4차_총괄발신.md`). ①업적 널 역참조 가드 + 무커리어 인스턴스화 회귀(**차단급 최우선** — 표시 규칙 [가안] 달성 0·통산 적재 층 이월 등재 필수) ②SYS-03·04 패드 순회 폐쇄(초기 grab_focus + ui_cancel — 정본 침묵 시 패드 B [가안]) ③O9 단독 경로 정합(`_boot()` `apply_options` 결선 + 하네스 사각 해소) ④`log_feed` 초기값 8 리터럴 해소(FONT-B 유형) ⑤PAL 검사 **경고형** 신설(.tscn float 표기 환산 유의 · 현행 위반 계수 보고 요구) | 판정 전건 IMPL-175 §2 확정분의 집행 전개. ①이 주력 아이콘 결선의 선행 조건이라 단독 선행 커밋 허용 명시 | `MS3_주력5차_총괄회신.md` §2 · `MS3_주력레인_5차_완료회신.md` | 발신 — 원격 회신 대기 |
| 2026-08-17 | IMPL-175 | **[총괄 접수·판정] 주력 5차 완료 회신 — 검수 전건 재현 + 판정 10건** (`docs/handoff/MS3_주력5차_총괄회신.md`). **검수** = PNG 직접 디코드(잉크 6종 448/433/118/72/42/32 정확 일치 · `achv_rival` 파편 x=31 3px `#2A211A` 좌표 일치) · 대비 4건 독립 계산 일치 · 씬 리터럴 float 표기 재현(`#E6EBF2`×61·`#8C94A3`×40 — 테두리는 총괄 재계수 **15곳** vs 보고 13, 판정 무영향) · 널 역참조/미결선 14/27/O9 미호출/grab_focus 부재/초기값 8 전건 코드 실독 일치. **판정 10건** — ①널 역참조 = **차단급·가드 신설**(비활성 기각 — D09 145행 "타이틀·일시정지 양측 진입" 명문·기록실 잠금은 §A-1 E02 별도 근거) · 무커리어 달성 0 표시 [가안] + 통산 적재 층(D12 §7.4) 이월 ②아이콘 결선 = 주력 배정(업적 아이콘 열·화자 아이콘 = §A-4·E10 명문 / 시스템 4종 = 규격 확인 후·부재 시 보고 / `status_ad_exempt` 부재 = 정상 확정) ③파편 3px = 에셋 재작업 확정 + **분리 성분 검사 강화** · 납작 11px = 재판독 유보 ④시각 질량 14:1 = 현행 유지(위험 신호 주목 우위 = 기능 순방향·도상 = D10 확정) + §9-⑫ MS-5 관측 병기 ⑤⑥씬 색 리터럴 116+곳 = **팔레트 수렴 원칙 확정** · 슬롯 판정 = 에셋 → 적용 = 주력(N16 수렴은 61곳 인상 변화 신중 판단 지시) ⑦**PAL 검사 신설 승인 — 경고형 → 정리 후 차단형 flip(FONT 전례)** ⑧패드 탈출 불가 = **D09 §1.3(89행 폐쇄 루프·H38) 저촉 확정** — 수정 원격·실기 주력 ⑨O9 단독 경로 = 경로 정합 채택(주석이 계약) ⑩초기값 8 = 해소 배정. 라우팅 = 원격 5·에셋 3(§9.2 포인터는 에셋 IMPL-174 선집행으로 종결 확인)·주력 4(전건 선행 대기). **ID 재번호 1건** — 본 행과 발신 행은 174·175로 부여했으나 에셋 트랙이 같은 트리에서 IMPL-174를 선점 커밋(356655c)해 규약(pull 직후 최대값+1·후발 재번호 — IMPL-119 전례)대로 175·176으로 재번호 | 검수 방식 = 헤드리스 재현 가능 주장 전수 기계 재현(PNG 디코드·대비 계산·grep 계수·코드 실독) + 실기 전속 실측은 로그·Locals 첨부 접수. **기계 게이트 전건 통과 상태에서 눈 확인이 구조 결함을 적발** — PAL 신설의 직접 근거 | `MS3_주력레인_5차_완료회신.md` · D09 145행·§1.3·별첨A §A-1·§A-4·E10 · D12 §5.6·§7.4 · D10 §5.1 · IMPL-171~173 | 확정 |
| 2026-08-17 | IMPL-174 | **팔레트 정본 §9.2 본표 2셀에 "§9.2.1 조정분" 포인터 부기 — 총괄 회신 §2-② 에셋 라우팅 집행.** 207행 뉴트럴 행의 `FRAME_LINE`(N08 `#424A54`)·`TEXT_DIM`(N11 `#6A737E`) 두 값 옆에 **`→ ⚠[§9.2.1 조정분 — N10]` / `→ ⚠[§9.2.1 조정분 — N13]`** 를 인라인 부기하고, 표 직후에 전사 경고 블록쿼트 1개를 신설했다(현행 확정값 `#5B646F`·`#8B949F` 병기). **값 이동이 아니라 포인터 추가다** — 구값 N08·N11 은 본표에 그대로 남겼다. 색값·판단 근거의 보유처는 §9.2.1 이며 본 행은 포인터만 진다. **구값을 지우지 않은 것이 판단의 요지** — §9.2.1 의 서술은 "1.93 → 2.89:1"·"3.60 → 5.64:1" 처럼 **조정 전 값을 대상으로 성립**하므로, 본표에서 구값을 없애면 그 대비 서술이 가리킬 출발점을 잃고 조정이 판단이 아니라 단순 대체로 읽힌다. 부기 문면에 "본표만 보고 전사하지 말 것"을 명시해 재전사 회귀(IMPL-167 부기가 경고한 실패 양식)를 문서 층에서 차단했다. **잔존 구값 전수 확인:** 저장소 전역 grep 결과 `#424A54`·`#6A737E` 의 미부기 잔존은 0(본표 1행 = 본 건 처리분 · §9.2.1 표 = 조정 전 열로 의도된 것 · 나머지는 handoff 이력 문서로 시점 기록). `ui_palette.gd:58,60` 은 신값 유지(IMPL-167 전사분 무접촉). **검증:** `node tools/palette/swatch_gen.js` 재실행 통과 — 생성기는 정본 §5·§6 만 파싱하므로 §9.2 편집에 무영향이며, PNG 3종이 바이트 동일 재현(git diff 0)으로 무영향이 실측 확인됐다 | 총괄 회신 `MS3_원격3차_총괄회신.md` §2-②·§3(에셋 1건) — 권고 채택 판정. 원 권고는 IMPL-167 부기("본표만 보고 재전사하면 조정이 되돌아간다"). 구조 유지 조건("§9.2.1이 판단 기록을 보유")은 판정 문면 그대로 이행 | 팔레트 정본 §9.2·§9.2.1 · `ui_palette.gd` · IMPL-164·165·167 · 불변규칙 2(색값 = 에셋 소관) | 확정 |
| 2026-08-17 | IMPL-172 | **[주력 회신] 5차 눈 확인 2건 집행 — 판독 완료 + 발견 상신** (`docs/handoff/MS3_주력레인_5차_완료회신.md`). 코드·데이터 변경 0(인계 §0 전제). **①아이콘:** 전제 정정 — **27종 중 소비 결선은 14종**(릴 6·속성 6·재화 2)뿐이고 13종(업적 6·화자 2·시스템 4·상태 1)은 소비부 미결선이라 실배치 판독 불가. **재작업 3종 중 실기 확인 가능분은 `attr_straight` 뿐**(양호). **릴 심볼 혼동쌍 = 0**(20+스핀·기본/O9 양쪽 — 형태 축 6종 상이). 대신 **잉크 픽셀 계측에서 시각 질량 14:1 격차**(브레이킹 448 ↔ 펄스 32) — 혼동이 아니라 주목도 위계 종속이라 **G-2 혼동 행렬이 안 잡는 축**이므로 조기 경보 상신. **`achv_rival` 우측 캔버스 열(x=31)에 3열 공백 건너뛴 분리 파편 3px `#2A211A` 검출** — 고립 픽셀 검사(단일)·팔레트 검사를 전부 통과한 잔존분. `symbol_line_alt` 라우터 실경로 실측 확인(도상 불변·백색 교체·게이지 alt 2단 동반), `symbol_trouble_alt` 는 미포착(시트+단일 분기 동일성으로 대체). **②대비:** TEXT_DIM 5.64:1 **위계 유지 — 이상 없음**. FRAME_LINE 은 **상수 4곳(2.89:1)에만 적용되고, 씬 리터럴 `#475261` 13곳(2.18:1·팔레트 밖)이 릴 프레임·존 패널·로그 존 테두리를 그린다** — "약하다" 소견은 이 13곳에 한정 성립. 부기로 **씬 주텍스트 `#E6EBF2` 61곳(14.47:1)이 코드 `TEXT_PRIMARY` N16(11.09:1)과 갈려** 한 화면에 주 텍스트 두 밝기. 씬 부텍스트 `#8C94A3` 40곳은 조정값과 **우연히 일치**(5.68 vs 5.64)해 무해화된 상태. `tools/validators/` 에 색 축 검사 부재 확인. **8px 한글 = 렌더 지점 0**(body=9·로그 존도 9 주입 — `log_feed.gd:21` 초기값 8만 잔존) → 경보 대상 부재 | 실화면 판독 = GDAI 런타임(라우터 실경로 주행) · 도상 계측 = 원도 PNG 픽셀 직독 · 색값 = 인스턴스화 씬에서 실제 렌더 색 수집(리터럴 추정 아님) · 대비 = WCAG 상대 휘도 계산. 캐시 재생성은 에디터 포트 3571 점유로 GDAI `scan()` 경로(IMPL-137) | 인계 IMPL-171 · IMPL-167(전사분) · D09 §3 · D10 §5.1 · 팔레트 정본 §6·§9.2.1 | 회신 — 총괄 판정 대기 |
| 2026-08-17 | IMPL-173 | **[주력 부수 발견 3건 — 배정 밖·보고 전속]** ★**차단성 후보: 타이틀 → 업적(SYS-04)이 커리어 미개시 상태에서 널 역참조.** `session.outgame` 은 `run_session.gd:67` `begin_career()` 안에서만 생성되는데 타이틀은 `setup()` 만 돌아 null이고, `achievement_screen.gd:74` `_refresh_summary()` 가 `session.outgame.achievements` 를 무조건 읽는다. **재현 100% — 세이브 유·무 양쪽 실측**(적재는 `계속하기` 전속이라 타이틀 진입만으로는 안 일어난다). 회신에 스택 트레이스 원문 + 정지 지점 Locals(`total=39 done=0 percent=<null>`) 첨부. **대조 확인**: 커리어 적재 후(일시정지 → 업적)에는 정상 렌더 → 진단 확정. **거동 정정 — 앞선 관측의 "정지"는 에디터 디버거 브레이크였다.** GDScript 널 역참조 의미론을 직접 확인(`caller:before → probe:before → caller:after` = 자기 함수만 중단·호출자 계속)한 결과, 디버거 없는 빌드는 프로세스 종료 없이 **헤더·탭만 있고 요약·항목이 빈 껍데기 화면 + 에러 약 78건**(`_build_row` 행마다 중단 → `_build_tabs:116` 이 `add_child(null)` 2차 에러)이 된다. `닫기` 는 :45 에서 이미 연결돼 살아 있다. **미측정 명기**: 디버거 없는 실행의 화면은 직접 보지 못했다(콘솔 exe 단독 실행은 `MainWindowHandle=0` 이라 입력 미도달) — 확정하려면 익스포트 빌드 확인 필요. **같은 파일이 같은 의존을 이미 안다** — `title_screen.gd:46–48` 은 기록실을 "커리어 세이브 문맥 필요"로 비활성화하는데 44행 SYS-04 만 가드 없음. MS-3 EA 타이틀 첫 진입 경로라 차단성 판단 요청(수정 방향 2갈래 = 무커리어 요약 표시 / 기록실 동형 비활성 — D09 §A-1 해석 필요라 구현이 정하지 않음). **②SYS-03·SYS-04 패드 탈출 불가:** 두 화면 모두 `grab_focus()` 없음 + ESC 처리 없음. 실측 — 무포커스 상태에서 `ui_down`·`ui_right`·`ui_accept`·`ui_cancel` 전건 무반응. 입력맵 실측 `ui_focus_next`/`prev` = Tab 전용(패드 바인딩 0) · `ui_cancel` = Escape 전용(패드 0) · 커스텀 액션 0. 키보드는 Tab 복구 가능하나 패드는 복구 입력 부재 → D09 §1.3 패드 순회 폐쇄 루프 저촉 소지. **③`race_screen` 단독 개시 경로가 O9 미적용:** `race_screen.gd:122–124` 주석이 "폴백이 아니라 동등한 개시 경로 — 라우터가 하는 일을 그대로 한다"고 하나, `_ready()`→`_boot()` 는 `UiPalette.apply_options()` 를 부르지 않는다(그 호출은 `flow_screen.gd:32` `bind()` 전속). 실측 — O9 저장 후 단독 실행 시 라인 심볼 기본 초록, 라우터 경유 시 대체 백색. 제품 경로는 라우터라 플레이어 영향 0이나 **하네스가 O9 를 관측 못 한다**(`test_ui_options.gd:117` 주석이 지적한 사각과 동계) | 3건 모두 인계 배정 밖이며 수정은 판정 경유(인계 §0). ①은 발견 즉시 보고 대상(인계 §3) | `achievement_screen.gd` · `title_screen.gd` · `run_session.gd` · `race_screen.gd` · `flow_screen.gd` · D09 §1.3·§A-1 | 보고 — 총괄 판정 대기 |
| 2026-08-17 | IMPL-171 | **[총괄 발신] 주력 5차 착수 인계 — 눈 확인 2건** (`docs/handoff/MS3_주력레인_5차_총괄발신.md`). ①아이콘 27종 실화면 판독(재작업 3종 반영 기준 · 릴 심볼 6종 = G-2 조기 경보 최우선 · 대체 심볼 2종은 O9 활성 경로 확인 포함) ②대비 조정 2건 눈 대조(TEXT_DIM 위계 유지 · FRAME_LINE 존재 인식 — 2.89:1 미달 공시분이라 "약하다" 소견 자체가 판정 자료 · 8px 한글 가독 상시 경보 승계). 실화면 육안 전속 — 코드 변경 0 전제, 발견은 보고·수정은 판정 경유 | 선행 조건 전건 해소 실측(아이콘 유입 IMPL-160·162·165 · 상수 전사 IMPL-167 커밋 `2555a50`) — 주력 2건 모두 대기 0. "이상 없음도 명기" 요구 = G-1·G-2 조기 경보 축의 실측 기록화 | `대비판단_총괄회신.md` §3 · `MS3_원격2차_총괄회신.md` §5 · `MS3_원격3차_총괄회신.md` §3 | 발신 — 주력 회신 대기 |
| 2026-08-17 | IMPL-170 | **[총괄 접수·판정] 원격 3차 완료 회신 — 검수 전건 재현 통과 · 부기 판정 3건** (`docs/handoff/MS3_원격3차_총괄회신.md`). **검수** = 상수 2줄·FONT 2지점 `_fail`·V7 `_warn` 단일·`close_tour()` 절상·회귀 5축 전건 코드 실독 일치 + 게이트 총괄 독립 재주행(`COMPILE_GATE_PASS scripts=88` · `VALIDATORS_PASS warnings=0` FONT 51검사 · `TESTS_PASS suites=13` · SEASON 497). SEAL 98↔91 편차는 하한 게이트(84·IMPL-054)가 흡수하는 설계 요동 — 하한만이 계약. **판정** ①`finish_tour`→`close_tour` 동일 대상 판단 승인 — 오지칭 근원 = 총괄(원격2차 회신 §4-② 문면) ②§9.2 본표 구값 잔존 권고 채택 — 포인터 부기 에셋 라우팅 ③`--import` "(필요시)" 문구 강화 채택 — CLAUDE.md 원격 워크플로 행 총괄 직접 개정(신규 class_name 유입 회차 = 필수·파스 에러 위장 실측 명기). **원격 3차 배분 = 종결** — MIX0·ARCH·FONT 3검사 전건 차단형 정렬 | 검수 방식 = 주장 재현(코드 실독 + 게이트 독립 실행). 돌연변이 6건은 `_fail` 경로 실독 + 회귀 잔존으로 접수 — 재주입 불요 | `MS3_원격레인_3차_완료회신.md` · IMPL-166~169 · CLAUDE.md 원격 워크플로 | 확정 |
| 2026-08-17 | IMPL-169 | **`close_tour()` 동형 절상 + 회귀 1축 — 총괄 인계 IMPL-166 ③ 집행.** 투어 결산의 `order.find(PLAYER_ID) + 1` 이 무절상이라 순위 밖에서 0 을 흘렸다. **시즌 건보다 날카로운 결함이다** — 소비처 `record_tour_result()` 가 `position <= 1` 로 투어 우승을 세므로 **무순위가 투어 우승으로 뒤집히고**, `tour_rank` 마일스톤도 0 을 '가장 좋은 순위'로 읽는다. `close_season()`(IMPL-142)과 **동형**으로 `_grid_size()` 절상, 절상은 이 단일 지점 전속. 회귀 4검사를 시즌 건 옆에 나란히 배치: 순위표 미등재 확인 / 0 아님 / 1 아님 / 그리드 규모 일치 + **소비처 실측 1축**(`season_tour_wins` 무증가) — 값만 고치고 소비처가 다른 축을 보면 교정이 성립하지 않으므로. 검사 수 하한 492 → 497. **부기: 인계 문면은 `finish_tour()` 로 지칭했으나 실물 함수명은 `close_tour()`** (season_state.gd:267) — 결함 서술은 전건 일치하므로 동일 대상으로 판단해 집행 | 총괄 판정 `MS3_원격2차_총괄회신.md` §4-② · 인계 IMPL-166 ③ · IMPL-142 동형 | `season_state.gd` · `test_season.gd` · `run_tests.sh` | 확정 |
| 2026-08-17 | IMPL-168 | **FONT 검사 차단형 전환 — 총괄 판정 IMPL-148 집행 (인계 IMPL-166 ②).** FONT-A·FONT-B 두 지점의 `_warn` → `_fail`. 신설 시 성격을 구현이 정하지 않는다는 원칙(V7 전례)에 따라 경고형으로 올렸던 판정이 차단형으로 확정됐다 — "폰트를 지정했는가"는 문법 수준 기계 판정이라 V7(작법 판단 = D04 소관) 사유가 적용되지 않는다. **부대조건 이행: 돌연변이 2건을 차단 경로로 재확인** — 경고 경로에서 검증했던 그 돌연변이 그대로, 이번엔 `[FAIL] FONT` + `VALIDATORS_FAIL` + **exit 1** 로 전환됨을 실측(FONT-A 오버라이드 제거 · FONT-B 리터럴 기입). 전환 후 현행 코드베이스 재주행 = **위반 0**(FONT 51검사·warnings=0) — 새 위반 없음. **V7 무접촉 확인** (`_fail("V7"` 0건) | 총괄 판정 IMPL-148 · 불변규칙 7 (MIX0·ARCH·FONT = 차단형) | `run_validators.gd` | 확정 |
| 2026-08-17 | IMPL-167 | **`ui_palette.gd` 대비 조정 2건 기계 전사 — 총괄 인계 IMPL-166 ① 집행.** `FRAME_LINE` N08 `#424A54` → **N10 `#5B646F`**(1.93 → 2.89:1) · `TEXT_DIM` N11 `#6A737E` → **N13 `#8B949F`**(3.60 → 5.64:1). 값 판단은 재개하지 않았다 — 색값은 에셋 소관(불변규칙 2 단서)이고 본 건은 정본 §9.2.1 확정분의 **diff 대조 전사**다. 소비부 무변경(FRAME_LINE 4곳·TEXT_DIM 11파일은 상수 참조 그대로 — 상수 분리 가설은 에셋이 소비부 실독으로 폐기). 전사 검증 = 스크립트 3축: ①두 hex 가 §9.2.1 절 문면에 실재 ②코드 상수 29종 전량이 정본과 일치(불일치 0) ③변경 2슬롯이 `master_56.gpl` 에서 **N10·N13 으로 해석**(발신 표의 마스터 ID 열과 일치). **부기: §9.2 본표 207행은 여전히 구값(N08·N11)** — §9.2.1 이 덮은 관계이나, 본표만 보고 재전사하면 되돌아간다 | 팔레트 정본 §9.2.1 (IMPL-164 확정 · IMPL-165 총괄 검증) · 총괄 인계 IMPL-166 ① | `ui_palette.gd` | 확정 |
| 2026-08-17 | IMPL-166 | **[총괄 발신] 원격 3차 착수 인계** (`docs/handoff/MS3_원격3차_총괄발신.md`) — IMPL-165 라우팅의 원격 몫 3건을 집행 사양으로 전개해 발신. ①`ui_palette.gd` 상수 2줄 기계 전사(FRAME_LINE `#5B646F` N10 · TEXT_DIM `#8B949F` N13 — 값 정본 = 팔레트 정본 §9.2.1) ②FONT 검사 차단형 전환(`_warn`→`_fail` + 돌연변이 재확인 — 판정 = 원격2차 총괄회신 §2) ③`finish_tour()` 동형 절상 + 회귀 검사 1축(판정 = 동 §4-②). 신규 판정 0 — 전건 기판정 확정분의 전사. 우선순위 ① 명시(주력 대비 실화면 대조의 선행 조건). 발신 전 총괄 실측 = 3건 전건 미집행 확인(`ui_palette.gd:58,60` 구값 · `run_validators.gd:797,805` `_warn` · `finish_tour` 무절상) | 신규 결정 없음 — 기판정분 라우팅 집행. 착수 규약(IMPL ID = pull 후 최대값+1 · 공유 파일 자기 몫만)을 발신문에 재명기 | `대비판단_총괄회신.md` §3 · `MS3_원격2차_총괄회신.md` §2·§4-② · 팔레트 정본 §9.2.1 · IMPL-148·164·165 | 발신 — 원격 회신 대기 |
| 2026-08-17 | IMPL-165 | **아이콘 보고 3건 종결 — 재작업 3종.** IMPL-162 가 올린 보고 전건을 닫았다. ①**`status_ad_exempt`** — 육각 실루엣은 맞았으나 사양서 §6.4 가 요구한 **면제 체크 문양이 없었다**. 속 빈 육각 링 + 굵은 체크로 재생성, **사양 충족** ②**`achv_rival`** — "교차하는 두 헬멧"이 32px 에서 안 읽혀 **도상 자체를 조정**(사양서 §6.7 갱신 · P-9 [가안] 범위) ③**`attr_straight`** — 홀로 채워진 장면형이라 속성 6종 세트에서 처리가 갈렸다. 사다리꼴 도로 + 중앙 파선 글리프로 재생성해 **세트 통일** | **②가 이번 재작업의 핵심 판단이다.** 재롤 2회로 실측한 바, 32px 에서 헬멧은 **원 + 가로 바이저 슬릿** 두 요소로 축약되는데 **겹치면 그 두 요소가 서로를 먹는다** — 프롬프트를 아무리 고쳐도 회복되지 않았다(1차 = 형체 불명 덩어리 2개, 2차 = 잡픽셀 동반 덩어리). 요소 수를 줄이지 않고 **배치만 나란히 바꾸고 색을 갈라** 대결을 표현하니 즉시 판독됐다. **도상 조정이 가능했던 근거는 §6.7 이 D10 미계상(P-9) 이라 정본 개정 사안이 아니라는 것**이다 — D10 확정 도상이었다면 `attr_battle_zone` 처럼 **도상을 지키고 기하 변환으로 맞췄어야** 한다(IMPL-162 전례). 조정 사실과 이유를 사양서에 병기했다. **③은 미학이 아니라 판독 규격 문제이기도 하다** — 초판은 회색 노면 채움이라 §6.1 "고대비 실루엣"보다 장면 묘사에 가까웠다. 다만 **1차 재롤(윤곽선만·채움 없음)은 파선만 남아 도로로 안 읽혔다** — 과교정이었고 사다리꼴 실루엣을 되살려 해결했다. **검사:** 아이콘 29파일 전건 32×32 · 팔레트 이탈 0 · **고립 픽셀 0**(상하좌우 4이웃이 모두 투명한 불투명 픽셀을 기계 검출 — 눈으로 본 가장자리 자국은 연결된 음영이었고 잡픽셀이 아니었다). 비용 $0.085(재롤 5회 — 성공 1·재시도 2×2) | 사양서 §6.4·§6.7·§6.1 · 대장 §4.1 · IMPL-162(보고 원본)·163 | 확정 (29파일 전건 실측) — 보고 잔여 0 |
| 2026-08-17 | IMPL-165 | **[총괄 접수·라우팅] 에셋 대비 판단 회신 — 조정 2건 접수·검증 전건 통과** (`docs/handoff/대비판단_총괄회신.md`). ①**조정 접수** — `TEXT_DIM` N11→**N13**(3.60→5.64:1)·`FRAME_LINE` N08→**N10**(1.93→2.89:1, 바탕 N03). 색값 = 에셋 소관(불변규칙 2 단서)이라 판정 아닌 접수. 총괄 검증 = 대비 4건 독립 계산 일치 · 스트립 되읽기 검산 통과(IMPL-161 부대조건 ② 이행) · 대체 심볼 2종 **알파 차이 0px**(도상 불변 기계 실측). N12 기각 근거(자기 원칙 '여유')·상수 분리 가설의 소비부 반증·2.89 미달의 투명 공시 + 승급 경로(N11 공석) 전부 적정 ②**인계 라우팅** — 원격 3건(ui_palette 상수 2줄 기계 전사 + FONT flip + finish_tour 절상) · 주력 2건(아이콘 27종 실화면 판독 — G-2 조기 경보 · 대비 적용 후 눈 대조) · 에셋 잔여 = 아이콘 재작업 3건(IMPL-162) ③**아이콘 27종 전량 유입 확인** — 이미지 에셋 첫 실물 카테고리 완성·파이프라인 5단계 실전 1회전 | 대비 독립 재계산 · PNG 알파 전픽셀 대조 · 생성기 재실행 | 마스터플랜 v2.41 · 팔레트 정본 §9.2.1(에셋 기갱신) | 확정 |
| 2026-08-17 | IMPL-164 | **총괄 회신 §5 에셋 몫 3건 종결 — 부속 대비 2건 조정 + §9-⑫ 관측 2항 병기.** ①**`TEXT_DIM` N11 `#6A737E` → N13 `#8B949F`** (3.60→**5.64:1**) ②**`FRAME_LINE` N08 `#424A54` → N10 `#5B646F`** (1.93→**2.89:1**) ③대장 §9-⑫ 에 MS-5 색각 관측 2항을 실측값과 함께 병기. ③(대체 심볼)은 IMPL-163 선행 완료. **값의 출처 = 팔레트 정본 §9.2.1 신설**, 적용은 `godot/ui/theme/ui_palette.gd` 상수 2줄로 **구현 트랙 인계**(§9.2 본표와 같은 형태 — `godot/ui/` 는 에셋 소유 경로가 아니다) | **①의 판단 축이 둘이고 서로 당긴다** — 읽혀야 하고(바탕 대비) 주 텍스트에 종속돼 보여야 한다(N16 과의 거리). N12(4.51:1·ΔL* 28.7)는 참고 지표를 **여유 없이 스치고** N14(7.11:1·ΔL* 14.9)는 **주/부 위계가 흐려진다**. **N13 만이 5.64:1 과 ΔL* 22.1 을 동시에 준다.** N12 를 배제한 근거는 팔레트가 §7-5 에서 스스로 세운 원칙이다 — *"명도비는 산술 확보했으나 획 뭉침·순간 판독은 계산으로 대체되지 않는다 — 그래서 대비에 여유를 뒀다"*. G-1·G-2 가 MS-5 로 밀린 상황이 그때와 같으므로 임계값에 붙이는 선택은 자기 원칙과 어긋난다. 총괄이 붙인 전제("이름은 부속이나 **본문 크기 소비 11파일이 실태**")를 그대로 기준으로 삼았고 WCAG 는 참고 지표로만 썼다. **②는 가설이 소비부에 뒤집힌 사례다.** 초기 가설 = *경계선 3 + 참조선 1* 이라 값 하나로 답이 갈린다 → **상수 분리**(`FRAME_LINE`/`GUIDE_LINE`) 검토. 소비부 4곳을 읽고 폐기했다 — `timer_ring.gd:67` *"소등 상태에서도 릴 존의 경계가 사라지면 레이아웃이 무너진다"* · `vane_waveform.gd:66` *"파형이 끊겨도 아바타의 존재 자체는 유지된다"* — **둘 다 존재 보장선이지 장식이 아니다.** `confirm_dialog` 는 패널 `bg_color` 가 바탕과 같은 N03 이라 **테두리가 유일한 경계 단서**다. 넷 다 가시성을 요구하므로 분리 불요·값만 상향. **N11(3.60:1)을 쓰지 않은 것이 판단의 핵심** — N11 은 구 `TEXT_DIM` 과 같은 밝기(L*48)라 테두리가 부텍스트만큼 밝아져 크롬이 내용과 경합한다. **N10(L*42)은 크롬과 텍스트 사이**에 앉는다(신 `TEXT_DIM` L*61.0 아래·바탕 L*9.3 위). 상대 대비 +50%. **2.89 가 비텍스트 참고 지표 3:1 을 근소하게 밑도는 것을 감추지 않는다** — 차단 규격이 아니라 위계를 우선했고, MS-5 에서 약하면 다음 단은 N11 이며 `TEXT_DIM` 승급으로 **N11 슬롯이 이미 비어 있다**. **③ 관측 2항 실측:** 경고 축 = 게이지 위험 `#7A5CFF` ↔ 베인 경고 `#FF4A3D` ΔL* **7.8**·상호 대비 **1.31:1** / 주의 축 = 게이지 주의 `#FFD400` ↔ 심볼 브레이킹 `#FFBF3D` ΔL* **5.0**·**1.15:1**(사실상 동색 — 분리는 도상·릴 맥락이 진다). 둘 다 **의도된 귀결**임을 문면에 못박았다 | 총괄 회신 `MS3_주력3차_총괄회신.md` §5-①②③ · 팔레트 정본 §7-5·§9.2.1 · 대장 §9-⑫ · D03 §1.5 · IMPL-139·163 | 확정 (대비·L* 실측) — 적용 2줄 구현 인계 |
| 2026-08-17 | IMPL-163 | **총괄 판정 IMPL-161 전항 집행 — 양자화 스트립 + 색각 두 줄 규칙 + 대체 심볼 2종.** ①**`tools/palette/master_56_strip.png` (56×1 순색) 산출** — 판정 ⓑ 승인분. `swatch_gen.js` 가 `.gpl` 에서 만들고 **색 리터럴은 여전히 0**이다. `res://` 밖이라 **`.import` 없음이 정상**이고 LFS 는 전역 `*.png` 로 자동 적용 ②**부대조건 ② 이행 — 생성기에 스트립 검산 1축 추가.** 쓴 파일을 **되읽어** 고유색이 정확히 56이고 `.gpl` 과 1:1인지 본다 ③**대장 §4.9 부속 색인 1줄** — "4파일 = 2식" 계약 문면 불변, 스트립의 존재·위치·성격(제작 공정 입력·비에셋)만 등재 ④**RD 지침 §4.3.1 갱신 + §4.3.2 신설** — 비아이콘 164장의 팔레트 입력을 스트립으로 확정하고, 색각 두 줄 규칙(ⓐ `colorblind_alt.png` 팔레트 입력 금지 ⓑ 대체 심볼 = 색 치환 전속) 명문화 ⑤**대체 심볼 2종 실물 산출** — `symbol_line_alt.png`·`symbol_trouble_alt.png` | **스트립 검산은 공허하지 않다 — 돌연변이로 확인했다.** 스트립 첫 픽셀을 임의색으로 바꾸는 변조를 넣자 `FAIL: 스트립에 N01 #080B11 없음 — .gpl 과 1:1 이 아니다` 로 즉시 중단했다. 총괄이 이 축을 요구한 근거("60색 실측이 바로 스와치를 그대로 먹으면 안 되는 증거였으므로 스트립 자신도 같은 방식으로 감시한다")가 그대로 성립한다. **⑤는 재생성이 아니라 결정적 색 치환이다** — 총괄 §2 보충이 지정한 경로이며 **도상 불변이 도구 수준에서 증명**된다: `symbol_line` `#7BE04A`→`#E8EEF5` 42px · `symbol_trouble` `#FF4A3D`→`#7A5CFF` 118px, **양쪽 알파 채널 차이 0px**(치환 대상 0이면 예외로 중단하는 가드도 넣었다). 교체쌍이 exact 값이라 치환도 exact 이고 값 출처는 정본 §6 단일이다 — AI 재생성이었다면 D10 §2.4 "색상만 교체·도상 불변"의 도상 불변을 보증할 수 없다. 소비부는 이미 서 있다: `race_screen.gd:_icon_texture()` 가 O9 활성 시 `<id>_alt.png` 를 우선 조회하고 **없으면 조용히 기본 도상으로 돌아가므로**, 이번 유입으로 폴백이 실경로로 전환됐다. 검사: 아이콘 29파일(27 + 대체 2) 전건 32×32 · 팔레트 이탈 0(대체 2색은 정본 §6 지정색이라 56+기능4 밖이 정상). **[작업 중 자기 실수 1건]** 돌연변이 복구에 `git checkout` 을 써서 **미커밋이던 스트립 코드까지 되돌렸다** — 즉시 알아채고 재적용했다. 미커밋 편집이 있는 파일에 checkout 을 쓰지 않는다 | 총괄 판정 `docs/handoff/팔레트스트립_총괄회신.md` §1·§2 · D10 §2.4 · 팔레트 정본 §6 · 대장 §1.1·§4.9 · RD 지침 §4.3 · IMPL-139·155·156·161 | 확정 (돌연변이 + 알파 불변 실측) |
| 2026-08-17 | IMPL-162 | **UI 아이콘 27종 전량 유입 — 아트 실물의 첫 묶음** (`godot/assets/ui/icons/`). RD(Retro Diffusion)로 생성하고 로컬 후처리(마스터 60색 양자화 + 배경 투명화)해 배치했다. **플레이스홀더 16종은 같은 파일명으로 교체**했으므로 참조부 수정 0. 신규 11종(면제권 1·시스템 4·업적 6)도 대장 §4.1 파일명 그대로. 커밋 6회 분할(심볼→속성→재화·상태→화자·시스템→업적) | **전량 검사 통과: 27종 · 전건 32×32 · 전 픽셀이 `master_56.png` 60색 안(팔레트 이탈 0).** 32×32 는 릴 `TextureRect` 가 `KEEP_CENTERED` 무배율이라 한 픽셀만 어긋나도 잘린다(대장 §4.1). **[파이프라인 실측 — 지침 IMPL-160 갱신 대상 4건]** ①**RD 공통 프리픽스를 아이콘에 쓰면 안 된다** — `night neon mood, telemetry glow` 를 넣은 1차 시안이 *기호*가 아니라 **우주선**으로 나왔다(2/2). 분위기 어휘가 장면 속 물체를 그리게 만든다. 아이콘 전용 공식 `flat game ui icon, <색어> symbol of <도상>, bold simple shapes, high contrast silhouette, centered, solid dark grey background` 로 교체하니 즉시 해결 ②**색어가 결과색을 지배한다** — "amber orange"→주황 `#F55804` · "almost white"→형태 붕괴(파형이 별이 됨). **양자화는 정상이고 편차는 생성에서 온다**(원본 상위색 대조로 원인 분리) ③**네이티브 32×32 직접 생성이 유효** — 셀블록 1× 실측(믹셀 0). 지침 [가안] ④의 ⓐ 확정, ⓑ(정수 축소)는 불요 ④**후처리는 로컬이 낫다** — `palette_converter` API 와 같은 정의를 로컬에서 수행하면 아이콘당 11KB base64 왕복이 사라진다. 배경 투명화는 **테두리 flood fill**(전역 색 매칭은 모티프 내부 동색을 지운다). **[정본 준수 교정 2건 — 임의 선택 아님]** ①`symbol_pulse` 가 `slipstream` 과 같은 `#12A6C4` 로 떨어져 **이중 부호화 색 축이 충돌** → 정본 §5 지정색 `#BFEFF7` 로 치환(단색이라 형태 무변경). **`UIPalette.SYMBOL_*` 6색은 선언만 있고 소비부 0건이라 원도의 색이 곧 색 축**이다 ②`attr_battle_zone` 이 D10 §5.3 확정 도상("마주 미는 두 화살표")과 반대로 바깥을 향해 **좌우 반쪽을 각각 반전**(도트 축 반전은 픽셀 손실 0). 전체 반전은 배치가 대칭이라 무효였다 — 1차 시도 실패 후 교정. **[비용]** 총 $0.663(잔액 10.04→9.377). 회당 상한 $0.05 전건 준수. **동기 호출 타임아웃 1건에서 $0.017 손실** — 과금은 됐는데 `request_id` 가 응답에만 있고 `list_inference_jobs` 는 async 전속이라(실측 `{"tasks":[]}`) 회수 불가. **[보고 3건]** ①`achv_rival` 의 "교차하는 두 헬멧"이 32px 에서 안 읽힌다 ②`status_ad_exempt` 는 육각 실루엣은 맞으나 **면제 체크 문양 누락**(모바일 MS-5 전속·소비부 0이라 비차단) ③속성 6종의 색은 정본 무규정이라 중성 스틸 블루로 통일[가안]했고, `attr_straight`·`attr_sweeper` 만 채워진 장면형이라 세트 내 처리가 갈린다 | 사양서 §6·§2.1~2.4 · 대장 §4.1·§7 · 팔레트 정본 §5 · D10 §5.1~5.3·§3.4 · D09 §5.5 · 지침 IMPL-160 · IMPL-124(SYS-04 소비부) | 확정 (27종 전건 실측) — 보고 3건 |
| 2026-08-17 | IMPL-161 | **[총괄 판정] 팔레트 스트립 귀속 = ⓑ `tools/palette/master_56_strip.png` 승인 + 색각 해석 승인·보충** (`docs/handoff/팔레트스트립_총괄회신.md`). ①**ⓑ 채택** — 결정 논거 = `res://` 오염 회피(대장 §1.1 `store/` 전례: 제작 공정 입력은 빌드 밖) + 대장 §4.9 "4파일 = 2식" 계약 무변경 + IMPL-139 §1(생성기·산출물 커밋) 정합. 부대조건 = 색 리터럴 0·수기 편집 금지·**생성기 검사에 스트립 검산 1축**(고유색 정확히 56·`.gpl` 1:1)·대장 한 줄 주석 ②**색각 해석 승인** — 대체 팔레트로 양자화할 일 없음(교체 4색 = 전부 기능 컬러·표시 층 틴트). `colorblind_alt.png` 팔레트 입력 금지 명문화 승인(63색 = 기본+대체 공존 — 총괄 재현 일치) ③**총괄 보충** — 유일 반례 후보인 대체 심볼 도상 2종은 양자화가 아니라 **기본 도상의 결정적 색 치환**으로 제작(D10 §2.4 "색상만 교체·도상 불변"의 기계 보장 — AI 재생성·재양자화 금지·값 출처 = 정본 §6 단일). 대체 스트립 영구 불요 ④§1.1 승인 — 아이콘 27종은 현행 60색 파일로 즉시 발주 가능 | §1 실측 총괄 재현(PNG 디코드 고유색 60·63 일치) · 대장 §1.1·§4.9 원문 | 마스터플랜 v2.40 · 대장 §4.9 주석(에셋 집행) | 확정 |
| 2026-08-17 | IMPL-160 | **Retro Diffusion 운용 지침 신설** (`docs/assets/retro_diffusion_운용지침_v1_0.md`) — 에셋 생성 도구의 **호출 규격 전속 문서**. 에셋 문서가 4층이 된다: 팔레트 정본(무슨 색) / 제작 사양서(무엇을 어떻게) / 배치 대장(어디에) / **본 지침(어떻게 호출)**. ①**사용자 지시 비용 규칙 3항 명문화** — 기본 모델 `rd_fast` · **1회 생성 `balance_cost` < $0.05** · 이탈 시 **호출 전 승인**(§7 요청 형식 부속) ②**실측 비용표 + 배치 상한** — 규칙이 곧 `num_images` 상한으로 환산된다: ~128px = **2장** · 129~384px = **1장** ③**카테고리별 표준 레시피**(A/B/C 3급 분류) ④**RD 전용 공통 프리픽스 확정** ⑤사양서 §2.3 에 교차 참조 부기 | **웹 가이드 2종(`/app/guide`·`/guide/api`)은 JS 렌더링이라 본문을 못 가져왔다**(제목만 반환) — 그래서 수치·이름을 **전부 MCP 무료 조회로 실측**했다(`estimate_inference_cost` 14회 등). 이 방식이 오히려 정확하다: 서비스가 값을 바꾸면 실측이 정본이고 문서가 낡으므로 §8에 재검 절차를 뒀다. **실측 3건이 계획을 바꾼다.** ①**RD는 네이티브 픽셀 그리드를 직접 낸다** → 사양서 §2.3 의 "①4~8배 생성 → ②정수 다운스케일"이 **한 단계로 접힌다**(원도 크기 직접 호출). 두 경로의 비용이 같으므로 실기로 확정 가능 — [가안] 등재 ②**네거티브 프롬프트 필드가 스키마에 없다** → **사양서 §2.2 공통 네거티브를 기계로 강제할 수 없다.** 방어는 스타일 선택·긍정 표현·육안 검수 3겹뿐이고 **기계 게이트가 0**이다. 구속 조항(베인 인체·주인공 맨얼굴·실존 브랜드·대치 구도) 검수를 건너뛸 수 없음을 절차에 못박았다 ③**`rd_fast` 상한 384px** → **씬 컷 68 + 배경 4 + 대형 8 = 80장이 RD 단독 불가**(`rd_pro` 는 256이라 더 작다 — 큰 그림에서 대안 아님). 씬 컷 408×100 은 **384 생성 + 블리드 24px 수작업**으로 실질 해결(사양서가 "데스크탑은 384 크롭"이라 핵심 영역 무손실)이나 **배경·CG 12장은 시안까지만** 나온다. A급 우선 발주(총괄 승인 `ui/icons/` 16종)의 실제 근거가 이것이다. **프롬프트 적응:** 사양서 §2.1 프리픽스가 `pixel art, dense dithering, mid-90s console 2D…` 로 시작하는데 RD 스키마 명문이 **"소재만 적고 pixel art 라고 쓰지 말 것 — 렌더링은 스타일이 전담"** 이라 그대로 넣으면 스타일과 경합한다. 렌더링 어휘를 걷어내고 **소재 3토큰**(`night neon mood, telemetry glow, cyan magenta amber accents`)만 남겼다. **비용 보호 절차 3항:** 추정 선행(무료) · **전달 실패 시 재생성 금지**(과금은 이미 발생 — `get_inference_result(request_id)` 무료 회수) · `seed` 기록. **[판정 요청 1건]** `palette_converter` 는 팔레트를 **이미지로** 받는데 현행 `master_56.png` 는 라벨·기능 13색이 섞인 스와치 시트다. 순색 스트립(`master_56_strip.png` 56×1)이 필요하나 **대장 §4.9 "4파일 = 2식" 계약 변경 사안**이라 임의 신설하지 않았다(생성기에 출력 1종 추가 = 구현 비용 사실상 0). **잔고 실측:** `credits 50` · `balance $10.04` — 아이콘 27종 1회차 약 $0.46 으로 예산은 병목이 아니다. 병목은 회당 상한이며 배치 크기로 나타난다 | 사용자 지시 (2026-08-17 비용 규칙 3항) · MCP `retro-diffusion` 도구 스키마·응답 실측 · 사양서 §2.1·§2.2·§2.3·§6~§9·§11 · 배치 대장 §4.9·§7 · 팔레트 정본 §5 · IMPL-137·140 | 확정 (MCP 실측) — 판정 요청 1건·[가안] 6건 |
| 2026-08-17 | IMPL-157 | **[총괄 판정] 주력 3차 완료 회신 — 판정 3건·확인 2건 + D13 v1.6 발행** (`docs/handoff/MS3_주력3차_총괄회신.md`). ①**SE-E05 = 미발화 확정 (권고 채택)** — 총괄 코드 재현: 정산 ②단계가 트러블 부재 시 `stableSector01` 필발행(D05 규칙 정합)이라 무사건 턴 구조적 부재. **발주 목록 유지**(68식 = D11 확정 계수·D13 결속 — 제외는 정본 개정+계수 연쇄)·후순위 배치 권고. D11 §2.4 종결 주석 = P-2 ㉚ ②**섀시 위험 임계 = D13 v1.6 원천 확정 (사용자 승인 · 결정 #16)** — 값이 정본 어디에도 없는 위임 공백(D09 §5.2 원문·별첨A §2.3 부재 대조). **최대치 25%** §8.1 편입·대장 95항 불변. 주력 후속 = `CHASSIS_WARN_RATIO` 리터럴 → `core_params` 이관 ③**VN 정조 태그 = [가안] 유지 승인** — D12 §5.4 원문 대조로 위임 미수령 확인(스키마에 필드 부재). T7 유입 시 열 신설과 함께 확정. D12 명시 = P-2 ㉛ ④**G-1 기준선 = 8px 유지** — D14 §5.1 원문: 로그 존 전용·8px 원도 탑재 전제. 본문 9는 대상 밖. 이월 신설 = 폰트 실물 유입 시 로그 존 8px 전환(T6·주력) ⑤**색각 축 분리 = 의도된 귀결·현행 유지** — IMPL-139(베인 = 파형 1차 부호화) 직접 귀결·"교체 단위 = 조달 슬롯" 판단 정확. 에셋 지시 = 대장 §9-⑫ MS-5 트리거에 두 축 관측 병기 ⑥**팔레트 부속 대비 미달 = 에셋 이관**(TEXT_DIM 3.60·FRAME_LINE 1.93 — TEXT_DIM 실태는 본문 크기 11파일임을 전제로 판단) ⑦게이트 독립 재현 전건 일치(COMPILE 88·suites=13) · 보이스 release 선재 결함 해소 접수 · **"선언·리터럴·직접 호출은 결선의 증거가 아니다" = 검증기 운영 원칙 기록** | 게이트 재현 + 원문 대조(D11 §2.4·§4.1·D09 §5.2·D12 §5.4·D14 §5.1·별첨A §2.3) + 코드 재현(race_engine 정산 ②) | D13 v1.6(+별첨A) · 마스터플랜 v2.39 · P-2 ㉚㉛ | 확정 |
| 2026-08-17 | IMPL-158 | **섀시 위험 임계 `core_params` 이관 — 총괄 판정 IMPL-157 §2-② 집행.** D13 **v1.6** §8.1 이 "최대치의 **25%**"를 원천 확정(결정 #16 · 대장 95항 불변)했으므로 `CHASSIS_WARN_RATIO := 0.25` 코드 상수를 `param_chassis_warn_ratio` 로 옮겼다 — FONT-B 가 잡는 바로 그 유형(값 창구 우회)을 코드에 남기지 않는다. 경고색·SE-D06 발화·점멸이 **단일 식** `_chassis_critical()` 하나만 본다(표시값은 판정값에서 파생). **검사 = 픽스처 갈아 끼우기**: 기본(100×0.25=25)과 픽스처(80×0.5=40) 사이의 섀시로 두고 어느 데이터를 보는지 가른다 — 기본에서 임계 밖·픽스처에서 임계 안. 리터럴로 되돌리는 돌연변이 2건 전건 검출(기본 데이터만으로는 리터럴과 데이터가 같은 값이라 구분되지 않는다). **총괄 정정 접수:** 내가 인용한 결정 #14 ④·#15 전례는 '정본에 값이 있는 전사 누락'이고 이 건은 '값이 정본 어디에도 없는 **위임 공백**'이라 결정 #14 ⑥(화면 층 신규 확정 기준값)이 정확한 전례다 | 총괄 판정 IMPL-157 §2 · D13 v1.6 별첨A §8.1 · D09 §5.2 · 불변규칙 2 | `race_screen.gd` · `core_params.csv` · `fixtures/tables/core_params.csv` · `test_audio_wiring.gd` | 확정 |
| 2026-08-17 | IMPL-159 | **[이월 등재] 로그 존 8px 원도 전환 — T6 트랙·주력 (총괄 판정 IMPL-157 §4 신설분).** G-1 은 D14 §5.1 상 **로그 존 전용** 게이트이고 전제가 "실기 렌더 빌드(비트맵 폰트 **8px 원도** 탑재)"다. 본문 계열 9(`param_font_size_body`)는 G-1 대상이 **아니며**, 현재 리포에 8 이 없는 것은 결함이 아니라 폰트 실물 미유입 상태의 임시 운용이다. **폰트 실물(8px 비트맵 원도) 유입 시 로그 존을 8px 로 전환 결선**한다 — 이것이 서야 MS-5 의 G-1 전제가 성립한다(문면대로 갔으면 전제 불성립으로 게이트가 헛돌 뻔했다는 총괄 판정) | D14 §5.1 G-1 전제 · D13 로그 존 폭 창구 · 총괄 판정 IMPL-157 §4 | D14 · `log_feed.gd`(유입 시) | **이월 (실물 대기)** |
| 2026-08-17 | IMPL-155 | **색각 대체 팔레트(O9) 소비부 결선 + UIOPT 검사 신설 (원격 회신 §5 이월분 — 화면 층 몫).** 옵션 항목만 있고 소비부가 없어 "켜지는데 화면은 그대로"였다. 구조 — ①**기본 팔레트 상수는 그대로 둔다**(조달처 대조 유지 — IMPL-144). 대체 4색은 별도 상수로 두고 **조회 창구 4함수**(`symbol_line`·`symbol_trouble`·`gauge_danger`·`gauge_caution`)만 O9 로 갈린다. 함수가 있다는 것 자체가 "여기는 갈린다"는 표시다 ②**교체 단위는 hex 가 아니라 조달 슬롯**이다 — `VANE_ALERT`(#FF4A3D)·`SYMBOL_BRAKING`(#FFBF3D)은 교체 대상과 값이 같지만 정본 §6 표에 없으므로 **바꾸지 않는다**. hex 일괄 치환은 정본에 없는 교체를 구현이 만드는 일이다 ③팔레트는 세션을 못 쥐는 정적 클래스라 옵션을 **밀어 넣는다** — 화면 결속·옵션 변경 두 지점이 O9 가 바뀔 수 있는 전부다 ④**심볼 도상은 색이 PNG 에 구워져 있다** — 런타임 틴트로 정본 색을 재현할 수 없으므로 `<id>_alt.png` 를 먼저 찾고 없으면 조용히 기본으로 돌아간다(부재 = 결함이 아니라 미유입). 검사 = 실물 `.gpl` CB 4항 전사 대조 · 토글 · **비교체 슬롯 불변** · 소비부가 상수를 직접 읽지 않는지. 반증 = 돌연변이 8건 주입·8건 검출(초기 1건 구멍 — 화면 결속 적용 누락 — 보강해 닫음) | 팔레트 정본 §6 · D10 §2.3 면제 조항·§2.4 · D09 §6.1 즉시 반영 | `ui_palette.gd` · `flow_screen.gd` · `timer_ring.gd` · `race_screen.gd` · `confirm_dialog.gd` · `test_ui_options.gd`(신규) | 확정 |
| 2026-08-17 | IMPL-156 | **[보고] 색각 대체 시 같은 축의 색이 갈린다 — 의도 확인 요청.** 정본 §6 이 교체를 4행으로 최소화한 결과, 대체 팔레트에서 ①**게이지 위험 = `#7A5CFF`(청자) vs 베인 경고 = `#FF4A3D`(적)** 로 같은 '경고' 축이 두 색으로 갈리고 ②**게이지 주의 = `#FFD400`(순황) vs 심볼 브레이킹 = `#FFBF3D`(앰버)** 로 두 근접 황색이 남는다. 정본대로 4행만 교체했고 **임의 확대하지 않았다** — 의도라면 그대로, 아니라면 정본 §6 에 행을 더하는 쪽이 창구다. 함께: **대체 심볼 도상 2종(`symbol_line_alt`·`symbol_trouble_alt`) 실물 미유입** — 선택 경로는 섰고 파일만 들어오면 붙는다(에셋 트랙 몫) | 팔레트 정본 §6 "교체는 최소로" · D10 §2.4 색상만 교체·도상 불변 | 에셋 트랙 · D10 | **확인 요청** |
| 2026-08-17 | IMPL-153 | **SYS-04 도전과제 미연동 표기 결선 (원격 2차 인계 §4-1) + 실주행 결함 1건 교정.** 요약 헤더 아래 고지 행(`%LinkNotice`, 씬 노드)을 두고 `session.achievement_service_linked()` **하나만** 읽는다 — 화면은 어댑터도 플랫폼도 모른다(혼입 0). **연동 상태는 달성 판정에 관여하지 않으므로 표기 전용**이고, 연동돼 있으면 아무것도 띄우지 않는다(정상 상태에 배지를 다는 것은 소음). 문면 `ui.achievement.serviceUnlinked` 신설 — "기록은 유지된다"를 함께 말한다: 미연동을 손실로 오독하면 잘못된 불안을 준다 **[가안]**. 실화면 캡처로 확인. **부수 교정:** IMPL-152 의 `_tier_open_snapshot()` 이 `data.skills`(id→행 딕셔너리)를 **키로 순회**해 문자열을 행으로 다뤘다 — 실주행(`capture_flow_walk`)이 잡았고, 스냅숏이 조용히 비면 성장 게이트 개방음이 영영 안 울리므로 AUDIO-W 에 래치 검사 3축을 추가했다(돌연변이 1건 검출 확인) | D09 §5.5·§6.5 · D12 §2.2 IAchievement · 총괄 판정 IMPL-128 ③ | `achievement_screen.gd/.tscn` · `strings.csv` · `run_session.gd` | 확정 ([가안] 문면) |
| 2026-08-17 | IMPL-154 | **[보고] 팔레트 실화면 확인 — 본문 통과·부속 텍스트 AA 미달 (총괄 §5 보충 지시분).** 총괄이 "8px 한글 가독"을 지목했으나 실제 본문 계열은 **9**(`param_font_size_body`)이고 8은 쓰이지 않는다. 눈대중 대신 실팔레트 값으로 대비를 계산했다 — **TEXT_PRIMARY 11.09:1(패널)·12.61:1(최심)** = 본문 기준 여유 통과. 문제는 **TEXT_DIM 3.60:1 / 4.09:1** 로 **본문 크기 텍스트 기준(4.5:1) 미달**인데 소비처가 잠금 표기·옵션 고지행·로그 부속·업적 고지 등 **본문 크기 11파일**이다(대형·부속 기준 3:1 은 충족). **FRAME_LINE 1.93:1** 은 릴·패널 경계를 지는 선인데 UI 경계 기준 3:1 미달이다. **색값은 에셋 실물 소관**(CLAUDE.md 불변규칙 2 단서)이라 임의 조정하지 않고 보고한다 — G-1 이 MS-5 로 재배치되어 있어 육안이 유일 감시선이라는 총괄 지시에 대한 회신이다. 부기: TUT-01 콜아웃이 상단 스트립(E01 순위·E02 랩)의 아래쪽을 가린다 — 문면 유입 시 콜아웃 크기가 바뀌므로 판정 B 와 함께 재배치 | 총괄 회신 §5 보충 ② · D10 §5.7 · WCAG 2.1 대비 기준(참고 지표 — D09 는 수치 미규정) | `ui_palette.gd`(무변경·측정만) | **보고** |
| 2026-08-17 | IMPL-149 | **SFX 호출 지점 결선 (원격 2차 인계 §4-3 · 총괄 우선 지정).** 디스패처를 `RunSession.audio` 에 배선하고 `sound_map` 86행이 쓰는 **이벤트 81종 중 71종을 화면·엔진 지점에 결속**, 나머지 10종은 미결선 대장에 이유와 함께 선언했다. 구조 결정 4건 — ①**호출 깔때기 단일화**: 엔진 사건은 `_push_events()` 한 곳에서 로그 한 줄과 소리 하나가 같이 나간다(`SOUND_BY_KEY` 14행 — 로그 키 → 이벤트 id. 화면은 SFX id 를 들지 않는다) ②**조작음은 `FlowScreen.bind()` 가 일괄 결속** — 버튼마다 손으로 걸면 나중에 만든 버튼에서 조용히 빠진다. 기본 결정음/토글음이고 예외는 `audio_event` 메타로 지정(탭·취소·무음) ③**파생 id** — 무대 BGM·베인 큐음·등급 스팅·VN 트랙은 상태에서 파생해 무대·단계가 늘어도 코드 무변경 ④**봉인 창 = 정지 연출과 공종점** — 스핀 커밋(T2)에서 열고 `_reveal_reels()` 종료에서 닫는다. 매치 고지음(D11 R-a)은 닫은 직후 · 리타이어는 BGM 정지 → JG-03 순서(§4.3). **선재 결함 1건 교정**: 표현 층이 `release_voice()` 를 부르지 않아 보이스 점유가 영구 누적 — P1 1발 뒤 P3 전량 억제·P2 12발 뒤 섀시 경고까지 거부(실측). 무음 스트림 길이는 0 이므로 `SilentAudioOutput`(화면 층·약참조)이 즉시 종료를 통지한다. 코어 무접촉 | D11 §1.4 이벤트 결속·§2 SFX 정의·§4.3 전이 규칙 · D12 §10.2 · 불변규칙 5 | `run_session.gd` · `flow_screen.gd` · `silent_audio_output.gd`(신규) · UI 16파일 | 확정 |
| 2026-08-17 | IMPL-150 | **AUDIO-W 검사 신설 — 표와 화면 사이의 공백.** AUDIO 가 표·정책을 보고 여기가 **호출 지점**을 본다. 부를 곳이 없는 행은 실물이 유입돼도 영원히 울리지 않는데, 무음 폴백 단계에서는 그 침묵이 정상과 구분되지 않는다. 축 4종 — ①표 전수 대비 호출(리터럴 스캔 + 파생 선언 + **미결선 대장**) ②역방향 오타 검출(발화 위치 리터럴 + `SOUND_BY_KEY` 상수 직접 대조 + 로그 키가 엔진 발행분인지) ③파생 실증(표 행 도달 + **선언한 서식이 소스에 실재하는지** — 선언만으로 커버리지가 통과하는 구멍을 막는다) ④실주행(RACE-01·SYS-01 인스턴스화 — 봉인 개폐·발화·조작음 결속). **미결선 대장이 핵심**: 목록에 없으면 실패하고, 목록에 있는데 결선돼 있어도 실패한다(대장이 코드를 대신 증언하지 못하게). 반증 = 돌연변이 **18건 주입·18건 전건 검출·미검출 0**. 초기 2건이 구멍이었고(중립 결과음 호출 제거·조작음 결속 무력화) 검사를 보강해 닫았다 | 검증 프로토콜 §1 · D14 TL-3 · SEAL-E 전례(라운드당 검사 수 고정) | `test_audio_wiring.gd`(신규) · `run_tests.sh` | 확정 |
| 2026-08-17 | IMPL-151 | **[가안·보고] 사운드 결선 중 드러난 정본·구현 공백 3건 — 임의 확정하지 않았다.** ①**SE-E05 중립 미결선** — D11 §2.4 는 "무사건·혼합 상쇄"로 정의하나 정산 ②가 트러블 부재 시 반드시 `stableSector01`(안정)을 발행해 **무사건 섹터 턴이 성립하지 않는다**(40 GP·섹터 턴 480건 전수 실측 0건). 결과 계열이 비는 것은 듀얼 패배 턴뿐인데 거기 넣으면 패배에 통과음이 겹친다 → 미결선 선언 ②**섀시 위험 임계 비율** — D09 §5.2 가 "임계값 D13"으로 위임했으나 D13 §2.3(별첨A v1.5)에 해당 행이 없다. 종전 게이지 경고색이 쓰던 값을 `CHASSIS_WARN_RATIO` 로 이름만 붙여 한 곳에 모아 표시(경고색)와 판정(SE-D06)이 같은 식을 보게 했다 — **값 자체는 판정 대기**(원격 판정 A 동형: 정본 위임처에 행이 없다) ③**VN 트랙 정조 태그 부재** — D11 §4.1 이 "트랙 선택 = 이벤트 데이터 태그(스키마 D12)"로 확정했으나 `vn_slots`·`milestone_vn` 어느 표에도 열이 없다. 페이로드 `tone` 으로 받고 미지정은 일상(BGM-09) | 불변규칙 2·9 (임의 해석 금지·값 창구) | D11 §2.4·§4.1 · D09 §5.2 · D13 §2.3 | **판정 대기** |
| 2026-08-17 | IMPL-152 | **JG-01/02 상위·중하위 경계 = 포디움 [가안].** D11 §4.2 는 JG-01 을 "상위 성적"이라고만 하고 수치를 두지 않는다. 게임이 이미 가진 유일한 상위 성적 경계인 포디움을 쓰되 **임계도 코드에 적지 않고** `milestone_first_podium` 행의 `threshold` 를 읽는다 — 값 창구 밖에서 수치를 만들지 않기 위해서다. 리타이어는 두 징글 다 내지 않는다(JG-03 은 리타이어 확정 시점에 이미 발화). 함께 **스킬 티어 개방 래치**(`newly_opened_tiers`)를 세션에 신설 — 성장 게이트 개방(SE-U13)은 마일스톤 파생이라 상태가 아니고 "방금 열렸다"는 경계에서만 알 수 있다(`newly_achieved` 와 같은 성격·세이브 비대상) | D11 §4.2 · D07 §4.3 스킬 티어 = 마일스톤 연동 · D02 §8 성장 게이트 | `gp_result_screen.gd` · `run_session.gd` | 확정 ([가안]) |
| 2026-08-16 | IMPL-148 | **[총괄 판정] 원격 2차 완료 회신 — 판정 3건 + D13 v1.5 발행 + 추가 발견 1건** (`docs/handoff/MS3_원격2차_총괄회신.md`). ①**A = D13 v1.5 전사 (사용자 승인·결정 #15)** — 가상 채널 상한 12·재트리거 0.05초. 원문 3개소 대조로 결정 #14 ④ 동형 전사 누락 확증, 대장 93→**95항**. 원격의 "중단 대신 진행"(core_params 선기입·값 발명 0) = 적법 — 불변규칙 2 중단 조건은 "값이 없으면"이고 이 건은 창구 정리 ②**B = FONT 차단형** — 기계 판정·오검출 0·비용 = 레이아웃 파손(눈 검증 무력 37건 실측)·V7(작법=미적 판단)과 성격 상이. 명칭 FONT 승인(V9 회피 — D12 §4.2 무접촉). flip 집행 = 원격 ③**C = P1~P3 배정 승인** — P1 6종 = D11 §6.3 명문 예시 전건 일치(임의 확대 0)·6+28+52=86 검산 ④**게이트 독립 재현 전건 일치**(COMPILE 85·VALIDATORS 0·suites=11·AUDIO 354). 재현 중 IMPL-137 함정 2회차 실증 — 에디터 가동 중 캐시 미갱신을 `execute_editor_script scan()`으로 해소 ⑤**총괄 추가 발견: `finish_tour()` 동형 미절상** — 투어 순위 `find+1`이 0 가능 구조인데 `record_tour_result`가 `position <= 1`로 투어 우승 계상 → **무순위 = 우승 역전**. 시즌 절상(IMPL-142)과 동형 처리 지시(원격 후속) ⑥오디오 봉인 "차단 로그에 id 불기재"(로그 = 결과 상관 신호 경로) 판단을 봉인 해석 전례로 기록 | 게이트 재현 + 원문 대조(D11 §6.3·D12 §10.2·별첨A §8.3) + 코드 실측(`season_state.gd`·`sound_map.csv` 86행·`core_params` 2항·FONT 경고형 대기) | D13 v1.5(+별첨A) · 마스터플랜 v2.38 · CLAUDE.md 불변규칙 2·7 | 확정 |
| 2026-08-16 | IMPL-147 | **FONT 정적 검사 신설 + 선재 결함 37건 결선 (원격 2차 인계 ⑥ · 마스터플랜 v2.35 ⑧ 지시분).** 축 2종 — **FONT-A** 코드 생성 텍스트 Control 의 `add_theme_font_size_override` 부재 / **FONT-B** 폰트 크기 리터럴 기입(불변규칙 2 저촉). **신설 즉시 34+3 = 37지점 적발**(12개 화면) — 코드 생성 Control 은 프로젝트 기본 폰트를 상속하지 않고 엔진 기본 테마 16 으로 해석되는데 `.tscn` 노드는 멀쩡해 눈으로는 잡히지 않는다. 결선 = ①`FlowScreen` 베이스가 `_body_font_size`·`_head_font_size` 를 `bind()` 에서 **`_on_bound()` 이전에** 한 번만 조달(화면별 중복 선언 2건 제거·`hub_screen` 지역 변수 통합) ②34지점 오버라이드 부여 ③`run_recap_screen` 리터럴 3건을 데이터 참조로 교체 ④`ConfirmDialog` 는 화면이 아니라 컴포넌트라 `_init`/`ask` 인자로 본문 계열 값을 받게 함(호출 4지점 동반). **검사 성격(차단형/경고형)은 총괄 판정 대기 — 현재 경고형** | v2.35 ⑧ "눈으로 훑으면 또 샌다" · V7 전례(성격을 구현이 정하지 않는다) · D10 §5.7 폰트 계열 · D13 `param_font_size_body/head` | `run_validators.gd` FONT · `flow_screen.gd` · UI 12파일 | 확정 (성격 판정 대기) |
| 2026-08-16 | IMPL-146 | **하네스 좀비 상주 가드 결선 + `tl5_runner.gd.uid` 커밋 (원격 2차 인계 ⑤).** `_initialize()` 가 런타임 오류로 중단되면 `_process` 가 아무 일도 못 하면서 false 만 돌려주어 헤드리스 프로세스가 상주한다(주력 실측 3.6시간). 4개 하네스에 **실시간 탈출 예산 + 인스턴스 부재 즉시 종료**를 넣었다 — `test_seal_ui`(CI 게이트 안이라 여기서 상주하면 게이트가 멈춘다·180초) · `capture_hub_shots`(120초) · `capture_race_flow`(120초) · `capture_flow_walk`(기존 예산 유지 + 부재 가드). **경과 시간은 `Time.get_ticks_msec()` 로 잰다** — `delta` 는 `Engine.time_scale` 에 스케일되므로 가속 하네스에서 예산이 뒤틀린다(v2.37 ⑤ⓑ 지시). 반증: `_screen`/`_current` 를 null 로 만드는 돌연변이 2건에서 상주 대신 exit=1 로 탈출 확인 | v2.37 ⑤ⓒ "하네스는 초기화 실패 시 `quit()` 필수" · v2.37 ⑤ⓑ time_scale 금지 | `test_seal_ui.gd` · `capture_*.gd` | 확정 |
| 2026-08-16 | IMPL-145 | **Steam 어댑터 널 배선 — SYS-04 동기 표기 지원 (원격 2차 인계 ④ · 총괄 판정 IMPL-128 ③).** ①`IAchievement.is_linked()` 신설 — **추상이 아니라 기본값 false**(널 구현이 곧 현 단계의 정상 상태) ②`PlatformServices.create()` 신설 = **플랫폼 선택이 일어나는 유일한 자리**. 코어는 물론 화면 층도 구현체 이름을 모른다 — 혼입 0 이 "지키는 규약"이 아니라 "위반할 자리가 없는 구조"가 된다 ③`RunSession.platform` 은 인터페이스 타입 전속·미주입(null)도 정상(헤드리스 테스트·TL-5 러너가 플랫폼 없이 돈다) ④업적 발행은 판정 이후 `_publish_achievements()` 단방향 — **어댑터가 판정에 관여할 경로 없음**(연동 실패가 게임 내 달성을 바꾸면 안 된다). 실 SDK 연동은 MS-3 스토어 준비물·G-7 트랙. **화면의 '미연동' 표기 자체는 주력 몫** — 조회 창구 `session.achievement_service_linked()` 를 열어 두었다 | 불변규칙 1 · D12 §2.2 어댑터 대장 7종 · IMPL-128 ③ | `i_achievement.gd` · `platform_services.gd` · `run_session.gd` · `test_tc_p.gd` | 확정 |
| 2026-08-16 | IMPL-144 | **`ui_palette.gd` [가안] 21건 종결 — 팔레트 정본 §9.2 기계 전사 (원격 2차 인계 ③).** 상수 25종 전량을 `docs/assets/팔레트_정본_v1_0.md` §9.2 매핑표에서 전사하고 **상수마다 조달처를 주석으로 남겼다**(부속 13색 = 접근성 요건 색 / 마스터 56색 = 식별 보조색). 값 형식은 `Color("#RRGGBB")` — 정본 표와 **같은 토큰**이라 기계 대조가 성립한다. 손 전사를 피하려고 스크립트로 ①정본 §9.2 절 안에 각 hex 가 실재하는지 ②코드 값이 표와 일치하는지 ③마스터 조달분 10종이 `master_56.gpl` 의 어느 스와치인지를 대조했다 — 25/25 일치, 마스터 10종 전건 스와치 ID 해석(C2·M2·N05·A2·C3·N01·N03·N08·N16·N11). **§9-⑬ 트리거 도달** — 기능 13색에 두 번째 출처가 생겨 에셋 트랙 생성기의 교차 검증 커버리지가 4/13 → 13/13 으로 열린다. 색값 소유처가 D13 이 아닌 근거 = D13 별첨A §8.2 명시 위임 + 마스터플랜 v2.36 ② | 팔레트 정본 §9.2 · D10 §2.3 면제 조항 · v2.36 ② | `ui_palette.gd` | 확정 |
| 2026-08-16 | IMPL-143 | **오디오 디스패처 결선 — D12 §10·§5.10 (원격 2차 인계 ② · 현 최대 병목 해소).** ①`sound_map.csv` 86행 신설 = SFX 채널 68(D11 §2.11 총계) + BGM 13(§4.1 — 예비 1은 미배정이라 행 없음) + 징글 5. 열 = `event_id·sfx_id·channel·priority·haptic·seal_gated·retrigger_param` ②`AudioDispatcher` = **사운드의 유일 발화 경로** — 호출부는 이벤트 id 만 던지고 무엇이 울릴지는 표가 정한다(D11 §1.4 이벤트 결속 원칙 → C-A2 구조 보장) ③정책 4종을 **호출부가 어겨도 통과 못 하는 층**으로 강제: 봉인(불변규칙 5 — `seal_gated` 행은 릴 정지 연출 완료 전 전면 차단, **차단 기록은 건수만** 남긴다 — 무엇이 막혔는지를 남기면 그 자체가 결과 상관 신호의 디버그 노출이다) / 재트리거 게이트 / 가상 채널 상한·P3 컬링 / P1 보호 ④`AudioOutput` 기본 구현 = **무음 폴백** — 사운드 실물 0 상태에서 전 경로가 성립하고, 실물 유입 시 싱크만 갈아 끼우면 디스패처·호출부 무접촉 ⑤`test_audio.gd` 신설(354검사) — 표 전수·턴 시퀀스 T1~T6 결속·무대 5종 BGM 결속·정책 4종·픽스처 리터럴 회피. **[가안] 우선순위 3층 배분** — D11 §6.3 은 P1~P3 의 성격만 정하고 86행 개별 배정을 하지 않는다(P1 = 명문 예시 6종 전속) | D12 §5.10·§10.2 · D11 §1.4·§2.11·§6.3·§4.3 · D13 별첨A §8.3 | `sound_map.csv` · `core/audio/` · `game_data.gd` · `test_audio.gd` · `config.json` | 확정 ([가안] 1건) |
| 2026-08-16 | IMPL-142 | **오버홀 순위 0 절상 집행 — 총괄 판정 IMPL-141 ① (원격 2차 인계 ①).** `close_season()` 의 `order.find(PLAYER_ID) + 1` 이 순위 밖에서 0 을 흘리던 것을 **최하위 순위 절상**으로 교정하고, **절상을 이 단일 지점 전속**으로 두었다 — `season_result_screen.gd:86` 의 `maxi(pos,1)` 제거(표시값은 판정값에서 파생), `record_season_result` 의 best rank 0 오염도 원천에서 동시 해소. 절상값은 `_grid_size()` = 필러 + 네임드 라이벌 + 플레이어 로 **구조 파생**(상수 16 기입 금지 — 불변규칙 2). 회귀 검사 3축 추가: 순위 밖 → 0 아님·1 아님·그리드 규모 일치(`test_season`) / 절상값이 최하위 등급 1슬롯·2후보를 문다 + 등급 표는 여전히 순위 1~16 전속(`test_tc_o`) / best rank 갱신 방향(`test_tc_o`) | D06 §5.3 G-M1 구속 · D13 별첨A §7.1 · 총괄 판정 IMPL-141 ① | `season_state.gd` · `season_result_screen.gd` · `test_season.gd` · `test_tc_o.gd` | 확정 |
| 2026-08-16 | IMPL-141 | **[총괄 판정] 주력 2차 발신 + TL-5 발신 일괄 회신 — 판정 4건·정본 개정 0** (`docs/handoff/MS3_주력레인_2차_총괄회신.md`). ①**오버홀 순위 0 = 최하위 등급 절상 (권고 ① 채택)** — D06 §5.3 G-M1 구속("성적 무관 최소 1슬롯 보장"·"시즌 완주 = 오버홀 발생 예외 없음")이 이미 답한다. 순위 밖 → 빈 추첨(현행 `draw_overhaul_candidates`)은 G-M1 위반, SET-02 `maxi(pos,1)` 1 절상은 성적 차등 취지 위반. 집행 규격 = 절상을 `close_season()` **단일 지점**으로(표시값은 판정값에서 파생 — IMPL-100·124·134 동일 해법 4번째) + 절상값 하드코딩 금지(엔트런트 수 등 구조값 파생) + TC-O 경계 검사 갱신. `record_season_result`의 best rank 0 오염도 같은 절상으로 동시 해소된다. 집행 = **원격 이관** ②**튜토리얼 5단 = D04 문면 확정 시 재편 (권고 채택)** — 현행 [가안] 유지. "무비용 행동 선행" 제약은 구속 기록(실기 적발 — 문면 재편 시 불변) ③**TL-5 ① = 조건부 통과 확정** — 증분 상한이 D13 확정값(2.5초·40%·1.0초)에 결속 → 규격 준수 시 투영 불가역. 이월 2건: T6 재생 결선 시 재측정 · 스킬 소비부 유입 시 변환(Convert) 행 ④**TL-5 전제 명문화 = D14 §8.5 기입란 부기 (사용자 승인)** — GP 길이 성분 실측(기계 강제 11.7초/턴·자율 ~9초/턴·자동 확정 하한 4.16분) 기입, 판번 무증가(D15 실일자 전례). D13 §4.1 산식 주석 = P-2 ㉙. 부수: **총괄 자기 결함 5호**(P-2 원장 행 v2.31 정지 — 3호 동형 재발) 교정 + 원장 행을 계수·포인터 전용으로 구조 전환 · 에셋 판정집행 회신(IMPL-140) 접수 — 생성기 기계 재현 검증 통과(2회 실행 바이트 일치·색 리터럴 0) | 발신 코드 주장 3개소 원문 실측 일치(`season_state.gd:367`·`outgame_state.gd:253~272`·`season_result_screen.gd:86`) · D06 §5.3·D13 별첨A §7.1 원문 대조 | 마스터플랜 v2.37 · D14 §8.5 기입 · CLAUDE.md 정본 포인터 | 확정 |
| 2026-08-16 | IMPL-140 | **총괄 판정 IMPL-139 2건 집행 — 스와치 생성기 커밋 + §9-⑫ 근거 교체.** ①**`tools/palette/swatch_gen.js` + `README.md` 커밋**(총괄이 `tools/palette/` 를 에셋 트랙 소유로 배정). **내 권고(비커밋)가 뒤집힌 것을 받아들인다** — IMPL-097(TL-2 체크리스트)이 같은 구조를 이미 확정했고, "원천이 `.gpl` 이라면 **그래서** 파생물의 일치를 보증할 생성기가 필요하다"는 논거가 내 ⓑ 논거를 정면으로 뒤집었다. 부대조건대로 **생성기가 색상값을 하나도 갖지 않게 재작성** — 마스터 56 = `master_56.gpl`(총괄 지정 입력) · 교체 4 = `colorblind_alt.gpl` 꼬리 · **기능 부속 13 + 교체 대응관계 = 팔레트 정본 §5·§6 파싱**(`.gpl` 밖이라 정본이 유일 기계 판독 출처). 생성기가 보유한 것은 한글→ASCII 라벨 매핑뿐(3×5 폰트가 한글을 못 그린다). 매 실행이 교차 검사 후 어긋나면 **PNG 를 쓰지 않고 중단** ②**대장 §9-⑫ 근거 교체** — 초판의 "베인 이웃은 적록 충돌쌍이 아니다"를 폐기하고 명도차 역전 + 부호화 채널 강도 차로 대체 ③**§9-⑬ 신설** — 생성기 커버리지 한계 공개 ④§4.9 의 재현 주체를 산문에서 명령으로 이관 · PNG **수기 편집 금지** 명문화 | **등가 증명 — 리팩터가 값을 바꾸지 않았음을 바이트로 확인했다.** 입력을 `.gpl`·정본 파싱으로 갈아엎은 뒤 `colorblind_alt.png` 가 **기존 산출과 바이트 동일**하게 재현됐다(md5 일치). 그 시트가 마스터 4블록 + 기능 13 + 교체 4를 **전부** 담으므로 세 입력 경로가 기존 인라인 표와 값이 같음이 증명된다. `master_56.png` 의 유일한 차이는 푸터 문구(`DO NOT EDIT BY HAND` 로 교체 — 날짜를 빼 재생성 결정성도 확보). 연속 2회 실행 해시 동일. **돌연변이 5건 실측 — 4건 검출·1건 미검출.** 검출: ⓐ`.gpl` RGB 1바이트 변조 ⓑ탭→공백(구분자 붕괴) ⓒ`colorblind_alt.gpl` 의 마스터 본체 오염 ⓓ정본 §6 대체색 변조. **미검출: 정본 §5 `VANE-ELATED` hex 1자리 변조.** 원인은 구조적이다 — **대조는 값이 두 곳에 있을 때만 성립**하는데 기능 13 중 §6 교체쌍 4건만 상대가 있고 나머지 9건은 정본이 유일 출처다. **검증한 척하지 않기 위해** 생성기가 매 실행에 커버리지 `4/13` 과 미검증 9종 목록을 출력하게 하고 §9-⑬ 에 등재했다. **해소 경로는 이미 정해져 있다** — 구현 트랙이 `ui_palette.gd` 를 정본 §9.2 매핑표대로 결선하면(현 [가안] 21건) 13색 전건에 두 번째 출처가 생긴다. **②의 ΔL\* 를 독립 계산으로 검산해 총괄 수치와 일치 확인**(sRGB→선형→Y→L\*): 게이지 위험↔주의 **22.7**(교체됨) vs 베인 경고↔고양 **29.2**(미교체) — **교체된 쪽이 더 좁다.** 내 초판 근거는 자기 논리로 반박된다(같은 적↔앰버 쌍인데 게이지는 교체됐다). 실제 비대칭은 **1차 부호화 채널의 강도 차**다 — 게이지는 채움량이 연속값이라 등급 경계를 색이 지고, 베인은 파형 A/F/S 가 상태를 직접 그려 색이 보조 중의 보조다. **검증기 전건 PASS(warnings=0)** — V1~V8·MIX0·ARCH. 생성기는 `tools/` 소속이라 검증기 주사 대상 밖이고 런타임 무접촉이다 | 총괄 판정 `docs/handoff/팔레트편입_총괄회신.md` §1·§2 · IMPL-097(생성기 커밋 전례) · D10 §2.3·§2.4 · D03 §1.5 · 팔레트 정본 §5·§6·§9.2 · IMPL-137·139 | 확정 (등가 바이트 증명 + 돌연변이 5건) — 커버리지 한계 1건 공개 |
| 2026-08-16 | IMPL-138 | **MS-3 T4 잔여 — TUT-01 튜토리얼 오버레이 구조 결선 (문면 제외).** kickoff §2 T4 는 TUT-01 을 주력(씬) 몫으로 두었으나 총괄 착수인계 §1 의 착수 6건에서 빠져 있었다 — 사용자 판정으로 **구조만 선행**한다(문안은 T7 유입 시 채운다). ①`tutorial_steps.csv` 신설(V1 스키마 등재) — `step_order`·`anchor_node`·`advance_on`·`text_key` 4축. **적재 시 step_order 로 정렬**해 화면이 CSV 행 순서에 의존하지 않게 했다 ②`ui/race/tutorial_overlay.gd` + RACE-01 내부 노드(라우터 비경유 — SYS-05 전례). **별도 튜토리얼 스테이지 불신설**(§6 확정) ③**하이라이트 = 딤 4분할 컷아웃** — 대상 사각형의 상·하·좌·우만 덮어 가운데를 비운다. 셰이더 없이 성립하고 대상 색이 왜곡되지 않는다 ④**단계 진행 = 지시 행동 수행**(§A-25) — 지목한 행동이 아니면 무시한다 ⑤**튜토리얼 중 타이머 비활성 고정** — O5 설정과 무관하게 강제(§6 확정 · 실측 `타이머비활성=true`) ⑥스킵 + 1회성 기록(`onboarding_seen`) — 옵션 초기화가 이미 `reset_onboarding()` 을 부르므로 **재활성 경로는 기존 것을 그대로 쓴다**(D09 §6.1 옵션 대장에 없는 9번째 옵션을 신설하지 않았다) | **[진성 결함 2건 — 실기가 잡았다]** ①**단계 순서가 도달 불가였다** — 초안은 스핀 → 홀드 → 리스핀 → 차지 개입 → 확정이었는데 **GP 개시 차지가 0**이고 홀드는 1·차지 개입은 2를 요구한다(D13 §2.2). 무비용 행동이 앞서야 순서대로 밟힌다 → **스핀 → 확정 → 홀드 → 리스핀 → 차지 개입**으로 교정. 규격상 잠기지는 않으나(지목 외 행동은 무시될 뿐 진행을 막지 않는다) 순서대로 배우는 경로가 성립하지 않았다 ②**콜아웃이 자기가 지목한 요소를 덮었다** — 패널이 하단 고정이라 확정 버튼(캔버스 y 309)을 그대로 가렸다 → 하이라이트가 하반부면 패널을 상단으로 보내는 배치로 교정. 부수: 앵커 스트레치와 코드 배치가 겹쳐 패널이 늘어난 것도 앵커 제거로 해소. **[가안] 3건:** 단계 5종의 구성·순서(도달 가능성 제약은 구조적이나 무엇을 어느 순서로 가르칠지는 D04/D09 content) · 하이라이트 여백 3px · 콜아웃 여백 8px. **[보고] 문면 미유입:** `text_key` 5종은 스트링에 등재돼 있으나 값이 `[문안 미유입]` 표식이다 — 콜아웃 문안은 D04 §8.1 베인 1단계 말투 산출물이고 구현이 지어내지 않는다(불변규칙 9). 유입 시 `strings.csv` 값만 채우면 되고 코드·데이터 무변경. **검사:** 단계 표-화면 계약을 못박았다 — `RaceScreen.TUTORIAL_ACTIONS` 밖의 행동을 데이터가 지목하면 그 단계에서 튜토리얼이 영원히 잠기므로 두 쪽을 대조한다. 돌연변이(화면 상수에서 `charge` 제거) 검출. TC-C 하한 1997→2002. **임포터 함정 재발 차단:** 신규 CSV 가 `csv_translation` 으로 자동 추론돼 `.translation` 4개가 생겼다 — `keep` 으로 교정(IMPL-003·021 규약) | D09 §6 · D09 별첨A §A-25 · D04 §8.1 · D13 별첨A §2.2 · 불변규칙 2·6·9 · IMPL-003·021·077·096·125 | 확정 (실기 캡처 + 돌연변이 실측) — 문면 T7 대기 |
| 2026-08-16 | IMPL-139 | **[총괄 검수·판정] 팔레트 편입 회신 — 5항 전건 통과·반려 0 · 판정 2건** (`docs/handoff/팔레트편입_총괄회신.md`). **검수는 기계 대조로 했다** — `master_56.gpl`을 인계 §2 본문과 `diff`해 **56행 전건 완전 일치**·엔트리 56·RGB 중복 0·`.import`가 PNG 2건에만 존재(`.gpl`은 임포터 없음이 정상) 확인. **§5 질의 판정 — 스와치 생성기 커밋(권고 뒤집음).** 근거 = **IMPL-097 전례**(TL-2 체크리스트: 생성기 커밋·산출물 수기 편집 금지·재생성=같은 명령)와 동형이다. 제시 논거 3건 반박: ⓐ`tools/`는 남의 경로가 아니라 **미배정 경로** → **`tools/palette/`를 에셋 트랙 소유로 배정**(자기 절제는 옳았고 배정으로 해소) ⓑ"원천이 `.gpl`"이면 **그래서** 생성기가 필요하다 — 파생물이 원천과 일치함을 보증하는 유일 수단 ⓒ**산문 규격 4항은 재현을 보증하지 못한다**(읽고 다시 그리면 재현이 아니라 재작화). 부대조건 = 생성기가 `.gpl`을 입력으로 읽을 것(값을 두 번 적으면 그게 곧 두 벌) **§4 보고 판정 — 결론 승인·근거 교정** | **§4의 근거가 자기 논리로 반박된다.** 에셋 트랙은 "베인 이웃은 적록 충돌쌍이 아니라 미교체"라 했으나, **베인 경고(적 #FF4A3D)↔고양(앰버 #FFD86B)은 게이지 위험↔주의와 정확히 같은 적↔앰버 쌍**이고 그 게이지 쌍은 교체 대상이었다. **명도 실측은 반대를 가리킨다** — 베인 ΔL\* **29.2** > 게이지 ΔL\* **22.7**(교체된 쪽이 더 좁다). **진짜 근거 = 1차 부호화 채널의 강도 차:** 게이지는 색+**채움량**이라 심각도 경계를 색이 지지만, 베인은 색+**파형 A/F/S 3파라미터**로 파형이 상태를 직접 그린다(D03 §1.5) — 색이 판정에서 멀다. 결론(비차단·최소 교체)은 이 근거 위에서 유지. 대장 §9-⑫ 근거 문면 교체 지시. **총괄 집행 2건:** ①IMPL-135·인계문의 이관 전 경로에 **현 위치 병기**(이력이므로 덮어쓰지 않음 — 에셋 트랙이 총괄 문서를 안 고치고 남겨 준 판단이 정확했다) ②**§6 실측 함정을 CLAUDE.md로 승격** — `--headless --import`가 에디터 동시 실행 시 MCP 포트 3571 충돌로 **접근 위반 종료**. 대장 §7은 에셋 소유라 타 레인이 안 읽고, 정작 CLAUDE.md는 `--import`를 안내하면서 이 함정을 빠뜨리고 있었다. 대처(에디터를 끄지 말고 `execute_editor_script`로 `scan()`)와 **"3레인 병렬에서 에디터를 끄면 타 세션을 끊는다"는 제약**이 함정 자체보다 오래 간다. **§7 발주 순서(icons 16종 우선) 승인** — 소비부 결선 유일 카테고리·32×32 단일 규격이라 파이프라인 첫 실행 비용 최소 | IMPL-097·135·137 · D03 §1.5 · D10 §2.4 · 다중 세션 규약 · CLAUDE.md 주력 절 | 확정 (기계 대조 + L\* 검산) |
| 2026-08-16 | IMPL-137 | **팔레트 실물 4파일 산출 — 에셋 트랙 최대 병목의 물리적 해소** (총괄 인계 `docs/handoff/팔레트_에셋트랙_편입인계.md` 5항 전건 집행). ①**`godot/assets/palettes/` 4파일**: `master_56.gpl`(56 엔트리) · `master_56.png`(288×874 스와치) · `colorblind_alt.gpl`(56 + 교체 4 = 60) · `colorblind_alt.png`(288×930) + `.import` 2건. `.gitkeep` 제거 ②**팔레트 정본을 `docs/assets/` 로 편입** — `docs/handoff/팔레트56색_총괄확정_v1_0.md` → **`docs/assets/팔레트_정본_v1_0.md`**(`git mv` — IMPL-135 등재 경로는 이 이관 전 표기다). 이로써 에셋 3문서의 층이 갈린다: **정본 = 무슨 색인가 / 사양서 = 어떻게 그리는가 / 대장 = 어디에 두는가** ③**사양서 v1.3** — §1.4 "⚠ 최우선 선행 과제" 문면 해소(정본 참조로 대체·hex 재기입 금지 명문화) · §2.3 파이프라인 ③ 양자화의 기준 파일 지정 + 주의 2건(부속 13색은 `.gpl` 밖이라 스와치에서 집을 것 · 자동 디더 금지) · §12-5 노스윈드 4색 배분 종결 ④**대장 v1.2** — §4.9 배치 완료 전환 + 산출 규격 4항 · **§9-⑪ 종결** · §9-② 팔레트분 종결 ⑤**대장 §2 규칙 ⑦ 인용 교정**(총괄 발견분) | **①의 값은 손으로 옮기지 않았다** — 정본 §1~§4 의 hex 를 기계 전사하고 인계 §2 의 RGB 전사 본문 56행과 **전건 대조**시켰다(불일치 시 산출 중단). 동시 검산: 구성 16+12+16+12 · 마스터 중복 0 · 기능 13 · 교체 4 · hex 형식. **`.gpl` 구분자는 탭**(공백이면 툴에 따라 이름이 깨진다). **기능 컬러 부속 13색을 `.gpl` 에 넣지 않은 것은 총괄 지시이고 근거가 갈린다** — 면제 조항은 이 색군이 마스터에 *우선한다*고만 했지 팔레트 에셋에 담으라고 하지 않았고 값의 정본은 `ui_palette.gd` 다. 다만 **심볼 6색은 아이콘 원도에 실제로 칠해지므로** 스와치 PNG 에 별도 구획으로 병기했다 — 아티스트가 툴 밖에서 집을 수 있어야 한다. **[실측 함정 1건 — 대장 §7 에 등재]** `--headless --import` 가 **에디터 동시 실행 시 죽는다**: 헤드리스도 GDAI 애드온을 적재하는데 에디터가 MCP 포트 3571 을 점유 중이면 `Port 3571 is already in use` 뒤 접근 위반(`0xC0000005`)으로 종료되고 `.import` 가 생기지 않는다. **에디터를 끄지 않고**(타 세션이 `race_screen.tscn` 편집 중이었다) GDAI `execute_editor_script` 로 `EditorInterface.get_resource_filesystem().scan()` 을 불러 임포트했다. 부수 확인: `.gpl` 은 Godot 임포터가 없어 **`.import` 가 안 생기는 것이 정상**(누락 아님) · 니어리스트는 `project.godot:35 default_texture_filter=0` 전역 적용이라 파일별 오버라이드 불요. **[신규 보고 1건 — 대장 §9-⑫]** 색각 대체에서 **`VANE-ALERT` 만 같은 적색 `#FF4A3D` 인데 교체 대상 4색에 없다.** 의도로 보인다 — 교체된 4색은 전부 적록 축 충돌 *쌍*(라임↔적 · 앰버↔적)의 구성원인 반면 베인의 이웃은 시안·밝은 앰버·무채라 충돌쌍이 아니다. 다만 경고(적)와 고양(`#FFD86B`)의 2색각 근접은 계산으로 배제되지 않는다. 파형 A·F·S 가 1차 부호화라 **비차단**, G-2 계열 실측(MS-5) 대기. **[비커밋 결정]** 스와치 PNG 생성기(Node·zlib 내장 PNG 인코더 + 3×5 비트맵 폰트)는 **커밋하지 않았다** — `tools/` 는 에셋 트랙 소유 경로가 아니고(다중 세션 규약) 산출의 진짜 원천은 `.gpl` 이다. 재현 규격은 대장 §4.9 에 4항으로 남겼다. 커밋 여부는 총괄 회신에 질의로 올림 | 총괄 인계 `팔레트_에셋트랙_편입인계.md` §1~§5 · D10 §2.3·§2.4·§8.2 · D13 별첨A §8.2(색값 명시 위임) · D12 §10.1(인용 교정 대상) · IMPL-003·021·105·131·132·135·136 | 확정 (RGB 전건 대조 + 임포트 실측) — 보고 1건·질의 1건 |
| 2026-08-16 | IMPL-134 | **HUB-08 헤더 후보 수를 추첨 결과에서 산출 — 총괄 승인분 집행 (IMPL-128 §6).** 헤더의 '후보 N'이 `overhaul_slots(_rank).candidates`(**규칙값**)에서 나오고 목록은 `overhaul_candidates`(**추첨 결과**)에서 나와, 두 rank 가 어긋나면 헤더가 목록과 다른 수를 말했다. `_shown_candidates()` 를 신설해 **헤더와 목록이 같은 배열을 본다** — 미추첨 폴백(전 오버홀)도 한 곳에서 결정되므로 어느 경로에서도 갈릴 수 없다. **집행 조건 성립 확인:** 총괄이 "SET-02 결선 시 동반"으로 배정했는데 **SET-02 → HUB-08 체인은 이미 결선돼 있었다**(`season_result_screen._on_next()` 가 `championship_rank`·`season_chain` 을 넘긴다) — 화면 헤더 주석의 "SET-02가 아직 없어 도달 불가"가 낡은 표기였다. 주석 교정 동반 | **실측(전·후 양방향):** 진입 rank 를 추첨 rank 와 어긋나게 준 재현 3케이스 — 교정 후 `추첨 5·규칙값 4·목록 5 → 헤더 "후보 5"` / `추첨 2·규칙값 5·목록 2 → 헤더 "후보 2"` 로 전건 목록과 일치. **교정을 되돌리면 즉시 재현**된다(`헤더 "후보 4" · 목록 5행` → 하네스 exit 1) — 검사가 공허하지 않음을 양방향으로 확인했다. **[신규 보고 1건 — 총괄 판단 대상]** 이 작업 중 **rank 절상의 비대칭**을 발견했다: `player_position` 은 `order.find(PLAYER_ID) + 1` 이라 **플레이어가 순위표에 없으면 0** 이 되는데, `close_season()` 은 그 0 을 그대로 추첨에 넘기고(→ `overhaul_slots(0)` 은 D13 §7.1 표가 1~16만 덮어 행 없음 → push_error·빈 dict) SET-02 는 `maxi(...,1)` 로 절상해 화면에 넘긴다. 순위 밖 플레이어에게 어느 등급을 줄지는 D06 §5.3·D13 §7.1 판단 사안이라 **임의로 정하지 않고** TC-O 에 경계 검사(`overhaul_slots(0).is_empty()`)만 두어 못박았다. 헤더 교정 덕에 표시 괴리는 이미 닫혔으므로 비차단이다. TC-O 하한 381→382 | D06 §5.3 · D13 별첨A §7.1 · D09 별첨A §A-18 · 총괄 판정 IMPL-128 §6 · IMPL-100·112·124 | 확정 (전·후 양방향 재현 실측) — 보고 1건 |
| 2026-08-16 | IMPL-133 | **MS-3 T9 잔여 — TL-5 미모델링 3항 실기 (D14 §8.3 ①②③). ①조건부 통과 · ③통과 · ②기기 차단.** ①**GP 길이 실측 20GP** (`docs/qa/TL5_실행기록_2026-08-16.md` §4.1) — RACE-01 실화면을 주행하며 게임 시간 델타를 누적. **화면 기계시간 평균 24.3초**(P90 25.6 · 턴 평균 14.4 = 섹터 12.0 + 듀얼 2.5 · 턴당 약 1.7초). **완급 비트 증분 = 섹터턴 12.0 × 1.0초 = 0.20분**으로 대역 폭 5분의 4% — 숙고 0~10초 전 구간 합성 4.36~6.76분이라 상한 15분에서 8분 이상 남는다 → **"실기 증분이 대역을 넘지 않음"이라는 ① 판정 문면 충족** ③**모멘텀×개입 4타입 매트릭스** — 같은 시드·같은 행동열로 엔진 두 대를 몰고 `confirm()` 잔여 비율만 다르게 주어 전방 게이지 차를 본다(개입의 난수 소비도 동일하므로 차이는 모멘텀 항 하나뿐이어야 한다). 홀드·보험·홀드+보험 전건 델타 불변(+5G, 중첩 0) · **증폭은 구조적 배타**(모멘텀 = 비듀얼 전속 / 부스트 = 듀얼 전속 — 양방향 확인) → **정의 외 상호작용 0**. 검사 10건 신설, TC-C 하한 1961→1997 | **[보고 3건 — 총괄 판단 대상 (§8.4 임의 조정 금지)]** ①**① 판정의 전제가 미성립** — 규격 문면은 "**완급 비트 정상 작동 하**"인데 **재생 소비부가 0건**이다. `param_pacing_beat_max_sec 2.5`·`param_pacing_beat_probability 0.40`·`param_time_pacing_beat_sec 1.0`이 데이터에 전사돼 있고 검사도 곱의 정합을 보지만, 화면에는 `E15ScenePanel` 노드만 있고 구동 코드가 없다(씬 컷 에셋도 미유입 — T6). 증분은 **데이터 산출 투영값**이므로 '조건부'다 — 통과로 확정할지 재생 결선까지 미실행으로 둘지가 판단 사안 ②**GP 길이 대역이 플레이어 자율 페이스에 지배된다** — 모델은 21초/턴인데 **기계가 강제하는 것은 연출 1.7초 + 개입 창 상한 10초 = 최대 11.7초/턴**뿐이고 나머지 약 9초/턴은 T1에서 로그를 읽고 스핀을 누르는 자율 시간이다(T1에는 타이머가 없다). 대역 달성은 구현 보장이 아니라 전제다 — 값 조정이 아니라 **전제의 명문화 여부**가 판단 대상 ③**변환(Convert) 개입은 매트릭스 미측정 행** — D05 §6.1이 스킬 데이터 인스턴스로 규정하는데 RACE-01 스킬 슬롯이 전 슬롯 잠금(IMPL-071)이라 주입할 개입이 없다. **② 입력 지연은 MP-L 실기기 조달 전이라 측정 경로 자체가 없다**(총괄 회신 §7 배정 = MS-5 전). **[계측 방법 교훈]** `Engine.time_scale` 가속본이 **실시간 대비 약 30% 과대**로 나왔다(8배속 31.6초 vs 실시간 24.3초) — 대기 구간이 프레임 양자화로 부풀기 때문이다. 실시간 3GP 대조로 잡아낸 뒤 전량 실시간 재측정했다. **연출 시간 계측에 시간 가속을 쓰지 않는다.** 돌연변이 양방향 실측: 홀드 시 보너스 2배 주입 → 2건 발화 / 보험 시 모멘텀 소거 주입 → 2건 발화 | D14 §8.3 ①②③·§8.4 · D13 §4.1·§8.1 · D05 §6.1·§7 · D09 §3.1.1 · 검증 프로토콜 §1 · IMPL-071·114·115 | 확정 (20GP 실시간 실측 + 돌연변이 양방향) — 보고 3건 총괄 대기 |
| 2026-08-16 | IMPL-130 | **총괄 판정 IMPL-128 B 이행 — CG-03(동기 · 주드) L3 조우 결선.** 판정 회신이 두 질문 모두 **정본에 이미 답이 있음**을 밝혔다: ①'인접' = D08 §8.7·D13 별첨A §6.5 확정 명문 `\|플레이어 챔피언십 순위 − 주드 순위\| ≤ 2` ②래치 = **역전 성립 후 최초의 인접 듀얼**. ①`race_screen.l3_kinship_for()` 신설 — 래치 + 상대 주드 + 인접 임계 3조건 ②**역전 래치 `OutgameState.jude_overtaken`** — 세션이 `close_gp()` 에서 챔피언십 순위 대조 후 1회 세운다. 되돌리지 않는다(이후 밀려도 '첫 역전'은 있었다). 직렬화 편입·구세이브 = 미성립 관용 ③`RunSession._jude_rank_delta()` → **공개** `jude_rank_delta()` — 이벤트 변형(D08 §7.3)과 CG-03 이 **같은 축을 본다**(두 소비부가 다른 값을 보면 인접 판정이 갈린다) ④임계 2 는 D13 확정값의 데이터 전사 `param_jude_adjacent_max` 로 두었다 — 코드에 2를 적지 않는다(불변규칙 2). 이벤트 DSL 쪽 상수와 별개 창구가 아니라 같은 D13 행의 전사다 | **판정 B-2 의 근거가 구현 제약과 정확히 맞물린다:** 챔피언십 역전은 **GP 결과로 확정**되고 듀얼은 그 GP **진행 중**에 일어난다 — 듀얼 시점에는 역전 여부가 미확정이므로 "역전이 일어난 그 GP 의 듀얼"은 구조적으로 판정 불가다. 그래서 래치를 `close_gp()` 에 두는 것이 유일한 실행 가능 해석이다. **검사 6건 + 돌연변이 실측:** 인접 조건 제거 주입 → 2건 발화. 경계 검사 4축(임계 정확값·초과·래치 미성립·상대 불일치) + **순위표 미등재(99) 가 인접으로 새지 않는가** — `jude_rank_delta()` 의 '큰 값' 규약이 무너지면 무득점 초반에 조우가 항상 성립한다. TC-C 하한 1955→1961. **CG-01 우선:** 두 조건이 동시 성립할 경로는 없으나(최종 무대 벽 = 로렌츠) 판정 순서를 왕좌 우선으로 고정했다 | D10 §7 결정 #6 · D08 §8.7 · D13 별첨A §6.5 · 불변규칙 2·5 · 총괄 판정 IMPL-128 B-1·B-2 · IMPL-114·126 | 확정 (돌연변이 실측 + 게이트 전건) |
| 2026-08-16 | IMPL-129 | **총괄 판정 IMPL-128 A-1·§5 이행 — 업적 달성 시즌 래치 + 데이터 축 교정.** ①**시각 축 = 인게임 시즌** — `OutgameState.achievement_seasons` 신설(id → 시즌). `evaluate_achievements(season)` 로 시즌을 받아 **최초 성립 시점 1회만** 적는다(래치). 시즌 0(문맥 없는 호출)은 아예 기록하지 않는다 — '시즌 0' 표기가 새지 않게. 직렬화 편입 · **구세이브(필드 부재) = 시즌 비움**, 표시 층이 `—` 로 적는다(없는 값을 지어내지 않는다 — 판정 부대 지시 2 · IMPL-090 관용 전례) ②`achievement_progress()` 에 `season` 편입 — 화면이 진척·달성·시즌을 한 창구에서 읽는다 ③SYS-04 항목 행에 달성 일시 열 결선 ④**데이터 축 교정 (판정 §5 지시)** — `achievements.csv` 4셀 `category` `discovery`→`rival` · V1 enum 6종→**5종** · TC-O 계수 `rival 14→18`·카테고리 축 5종 못박기 + 조건 3형식(reach 31·cumulative 4·discovery 4) 검사 신설 · **화면 탭 정의에서 흡수 매핑 제거**(`["rival","discovery"]` → `"rival"` 단일) | **판정 A-1 의 근거가 IMPL-124 의 보고를 정확히 뒤집었다** — 나는 "시각 축이 정본에 없다"고 올렸으나 **D09 §5.5 같은 표의 칭호 이력 행이 `(시즌 N)` 으로 이미 확정**돼 있었다. 같은 표의 두 행이 다른 시각 축을 쓸 이유가 없다는 판정이 옳다. 벽시계가 아닌 이유도 구조적이다: 재로드 재판정으로 소급 달성이 성립하므로 해제 시각은 실제 달성 시점과 어긋난다. **교정 §5 는 구현을 단순하게 만들었다** — 발신 시점에는 "데이터를 고치면 TC-O·V1 을 함께 고쳐야 한다"를 비용으로 봤으나, 실측하니 `condition_type` 열이 이미 D07 §7.1 3형식을 정확히 담고 있어 `category` 만 6번째 값을 물고 있었다. 교정 후 `category` 가 D07 과 1:1 이 되고 화면의 흡수 매핑이 사라졌다. **검사 7건 + 돌연변이 2종 실측:** ①이미 달성분 재스탬프 주입(래치 파괴) → 2건 발화 ②`category` 1셀 `discovery` 원복 → **V1 이 즉시 차단**(`'discovery' not in enum`). 왕복 보존·구세이브 관용 양방향 포함. TC-O 하한 374→381 | D09 §5.5 · D07 §7.1 · D08 §8.11 · D12 §7 · 불변규칙 2 · 총괄 판정 IMPL-128 A-1·§5 · IMPL-090·121·124 | 확정 (돌연변이 2종 실측 + 캡처 눈 대조) |
| 2026-08-16 | IMPL-127 | **HUB-04 스킬 티어 개방·HUB-08 오버홀 추첨 실기 확인 (착수인계 ③④ — 코드 무변경).** ①**티어 개방 실증** — 원격 결함 ②(마일스톤 기록 주체 0건 → 스킬 티어 2·3 영구 잠김)의 해소분을 화면에서 확인했다. 커리어 개시 = `t1 개방 · t2·t3 잠금` → **첫 포디움 1건 기록 후 `t2` 개방**(마일스톤 3건 적재 실측: first_finish·first_point·first_podium) → **첫 GP 우승 후 `t3` 개방**. 화면도 따라간다 — 잠금 상태 헤더 `스킬 티어 2 — 개방: 첫 포디움` + 항목 '잠금' 표기가, 포디움 후 헤더에서 조건이 사라지고 항목이 `해금 55 DP`로 활성 ②**문면 정본 확인** — 화면이 D07 §4.3(티어2 = 첫 포디움 / 티어3 = 첫 그랑프리 우승) 준거로 이미 서 있다. D09 별첨A §A-14 와이어프레임 예시의 '첫 투어 우승'은 오기(P-2 ㉒)이며 구현은 무영향 ③**HUB-08 등급별 후보 수 = D13 §7.1 정확 일치** — P2 → 슬롯 2·후보 5 / P6 → 후보 4 / P12 → 후보 2, 화면 요약행·목록 행수 양쪽 대조 ④**재로드 리롤 무효 실증**(불변규칙 4) — 직렬화 왕복 후 후보 5종이 순서까지 동일 | **[착수인계 오기 1건]** 인계 §1-③ 이 '**HUB-05** 스킬 티어'라 적었으나 D09 §8.1 대장상 **HUB-05 = 기록실(3탭·§A-15)** 이고 스킬·덱은 **HUB-04 전략실(§A-14)** 이다. 인계문이 병기한 §A-14 쪽이 맞다 — 실기 대상은 HUB-04. **[보고] HUB-08 표시-상태 괴리 가능성 1건 (잠복·비차단):** 요약행의 '후보 N'은 `overhaul_slots(_rank)` 가 돌려주는 **규칙값**인데 목록은 `outgame.overhaul_candidates` 라는 **추첨 결과**다. 두 축의 rank 가 어긋나면 헤더가 목록과 다른 수를 말한다 — 하네스가 payload 키를 틀렸을 때 '후보 4 · 목록 5행'으로 실제 재현됐다. 현행 정상 경로에서는 `close_season()` 추첨 rank 와 진입 payload 가 같아 드러나지 않으나, **HUB-08 의 유일 진입인 SET-02 가 아직 없어** 그 계약이 아직 안 굳었다. 헤더 수치를 추첨 결과 크기에서 끌어오면 구조적으로 갈릴 수 없다 — SET-02 결선 시 동반 판단 대상으로 등재 | D07 §4.3 · D13 별첨A §4.1·§7.1 · D09 §8.1 · D09 별첨A §A-14·§A-18 · 불변규칙 4 · IMPL-101·112·113 | 확정 (세션 상태 실측 + 캡처 5컷) — 보고 1건 |
| 2026-08-16 | IMPL-126 | **L3 조우 기록 결선 — CG-01(왕좌·로렌츠) 한정 (착수인계 ⑤).** `race_screen.gd` 에 `l3_encounter_for()` 신설 — 조건 = **시즌 최종 무대의 벽 라이벌과의 듀얼 결판 국면**(D10 §7 결정 #6). 성립 시 `outgame.record_discovery("cg_01_throne")` + `trigger_signature_event` 승격. 발견형 히든 업적의 발화 경로가 이것으로 닫힌다(업적 판정 자체는 GP·투어·시즌 경계의 `evaluate_achievements()` 몫) | **[보고] 인계 전제가 성립하지 않았다 — '1줄 연결'이 아니었다:** 인계 ⑤는 "L3 컷인 재생(T6)에서 1줄 연결"을 전제했으나 **재생 소비부도, L3 발동 판정도 production 에 없었다** — `trigger_signature_event` 는 테스트에만 존재했고 발화 주체가 0건이었다. 즉 `record_discovery` 를 걸 지점 자체가 없었다. D10 §7이 3종의 조건을 확정해 두었으므로 **조건이 온전히 서는 CG-01만 결선**하고 나머지는 올린다: ①**CG-02(재회·카이) = 데이터 차단** — '대면' 도달 축이 `relation_axes.csv` 에 부재(현행 3축), T7 서사 유입 의존(IMPL-113 §5와 같은 축) ②**CG-03(동기·주드) = 규격 부재** — '첫 순위 역전 인접 듀얼'의 판정 규격이 정본에 없다(‘인접’의 기준·‘첫’의 래치 지점 미규정). 임의 해석 금지(불변규칙 9)로 미착수 — **총괄 판정 대상**. **연출은 여전히 미결선** — 도트 CG(CG-01~03) 미유입이라 컷인 재생은 T6 몫이고, 이번에 닫은 것은 기록·정보 축뿐이다(조우 자체는 성립했으므로 기록을 미루지 않는다). **검사 설계 — 공허한 검사를 실측으로 걸러냈다:** 조건식을 화면 상태에 의존하지 않는 `static` 순수 함수로 분리해 무대 5 도달 없이 검사되게 했다. 1차 대조군을 무벽 무대(메트로)로 두었더니 `wall.is_empty()` 가드가 먼저 걸려 **최종 무대 조건을 통째로 제거해도 통과**했다(돌연변이 미검출 실측) → 벽이 **있는** 비최종 무대(아주르 코스트) + 그 무대의 벽을 상대로 바꿔 재실측, 돌연변이 검출. IMPL-100 의 '두 절단이 갈라지는 지점' 규율과 같은 축이다. TC-C 하한 1949→1955 | D10 §7 결정 #6 · D08 §8.5·§8.11 · D09 별첨A §A-6 · 불변규칙 5·9 · 검증 프로토콜 §1 · IMPL-100·113 | 확정 (돌연변이 실측 — 공허 검사 교정 후) — 판정 요청 1건·차단 1건 |
| 2026-08-16 | IMPL-125 | **[선재 결함] 코드로 만든 Control 이 본문 폰트 9 가 아니라 엔진 기본 16 으로 서 있었다 — SYS-03 교정.** `gui/theme/default_font_size=9` 는 **씬에 놓인 노드에만** 먹고, `Label.new()`·`Button.new()` 로 런타임 생성한 Control 은 엔진 기본 테마의 **16** 으로 해석된다(실측: `get_theme_font_size("font_size")` = 씬 노드 9 · 코드 생성 16). **SYS-03 옵션 화면이 이 상태였다** — 탭 5개와 전 옵션 행(라벨·값·◀▶·O5 고지행)이 16px, 씬에 박아 둔 초기화·닫기 버튼만 9px. D10 §5.7 본문 계열 9 위반이고, 같은 화면 안에서 두 크기가 섞여 있었다 | **교정:** `param_font_size_body` 를 데이터 창구에서 읽어(`불변규칙 2` — 코드에 9를 적지 않는다) 코드 생성 컨트롤 전량에 `add_theme_font_size_override`. 전례 = `log_feed.gd`(이미 같은 방식). 대상 = SYS-03 전 코드 생성 컨트롤 + SYS-04 신규분. **적발 경위:** SYS-04 첫 캡처에서 행 텍스트가 씬 노드인 요약 라벨(9px)보다 눈에 띄게 커 보였고, 눈대중으로 단정하지 않고 `get_theme_font_size` 를 찍어 16을 확인한 뒤 SYS-03 을 같은 방법으로 대조해 선재 결함임을 확정했다. **범위 판단:** 인계 배정 밖이지만 (a)같은 계열·같은 층의 결함이고 (b)교정이 3행이며 (c)총괄이 방금 공통 바 폰트로 D10 을 개정한(IMPL-117) 직후라 폰트 계열 위반을 남겨 둘 이유가 없어 동반 처리했다. **미점검 잔여:** 코드로 컨트롤을 만드는 다른 화면(HUB 계열 카드 등)에 같은 축이 남아 있을 수 있다 — 전수 점검은 별건으로 둔다 | D10 §5.7 · D13 별첨A §10 · 불변규칙 2 · IMPL-082·117 · `log_feed.gd` | 확정 (폰트 크기 실측 — 교정 전 16 · 교정 후 9 양방향) |
| 2026-08-16 | IMPL-124 | **MS-3 T4(주력 몫) — SYS-04 업적 화면 결선.** D09 §5.5 · 별첨A §A-4 이행: **5탭**(커리어·라이벌·드라이빙·개러지·아카이브) · **39종** · 달성률 요약 헤더 · 달성/미달성 표기 · **히든 9종은 달성 전 "???" 슬롯**(명칭·조건 비노출, 자리만 존재 — 총수 가늠 허용) · **보상 열 부재**(무보상 명예형). 라우터 `SYS-04` 등재 + **진입 2경로 결선** — 타이틀 메뉴(§A-1 이탈 대상 = SYS-02·03·**04**)와 일시정지 오버레이(§A-5 확정 4항 '재개/옵션/**업적**/타이틀로' — 둘 다 IMPL-077 범위 제외로 비어 있던 자리다). SYS-03 전례대로 라우터·오버레이 양 모드 지원. 스트링 15키 신설(하드코딩 0) | **[구조] 발견형 4종은 라이벌 탭에 넣되 데이터는 건드리지 않았다** — 총괄 판정 ②(발견형 = 조건 유형이지 카테고리가 아니다)를 **표시 층 매핑**으로 이행했다. 데이터의 `category` 열은 `discovery` 로 두는데, TC-O 가 category 계수를 6종(…discovery 4)으로 못박고 V1 enum 도 6값이라 열을 바꾸면 데이터 검사와 화면 규격이 서로를 무효화한다. 탭 정의에 `categories: ["rival","discovery"]` 로 흡수 → 라이벌 18행 실측(14+4). **[신설] `OutgameState.achievement_progress()`** — '조건' 축의 조회 경로. `_achievement_met()` 이 **같은 계산의 반환값을 임계와 비교**하도록 `_achievement_current()` 로 원천을 합쳐 표시와 판정이 갈릴 수 없게 했다(IMPL-100 `field_repair_preview` 구조 승계). 미지 소스는 `-1` 로 돌려 임계 0 데이터에서도 '달성'으로 새지 않는다. G1 화이트리스트 등재. 검사 6건 + **39종 전수 패리티**, 돌연변이(진척을 0 고정) 3건 검출. TC-O 하한 368→374 | **[가안] 1건:** 조건 열은 **임계 2 이상인 축적형에만 진척(`n / N`)을 적는다** — 임계 1 도달형은 명칭 자체가 조건 문면이라("첫 포디움") "0 / 1"이 정보를 더하지 않는다. **[보고] §A-4 행 구성 4요소 중 3개가 소재 부재로 미결선 — 총괄 판단 대상:** ①**달성 일시** — §5.5·§A-4가 명시하나 `achievements` 는 `id → true` 뿐이고 **벽시계 개념이 세이브 스탬프 외에 없다.** 게다가 재로드 시 `evaluate_achievements()` 가 현 상태로 재판정해 **소급 달성이 성립**하므로(코어 주석 명문) 해제 시각을 찍어도 의미가 어긋난다 — 시각 축을 무엇으로 둘지가 설계 판단이다(§5.5 칭호 이력이 `(시즌 N)` 을 쓰는 전례 있음) ②**아이콘** — D10 §8.2 에 업적 아이콘 항목 자체가 없다(에셋 대장 미계상) ③**Steam 도전과제 동기 상태** — `IAchievement`·`DesktopAchievement` 는 있으나 `desktop_services.gd` 에만 있고 **세션에 배선되지 않았다**. 어댑터를 화면 층에 어떻게 넣을지는 D12 §2.2 구조 결정이라 임의로 잇지 않았다. 세 축 모두 **자리를 비워 둔 것이 아니라 소재가 없다** — 유입·판정 시 이 화면에 붙는다 | D09 §5.5 · D09 별첨A §A-1·§A-4·§A-5 · D07 §7.1 · D08 §8.11 · D12 §2.2 · 불변규칙 2·6·9 · 총괄 판정 IMPL-121 ①② · IMPL-077·100·113 | 확정 (돌연변이 3건 + 캡처 5컷 눈 대조 + 게이트 전건) — 보고 3건 |
| 2026-08-16 | IMPL-131 | **에셋 트랙 분리 + 사양서 v1.2·배치 대장 v1.1 발행** (`docs/handoff/에셋트랙_총괄발신.md`). ①**§6.1 귀속 표기 교정 집행** — IMPL-116이 "사양서 차기 개정 시"로 이월한 분. 32×32 는 D10 §5.1이 **심볼 6종에 한해** 확정한 값(원문 "릴 존 규격 내"·D13 별첨A §10 전사도 "심볼 셀 원도")이고 나머지 21종은 구현 준용·조정 창구 실기임을 명문화 — **값 무변경, 바뀐 것은 조정 권한 소재** ②**업적 아이콘 6종 신규 계상**(§6.7) — 총괄 P-9 판정 이행, 수량 권고(카테고리 5 + 히든 1) 채택. 파일명 `achv_*`·도상·프롬프트 전개. UI 아이콘 총계 21→**27종** ③배치 대장 현행화(업적 경로 등재·미결 2건 추가) | **트랙 분리 사유 = 동시 편집 충돌 실측 3건.** ⓐIMPL ID 중복(110·111·112 각 2건 — IMPL-119로 해소) ⓑ`strings.csv` 에 타 세션 SYS-04 키 14종 혼입 → HEAD 복원 후 내 키 7종만 재적용 커밋·타 세션분 트리 복원(IMPL-123) ⓒSYS-04 발신문 작성 중 타 세션이 선결선(IMPL-124). **이후 본 세션은 `docs/assets/`·`godot/assets/` 전속**이며 화면·코어·데이터·테스트 비접촉. **판정 요청 3건 발신:** A=팔레트 56색 실색상값 확정 경로(**에셋 트랙 최대 차단 요인** — D10 §2.3이 개수·역할만 확정하고 색상값이 정본 부재. 팔레트는 AI 산출물의 양자화 기준이라 없으면 전 이미지 에셋이 재작업 위험) · B=`audio/`·`platform/`·`store/` 3계의 D10 카테고리 개정 여부 · C=D11 §2.9 헤딩 18 vs 실제 19(D08 "35 vs 39"와 동형 — 총괄이 그 건을 "열거가 정본"으로 판정한 준거 적용 요청). **이관 1건:** TL-5 미모델링 3항(실플레이 세션 몫). 업적 아이콘은 D10 P-9 개정 전까지 [가안] | D10 §2.3·§5.1·§8.2 · D09 §5.5 · D11 §2.9·§2.11 · D13 별첨A §10 · IMPL-105·116·119·123·124 · 총괄 회신(P-9) | 확정 (문서 발행) — 판정 3건 총괄 대기 |
| 2026-08-16 | IMPL-123 | **MS-3 T5 — RUN-01 E03 소모품 보충·E04 덱 교체 인라인 카드 결선** (`ui/run/run_recap_screen.gd`·`.tscn`). IMPL-077이 잠금으로 세워 둔 두 카드를 실행 개방했다. E03 = 소모품 3종 구매 버튼(품목명+비용 병기) + 재고 `n/2` 헤더 · E04 = 덱 `used/slots` 헤더 + 해금 스킬 토글(●/○). 스트링 7종 신설·미사용 2종(`ui.recap.consumable`·`.deck`) 제거 | 규격 이행 요점: ①**전 기능 화면 내 인라인 완결** — 하위 화면·모달 0. §A-9 E03이 "COM-01 불요(저액·가역)"를 확정했고 E04는 "HUB-04 비전환"이라 전략실로 보내지 않는다. 3기능 상한(D07 §1.2) 무증설 ②**판정은 전부 코어** — 반입 상한 2·지불 능력은 `buy_consumable`, 중복·슬롯 초과는 `set_deck`(HUB-04와 동일 경로). 화면은 활성 표시만 맞추고 거부 시 상태를 바꾸지 않는다 ③구매 후 `_refresh_repair_button()` 동반 — 잔액이 줄면 정비 지불 능력이 함께 바뀐다 ④초기 포커스는 §A-9 E05대로 [다음 대회로] 유지 — **무조작 1버튼 통과**(1분 리듬) 불변. **실측(상태 덤프 9단):** 900Cr → P1 구매 650(−250, 1/2) → P2 구매 330(−320, 2/2·**전 버튼 비활성**) → 상한 초과 시도 무변경 / 스킬 2종 해금 ○○ → 장착 ●○(1/2) → ●●(2/2) → **3번째 장착 시도 시 덱 2/2 유지**(코어 거부) → 해제 1/2. 라우터 실경로 캡처로 세 카드 동시 표시 확인. **[보고] 덱 프리셋 전환 미결선** — §A-9 E04가 "프리셋 전환(G3 개방 시)"을 두고 D07 §2.2가 시설 G3를 "덱 구성 2세트 저장·즉시 전환"으로 확정했으나 **코어에 프리셋 상태·API가 없다**(`garage_facilities.csv`의 `deck_preset_slot` 효과에 소비부 0). 저장 상태 신설 = 직렬화·세이브 마이그레이션 동반이라 원격 레인 몫으로 등재한다. **[부기] 덱의 인게임 소비부는 여전히 부재** — RACE-01 스킬 슬롯이 전 슬롯 잠금 표기다(IMPL-071). 편집은 성립하나 주행 효과는 별도 트랙이며 본 건 범위 밖이다. **하네스 함정 재확인:** `_initialize()`에서 `add_child` 해도 `_ready()`가 아직 돌지 않아 `@onready`가 null이다 — 라우터 순서(`session` 주입 → `add_child` → `bind`)를 그대로 따라도 프레임 경과 전에는 `_on_bound`가 깨진다(IMPL-120에서 캡처 도구가 겪은 것과 같은 축) | D09 별첨A §A-9 E03·E04·E05 · D07 §1.2·§2.2·§7.2 · D06 §3.5 · 불변규칙 2 · IMPL-077·078·111 · MS3_T2_소모품_원격회신 §2-2 | 확정 (상태 덤프 9단 + 라우터 실경로 캡처 + 게이트 전건) — 프리셋 1건 원격 이월 |
| 2026-08-16 | IMPL-120 | **MS-3 T2 협업(주력 몫) — RACE-01 E13 소모품 사용 UI 결선** (`ui/race/race_screen.gd`·`.tscn`). 2슬롯을 표시 전용 `PanelContainer`에서 **`Button`으로 전환**해 사용·포커스 가능하게 하고(별첨A §A-4 패드 순회 `E08→E06→E13→E14`가 E13의 포커스 가능성을 전제한다), 재고→슬롯 매핑·사용·활성 판정을 결선했다. 신규 키 1종 `ui.race.consumableFilled` | 규격 이행 요점: ①**활성 = T1 섹터 개시 전속** — 개입 창(T4)이 아니라 그 **반대 국면**이다(별첨A E13 "T1에만 활성" · D06 §3.5 R-B). 화면 가드 = `turn_phase==T1 and gp_state==SECTOR_TURN and not current_turn_is_duel and not _timer_active`, 엔진이 같은 가드를 다시 걸므로 이중이다 ②**표시 순서를 화면이 고정한다** — 엔진이 딕셔너리를 돌려주므로 데이터 정의 순서로 펼쳐 같은 인벤토리가 항상 같은 슬롯에 앉게 했다 ③**도상 없이 채움/빔 2상태** — 소모품 아이콘은 D10 리소스 목록에 없다. 품목명은 툴팁(COM-02)으로 준다(슬롯 16×12 에 이름이 안 들어간다) ④**지속 효과(P2 실드·P3 경감) 표시 요소는 신설하지 않았다** — 원격 회신이 소재를 넘겼으나 D09 별첨A 요소 대장에 해당 행이 없다(IMPL-086 "신규 슬롯 불신설" 준용) ⑤슬롯 수 2 = `param_consumable_carry_cap` 2 와 1:1이라 넘침이 구조적으로 없다. **실측 3단(헤드리스 상태 덤프):** T1·재고2 → `▣`ON/`▣`ON·툴팁 품목명 / 사용 → 재고 감산·남은 항목이 슬롯1로 당겨짐·**섀시 70→85**(P1 회복 15 = D13 §3.6 정합)·로그 "리페어 키트 사용." / **스핀 직후(T2) → 재고가 남아도 전 슬롯 비활성** = "T1에만 활성" 이행. **캡처 도구 결함 1건 교정:** `capture_race_flow`의 재고 주입이 `_initialize()`에 있어 **조용히 무시**됐다 — 그 시점엔 화면 `_ready()`가 아직 돌지 않아 `engine`·`data`가 null이다(실측). 첫 프레임 이후 1회 주입으로 옮기고 E13 컷 1장 추가(9컷) | D09 별첨A §A-4 E13 · D06 §3.5 R-B · D07 §3.1 · D13 §3.6 · D10 §5.2 · 불변규칙 2·5 · IMPL-086·111 · MS3_T2_소모품_원격회신 §2 | 확정 (상태 덤프 3단 + 캡처 9컷 + 게이트 전건) |
| 2026-08-16 | IMPL-136 | **[인계] 팔레트 편입 — 에셋 트랙 (IMPL-135 후속)** (`docs/handoff/팔레트_에셋트랙_편입인계.md`). 배치 대장 §4.9가 이미 **4파일=2식**(`master_56.gpl/.png` · `colorblind_alt.gpl/.png`)으로 확정해 둔 구조를 그대로 따르되, 총괄이 **`.gpl` 전사용 본문을 기계 생성해 첨부**했다 — hex→RGB 변환을 손으로 하면 56행에서 오타가 난다(생성 후 **56색·중복 0** 검산). **구조 판정 1건:** 기능 컬러 부속 13색은 `.gpl`에 넣지 않는다 — D10 §2.3 면제 조항은 '마스터에 우선한다'고만 했지 팔레트 에셋에 담으라 하지 않았고, 소비부 대부분이 런타임 UI 틴트라 원도 양자화 대상이 아니다(값의 정본 = `ui_palette.gd`). **단 심볼 6색은 아이콘 원도에 실제로 칠해지므로 스와치 PNG에 별도 구획 병기** — `.gpl`은 56색만 담아 파일명 계약(`master_56`) 유지 | **대장 §9-⑪ 종결 판정** — "색상값이 정본 어디에도 없다"던 공백이 **정본 개정 없이** 해소됐다(D13 별첨A의 위임을 확정이 채움). **총괄 발견 — 대장 규칙 ⑦ 인용 오류:** *"`.gpl`(팔레트) — D12 §10.1 확정"* 이라 적혀 있으나 **D12 §10.1 원문은 오디오 전속**(SFX WAV/PCM·BGM OGG)이고 팔레트 무언급이다. 대장 자신이 §9-②에서 팔레트 형식을 [가안]으로 등재해 **같은 문서 안에서 '확정' 대 '[가안]'이 어긋나** 있었다. **값(.gpl)은 적절하므로 근거만 교정** — D12 확정이 아니라 대장 구현 재량(§9-②가 정확). `.wav`·`.ogg`의 D12 인용은 정확하므로 유지. 에셋 소유 문서이므로 **에셋 트랙 자체 집행**. **유형 반복 기록:** 금주 3건째의 동형 결함이다(D08 §8.11 35 vs 39 · D11 §2.9 18 vs 19 · 본 건) — 전부 **표·열거는 맞고 요약·인용만 어긋난** 형태 | 배치 대장 §2 ⑦·§4.9·§9-②⑪ · D12 §10.1 · D10 §2.3·§2.4 · IMPL-003·132·135 | 확정 (원문 대조 + 생성 검산) |
| 2026-08-16 | IMPL-135 | **[총괄 확정] 마스터 팔레트 56색 v1.0 — 사용자 승인. 에셋 트랙 최대 병목 해소** (발행 시점 경로 `docs/handoff/팔레트56색_총괄확정_v1_0.md` → **현 위치 `docs/assets/팔레트_정본_v1_0.md`** ⟨2026-08-16 이관 · IMPL-137⟩ · 시각 검토판 = 아티팩트 「보레아스 팔레트 스펙」). **정본 개정 0** — D10 §2.3 확정 구성(16+12+16+12)에 값만 채웠고 색값은 D13 별첨A가 팔레트 에셋으로 명시 위임한 항목(IMPL-132 판정 A 승계). 구성: 코어 뉴트럴 16(야간 가중 계조·한색 편향 감쇠) + 텔레메트리 네온 12(3계열×4단) + 팀 컬러 16 + 환경·피부·머티리얼 12 = **56**, 별도로 **기능 컬러 부속 13**(면제 조항 — 적·라임은 네온 3계열에 자리가 없어 56 밖이 유일 해법) + 색각 대체 교체 4색 | **v0.1→v0.2 교정 3건이 이번 처리의 실질 소득 — 전부 측정이 뒤집은 것이다.** ①**사용자 요청("스틸 반단계 하향")이 목적 미달임을 실측으로 반증:** 반단계(ΔL* −2.7)면 악시온 팀블루(L*41.0)와 거리가 2.1로 **여전히 JND 미만**이고 그 값을 **가로질러** 반대편에 붙는다 — 교차만 하고 거리를 못 번다. **1단(#4A545D·L*35.2·ΔL* 5.8)** 이라야 명도 축이 갈린다 ②**NW1 암부는 하향 금지** — v0.1에서 이미 아스팔트와 ΔL* **0.5**로 실루엣 하단이 소실되는 상태였다. **같은 결함이 악시온에도(AX1 ΔL* 2.8)** 있어 노스윈드 고유 문제가 아니라 **아스팔트 기본값이 팀 암부 대역과 겹친 구조 문제**였고, 램프 재분포로 양 팀 자동 해소(5.6/7.9) ③**램프 자기 적발** — 내가 승인 기준 ①로 내건 '계조 균질성'이 실측에서 불통과였다(단 간격 1.4~16.1: 최하단 2단 JND 미만·최상단 3배 턱) → 3.0~7.7 재분포. **단 전 구간 등간격은 오답**임을 확인하고 배제했다 — 야간 게임은 픽셀 대부분이 L*<35에 살아 등간격은 그 대역을 6단으로 줄인다(야간 가중 8단 유지·한색 편향 감쇠 곡선 보존). **적용 시점 공백 4건도 이번에 확정:** 부속 13색이 `ui_palette.gd` 소비부를 전부 덮지 않았다(배틀 게이지 전방/후방/트랙·재화 2종) — 넷 다 접근성 요건이 아니라 **식별 보조색**이므로 면제 대상이 아니고 **마스터에서 조달**(게이지 방향은 심볼과 겹치지 않게 베이스 2단, 심볼은 브라이트 3단). **후속 3트랙 배정:** 에셋(실물 2식 산출·docs/assets 편입·사양서 §1.4 해소) / 구현(`ui_palette.gd` [가안] **21건 종결** = IMPL-071·075 종결) / 실기 재검 대기(G-1·G-2는 MS-5 · EV4 사암 양피지 근접) | D10 §2.3·§2.4·§5.2·§9 · D13 별첨A §10·§8.2 · D03 §2.2 · D09 §3.2·§3.4 · IMPL-071·075·117·132 | 확정 (사용자 승인 — L* 측정 근거) |
| 2026-08-16 | IMPL-132 | **[총괄 판정] 에셋 트랙 판정 3건 + 트랙 분리 승인 — 정본 개정 0** (`docs/handoff/에셋트랙_총괄회신.md` · 마스터플랜 v2.36). **A 팔레트 56색 = ⓐ(에셋 제안 + 사용자 승인)·D10/D13 개정 0** — 핵심 = **정본이 애초에 색값을 소유하지 않는다**: D13 별첨A §10은 `D10 §2.4 \| 56색(16+12+16+12)`로 **개수·구성만** 전사했고 §8.2 베인 행이 **"상태색 = 팔레트 에셋 A-팔레트 소관"으로 명시 위임**한다. D10 §8.2는 팔레트 2식을 리소스 항목으로 둔다 = 색값은 산출물의 내용물. D13에 넣으면 대장이 위임분을 회수하는 모양, D10에 넣으면 목록이 자기 항목을 정의하는 모양 — 둘 다 구조 오류(2026-08-15 기판정 승계). **부대조건 5건**(팀 컬러 D03 §2.2+노스윈드 IMPL-117 / 기능 컬러 면제 우선 / 색각 대체 도상 불변 / **베인 4상태 상태색 포함 의무** / **고대비 여백** — G-1·G-2가 MS-5 재배치라 팔레트가 게이트보다 선행) **B 3계 = 구현 재량·D10 개정 불요** — `audio/`는 D10 §0 범위 가드상 D11 소관이라 **부재가 정상**(D11 경로 무규정 실측)·`platform/`은 사용자 계상 승인분(IMPL-116)·`store/`는 D15 §2.2. **D10 §8.2는 "카테고리별 총계" 수량 집계표이지 디렉토리 규격이 아니다**(정본 = D12 §2.1 + CLAUDE.md) **C D11 §2.9 = 19 확정** — D08 §8.11 선례 그대로. 실측 4지점: 표 열거 19(마지막 칸 `SE-U18 / U19` **병합 셀**이라 9행 표가 18로 오산됨) · §2.11 U=19 · 소계 5+14+3+6+1+7+19+5+3=63 · **총계 68은 19로만 성립**(18이면 67 = 확정 기준값 파손). 헤딩만 오기 → **P-2 ㉘** | **트랙 분리 승인** — 3레인 병렬 중 작업 트리 공유로 실사고 5건(IMPL ID 중복 4·`strings.csv` 동시 편집 1). **규약 부재의 산물**로 판단해 **CLAUDE.md에 다중 세션 규약 4항 신설**(소유 경로 선언 / IMPL ID = pull 직후 최대값+1 / 공유 파일은 자기 몫만·남의 미커밋분은 HEAD 복원 후 되돌림 / 착수 전 log·status 대조) + **디렉토리 계약에 3계 부기**(오해의 발원지가 괄호 열거였다 — "사운드 실물"이라 쓰면서 사운드 자리 부재). **§3 이관 처리에서 분리 1건:** TL-5 미모델링 3항을 한 덩어리로 넘겼으나 **실행 조건이 다르다** — ①완급 비트 20GP·③모멘텀×인터럽트는 즉시 가능, ②입력 지연만 MP-L 대기 → 묶어 둘 이유 없음(①③만으로 D14 §8.3 2/3 종결). **§1 통지 2건 집행 확인**(사양서 §6.1 귀속 교정 = 값 무변경·§6.7 업적 6종 = P-9 권고 채택·"[가안]" 층 구분 준수) | D10 §0·§2.3·§2.4·§8.2 · D13 별첨A §10·§8.2 · D11 §2.9·§2.11 · D03 §2.2 · D15 §2.2 · D12 §2.1 · D14 §8.3 · IMPL-116·117·121·123·131 | 확정 (원문 대조 3건 + 산술 검산) |
| 2026-08-16 | IMPL-128 | **[총괄 판정] MS-3 주력 1차 판정 요청 2건 — 정본 개정 0 (둘 다 정본에 답이 있었다)** (`docs/handoff/MS3_주력레인_1차_총괄회신.md` · 마스터플랜 v2.35). **A-1 달성 일시 = 인게임 시각 축(시즌 N)** — **D09 §5.5 같은 표 안에 확정 전례**: '칭호 이력 문면 규격(P-1 ③·확정)' = `[칭호명] — 달성 (시즌 N)`. 같은 절 두 행이 다른 시각 축을 쓸 이유가 없다. 보강 = 벽시계는 소급 재판정 구조와 원리적 충돌·시즌은 단조 증가. 부대 지시 = 최초 성립 시즌 래치(재기록 금지)·구세이브 소급분은 표기 생략(`—`, IMPL-090 관용 전례) **A-2 업적 아이콘 = 진성 미계상 → P-9 신설**(D09 §5.5·§A-4가 아이콘 존재를 전제하는데 D10 §8.2 21종에 부재 = 문서 간 전제 불일치). 비차단·플레이스홀더 운용. 수량 권고 = 카테고리 5 + 히든 1 = 6종 재사용 **A-3 Steam 동기 = 어댑터 배선(원격 구조 몫)** — 화면이 어댑터 직접 참조 시 혼입 0 저촉. 현 단계는 널 구현 배선 + '미연동' 표기 **B-1 '인접' = 판정 불요** — D08 §8.7 확정 명문 `\|플레이어 챔피언십 순위 − 주드 순위\| ≤ 2`(그 GP 주행 순위 아님·±2는 결정 #14 승인). `jude_rank_delta`가 정확히 그 축 **B-2 래치 = 역전 성립 후 최초의 인접 듀얼** — ⓐD10 §7 병렬 문형(CG-02 = "'대면' 도달 듀얼") ⓑ**다른 해석은 실행 불가능**: 역전은 GP 결과로 확정되는데 듀얼은 GP 진행 중이라 그 시점 미확정 | **데이터 축 교정 지시 — 교정이 예상보다 훨씬 작다:** 판정 ②(IMPL-121)로 제거한 축 혼용이 데이터에 남아 있었으나, 실측 결과 **`condition_type` 열이 이미 D07 §7.1의 3형식을 정확히 담고 있고**(reach 31·cumulative 4·discovery 4) `category`만 6번째 값을 문 상태였다 → **4개 셀만 교정**(`category` discovery→rival) + V1 enum 5종 + TC-O 계수 6→5. 화면 탭이 `["rival","discovery"]` 흡수 매핑에서 `["rival"]` 단일로 **단순화**된다 — 교정이 구현을 복잡하게 만들지 않는다. **§4 HUB-08 = 발신 제안 승인**(헤더를 추첨 결과 크기에서 산출·SET-02 동반). **구조 원칙 승격 제안** — IMPL-100·124에 이어 세 번째 동일 해법: "표시값은 판정값에서 파생시키고 같은 것을 두 번 계산하지 않는다". **§5 폰트 결함 = 승인 + 전수 점검 기계화 지시**(코드 생성 Control의 폰트 오버라이드 누락 정적 검사 = V-계열 확장·원격 트랙). **총괄 자기 결함 4호:** 인계문 §1-③ 'HUB-05 스킬 티어' 오기 — D09 §8.1 대장은 HUB-04 전략실(§A-14)/HUB-05 기록실(§A-15). **원격 회신문 표기를 대조 없이 승계**한 것이 원인(인계문은 다음 레인의 유일한 출발점이므로 좌표는 전건 대조 대상) → 교정 완료. **게이트 총괄 재현:** scripts=80 · TC-C 1955 · TC-P 140 · EVENTS 7039 · SEASON 488 · TC-O 374 · NARRATIVE 68 · DATA 55 · SEAL 98 · suites=10 — 발신 보고와 전건 일치 | D09 §5.5·§8.1·별첨A §A-4 · D10 §7·§8.2 · D08 §8.7·결정 #14 · D07 §7.1 · D12 §2.2 · 불변규칙 1·2·5·9 · IMPL-113·114·121·124~127 | 확정 (원문 대조 + 게이트 재현) |
| 2026-08-16 | IMPL-122 | **D14 v1.1 발행 — TL-5 판정 규격 개정 (사용자 승인) + P-2 3건 동시 집행 + MS-3 주력 착수 인계.** ①**§8.2 척도 구분 신설(결정 #6-ⓐ)** — ±15%는 '규모 충분 지표' 전속, **소척도(ⓐ1단위가 수십 %인 서수·정수 / ⓑ기대값 1 미만 희소 사건)는 허용 범위 판정 전속**. 근거 = 해당 지표군에 D08 §8.3·D13 §4.3이 허용 범위를 이미 발주했고 ±15%를 겹치면 '완전 일치' 요구가 되어 자기모순 ②**§8.2 대조 기준선 이관(결정 #6-ⓑ)** — 1차 = D13 §4.3 모델값 / **2차 이후 = TL-5 1차 실측값**. 근거 = 시뮬레이터 실물 부재로 모델 기준선은 **재산출 불가**(§4.2 범위 가드)인 반면 TL-5 러너는 리포 내·시드 고정·재실행 문서화로 재현 가능. ±15%의 의미가 "모델과 같은 크기인가" → "**직전 대비 회귀했는가**"로 전환. **기준선 갱신 = 총괄 경유**(갱신이 곧 판정 완화이므로 임의 갱신 금지) ③§8.5 TL-5 1차 실측 기입 + 소척도 표기 + **임계 근접 2건 명기**(E1 −14.9%·0.1%p / 격파율 −14.6%) ④**P-2 3건 동시 집행** — ⑬(§8.5 허용 열 2행)·㉖(본 ①)·㉓ D14분(참조 행 D10 v1.1). 잔여 P-2 22→**19건** ⑤마스터플랜 v2.34·§1 D14 행 동기 | **개정의 발단이 실행이었다** — TL-5 1차(IMPL-115)가 규격의 두 결함을 주행으로 드러냈고, 총괄 판정(IMPL-121)이 원문 대조로 확인한 뒤, 사용자가 구조 권고를 채택해 정본을 열었다. 문서→구현이 아니라 **구현이 문서의 결함을 되먹인** 사례로 기록한다. **주력 착수 인계 동시 발행** (`docs/handoff/MS3_주력레인_착수인계.md`) — 6건 전건 판정 해소·SYS-04 확정 규격(5탭·39종·라이벌 18·발견형 4종 라이벌탭 H)·D14 v1.1 변경점·임계 경고·중복 착수 주의(IMPL-120 소모품 UI 활성)·IMPL ID 충돌 주의(금주 4건). 레벨·시나리오·게이트·표본·시드 무변경 → 🔁 미경유·DoD 재심사 불요 | D14 §8.2·§8.5 결정 #6 · D13 §4.2·§4.3 · D08 §8.3 · 마스터플랜 §0.8.7·P-2 · IMPL-115·121 | 확정 (사용자 승인 — 권고안 채택) |
| 2026-08-16 | IMPL-121 | **[총괄 판정] MS-3 원격 레인 판정 대기 8건 — 7건 확정·1건 접수·정본 개정 0** (`docs/handoff/MS3_원격레인_총괄회신.md` · 마스터플랜 v2.33). ①**업적 39종 확정** — D08 §8.11 헤딩 '총 35종'은 5 카테고리 괄호 합이고 발견형 4종 미계상. 열거가 정본 ②**발견형은 카테고리가 아니다(원격 판단 정정)** — D07 §7.1이 '카테고리 5종'과 '조건 유형 3형식(도달형·누적형·발견형)'을 명시 분리하고, **D09 별첨A가 SYS-04를 '카테고리 5탭'으로 확정**해 6번째 탭을 구조적으로 차단한다 → 발견형 4종 전부 **②라이벌 귀속**(전건 네임드 라이벌 대상 사건). 히든(H)은 조건 유형 속성이라 유지 ③**알타 리지 4번 = 별첨A 준거**('산악 구조대 격려') — §4.2가 자기 절 제목에서 "상세는 별첨A"라 선언하고, 별첨A의 무대별 **C1~C5 비중복 배분**을 §4.2 판이 깨뜨린다(C4 중복·C5 공백) ④**오버홀 추첨 = reserve 확정(D12 무개정)** — reserve 정의가 "확장 대비"이고 shuffle·resonance는 명시된 단일 소비 지점 귀속. 결정적 근거 = **소비 순서 결합 회피**(공유 시 캘린더 셔플 소비량이 오버홀 결과를 민다) ⑤**±15% = 규모 충분 지표 전속 / 소척도는 허용 범위 판정 전속** — 소척도 = ⓐ1단위가 수십 %인 서수·정수(순위·투어 슬롯) ⓑ**기대값 1 미만 희소 사건**(투어 리타이어). 근거 = D08/D13이 그 지표군에 허용 범위를 이미 발주했고, ±15%를 겹치면 '완전 일치' 요구가 되어 자기모순 ⑥**모델 벽 가중 = 재산출 불요·불가** — 값은 별첨A §6.2에 v1.0부터 존재했으나 §4.2 모델 범위에 라이벌 배치·무대 보정 부재·'미모델링'에 서킷별 섹터 배열 명시 → 무대 특성 추상화 확인. **단 시뮬레이터 실물이 리포에 없어**(§4.2 범위 가드) 재산출 대상 자체가 없다 → '계통 차이'로 기록 ⑦**0~2% = 표준 프로파일 기준** — §4.3 표 제목·서열 정합 문단·**결정 #8 근거 ②("D08은 S1 달성률을 발주한 바 없다")**. 숙련 2.4%는 이탈 아님 ⑧관계 축 2종 = 접수(비차단) | **⑤+⑦의 귀결 = D14 §8.2 종합 통과** → **MS-3 'TL-5 1차 통과' 조건 성립**(허용 범위 이탈 0 · ±15% 대상 전건 통과). **원격 분류 보정 1건:** 실행기록이 "연속 지표 전량 통과"라 했으나 **투어 리타이어(−39%)가 목록에서 누락**돼 정수 소척도·연속 통과 어느 쪽에도 안 들어가는 분류 공백이었다 — 소척도 정의 ⓑ가 덮는다. **⚠ 총괄 경고 2건:** E1 −14.9%(임계까지 **0.1%p**)·로렌츠 격파율 −14.6%(0.4%p) — 통과했으나 임계 근접이고, **E1은 루트 앵커 A3**이다. 두 건의 공통 설명이 ⑥ 계통 차이일 개연이 높다(실기 결함이 아니라 기준선이 낡은 것). **구조 권고(미집행·사용자 판단):** 모델 기준선은 재산출 불가인데 TL-5 러너는 시드 고정·재실행 문서화로 **재현 가능**하므로, 2차부터 기준선을 'D13 §4.3' → '**TL-5 1차 실측**'으로 이관하고 ±15%를 회귀 감시로 재정의하는 편이 옳다(D14 §8.2 개정 사안). **P-2 ㉔㉕㉖㉗ 신설.** §3 주력 인계 6건은 전건 판정 해소 — 착수 차단 0 | D07 §7.1 · D08 §4.2·§8.11·별첨A §3.0·§6.2 · D09 별첨A(SYS-04 5탭) · D12 §6.1 · D13 §4.2·§4.3 · D14 §8.2 · 불변규칙 4·9 · IMPL-110·113·115 | 확정 (원문 대조 8건 전수) |
| 2026-08-16 | IMPL-119 | **[형상] IMPL ID 충돌 3건 해소 — 주력/총괄 레인 등재분을 116·117·118로 재번호.** 두 레인이 같은 날 동시 진행하며 **110·111·112를 각각 2건씩 부여**했다(원격 = T1·T2·T3 / 주력·총괄 = 폰트 판정·공통 바 결선·uid 보고). ID가 결정을 유일 식별하지 못하면 상호 참조가 다른 행을 가리키므로 로그의 기능 자체가 깨진다 | **원격 쪽을 보존하고 주력 쪽을 옮긴 이유 = 참조 파손 최소화.** 전수 대조 결과 외부 문서의 `IMPL-110~112` 참조는 **전건 원격 의미**였다(원격 완료회신 §1·§4 · T1·T2·T3 개별 회신 3종 · TL5 실행기록 · 에셋 배치 대장 2개소). 주력 3건을 참조하는 외부 문서는 **0건**이라 재번호 비용이 없다. 행 내부 상호 참조 2개소(IMPL-117의 "IMPL-110 후속"·근거란 "IMPL-107·110")도 동시 교정. **재발 방지 — 부여 시점이 아니라 커밋 직전에 재확인한다:** 이번 충돌은 부여 전 pull을 했더라도 그 뒤 반대 레인이 먼저 커밋하면 그대로 발생한다(실제 경로). 기계 가드(pre-commit 중복 ID 검사)는 IMPL-076 전례가 있으나 본 건에서는 신설하지 않고 규칙으로 둔다 — 재발 시 가드 신설을 총괄 안건으로 올린다 | CLAUDE.md 작업 방식(impl_log 등재) · IMPL-076 전례 · IMPL-110~118 | 확정 (전수 참조 대조) |
| 2026-08-16 | IMPL-115 | **TL-5 1차 실행·판정 (`docs/qa/TL5_실행기록_2026-08-16.md`) — D14 §8.2 규격 실행, 결과 = MODEL_DEVIATION → 값 조정 없이 총괄 보고.** 표본 표준 500·저숙련 250·숙련 250 · 시드 42 · **3시즌 주행**. **허용 범위(D08 §8.3) 이탈 1건**(숙련 S1 챔피언율 2.4% vs 허용 0~2% — 표본 250에서 6런) · **±15% 괴리 13건**(항목 5종). **통과 항목:** E1 699(−14.9%) · GP 길이 10.4분(+1.0%) · P90 11.5 · 듀얼 2.23/GP · AI 리타이어 0.74·상한 2 · 첫 투어 우승 8투어 · 로렌츠 S1 격파율 31.6% · **프로파일 서열 정합 실기 확인**(격파율 14.8/31.6/33.2 단조) | **원인 분석 3건 (총괄 판정 요청 A~C):** ①**±15% 규격과 정수 소척도의 부정합** — 순위·투어는 정수이고 값이 작아(4~8) 1단위 차이가 25~60%가 된다. **연속 지표는 전량 통과**했고 괴리 4종 모두 **허용 범위 내**다 → 정수 지표군을 허용 범위 판정 전속으로 볼지 판정 요청 ②**첫 GP 우승 지연(실기 투어 8 vs 모델 5)** — 가설: 시뮬 측정(2026-08-06)이 **벽 라이벌 가중 결선(IMPL-110, 08-16) 이전** 산출일 개연. 사실이면 대조 기준선 자체의 재산출이 선행돼야 한다 → 확인 요청 ③숙련 챔피언율 2.4% — 허용 0~2%가 표준 프로파일 기준인지 전 프로파일 공통인지 판정 요청(전자면 이탈 0). **미모델링 4항(D14 §8.3):** ④서킷 섹터 배열은 TC-C 20종 전수 기계 대조로 충족, ①②③은 주력 실기 몫으로 이월 | D14 §8.2·§8.3·§8.4 · D13 §4.3·§6.2 · D08 §8.3 · IMPL-110·114 | 확정 (실행 완료) — 판정 요청 3건 총괄 대기 |
| 2026-08-16 | IMPL-114 | **MS-3 T9 — TL-5 자동 러너 구축·1차 실행 + 러너가 적발한 선재 결함 4건 결선.** ①**러너 신설** `godot/tests/tl5_runner.gd` (D14 §8.2 규격) — 정책 3종(D13 §4.2 개입 판단률·모멘텀 성공률)·구매 그리디·시드 42+인덱스·**실코드 경로 주행**(RaceEngine·SeasonState·OutgameState·RunSession 실주행 — 시뮬 코드 재사용 금지 명문 이행). 정책 난수는 게임 RNG 스트림과 분리(러너 조작이 reel/ai 스트림을 소비하면 시드 재현성이 오염). 다시즌(기본 3시즌) 주행 — D13 §4.3 지표에 S2~S3 항이 있어 시즌 1만 돌면 미도달 제외 편향이 생긴다 ②판정 리포트 `tools/validators/tl5_report.py` — D13 §4.3 목표·시뮬 측정 전사 + 허용 범위·±15% 괴리 판정 | **[진성 결함 4건 — 전부 러너가 적발]** ①**시작 그리드 산정식 미구현 (최중대)** — 엔진이 매 GP 디버그 고정값(P16)을 썼다. D13 별첨A §6.3은 개막전만 P16 고정이고 투어 첫 GP = 챔피언십 순위, 투어 내 2~4전 = 직전 GP 결과 기반이다. **순위 상승 경로가 구조적으로 없어** 챔피언십 중앙값이 항상 최하위였고 §4.3 도달률 발주가 성립 불가였다 → `SeasonState.player_start_rank()` 신설·주입. 실측 P16 → P5 · 첫 포디움 도달률 0% → 100% ②**S5 마일스톤 보상 미결선** — D06 Source 대장의 **축적형 주 수입**(D13 §3.3 합계 620 DP)이 지급 경로 0건. E1 실측이 모델의 54%(444/821)로 나온 직접 원인 → `milestones.csv` `reward_dp` 열 전사 + 달성 즉시 지급. 실측 E1 444 → 699 ③**S6 시즌 결산 보상 미결선** — `settlement_rewards.csv` season 행 6개(800~4000 Cr·30~120 DP)를 부르는 코드가 전무했다 → `settle_season()` 신설 ④**이벤트 변형 컨텍스트 2축 누락** — `vane_stage`·`jude_rank_delta`가 세션 판정 문맥에 없어 DSL 오류로 해당 변형이 영구 미발동(IMPL-095의 '미지 필드 = 오류' 처리가 드러냄) → 결선(주드 인접은 챔피언십 순위 차, 미등재 시 99로 비인접 처리). **부수 구조 정리:** 정산 지급이 화면 층(gp_result·tour_report)에 있어 러너·테스트가 같은 경로를 탈 수 없었다 → `settle_gp()`·`settle_tour()`·`settle_season()`로 세션 상향, 화면은 반환값 표시 전용 | D14 §8.2·§8.3 · D13 §4.2·§4.3·§3.3·§6.3 · D06 §2.1 Source 대장 · D08 §7.3 · D12 §6.1 · IMPL-095·113 | 확정 (러너 실측) — TL-5 판정 결과는 IMPL-115 |
| 2026-08-16 | IMPL-113 | **MS-3 T4(원격 몫) — 업적 데이터 39종 + 마일스톤·통산 지표 결선.** ①**`achievements.csv` 39행 전사** (D08 §8.11 열거 전량) — 카테고리 6(커리어 10·라이벌 14·드라이빙 6·개러지 4·아카이브 1·발견형 4) · 조건 3형식(reach/cumulative/discovery) · **비노출(H) 9종**(관계 최종 5 + 발견형 4 — D07 §7.1 규칙) · 판정 소스 5계(milestone/career_stat/relation/discovery/state). **무보상 명예형 이행 = 보상 열 자체를 두지 않음**(D06 Source 전수 무개정) ②**`milestones.csv` 15행** (D08 §8.2 마스터 표) + **마일스톤 기록 경로 신설 — 선재 결함 해소**: `milestones` 딕셔너리에 소비부(`skill_tier_open`)만 있고 **기록 주체가 0건이라 스킬 티어 2·3이 영구 잠김 상태였다**(HUB 화면이 티어 개방을 못 여는 구조). GP·투어·시즌 결산에서 데이터 조건으로 판정 ③**통산 지표**(D07 §6.2 확정 6종 + 업적 소재) — 완주·우승·포디움·듀얼 전적·최고 챔피언십 순위·시즌 수 + clean_gps·no_intervention_wins·final_lap_comebacks·circuits_won(집합)·named_beats ④**엔진 GP 요약 계수 5종 신설** — duel_wins·trouble_turns·hold_uses·chance_three_matches·final_lap_entry_rank + result·직렬화 편입. 엔진은 세기만 하고 업적 규칙은 아웃게임 층 전속(혼입 0) ⑤시즌 층 전승 계수(tour_gp_wins·season_tour_wins) + `is_scripted_loss_gp()` 신설(발견형 '왕좌의 코앞' 판정 소재) ⑥세션이 GP·투어·시즌 경계에서 기록·판정 호출 + `newly_achieved` 노출(표시 층 고지용) | **[보고] 정본 내부 불일치 1건 — 총괄 판정 대기:** D08 §8.11 헤딩은 **"총 35종"**이나 표 열거 실측은 **39종**이다(5카테고리 10+14+6+4+1=35 **+ 발견형 행 4종**). 발견형 4종이 5카테고리 어느 행에도 포함될 자리가 없어(각 카테고리 괄호 수치와 행 내용이 정확히 일치) 계수와 열거가 갈린다. **열거를 정본 실체로 보아 39종 전량 등재**했다 — 열거 항목을 빼는 것은 정본 내용의 삭제이기 때문(IMPL-101 DF-04·IMPL-110 전례: 기능 정본 우선 + 보고 병행). **[가안] 4건:** ①발견형 = 6번째 카테고리 취급(D07 §7.1은 조건 유형으로 규정하나 D08 표가 카테고리 열에 별도 행으로 열거) ②라이벌 파일 '전 항목 개방' = 네임드 8인 첫 선착 전량(개방 조건 정본 미규정 — 파일의 실질 내용이 대전 기록이라는 근거) ③네임드 선착 = **완주자 한정**(리타이어 상대를 앞선 것은 선착이 아님) ④리타이어 GP는 순위 마일스톤 비대상(D05 §9.2 포인트 0과 동일 취지). **[보고] 판정 소스 미결선 2종 — T7 의존:** 관계 축 5종(D07 §5.5 확정) 중 **재회(카이)·계승 2번째 축(셀린)이 `relation_axes.csv`에 부재**(현행 3축)라 해당 업적 2종이 판정 불가. 조용한 영구 미달성을 막기 위해 `pending_achievements()`로 계수하고 검사로 못박았다 — 재회 축은 형식 A(VN 체인)라 D04 서사 유입(T7) 의존. **돌연변이 8종 전건 검출:** 순위 임계 완화·리타이어 완주 인정·서킷 제패 중복 계수·업적 1회 발화 가드 제거·직렬화 누락·라이벌 파일 축 중복 계수·히든 플래그 변조·엔진 홀드 계수 미기록. 하한 상향: TC-C 1949 · TC-O 353 | D07 §6.2·§7.1 · D08 §8.2·§8.11 · D12 §5.6 · D13 §4.1 · D10 CG-01~03 · 불변규칙 2·9 · 검증 프로토콜 §1·§4 | 확정 (돌연변이 8종 실측) — 보고 2건 총괄 대기 |
| 2026-08-16 | IMPL-112 | **MS-3 T3 — HUB-08 시즌 오버홀 후보 추첨 결선 (시즌 결산 층 · reserve 스트림).** ①`OutgameState.draw_overhaul_candidates(rank, rng)` 신설 — 등급별 후보 수(D13 §7.1: 1~3위 5 / 4~8위 4 / 9위↓ 2) · 풀 = 전 12종 − 기장착(D06 §5.3 "선택하지 않은 후보는 다음 시즌 재등장"의 여집합 구현) · 풀 부족 시 잔여분만 · **결과를 상태로 보존해 재로드 리롤 무효**(스트림 저장만으로는 재추첨을 못 막는다 — D12 §6.2 취지 승계). 세션 `close_season()`이 결산 직후 1회 호출 ②`install_overhaul` 강화 2건 — **후보 밖 장착 거부**(추첨 후부터 — 미추첨 상태는 구세이브·개발 진입 관용) + **[결함 교정] 슬롯 한도 축을 누적 장착 수 → 결산 단위 카운터로** (선재 골격은 `overhauls.size() >= slots`라 시즌 2+에서 슬롯이 영구 소진 — 12종 풀을 여러 시즌에 소화하는 구조가 성립 불능이었다. HUB-08 미결선이라 잠복) ③아웃게임 직렬화 2필드 편입(구세이브 = 빈 후보·가드 비활성 관용) ④HUB-08 화면 로직(.gd) — 후보 일람 = 추첨 결과(미추첨 폴백 = 전 오버홀), **상위권 슬롯 2 소화**: 장착 후 잔여 슬롯·선택 가능 후보가 남으면 화면 잔류·목록 재구성. G1 공개 메서드 화이트리스트 등재 | **[가안] 2건:** ①추첨 스트림 = **reserve** — D12 §6.1 확정 6스트림에 오버홀 배정이 없어 예비 스트림("확장 대비")을 첫 소비. 릴·셔플·레조넌스와 채널 분리 유지가 근거. 총괄 확인 대상 ②잔여 슬롯은 후보 잔존 시 선택 필수 — 포기 동선은 씬 개편 사안(주력 몫·비차단). 추첨 풀 정렬 후 무작위 발췌 = 데이터 적재 순서 의존 제거(결정성). **돌연변이 7종 전건 검출 실측:** 기장착 제외 제거·스트림 변조(reserve→reel)·후보 가드 제거·슬롯 축 원복·직렬화 누락·후보 수 무시·세션 호출 제거. 검사: 등급별 후보 수 앵커·중복 없음·스트림 분리(reel 무소비/reserve 소비)·차기 결산 재장착 회귀 가드·왕복·구세이브 관용. 하한 상향: TC-O 257→286 · TC-P 139→140 | D06 §5.3 (G-M1·비가역·풀 재등장) · D07 §3.4 · D13 별첨A §7.1~7.2 · D12 §6.1~6.2 · D09 별첨A §A-18 · IMPL-079 (G1) · 검증 프로토콜 §1·§4 | 확정 (돌연변이 7종 실측 + 게이트 전건) |
| 2026-08-16 | IMPL-111 | **MS-3 T2 — 소모품 사용 경로 결선 (엔진 API·P2/P3 지속 효과·R5 반입).** ①엔진 `use_consumable()` 신설 — 사용 지점 = **T1 섹터 개시 전속**(D06 §3.5 R-B: SECTOR_TURN + T1 + 비듀얼 가드), 거부 = 빈 배열 + 상태 무변경. 효과 3종 = D07 §3.1·D13 §3.6 전사: P1 섀시 +15(최대치 절단) · P2 +8 + **다음 트러블 1회 섀시 소모 반감**(지속 카운터 `trouble_shield_charges`, 계수 = `param_consumable_shield_mult` 0.5 신설 전사) · P3 **잔여 구간 섀시 소모 −20%**(`wear_reduction`) ②지속 효과 소비부 3경로 — 해저드 턴당 소모·트러블 섀시 소모(실드 반감 → 경감 순 적용)·듀얼 실패 페널티, 전부 (1 − 경감) 곱 ③**R5 반입/이월** — begin_gp: `outgame.consumables` → `engine.consumables_carry_in` 주입 / close_gp: 잔여 `consumables_held` 회수. 인벤토리 정본 = 아웃게임 층(IMPL-090 섀시 이월과 동형), 휴대 상한 가드는 구매 지점 전속(사용은 수량을 늘리지 못하므로 회수분이 상한을 넘을 경로 없음) ④엔진 직렬화에 3필드 편입(구스냅샷 키 부재 = 빈 인벤토리·무효과 — IMPL-090 충실값 전례) + 로그 키 `raceLog.consumableUse01` ⑤검사: D13 §3.6 전사 대조(단가·효과값·반감 계수·R4 프리미엄 2.5배 산식·휴대 상한 2) + T1 전속/듀얼 턴/재고 0 거부 + 절단 + P2 1회 소진 + P3 3경로 − 20% + 직렬화 왕복 + 세션 R5 왕복(TC-P). 하한 상향: TC-C 1910 · TC-P 139 | **해석·[가안] 4건:** ①듀얼 턴의 T1 사용 제외 — R-B 문면이 "T1 **섹터 개시**"이고 듀얼 턴은 섹터 개시가 아니다(전용 스핀 턴) ②P2 실드는 사용 횟수만큼 적립 [가안] — 문면 "다음 트러블 1회"가 중복 사용을 규정하지 않아 회수 적립으로 해석(각 사용이 각 1회를 보호) ③P3 중복 사용 = 허용·비누적 [가안] — 고정 계수라 누적 근거 없음(UI 층이 중복 안내 몫) ④P3 경감 대상 = 인레이스 섀시 감소 전 경로(해저드 턴당·트러블·듀얼 실패 페널티) [가안] — 문면 "잔여 구간 섀시 소모"가 소모원을 한정하지 않음. **잔여(주력 몫):** RACE-01 E13 사용 UI 결선(T2 협업 — 별첨A §A-4 "T1에만 활성") · RUN-01 E03 보충 UI(T5). **돌연변이 7종 전건 검출 실측:** T1 가드 제거·실드 소진 제거·해저드 경감 미적용·재고 감산 제거·직렬화 누락·세션 회수 누락·반감 계수 변조(0.5→1.0) | D06 §3.5 (R3·R4·R5·R-A·R-B) · D07 §3.1 · D13 별첨A §3.6 · D05 §2.3·§5.4 (무개정 확인) · IMPL-090 · 검증 프로토콜 §1·§4 | 확정 (돌연변이 7종 실측 + 게이트 전건) |
| 2026-08-16 | IMPL-110 | **MS-3 T1 — 무대 2~5 콘텐츠 결선 (서킷 16종·무대 4종·벽 라이벌·캘린더 풀).** ①D08 별첨A §2~§5 전사 — `circuit_{ac,ar,mf,pd}1~4` 16종 (속성 배치·슬롯 역할 4/5/4/5·랩 3 문면 그대로) + `stage_azure_coast`·`stage_alta_ridge`·`stage_mirage_flat`·`stage_pulse_dome` (season1_tour_slot 2~5 · wall_rival · 레조넌스 보너스 D13 §6.6 · 전용 이벤트 풀 4종씩 — 에셋 대장 §9-⑥ 예정 id 그대로, [가안] 종결) ②`season_calendar` shuffle_pool = [아주르, 알타, 미라지] (D08 §4.1 정순) + fixed_final_stage = 펄스 돔 (D08 §5.1 로렌츠 슬롯·무대 이중 고정) — NAR-02 캘린더 5행·시즌 2+ 셔플이 코드 무변경으로 결선 (기확인 구조) ③이벤트 16종 유입 — 총 48종 = 공통 28 + 무대 20 (별첨A §6 확정 총계 정확 일치) + 스트링 108키(ko — 무대 4·서킷 16·섹터 72·이벤트 16) ④**벽 라이벌 소비부 3종 신설 — D13 확정값의 엔진 소비부가 전무했다** (MS-2 = 무벽 메트로라 전 경로 잠복): 페이스 가중 `wall_pace_add` 열 전사 (디아스 1.4·카이 1.9·홀로웨이 1.4·로렌츠 1.0 — §6.2 벽 무대 가중 행) · 듀얼 임계 `param_duel_wall_threshold_add` 6 (§2.4 추월·방어 공통) · AI 리타이어 벽 면제 (§6.2 명문) — IMPL-089 격파 연출 트리거는 wall_rival 유입만으로 자동 결선 ⑤V1 확장: stage 스펙 템플릿화(동형 5무대 — circuit 템플릿 전례) + 신규 20파일 등재 + 캘린더 fixed_final_stage string→structure_ref 강화 ⑥검사 확장: D08 레이아웃 20종 전수 전사 대조·슬롯 역할·무대 결속·벽 배치·레조넌스 5무대 앵커·캘린더 정순·무대 풀 카테고리 구성·교차 유입 0 — 하한 상향 TC-C 942→1862 · EVENTS 6000→7034 · SEASON 450→480 (`run_tests.sh` + 스위트 내부 하한 동기) | **[가안] 2건:** ①펄스 돔 `resonance_bonus_value` = 0 — 보너스가 "즉시 만충"이라 값 미소비(엔진 front_gauge_full 경로가 만충 임계를 직접 읽음), 스키마 float 필수라 0 기입 ②벽 듀얼 임계 +6은 방어 override(로렌츠 55→61)에도 가산 — §2.4의 벽 행은 필러/네임드/로렌츠 행과 독립된 조건부 가산 행(그리드 레벨 +3 행과 동형 구조)이고 면제 문면이 없다. **[보고] 정본 내부 불일치 1건 — 총괄 발신 대기:** D08 §4.2 총괄표는 알타 리지 4번 이벤트를 '낙석 경보'로, 별첨A §3.0 확정 표는 '산악 구조대 격려'(C5)로 기재. §4.2 열 헤더가 "(개요 — 별첨A)"로 별첨A를 정본 지시하므로 별첨A를 따랐다 (IMPL-101 DF-04 전례 — 기능 정본 우선 + 보고 병행. 문서 수정은 D08 트랙 소관, 임의 수정 안 함). **돌연변이 8종 전건 검출 실측** (주입 성공 assert 포함 — 검증 프로토콜 §1): 서킷 속성 변조·레조넌스 값 변조·벽 가중 데이터 소거·엔진 가산 제거·리타이어 면제 제거·임계 가산 제거·캘린더 정순 변조·무대 풀 교차 오염. 리타이어 면제 검사는 확률 경로라 3000회 표본 + 표본 확보 단언 동봉 (프로토콜 §1-5) | D08 §2~§5·별첨A 전문 · D13 별첨A §2.4·§6.2·§6.6 · D12 §4.2 · D14 §3 · 검증 프로토콜 §1·§4·§5 · IMPL-089·091·092·101·105·109 | 확정 (돌연변이 8종 실측 + 게이트 전건) |
| 2026-08-16 | IMPL-107 | **[발신] UI 폰트 계열 배정 판정 요청 — HUB 공통 바 재화 수치** (`docs/handoff/UI폰트계열_총괄발신.md`). 사용자 요청("아이콘 유지·텍스트 상향")의 처리 주체를 판정받는다. 질문 = **D10 §5.7 대형 계열 적용 대상의 '헤더'에 HUB 공통 바가 포함되는가** — 포함이면 정본 무개정으로 구현 레인 즉시 처리, 아니면 적용 대상 표 개정 여부를 총괄이 판단 | 레인 단독 처리를 하지 않은 이유 3건: ①폰트 크기 = D10 §5.7 확정 2계열 3단이고 D13 별첨A §10이 "채택" 전사 — 값의 유일 창구는 D13(불변규칙 2) ②D10 §5.7이 요소별 계열도 지정한다("본문 = 로그·메뉴·**라벨**·툴팁" vs "대형 = VN·선택지·**헤더**·결산") — 공통 바 수치를 라벨로 보느냐 헤더로 보느냐에 따라 갈리는 **문면 해석 지점**이라 임의 해석 금지(불변규칙 9) ③대안(본문 base 9→10)은 구조를 소진한다 — 로그 슬롯 가용 폭 226px 대 전각 22자가 10px에서 220px(여유 6px)이고 그 위 단이 없어 **O7 3단 매핑이 고갈**된다(O7은 IMPL-087에서 소비부 미결선이라 지금은 잠복). **실측 근거:** 크레딧 아이콘 잉크 27캔버스px 대 수치 자면 8캔버스px = 3.4:1 · 대형 계열(14px, 제목 자면 13px) 적용 시 2.1:1. 아이콘 축소안은 사용자 배제 | D10 §5.7 · D13 별첨A §10 · D04 §5.1 · D09 §3.3·§6.1 O7 · 불변규칙 2·9 · IMPL-080·082·087·106 | 발신 (총괄 판정 대기 — 비차단) |
| 2026-08-16 | IMPL-106 | **[정정] IMPL-105 보고 ①(재화 아이콘 비정수 축소) 철회 — 실측 결과 결함 없음.** 현행 렌더는 **텍스처 1:1 무배율 + 캔버스 ×3 정수 확대**로 도트 규격에 완전 정합한다(믹셀 0). 코드 변경 없음(시도분 전량 원복) | **오진의 원인:** 씬의 `custom_minimum_size = Vector2(14,14)`를 표시 크기로 읽었다. 그러나 `TextureRect`의 `expand_mode` 기본값 = `EXPAND_KEEP_SIZE`라 **컨트롤 최소 크기 = 텍스처 크기(32×32)** 이고, 14는 그 아래여서 애초에 구속력이 없었다 — 즉 축소 자체가 일어나지 않는다. **실측 증거(1920×1080 캡처 픽셀 측정):** `currency_credit.png` 잉크 27×27 → 렌더 81px(=27×3 정확) · `currency_data.png` 잉크 23×25 → 렌더 69×75(=23×3, 25×3 정확). **A/B 반증:** 14→16 교체 후 캡처가 변경 전과 **바이트 수준 동일 크기**(코인 81px·카트리지 69×75)로 나와 값이 무효임이 먼저 드러났고, `size_flags_vertical = SHRINK_CENTER` 추가도 무효였다 — 두 무효 실험이 "최소 크기의 실제 출처는 텍스처"라는 결론으로 수렴했다. **교훈(기록):** 표시 크기를 `custom_minimum_size`만 보고 판정하지 말 것 — `expand_mode`가 상한을 쥔다. 화면에서 더 작은 아이콘이 필요하면 상자를 줄이는 게 아니라 **그 크기의 원도를 별도 제작**해야 한다(상자만 줄이면 `expand_mode` 변경이 동반되고 그 순간 ÷2 다운샘플로 디테일이 절반 소실). 배치 대장 §4.1·§9-⑧에 정정 반영. **검증:** 원복 상태에서 검증기 10종·테스트 10스위트 전건 통과 재확인 | D10 §2.2 · D12 §9.1 · IMPL-075·105 | 확정 (실측 — 보고 철회) |
| 2026-08-16 | IMPL-105 | **에셋 배치 대장 신설** (`docs/assets/에셋_배치_대장_v1_0.md`) — 사양서(무엇을 그리는가)와 분리된 **배치 계약**: 전 에셋의 실파일명·폴더 구조·경로 유도 규칙. 이미지 234파일 + 오디오 91(SFX 68·BGM 18스템·징글 5) + 폰트 5 + 팔레트 4 + 플랫폼 13. **명명 규약 = 현행 코드 실측 규약의 전 카테고리 확장** — `res://assets/ui/icons/` + 데이터 id + `.png`(race_screen.gd:455)를 그대로 승계해, 캐릭터·머신·무대 파일명을 **실존 데이터 id**(`ai_lorentz`·`crew_sasha`·`team_silvertrail`·`stage_metro_night`)에 1:1 결속했다 — 신규 식별자 0. 디렉토리 3계 신설: `assets/audio/{sfx,bgm,jingle}`·`assets/platform`·`store`(빌드 밖) | D10 §8.2 카테고리 11계에 **사운드·플랫폼 부속의 자리가 없어**(CLAUDE.md 계약도 "아트·사운드 실물"이라 쓰고 목록엔 사운드 부재) 신설이 불가피 — 총괄 확인 대상. 오디오 포맷은 D12 §10.1 확정분(SFX=WAV 44.1/16 · BGM=OGG q0.7) 적용, 징글은 2분류 어디에도 없어 **덕킹 주체 = BGM 버스 비소속** 근거로 SFX 포맷 채택[가안]. **보고 3건:** ①**재화 아이콘 비정수 축소** — 32×32 원도를 14px 상자에 KEEP_ASPECT_CENTERED로 넣어 ×0.4375(전 HUB 동형·실측), D10 §2.2 믹셀 금지·D12 §9.1 "비정수 리샘플 금지"와 충돌 소지 → 판정 필요(임의 수정 안 함) ②D11 §2.9 헤딩 "18식" vs 실제 SE-U01~U19 19개(총계 68은 19 기준 성립) — 정본 내부 불일치 ③`currency_*` 2종은 데이터가 아니라 코드 하드코딩(4파일 5줄) — 규약 ①의 유일 예외. **부수:** 파일명 오타의 증상이 실패가 아니라 **SEAL-E 진행 불능(멈춤)**임을 실측 근거로 §8 증상 사전에 기록(test_seal_ui.gd:78 — 텍스처 null이면 공개 판정 불성립) | D10 §8.1~8.2 · D11 §1.4·§2.11·§4.1·§7 · D12 §5.10·§9.1·§10.1 · D15 §2.2·§3.1 · IMPL-003·021·075·091·098·099 | 확정 (문서 신설) — 보고 3건 총괄 대기 |
| 2026-08-10 | IMPL-001 | **엔진 버전 = Godot 4.7 계열 고정** (설치 실물 4.7.1-stable) | 착수 전 문서 충돌 검토에서 CLAUDE.md 내부 모순(4.7 vs 4.4) 발견 → 사용자 보고 → 사용자 결정 → **총괄 경유 D12 v1.1 발행으로 정본 반영 완료**. D12 §1.1 재검증 조항이 예정한 경로 그대로 | **D12 v1.1 §1.1 결정 #11** · 마스터플랜 §0.7-3 · §0.8.1 | ✅ **종결 — 정본 정합** |
| 2026-08-10 | IMPL-002 | **V7(금칙 어휘 검사)은 경고 전용으로 구현** — 빌드 차단하지 않음. V1~V6·V8 + 혼입 0 스캔만 차단형 | CLAUDE.md 불변규칙 7이 D12 §4.2 표 머리글("위반 1건 = 빌드 실패")만 승계하고 같은 표의 V7 예외 행을 누락. 정본 2건이 일치하여 해석 여지 없음 | D12 §4.2 V7 행("경고 — 차단 아님") · D14 §2.1 TL-1("빌드 차단, V7은 경고") | 확정 (CLAUDE.md 규칙 7 교정 완료) |
| 2026-08-10 | IMPL-003 | **`*.import`를 `.gitignore`에서 제거 — 커밋 대상 확정** | `.gitignore`가 `*.import`를 제외하여 CLAUDE.md 커밋 규칙("에셋과 `.import`")과 정면 충돌. 누락 시 반대편 머신이 기본 설정으로 재임포트하여 니어리스트·정수 스케일이 무너짐 | CLAUDE.md 공통 불변 규칙 · D12 §9.1(임포트 기본값 고정) · D10 §2.2(믹셀 금지) | 확정 |
| 2026-08-10 | IMPL-004 | **STEP 0 환경 판정식에 OS 분기를 선행 배치** | 구 판정식은 `DISPLAY`/`WAYLAND_DISPLAY`만 검사. 두 변수는 X11/Wayland 전용이라 **Windows에서 항상 공란** → 주력 GUI 머신(Windows 확정)을 "원격 헤드리스"로 오판. 오판 시 에이전트가 GDAI·`run_project`·스크린샷을 자기 금지하여 눈이 필요한 작업 전체가 차단됨. 교정 후 `ENV=주력GUI(Windows)` 실측 확인 | CLAUDE.md STEP 0 | 확정 (실측 검증 완료) |
| 2026-08-10 | IMPL-006 | **`export_presets.cfg`는 커밋 대상 — 서명 자격증명만 `.gitignore` 제외** (A안) | preset의 포함/제외 규칙·피처 태그가 **혼입 0 구속의 이행 장치 그 자체**다(D12 §2.3 "export preset 분리"·D16 §8.2-⑦ 동렬 구조 보장). 제외하면 ①두 머신 preset이 갈리고 ②CI(TL-1)가 빌드를 재현 못 하며 ③MS-1 완료 판정 2번(혼입 0 스캔 CI green)의 재현성이 무너진다. 통상 제외 사유인 Android 키스토어 평문 노출은 자격증명 분리로 해소 | D12 §2.3 · D14 TC-P10 · D15 §1.2(TC-P10 = MS-1 이후 상시) · CLAUDE.md 불변규칙 1 | 확정 — **분리 실효성은 MS-1 §1에서 실측**(아래) |
| 2026-08-10 | IMPL-005 | **주력 머신 CLI 호출은 `Godot_v4.7.1-stable_win64_console.exe` 전속** (에디터 실행만 GUI exe) | 실측: GUI 바이너리는 Windows GUI 서브시스템이라 **stdout을 셸에 반환하지 않음**(`--version` 공란). console 래퍼는 정상 반환(`4.7.1.stable.official.a13da4feb`). CLAUDE.md 자가 수정 루프가 `--check-only` 에러·`validate` 결과·테스트 PASS/FAIL 캡처에 의존하므로 GUI exe 사용 시 루프가 구조적으로 성립 불가 | CLAUDE.md 자가 수정 루프 · D12 §1.3(CI 훅) | 확정 (`test/logic_test.gd` 관통 검증 — 종료코드 0) |
| 2026-08-10 | IMPL-007 | **[가안] MS-1 디버그 서킷 = 3랩 × 4섹터 (12턴)** — `godot/data/structures/circuit_debug.json` | 서킷 실데이터는 D08 소관·MS-1 범위 외. D05 §2.2 기준값(랩 3·섹터 4~5)과 D13 §4.1 GP 길이 검증의 12턴 모델을 그대로 차용 — 임의 산정 아님 | D05 §2.2 · D13 §4.1 | [가안] |
| 2026-08-10 | IMPL-008 | **듀얼 턴 = 섹터 비소모 삽입 턴**으로 구현 (섹터 진행 카운터를 소모하지 않음) | D05 §2.2 "기본 턴 수 12~15 (+듀얼 삽입 시 가변)"과 D13 §4.1 산술(`12턴×21초 + 듀얼 2.5회×45초` — 듀얼이 12턴 밖의 가산항)이 삽입 모델을 지시. D05 §4.3 "다음 섹터가 듀얼 섹터로 전환" 문면은 '다음 턴이 듀얼'로 해석 | D05 §2.2·§4.3 · D13 §4.1 | 해석 확정 (충돌 아님 — 산술 정합 우선) |
| 2026-08-10 | IMPL-009 | **[가안] 듀얼 전용 스핀 = 판정 환산 전속** — 통상 심볼 정산(게이지·차지·섀시) 미적용. 정산 8단계 중 ①~⑤ 무동작, ⑥에서 판정 해소, ⑦⑧ 정상 수행 | D05 §4.3 "듀얼 전용 스핀 1회 → 개입 창 → 판정" + 별첨A §2.4 심볼 환산표가 6분류 전부를 판정치로 환산(트러블 −15 등) — 이중 적용 방지 | D05 §4.3 · D13 별첨A §2.4 · C-2 | [가안] |
| 2026-08-10 | IMPL-010 | **[가안] 정산 세부 배치 3건** — ①앞차 저항·뒤차 압박 적용 = ⑤ 만충 판정 직전 ②모멘텀 보너스(+5G) 적용 = ④ 전진·공략 단계, 듀얼 턴 제외 ③양 게이지 동시 만충 시 전방(추월) 우선 | D13 §2.1·§2.2가 값만 확정하고 8단계 내 배치를 미지정. 압박·저항을 만충 판정 직전에 두면 "이번 턴 방어(③)가 이번 턴 압박을 상쇄"하는 통제 문법 성립 | D05 §5.3 · D13 별첨A §2.1 | [가안] |
| 2026-08-10 | IMPL-011 | **[가안] 배경 AI·게이지 경계 2건** — ①배경 스왑으로 인접 상대 교체 시 해당 게이지 리셋 + 예약 듀얼 해제, 듀얼 시점 비인접이면 무산 ②최종 랩 계수 ×1.2는 게이지 증감 전체에 곱 적용 ③최종 섹터 정산에서 발생한 만충은 폐기(다음 섹터 부재) | ①D05 §4.2 "새 인접 상대와의 게이지가 개시"의 연장 ②별첨A §1.3 "3랩째 전 게이지 계수" 문면 ③"다음 섹터가 듀얼 섹터로 전환" — 다음 섹터가 없으면 전환 대상 부재 | D05 §4.2~4.3 · D13 별첨A §1.3 | [가안] |
| 2026-08-10 | IMPL-012 | **듀얼 임계 산식은 개인 시드 파라미터 기준** (팀 프로파일 가산 미적용). 팀 가산·소속 계수는 주행 층(배경 판정·압박)에 적용 | 별첨A §2.4 명시값 정합: 필러 48.5 = 35+3×4.5, 로렌츠 127.5 = 35+**5.0**×4.5+70 (악시온 +0.5 적용 시 129.75로 불일치), 디아스 방어 55 = 30+**5**×5. 값 정본(D13)의 산출 예가 시드 값을 사용 | D13 별첨A §2.4·§6.2 | 해석 확정 |
| 2026-08-10 | IMPL-013 | **V6 이원 처리: ID 중복·도메인 접두 위반 = 차단 / 고아 데이터 = 경고** (코드 소비 루트 테이블은 고아 검사 면제 플래그) | D12 §4.2 V6 행 문면이 "고아 데이터(…) **경고**"를 명기하면서 CLAUDE.md 규칙 7·D14 TL-1은 V6을 차단형에 포함 — 중복=차단·고아=경고의 이원 독해가 유일한 무모순 해석. 고아 차단 시 코드가 동적 id로 소비하는 테이블(match_·param_ 등)이 전부 오탐 | D12 §4.2 · D14 §2.1 TL-1 · CLAUDE.md 규칙 7 | 확정 (검증기 구현 반영) |
| 2026-08-10 | IMPL-014 | **[가안] CI 러너 = GitHub Actions 래퍼** (`.github/workflows/validate.yml`) — 로컬 스크립트(`tools/validators/validate.sh`·`.ps1`)가 정본. 러너 엔진 조달 = godot-builds 4.7.1-stable | MS1 킥오프 §작업범위 2의 [가안] 문면 그대로. 검증기 본체는 엔진 스크립트로 양 머신 공통 실행 (프로젝트리스 실행 실측 확인) | 킥오프 §2 · D14 TL-1 | [가안] |
| 2026-08-10 | IMPL-015 | **구현 배치 4건** — ①데이터 CSV 임포터 = `keep` 고정(번역 리소스 오생성 차단, 소비는 자체 로더 전속) ②autoload 0개(헤드리스 안전 — 전 코어가 class_name 순수 클래스) ③로직 테스트 = `godot/tests/`(res:// 접근 필요) ④세이브 payload는 문자열 내장+체크섬, RNG 시드·상태는 문자열 직렬화(JSON double 정밀도 손실 실측 → 릴 재현 깨짐 방지) | ①Godot 기본 csv_translation 임포터가 테이블 CSV를 오인 ②CLAUDE.md "autoload가 깨지면 원격 헤드리스 작업 전체 실패" ④int64 state가 double 왕복에서 정밀도 손실 — 재현 테스트로 실측 후 교정 | D12 §4.1·§6.2·§7.1 | 확정 |
| 2026-08-10 | IMPL-016 | **MS-1 미구현 유보 2건** — ①연출 등급 L0~L3 매핑·L2+ 상한 카운터(표현 채널 부재 — 아트·사운드·햅틱 범위 외) ②비앙카 '압박 시 안정성 2.5' 조건 분기(D08 §6.3 조건 DSL 트랙 — 이벤트·조건 시스템 범위 외) | 킥오프 범위 외(Out) — 경계 인터페이스만 유지: 이벤트 구조에 등급 필드 추가 여지, AI 테이블에 조건 열 추가 여지. MS-2+에서 구현 | 킥오프 §범위 외 · D12 §5.8·§5.12 | 유보 (범위 판단) |
| 2026-08-11 | IMPL-019 | **MS-1 사후 검증(돌연변이 주입) 및 교정 2건** — ①정산 8단계 C-2 가드 신설(`tests/test_core_loop.gd` — enum 선언 순서·배열 순서·단계 수 동시 고정) ②구조 JSON의 **침묵 기본값 제거** — `data.circuit.get("laps", 3)` 류를 `circuit_int()`/`grid_int()`/`circuit_str()`/`grid_array()`로 교체(누락 시 `push_error` + `is_ok()` false), 테스트가 주행 후 `is_ok()`를 단언 | **검증 방법 = 결함 주입 후 잡히는지 확인.** 교정 전: 정산 ③↔④ 교환에도 `CORE_LOOP_TEST_PASS`(출력까지 동일) · `laps` 키 삭제에도 조용히 3으로 진행 → 불변규칙 2("D13에 없으면 중단·보고")·규칙 3(C-2)이 무방비. 교정 후: 양 돌연변이 전건 exit=1로 검출. 값 3/4/16 자체는 D05 §2.2·IMPL-007 근거 있음 — 문제는 **대체값 패턴** | CLAUDE.md 규칙 2·3 · D12 §3.4 · D14 TC-C10(MS-2 결속의 선행 최소 가드) | 확정 (반증 실험으로 이빨 확인) |
| 2026-08-11 | IMPL-020 | **검증기 이빨 실측 — 전건 통과** | 결함 주입 결과: `core/`에 `OS.get_name()` → MIX0 FAIL exit=1 · UI 한글 리터럴 → V4 FAIL · 잘못된 스트링 키 → V8 FAIL · **V7 금칙 어휘 → WARN + exit=0(차단 안 함)** · RNG 직렬화를 정수 JSON 왕복으로 되돌림 → `SAVE_RELOAD_TEST_FAIL`. 검증기·테스트가 형식 통과가 아님을 확인 | D12 §4.2 · D14 TL-1 · IMPL-002 | 확정 (실측) |
| 2026-08-10 | IMPL-017 | **자격증명 분리 실효 — A안 유지, C안 후퇴 불요.** 4.7 엔진의 자격증명 저장 경로 = `res://.godot/export_credentials.cfg` → `.gitignore`의 `.godot/`로 이미 차단. `.gitignore` 주석에 실측 경로 명기 | 엔진 바이너리(4.7-stable) 문자열 실측으로 경로 상수 확인 — `export_presets.cfg`와 별 파일. **잔여: 에디터에서 preset을 실제 생성해 평문 미기입을 종단 확인**하는 절차는 익스포트 다이얼로그 조작이 필요(헤드리스 에디터는 preset 미변경 시 재저장하지 않음 — 실측) → **주력 GUI 머신 몫** | IMPL-006 · D12 §2.3 · 킥오프 실측 #1 | 확정 (경로 실측) — 종단 확인만 주력 머신 이월 |
| 2026-08-11 | IMPL-021 | **MS-2 착수 확인 2건 — 사용자 결정** ①TC-O 적용 범위 = **아웃게임 시스템 전량 구현 + 콘텐츠만 무대 1 한정** ②`godot/assets/` **유지 + D10 §8.2 카테고리별 하위 디렉토리**로 디렉토리 계약 편입 | MS2 킥오프 §2가 "구현 시작 전 사용자 확인, 임의 판단 금지"로 지정한 2건. ①은 "무대 1 수직 절단면"이 시스템 축소를 뜻하지 않음을 확정 — D15 §1.1이 MS-2 완료 조건에 TC-O 8항 전량을 넣은 문면과 정합 ②D10이 에셋 ID 체계(`A-<분류>-##`)와 카테고리 11계만 정하고 res:// 배치 경로는 미지정 → 카테고리와 1:1 대응이 에셋 ID 추적·V2 결속에 유리 | MS2 킥오프 §2 · D15 §1.1 · D10 §8.1~8.2 · impl_log U-3(해소) | 확정 (사용자 결정) |
| 2026-08-11 | IMPL-022 | **무대 1 데이터 충전 구조** — 서킷 = 구조 JSON 1파일/서킷(`circuit_mn1~mn4.json`, 섹터 배치를 `sectors` 배열로) · 속성 6축 계수 = `sector_attributes.csv` · 무대 = `stage_metro_night.json`(레조넌스 보너스 포함) · 필패 = `scripted_loss_s1.json`(anchor_type=tour_slot·tour_slot=1). **서킷별 '심볼 가중 정성 방향'은 별도 계수를 신설하지 않고 속성 배치로만 구현** | D08 §3.4 "서킷 개성은 속성 배치와 가중치로 구현"·심볼 인스턴스 불확장 확정. D13 별첨A §1.3이 계수를 속성 단위로만 확정하므로 서킷 단위 가중 계수는 정본에 부재 — 신설 시 불변규칙 2 위반. **A5 배틀 존의 '듀얼 성향 ↑'도 별도 수치가 D13에 없어 게이지 계수 ×1.5의 결과로만 실현**(임의 산정 금지) | D08 별첨A §1 · D08 §3.3~3.4 · D13 별첨A §1.3·§6.4·§6.6 · 불변규칙 2 | 확정 |
| 2026-08-11 | IMPL-023 | **검증기 확장 + V5 결함 교정** ①구조 JSON도 테이블과 동일하게 **타입 검사**(존재 확인만으로는 `"laps": "three"`가 통과) ②중첩 배열 검사 신설(`arrays` 스펙 — 원소 수 = 선언 필드 일치·원소별 타입/참조 검사) ③`structure_ref`·`structure_ref_array`·`fk_optional` 참조 타입 신설 ④구조 템플릿(`structure_templates`)으로 동형 서킷 20종 대비 ⑤**V5 float/int 비교 결함 교정** | ⑤가 핵심: JSON 수치는 float로 적재되는데 허용 슬롯 목록과 int 비교를 해 **허용값 `tour_slot: 1`이 위반으로 잡혔다.** MS-1에는 앵커 필드를 가진 구조 JSON이 0건이어서 V5의 구조 경로가 한 번도 실행되지 않았고 결함이 잠재. 교정 후 8종 돌연변이(섹터 수 불일치·속성 FK 오타·미등재 name_key·laps 타입·stage_id 참조·tour_slot 3·circuits 참조·wall_rival FK) **전건 exit=1 검출** | D12 §4.2 · D14 TL-1 · IMPL-019(검증 방법 = 결함 주입) | 확정 (돌연변이 8종 실측) |
| 2026-08-11 | IMPL-024 | **TC-C 헤드리스 스위트 신설** (`godot/tests/test_tc_c.gd` — 236검사) + 엔진 관측점 3종: ①`phase_log`(턴 T1~T6 실통과) ②`settle_log`(정산 8단계 열거) ③`transition_errors`(비정의 전이 누계). **D13 확정값 전사 블록을 한 곳에 격리** | D14 §3의 TC-C 판정 근거가 "로그 캡처·대조"인데 MS-1 엔진은 T2·T3 단계를 상태로 갖지 않아(스핀이 T4로 직행) 대조 대상 자체가 없었다. `transition_errors`는 `_transition`이 `push_error`만 하고 진행하던 탓에 **비정의 전이가 테스트를 통과해버리는** 문제를 기계 판정으로 전환. 값 대조 블록을 격리한 이유: 다른 검사는 기대값을 GameData에서 읽어 **자기 일관성** 때문에 표류를 못 잡는다(실측: 부스트 상한 4→2 돌연변이가 이 블록 없이는 미검출) | D14 §3 TC-C1~C12 · D12 §3.3·§3.4 · D13 별첨A §1.1~2.4·§6.1·§6.6 | 확정 (돌연변이 실측 — 아래) |
| 2026-08-11 | IMPL-025 | **포인트 테이블 침묵 기본값 제거** — `points_tier1.csv`를 P1~P16 전 순위 명시로 채우고(P9↓ = 0), 엔진의 `points_tier1.get(rank, 0)`를 명시 조회 + `push_error`로 교체 | D13 별첨A §6.1의 "P9↓ 0"이 테이블에 부재해 코드의 대체값이 그 자리를 메우고 있었다 — IMPL-019가 구조 JSON에서 제거한 것과 **동일한 침묵 기본값 패턴**. 표가 비어도 조용히 0이 되어 표류가 숨는다 | IMPL-019 · 불변규칙 2 · D13 별첨A §6.1 | 확정 |
| 2026-08-11 | IMPL-028 | **세이브 정책 층 신설** (`godot/core/save/save_manager.gd`) — 프로필별 경로 분리 · **백업 1세대 회전**(새로 쓰기 전 정본을 백업으로 복사, 회전 실패 시 저장 자체를 중단) · **마이그레이션 체인**(단일 격차 단계만 · 다운그레이드 거부 · 미등록 판번 거부) · 클라우드 충돌 정책 · 서스펜드 스냅샷 1회성(소거를 호출 층 재량에 두지 않음). 프로필 수는 `param_save_profile_count` 데이터 경유 | D12 §7.1~7.3. **회전 실패 시 저장 중단**을 택한 이유: 백업 없이 정본을 덮으면 손상 시 복구 경로(TC-P8)가 사라지므로, 저장 실패가 복구 불능보다 낫다. **스냅샷 소거를 `consume_snapshot`에 내장**한 이유: 남으면 같은 지점 반복 재개 = 스커밍 경로(§6.2). 프로필 수 3은 D12가 '확정 기준값'으로 명시(조정 창구 D13/D14)하므로 코드 상수화하지 않고 데이터로 | D12 §7.1·§7.2·§7.3 · D14 TC-P8·P9 · 불변규칙 2·4 | 확정 (돌연변이 6종 실측) |
| 2026-08-11 | IMPL-029 | **세이브 스키마 판번 상수 단일화** — `SaveService.SCHEMA_VERSION`(= 2)을 유일 정의로 두고 `SaveManager`가 승계 | 신설 당시 두 곳에 판번을 두었더니(Service 1 / Manager 2) **새로 쓴 세이브가 봉투에는 1로 적히고 로드 시 2로 마이그레이션되는** 상태가 됐다 — 매 로드마다 마이그레이션이 도는데 아무 증상이 없어(결과가 같아서) 조용히 굳을 수 있었다. TC-P9의 `migrated_from` 단언이 잡았다 | D12 §7.1 | 확정 (테스트가 검출) |
| 2026-08-11 | IMPL-030 | **[가안] 클라우드 충돌 '불일치' 판정** — 진행도와 타임스탬프의 순서가 **서로 어긋날 때**를 사용자 선택(ASK)으로 본다. 한쪽이 두 축 모두 앞서거나 진행도만으로 결판나면 자동 선택 | D12 §7.3은 "최신 진행 우선(진행도 비교 → 동률 시 최신 타임스탬프) + 불일치 시 사용자 선택"까지만 확정하고 '불일치'를 정의하지 않는다. 두 축이 반대를 가리키는 상태가 곧 "어느 쪽이 최신인지 정본이 결정하지 않은 상태"이므로 이때만 사용자에게 넘긴다 — 자동으로 한쪽을 지우지 않는 것이 세이브 손실 방지에 부합. **진행도 축 = 정식 저장마다 증가하는 `progress_counter`** (구조 정의, 수치 아님) | D12 §7.3 · D14 TC-P9 | [가안] |
| 2026-08-11 | IMPL-034 | **연출 등급 L0~L3 + GP당 상한 카운터 결선** (IMPL-016 유보분 해소) — `presentation_grades.csv`(등급→채널 구성·스팅 길이·상한) + `presentation_triggers.csv`(후보→등급·우선순위) + `core/state/presentation_grade.gd`. 상한 = **L2 2회 · L3 1회 각각 독립**(D08 §8.5), 초과 시 **L1 강등**(D13 별첨A §8.1), 겹칠 때 소진 순서 = **듀얼 결판 > 벽 라이벌 비트 > 찬스 3매치**(D08 §8.5). `resolve()`가 후보 **목록**을 받는 이유: 개별 호출로 처리하면 호출 순서가 우선순위를 덮어써 듀얼 결판이 찬스 3매치에 슬롯을 빼앗긴다 | D04 §5.5(등급→채널 표)·D11 §2.5(음향 채널 합성표)·D08 §8.5(상한·우선순위)·D13 별첨A §8.1·§8.3(스팅 0.8/1.5/2.5초)의 데이터화. **등급 판정 자체는 불변**이고 채널 감쇠는 출력 층 마스킹 소관(D12 §5.8 명문)이라 여기서 다루지 않는다 | D12 §5.8 · D04 §5.5 · D08 §8.5 · D11 §2.5 · D13 별첨A §8.1·§8.3 | 확정 (돌연변이 3종 실측 — 상한값·우선순위 정렬·GP 리셋) |
| 2026-08-11 | IMPL-035 | **[보고] 햅틱 진동 지속 시간이 D13 대장에 미전사** — D11 §3.1이 L1 0.1초 · L2 0.25초를 '확정 기준값(창구 D13)'으로 명기하는데 D13 별첨A §8.3 오디오 값 표에는 이 두 항이 없다. 따라서 연출 등급 테이블은 **햅틱을 세기 등급(none/weak/strong)까지만** 담고 지속 시간 수치는 담지 않았다 | 값을 D11에서 옮겨 적으면 '값의 유일 창구 = D13'(불변규칙 2)을 구현이 우회하는 셈이 되고, 임의 산정하면 규칙 위반이다. **구조는 열어 두고 수치는 비운다** — 햅틱 실구현(주력 머신·D11 트랙) 착수 시점에 D13 전사 여부를 총괄에 확인해야 한다. 차단성 없음(MS-2 원격 레인 산출물 무영향) | D11 §3.1 · D13 별첨A §8.3 · 불변규칙 2 | 보고 (총괄 확인 대기) |
| 2026-08-11 | IMPL-033 | **§0.3 iOS 하한 여유 0 경보 — MS-2 착수 시점 재확인 결과: 3항 전건 유지, 상향 없음.** 중단·보고 조건 불성립 · D14 디바이스 매트릭스 조정 불요 | 4.7 브랜치 실측: 엔진 iOS 배포 타겟 `-miphoneos-version-min=14.0` / `-mios-simulator-version-min=14.0` · 엔진 Android `minSdk 24`(targetSdk 36) · AdMob 플러그인 iOS `platforms: [.iOS(.v14)]` / Android `minSdk 24`. **경보 자체는 유효**하다 — 엔진 14 · 플러그인 14 · D12 §1.2 하한 14가 여전히 정확히 일치해 완충이 0이므로, MS-3 이후 매 착수 시점 재확인 대상으로 승계한다 | MS2 킥오프 §0.3 · IMPL-018 · D12 §1.2·§11.1 · D14 §6.2 MP-L | 확정 (실측 — **독립 레인 교차 확인 일치**) |
| 2026-08-11 | IMPL-037 | **독립 반증 2레인 결과 반영 — 제품 코드 실결함 6건 교정** ①`progress_counter`가 인자 payload에서만 파생돼 **정상 저장 흐름에서 항상 1**이었다(호출 층에 새 값을 돌려주지 않아 인메모리 dict에 카운터가 없음) → **디스크 값 기준 단조 증가** + 저장 결과가 갱신된 payload를 반환 ②`save_progress`가 회전 전 정본 무결성을 확인하지 않아 **손상본이 백업 슬롯을 덮었다**(복구 직후 자동 저장이 유일한 성한 세대를 파괴 — 자동 저장 단일 모델에서 자동 발생) → 정본이 성할 때만 회전, 아니면 보류 사유 기록 ③레조넌스에 **서킷 축이 없어** 투어 층이 슬롯을 주입하면 무대의 서킷 4종 전부에서 발동(D08 §3.7 R6 "무대당 1회" 미성립) → `resonance_circuit_id`·`resonance_consumed` 신설, 세이브 판번 3으로 상향 + v2→v3 마이그레이터 ④`save_progress`가 **프로필 범위를 강제하지 않아** `profile_9`·`profile_-1`이 실제로 생성됐다(`is_valid_profile` 호출부가 테스트뿐) → `configure()` 필수화 + 진입점 3곳 가드 ⑤**세이브 정책 층에 프로덕션 호출부가 0건**이었다(디버그 UI가 `SaveService`를 평면 경로로 직접 호출 — 프로필·백업·마이그레이션 전량 우회) → 정책 층 경유로 결선 ⑥`load_progress`가 다운그레이드 거부를 '복구 불능'으로 보고(무손상 상위판 세이브가 파손으로 보임) → `version_too_new`로 구분 | 6건 전부 **실측 재현 후 교정**. 위임 레인이 사본에서 돌연변이·프로브로 잡아낸 결과이며, 자기 승인을 피하기 위해 내가 쓴 코드의 반증을 별도 컨텍스트에 맡긴 판단이 실제로 값을 냈다 | D12 §7.1~7.3 · D08 §3.7 R6 · D14 TC-P8·P9 · 불변규칙 2 | 확정 (돌연변이 재검증 — 19종 중 18종 CAUGHT) |
| 2026-08-11 | IMPL-038 | **뒤차 압박 산식을 시드 공격성 기준으로 교정 — IMPL-012 부분 개정** | 실측: 팀 가산 포함 공격성(디아스 5.0+불카 0.5=5.5)에 소속 계수 ×1.3을 곱해 **18.33**이 나왔고, D13 별첨A §2.1에 인쇄된 **16.9가 엔진에서 도달 불가능**했다. §2.1의 산출 예는 전부 시드값 기준이다(필러 8.6 = 2.0+3.0×2.2 / 디아스 13.0 = 2.0+**5.0**×2.2 → ×1.3 = 16.9). IMPL-012는 "팀 가산·소속 계수는 주행 층(압박)에 적용"으로 확정했으나 근거로 든 것은 §2.4(듀얼 임계)의 예시뿐이고 §2.1의 압박 예시와 대조하지 않았다. **개정: 압박의 공격성 = 시드값 / 소속 계수(pressure_mult)는 그대로 곱한다** — 이러면 §2.1 인쇄값 3종(7.5·8.6·16.9)이 전부 도달 가능해진다. 런타임 대조 검사 추가 | D13 별첨A §2.1·§6.2 · IMPL-012(부분 개정) · 불변규칙 2 | 확정 (인쇄값 도달성 실측) |
| 2026-08-11 | IMPL-039 | **TC-C11 봉인 검사가 항진명제였다 — 교정** | `info.get("events", [])`가 같은 배열의 **참조**를 돌려주므로 `info.get("events") == t1_events` 비교는 스핀이 결과를 append해도 항상 true였다. 실측으로 확인(누출 주입 상태에서도 판정식 = true). 교정: ①사본(`duplicate(true)`)과 비교 ②**엔진 공개 속성 전수 스캔** — `provisional` 외의 어떤 속성에도 심볼 id가 실려 있으면 실패(개별 필드를 열거하지 않아 새 필드가 자동으로 검사 대상이 된다) ③이벤트의 키·파라미터 심볼 id 스캔 | D02 §4 · D12 §6.3 · 불변규칙 5 · D14 TC-C11. **"통과했다"가 아니라 "틀리게 만들면 실패한다"로 판정하지 않으면 검사식 자체의 결함이 무한히 숨는다**는 사례 | 확정 |
| 2026-08-11 | IMPL-040 | **테스트 공백 대량 충전** — TC-C 255 → **595검사** / TC-P 51 → **108검사**. 신설 축: ①속성 6축 계수 **36칸 D13 전사**(전사 없이는 부호 반전도 미검출 — 자기 일관성) ②섹터 속성 배정 D08 별첨A §1 대조 + 펄스 섹션 제3전 전속·배틀 존 최소 1 규칙 ③릴 가중 전 서킷 전 섹터 산식 대조 ④레조넌스 런타임 7항(R3 트러블 3매치·R6 서킷 축·무대당 1회·R7 듀얼 무관여·위치 비공개) ⑤**음성 검사**(비정의 전이 거부·미등재 값 조회가 `is_ok()`를 내리는지·T4 게이트를 잔액 충분 상태에서) ⑥순위→포인트 1:1·리타이어 정렬·시간 모델 산식·타임아웃 무페널티·모멘텀 +5G·최종 랩 ×1.2·부스트 실효 ⑦세이브: 프로필 범위 강제·손상본 회전 금지·카운터 단조성·**디스크 구버전 봉투 결선**·경로 분리·손상 스냅샷 격리 | 독립 레인이 "데이터 337칸 중 240칸 미검출 / 코드 51건 중 33건 미검출"로 공백 지도를 그려 준 것을 그대로 메웠다. **음성 검사가 없으면 강제 코드가 죽어도 사후 조건이 0을 읽어 초록이 된다**(실측: 전이 검사 무력화 시 4스위트 통과)는 지적이 특히 결정적이었다 | D14 §3·§7 · IMPL-024 | 확정 (재검증: 이전 미검출 19종 중 18종 CAUGHT) |
| 2026-08-11 | IMPL-062 | **데이터 주입 경로 신설 — 불변규칙 2의 코드 측 가드** (`GameData.tables_override_dir` + `tests/fixtures/tables/` + `test_data_driven.gd` 34검사). 픽스처는 기본과 **의도적으로 다른 값**을 담고, 파일 단위 대체이므로 갈아 끼우지 않은 표는 기본에서 읽힌다 | 다른 스위트는 기대값을 같은 데이터에서 읽으므로 코드가 데이터 대신 **현재 값과 같은 리터럴**을 써도 구분하지 못한다 — `비용 == data.param(...)` 형태의 단언은 `charge -= 1` 구현을 그대로 통과시킨다. **유일한 판별법은 데이터를 바꿔 보는 것**이며, 그래서 오버라이드 경로를 프로덕션 코드에 열었다(테스트 전용 목적 명기). 돌연변이 6종 전건 검출: 홀드 비용·차지 상한·크루 패시브·릴 열 인덱스·섀시 최대치 리터럴화 + 컴파일 게이트 자체 파손 | 불변규칙 2 · D12 §4 · IMPL-054 · 독립 검증 합의분(E4·R-g·E9·E12·SM10) | 확정 (돌연변이 6종 실측) |
| 2026-08-12 | IMPL-066 | **U-4 판정 이행 — 슬롯 진행 보정의 축 = 투어 내 GP 슬롯(제1~4전).** `tour_slot_mods.csv` 신설(+0.00/+0.05/+0.10/+0.15) · `RaceEngine.race_slot` 입력 · `SeasonState.apply_to_engine()`로 주입 · **AI 기저 강도에만 가산**(플레이어 제외) | 사용자 판정(2026-08-12). D13 별첨A §6.2의 값 4개가 그대로 맞아 D13 개정 없이 결선 가능하다는 점이 근거. 플레이어를 제외한 이유는 D08 §2.4가 보정의 목적어를 "**필러·경쟁 풀의 기본 파라미터**"로 명시하기 때문이다. **무대 1 즉시 영향:** MN-1~4의 AI 페이스가 +0.00/+0.05/+0.10/+0.15로 갈려 "투어 후반전이 팽팽해진다"가 성립한다. **잔여 발신:** D08 §5.3의 "슬롯 진행 보정이 **시즌 내** 미세 상승 곡선을 담당" 문면은 GP 슬롯 축과 어긋나므로 총괄 경유 문면 교정 대상(U-2 계열로 승계) | D08 §2.4·§4.3 · D13 별첨A §6.2 · D12 §5.12 | 확정 (사용자 판정) |
| 2026-08-12 | IMPL-067 | **U-5 판정 이행 — 결산·이벤트 210 → 225초.** GP 목표 하한 10분 충족: 12턴 = 252+112.5+12+225 = 601.5초 = **10.025분** · 15턴(듀얼 비례 3.125) = **11.594분** | 사용자 판정(2026-08-12). 성분·총계·목표가 모두 정합하는 최소 변경을 택했다 — 다른 성분(턴 21초·듀얼 45초·완급 비트 1초)은 각각 다른 규격에 결속돼 있어 건드리면 파급이 크고, 결산·이벤트 시간은 그 자체가 완충 항이다. **잔여 발신:** D13 §4.1 표의 인쇄 총계 2셀(12턴 10.0 → 10.0 유지 가능 · 15턴 11.3 → **11.6**)과 투어/시즌 길이 파생 행이 총괄 경유 갱신 대상 | D13 §4.1 · D05 §2.2 · D13 별첨A §8.1 | 확정 (사용자 판정) |
| 2026-08-12 | IMPL-068 | **U-6 판정 이행 — §2.4 인쇄 범위를 실제 스프레드로 정정** (추월 48.5~57.5 → **44.0~57.5** / 방어 45~55 → **40.0~55.0**). 구현·데이터·테스트 무변동 — 문서 층 전속 | 사용자 판정(2026-08-12). 밸런스 판정자가 표만 보고 실제 범위를 알 수 있어야 V-1 부등식 재검토가 가능하다는 것이 근거. **부기:** 하한이 내려가면 "네임드가 필러보다 어렵다"는 진술이 약해진다(필러 48.5 > 네임드 하한 44.0) — 이는 안정성 2.0 라이벌(디아스·볼코바·주드)이 **추월은 쉽고 다른 축이 어렵다**는 설계의 결과이며 모순이 아니다. **잔여 발신:** `docs/design/` 수정 권한 밖이므로 §2.4 표 2셀 갱신은 총괄 경유 | D13 별첨A §2.4·§6.2 | 확정 (사용자 판정) |
| 2026-08-12 | IMPL-069 | **RB-1 판정 이행 — 부속성 ½ 규칙 계수 적용 유지 + `main_attr ≠ sub_attr` 검증기 규칙 신설.** V1의 중첩 배열 검사에 `distinct` 스펙을 추가해 주·부속성 동일 섹터를 **데이터 단계에서 차단**한다 | 사용자 판정(2026-08-12). ½ 독법을 유지하되 그 독법의 유일한 실위험(주·부 동일 시 1.5 × 1.25 = 1.875 > 명문 상한 ×1.8)을 기계로 봉인하는 선택. **IMPL-022의 [가안] 표기는 유지**한다 — §1.3 문면에 부속성의 규칙 계수 적용 근거가 없다는 사실은 판정으로 바뀌지 않으므로, 문면 보간을 총괄에 발신한다. 무대 2~5 서킷이 들어올 때 `expected_layout` 단언이 덮지 않는 신규 데이터도 이 규칙이 지킨다(실측: 규칙 단독으로 V1 FAIL 발화) | D13 별첨A §1.3 · D08 §3.3 · IMPL-022 | 확정 (사용자 판정) |
| 2026-08-11 | IMPL-065 | **SM10 폐쇄 — 세이브 층의 프로필 수 리터럴** (`test_data_driven.gd`에 `SaveManager` 경유 단언 추가: 판정 함수 `is_valid_profile`와 **강제 경로** `save_progress` 양쪽) | 픽스처는 이미 프로필 수를 2로 내려놓고 있었는데(기본 3) **스위트가 `SaveManager`를 한 번도 부르지 않아** 리터럴 3이 드러나지 않았다 — 데이터 주입 경로를 만들어도 **소비부를 호출하지 않으면 아무것도 잡히지 않는다.** 픽스처 준비와 단언은 별개 작업이다. 실측: `is_valid_profile`를 리터럴 3으로 되돌리면 `[FAIL] 프로필 3 무효 = 데이터 값 준수` | IMPL-062 · 불변규칙 2 | 확정 (돌연변이 실측) |
| 2026-08-11 | IMPL-063 | **픽스처 설계 교훈 2건 (실패에서 배운 것)** ①파일 단위 오버라이드에서 `core_params.csv`처럼 **평면 키-값 표는 전체를 복사한 뒤 일부만 바꿔야 한다** — 일부만 담으면 나머지 파라미터가 통째로 사라진다(실측: `is_ok()` false로 침묵 기본값 가드가 정확히 잡았다) ②**순수 열 픽스처는 섹터 속성 Δ에 오염된다** — 릴 열 분리를 단발 스핀으로 판정하면 속성 가산(스트레이트 = 슬립 +0.06) 때문에 오검출된다. 다수 표본의 최빈값·지배율(≥80%)로 판정한다 | 두 실패 모두 스위트를 처음 돌렸을 때 드러났고, ①은 기존 가드(침묵 기본값 금지)가 신규 코드의 설계 오류를 잡아 준 사례다 | IMPL-019 · IMPL-062 | 확정 |
| 2026-08-11 | IMPL-064 | **컴파일 게이트의 성공 토큰 부재를 실패로 판정** (`run_tests.sh`) — 게이트가 자기 파스 에러로 죽으면 종료코드는 잡히지만 **진단이 사라진다.** `COMPILE_GATE_PASS` 토큰의 부재 자체를 실패 조건에 넣어 원인이 드러나게 했다 | 독립 레인 제안. 게이트는 구조적으로 자기 자신을 검사할 수 없으므로(자기가 안 돌면 아무 말도 못 한다) 호출부의 토큰 확인이 유일한 완충이다. 실측: 게이트에 파스 에러 주입 시 `TESTS_FAIL compile gate (종료코드 1 · 성공 토큰 없음)` | IMPL-059 · D14 §2.1 | 확정 (실측) |
| 2026-08-11 | IMPL-059 | **컴파일 게이트가 무동작이었다 — 교정** (`load()` → `reload()` 판정). Godot 4.7의 `load()`는 **파스 에러 스크립트에도 non-null을 반환**한다 (실측: 정상 `reload()=0` / 파손 `43`). 따라서 IMPL-054로 세운 게이트 ①이 아무것도 잡지 못했고, **메인 씬 스크립트(`debug_race.gd`)가 로드 불가인 상태로 전 게이트가 초록**이었다. 검사 대상에 테스트 스크립트도 포함(자기 자신만 제외) | IMPL-054에서 차단된 것으로 보였던 2건은 실제로는 게이트 ③(검사 수 하한) 덕이었다 — 그리고 ③은 **스위트가 참조하는 클래스가 깨질 때만** 발화하므로 UI·어댑터·플랫폼 계층의 파스 에러는 그대로 통과했다. 교정 후 3종 전건 검출: 메인 씬 · **미참조 신규 파일** · 스위트 자체. **부기(자기 실증):** 이 교정을 넣으면서 상수명을 바꾸고 참조부를 놓쳐 게이트 자신에 파스 에러를 냈고, 게이트가 실행조차 되지 않아 출력이 사라졌다 — 게이트는 자기 자신을 검사하지 못한다는 사실의 실증 | D14 §2.1 TL-1 · IMPL-054 | 확정 (돌연변이 3종 실측) |
| 2026-08-11 | IMPL-060 | **G1 환전 가드의 두 우회 경로 폐쇄** ①`has_method` 블랙리스트는 **닫힘 성질이 아니다** — 이름만 바꾸면 우회된다(실측: `top_up_pulse`로 역방향 환전 신설 시 통과) → **공개 메서드 집합 == 커밋된 기대 집합** 화이트리스트로 전환 ②`consumables.effect`가 자유 문자열이라 **코드 한 줄 없이 데이터만으로** Cr→차지 소모품을 만들 수 있었다 → enum으로 좁힘. 같은 규율을 `tuning_lines.target`·`overhauls.effect/drawback`에도 적용 | ②가 더 중요하다: `event_categories.reward_type`은 charge를 의도적으로 배제하는 enum인데 소모품·튜닝·오버홀의 효과 열만 무제약이었다. 효과 소비부를 결선하는 순간 G1이 데이터로 열리고 어떤 게이트도 발화하지 않는다. 화이트리스트 전환은 검사 수 하한과 같은 **"의도적 변경만 통과"** 구조다 | D06 §4.3 G1 · D05 §6.1 · D12 §4.2 V1 | 확정 (돌연변이 3종 실측) |
| 2026-08-11 | IMPL-061 | **봉인 검사: 화이트리스트 '안'의 값 도메인 단언 추가** — 허용 속성(`phase_log`·`settle_log`)은 변경이 허용되므로 **내용을 검사하지 않으면 그 안에 숨길 수 있다**(실측: 3매치일 때만 `phase_log.append(999)`를 넣으면 다수 스핀 검사도 통과). 스핀 후 `phase_log == [T1,T2,T3,T4]` · `settle_log ⊆ SETTLE_ORDER`를 단언 | 봉인 검사 4차 확대. 변경 화이트리스트만으로는 "허용된 그릇에 담긴 누출"을 막지 못한다 — 허용 범위를 좁히는 것과 허용 대상의 값 도메인을 고정하는 것은 별개 조치다 | D02 §4 · D12 §6.3 · IMPL-047·IMPL-055 | 확정 (돌연변이 1종 실측) |
| 2026-08-11 | IMPL-057 | **서사 층 신설** (`core/state/narrative_service.gd` + `vn_slots.csv` · `vane_lines.csv` · `milestone_vn.csv`) — VN 슬롯 밀도·상한(시즌 15 / 마일스톤 8) · **형식 A 관계 전이** · 아카이브 재열람 · **베인 대사 3단계 필터**. TC-O4·TC-C8 결속 (`test_narrative.gd` 68검사) | **두 구속이 서로 맞물리므로 같은 자리에서 구현했다:** ①형식 A 전이는 VN의 '발생(도달)' 기준이며 **스킵 여부를 조건으로 삼지 않는다** — 스킵이 진행을 막으면 비강제성(D01 §4 G2 조건 2)이 역전된다 ②**아카이브 재열람은 전이를 재발화하지 않는다**(이벤트 id당 1회 소비 멱등 플래그). 하나만 지키면 다른 하나가 깨진다: 발생 기준만 두고 멱등이 없으면 재열람이 카운터를 밀어 올리고, 멱등만 두고 열람 기준을 쓰면 스킵한 플레이어가 영구히 전이를 못 받는다. 베인 대사는 **키·풀 불변 + 선택 계층 필터**(D12 §5.7) — 풀 복제가 아니라 필터 전환이다 | D04 §3·§5 · D07 §5.5·§6.3 · D08 §8.4 · D12 §5.7 · D14 TC-O4·TC-C8 | 확정 |
| 2026-08-11 | IMPL-058 | **검사 수 하한이 즉시 값을 냈다 — 자기 신고의 과대 선언을 잡음** `test_narrative.gd`의 하한을 90으로 선언했는데 실제 실행은 68검사였고, 게이트가 이를 `NARRATIVE_TEST_FAIL`로 보고했다. 실패 단언은 0건이었으므로 하한을 실측값(68)으로 교정 | IMPL-054로 세운 게이트가 **처음 적용된 신규 스위트에서 바로 작동**했다. 하한을 실측값에 맞춰 두면 이후 어떤 축소도 실패로 드러난다 — 스위트를 줄이려면 하한도 함께 내려야 하므로 의도적 축소만 통과한다 | IMPL-054 · D14 §2.1 | 확정 |
| 2026-08-11 | IMPL-052 | **아웃게임 시스템 전량 신설** (`core/state/outgame_state.gd` + 데이터 10표) — 재화 2계 · 정비(필드 체증·전면·무상 복원선) · 튜닝 6계통 · 오버홀 12종·슬롯·**G-M2 캡** · 스킬 16종·티어 개방·덱 · 크루 5인 패시브 · 스폰서 4종·슬롯 · 관계 카운터 3축 · 시설 G1~G4 · 소모품 3종 · 결산 보상 · 베인 3단계. TC-O 결속 (`test_tc_o.gd` 237검사) | 사용자 확정 범위(IMPL-021)대로 **시스템은 전량 · 콘텐츠만 무대 1**. **G1 역방향 환전 금지는 조건문이 아니라 경로 부재로 이행**했다 — 조건문으로 막으면 그 조건문을 지우면 열리지만, 함수가 없으면 지울 것이 없다. 테스트도 `has_method()`로 **경로 부재**를 단언한다(같은 방식으로 관계 카운터의 감산 API 부재도 단언). 아카이브는 시설 해금과 무관하게 항상 열려 있다 — 비강제성 안전망을 자원 게이트 뒤에 두지 않는다는 R-D07-VN의 이행 | D06 §3~§5·§7 · D07 §2·§3·§4·§5·§6 · D13 별첨A §3~§5·§7 · D14 TC-O1~O8 | 확정 |
| 2026-08-11 | IMPL-053 | **S1 상금과 S2 완주 보너스를 분리** — 신설 당시 하나로 합산해 P1 상금이 1,500 Cr로 200 부풀어 있었다 (D13 별첨A §3.1 표는 S1 P1 = 1,300 / S2 = 200을 별개 행으로 인쇄) | 표의 값을 각각 대조하는 검사를 만들자마자 드러났다. 합쳐 두면 ①어느 축이 바뀌었는지 추적할 수 없고 ②리타이어 시 지급 규칙(둘 다 0)이 뭉개진다. D06 §2.1이 S1~S10을 별개 Source로 열거한 구조와도 어긋난다 | D13 별첨A §3.1 · D06 §2.1 | 확정 |
| 2026-08-11 | IMPL-054 | **게이트가 "실행되지 않음"과 "통과"를 구분하지 못했다 — 3중 게이트 신설** ①**컴파일 게이트**(`tests/compile_gate.gd` — 전 `.gd` 로드 성립) ②스위트 종료코드 ③**검사 수 하한**(스위트 내부 단언 + `run_tests.sh`의 커밋된 기대값 대조) | 독립 레인 실측: `save_manager.gd`가 **파스 에러**인 상태에서 TC-P 검사 수가 108 → **2로 붕괴**했는데 `TESTS_PASS suites=6` · `VALIDATORS_PASS warnings=0` · **exit=0**이었다. 로드 실패한 클래스 호출은 `Nonexistent function`으로 끝나 실패 카운터를 올리지 않으므로 검사가 실행되지 않은 것과 통과한 것이 구분되지 않는다. **파스 에러 — 가장 흔한 파손 — 가 커밋·CI를 통과할 수 있었다.** 인계 문서의 "파이프를 걸면 종료코드가 가려진다"와 같은 계열이며, 이번에는 종료코드 자체가 거짓말을 했다. 실측: 파스 에러 주입·스위트 축소 주입 양건 검출 | D14 §2.1 TL-1·TL-3 · IMPL-031 | 확정 (돌연변이 2종 실측) |
| 2026-08-11 | IMPL-055 | **세이브 교정 2건 + 봉인 검사 3차 확대** ①`_disk_counter`가 **백업을 보지 않아** 손상 1회로 진행도 카운터가 후퇴했다(3 → 1) — 후퇴한 카운터는 클라우드 충돌에서 최신 진행을 뒤진 것으로 오판시킨다 ②`configure()` 미호출을 `profile_out_of_range`로 보고해 원인을 숨겼다 → `not_configured` 분리 ③봉인 검사를 **다수 스핀(400회)**으로 확대 — 단발 스핀 diff는 3매치처럼 **희귀 분기에 숨은 누출**을 놓친다 ④공개 표면 스캔에서 **밑줄 접두 제외를 걷어냈다** — GDScript의 밑줄은 관례일 뿐 접근 제한이 아니라 누출 경로로 성립한다 ⑤ARCH 금지 목록에 `push_warning(` 추가 (stderr도 출력 경로) | ①②는 독립 레인이 신규 코드에서 찾아낸 실결함이고 ③④는 같은 레인의 실증(400회에서 3매치 12회 → 누출 12회 검출)을 반영. **일반 원칙: 결과 상관 누출은 희귀 분기에 숨을 수 있으므로 봉인 검사는 단발이 아니라 다수 스핀에 걸쳐야 한다** | D12 §7.1·§7.3 · D02 §4 · D12 §6.3 · IMPL-039·IMPL-047 | 확정 (돌연변이 5종 실측) |
| 2026-08-11 | IMPL-056 | **구조적 0 축 단언 확대** — 심볼 매치 효과의 비관여 축(49칸)에 더해 **듀얼 환산의 축 분리**(match1~3 축 = 슬립·브레이킹·라인 / per_symbol 축 = 찬스·트러블, 9칸)와 **비0 소유 대장**(`duel_overtake_add`는 로렌츠만 · `rush_lap1`은 마로·볼코바만 … 25칸)을 구조로 고정. 연출 등급의 "상한 0 = 무제한" 규약도 단언 | 0은 값이 아니라 **효과 축의 부재**라는 대장 사실이므로, 개별 0을 전사하면 칸 수만 늘고 표류 탐지력은 늘지 않는다(전사값과 테이블값이 같이 움직여 자기 일관성). 독립 레인의 처방 그대로이며 같은 검사가 E1 계열(심볼별 계수 누락)까지 덮는다 | D13 별첨A §1.2·§2.4·§6.2 · D08 §8.5 | 확정 (돌연변이 3종 실측) |
| 2026-08-11 | IMPL-049 | **시즌·투어 층 신설** (`core/state/season_state.gd` · `points_tier2.csv` · `season_calendar.json`) — 챔피언십 포인트 **2계층**(GP 순위→투어 포인트→투어 종합 순위→챔피언십 포인트) · 투어 종합 순위 · 투어 결산 · 시즌 결산 · 조기 확정 공표 · 그리드 레벨 · 에필로그(2연속 챔피언) · 캘린더 셔플 · **레조넌스 추첨(투어 개막 1회)**. TC-C13 결속 (`test_season.gd` 471검사) | 2계층의 요점은 "투어당 그랑프리 수가 바뀌어도 챔피언십 층 밸런스가 불변"(D05 §9.4)이므로 두 층을 섞지 않았고, 그것을 단언으로 고정했다(돌연변이 `mix`·`tier2` 검출). 2층 값은 **D13 별첨A §6.1의 조정분**(25/17/14/11/9/7/5/3/2/1 — D05 기준값에서 2~8위 −1)을 쓴다: D08 §5.6이 미세 조정 권한을 명문화했고 값의 창구는 D13이다. 시즌은 항상 5투어를 소화하며 탈락도 시즌을 중단시키지 않는다(D05 §9.3·§9.5 · D08 §5.5) — 조기 확정은 **공표만** 하고 타이틀·결산은 시즌 종료 시점에 수행 | D05 §9.3~§9.5·§10 · D08 §2·§5.5·§5.6 · D13 별첨A §3.2·§6.1·§7.4 · D14 TC-C13 | 확정 (돌연변이 10종 실측) |
| 2026-08-11 | IMPL-050 | **[가안] 투어 종합 동점 처리 = 직전 그랑프리 결과 순** | 정본은 투어 층 동률을 명시하지 않는다. D13 별첨A §6.3이 챔피언십 동률에 "직전 그랑프리 결과 순"(V-2 — 등반 서사 보존)을 쓰므로 동형 적용했다. 임의 규칙을 새로 만들지 않고 정본에 있는 유일한 동률 규칙을 승계한 것 | D13 별첨A §6.3 · D05 §9.4 | [가안] |
| 2026-08-11 | IMPL-051 | **캘린더 셔플 = 순열 전개 후 직전 조합 제외 · 균등 추첨** (재추첨 루프 불채택) | D08 §2.2 "6조합 중 직전 제외 5개 균등 추첨"의 기계적 이행. **재추첨 루프를 쓰지 않은 이유:** ①이론상 종료를 보장하지 못하고 ②추첨 횟수가 가변이라 shuffle 스트림 상태가 오염되어 같은 시드에서 재현이 깨진다. 무대 2~5 콘텐츠가 MS-2 범위 밖이므로 `shuffle_pool`은 비어 있고, **규칙 자체는 순수 함수로 검사**한다 (합성 풀 3원소로 5조합 전수 등장·직전 재출현 0·단일 풀 무셔플 확인) — 콘텐츠 없이 규칙을 검증하는 방식 | D08 §2.1~§2.2 · D12 §6.1 shuffle 스트림 | 확정 |
| 2026-08-11 | IMPL-044 | **비전투 이벤트 시스템 신설** — `event_categories.csv`(배분·보상 범위) · `events.csv`(공통 풀 28종 + 무대 1 전용 4종) · `event_variants.json`(변형 + 조건 DSL) · `core/state/event_service.gd`(발생 판정 80% · 카테고리 가중 추첨 · 쿨다운 3회 · 변형 4축 · 보상 산출) · `core/state/condition_dsl.gd`(**제한 조건 DSL** — 필드 비교·AND/OR만, 스크립트 임베드 불가). 테스트 `test_events.gd` 신설 | D08 §7·별첨A §6 · D13 별첨A §6.5 · D12 §5.4. **설계 판단 3건:** ①카테고리가 뽑혔는데 후보가 전부 쿨다운이면 **미발생**으로 둔다 — 다른 카테고리로 옮겨 뽑으면 §6.5의 배분 비율이 조용히 왜곡된다 ②DSL의 미지 필드·미지 연산자는 **거짓이 아니라 오류** — false로 흘리면 오타 하나로 변형이 영구 미발동되는 상태가 만들어진다 ③순서 비교는 수치 전용(문자열 순서는 로케일 의존). C1 회복은 필드 정비 회당 상한(30)으로 절단 — D08 §7.2 경제 가드 | D08 §7 · D08 별첨A §6 · D13 별첨A §6.5 · D12 §5.4 | 확정 |
| 2026-08-11 | IMPL-045 | **비앙카 압박 훅 결선 — IMPL-016 유보분 2/2 해소** (`ai_rivals.csv`에 `stability_under_pressure` 열 신설 · 비앙카 2.5, 나머지 공란=훅 없음). **[가안] '압박 시'의 판정 = 추월 듀얼 상황** — 플레이어가 뒤차로서 앞차를 밀어붙이는 국면이 압박의 정의에 부합한다고 읽었다 | D13 별첨A §6.2 "안정성 4.0(압박 시 2.5)" · D08 §6.3이 "압박 훅(비앙카)… 파라미터 조건 분기로 구현 — 수치는 D13"으로 위임한 값이다. 정본이 '압박'을 별도 정의하지 않아 해석이며, 같은 문장이 위임한 3인 중 카이·주드의 가변 분산은 `rush_random`으로 이미 구현돼 있어 비앙카만 남아 있었다. 검사: 비앙카 추월 임계 = 35+2.5×4.5 · 훅 없는 동일 안정성 홀로웨이 = 35+4.0×4.5 대조 | D13 별첨A §6.2 · D08 §6.3 · D12 §5.12 · IMPL-016 | [가안] |
| 2026-08-11 | IMPL-046 | **아키텍처 규칙 정적 스캔 신설** (`architecture_rules` — 혼입 0 스캔과 같은 축) ①**세이브 정책 층 우회 금지**: `SaveService.*` 직접 호출은 `core/save`·`tests` 밖에서 차단 ②**코어 표준출력 금지**: `godot/core` 안의 `print(` 계열 차단 | 둘 다 **테스트로 닫을 수 없는 종류**다. ①은 디버그 UI가 정책 층을 우회해도 어떤 테스트도 실패하지 않았고 (UI 커버리지 0), ②는 스핀이 결과를 `print()`로 흘리면 헤드리스 테스트가 자기 stdout을 못 잡아 봉인 누출이 통과했다. 정적 규칙이 유일한 차단 수단. 실측: 두 위반을 주입해 전건 ARCH FAIL 검출 | D12 §2.1 혼입 0 · §6.3 봉인 · D02 §4 | 확정 (돌연변이 2종 실측) |
| 2026-08-11 | IMPL-047 | **봉인 검사 기준 확대 — '심볼 id 포함'에서 '스핀이 바꾼 공개 표면 일체'로.** 스핀 전/후 공개 스크립트 변수를 전수 스냅샷해 화이트리스트(`provisional`·`turn_phase`·`phase_log`) 외의 변경을 위반으로 본다 | 종전 기준은 결과 상관 신호를 **다른 표기**(연출 트리거 id 등)로 실어 보내는 경로를 통과시켰다 — 심볼 id 문자열을 담지 않기 때문이다. 변경 자체를 막으면 표기 방식과 무관하게 닫힌다 | D02 §4 · D12 §6.3 · 불변규칙 5 · IMPL-039 | 확정 |
| 2026-08-11 | IMPL-048 | **전사 감사 반영 3건** ①`param_form_var_named`(코드 소비 0건)를 제거하고 단언을 실제 소비 경로인 `ai_rivals.form_var` 8칸으로 이전 — **단언이 죽은 쪽에 붙어 산 값 7칸이 무방비**였다 ②완급 비트 성분 2쌍 신설(`param_pacing_beat_max_sec 2.5` · `param_pacing_beat_probability 0.40`)과 `평균 = 상한 × 빈도` 단언 — 평균만 손으로 맞춰 통과시킬 수 있던 구멍 ③접전 판정 임계 70 G 신설 ④15턴 총계 검사 라벨에 **[파생]** 명시(듀얼 3.125는 문면 미인쇄 — 12턴의 2.5를 턴 비례로 파생한 [가안]) | 독립 감사가 220칸 대조로 **오전사 0건**을 확인하는 한편, 위험이 오전사가 아니라 ①이중 정의의 죽은 쪽 단언 ②파생값의 정본 라벨링 ③문면 불일치를 흡수하는 마법수에 있다고 지적 | D13 별첨A §6.2·§8.1·§4.1 | 확정 |
| 2026-08-11 | IMPL-042 | **GP 소요 모델에서 완급 비트 항이 빠져 있었다 — 교정** (`param_time_pacing_beat_sec = 1.0` 신설). D13 §4.1 산식은 `턴×21초 + 듀얼×45초 + **완급 비트 턴×1초** + 결산·이벤트 210초`인데 엔진이 세 항만 합산했다 | 성분 4종을 전사 대조하는 검사를 만들자마자 드러났다. **부기:** §4.1이 인쇄한 12턴 총계 '10.0분'은 반올림이다 — 성분 산술은 252+112.5+12+210 = 586.5초 = **9.775분**. 15턴 총계 11.3분은 성분과 정확히 일치(듀얼이 턴 수에 비례: 3.125회). 성분값이 정본(§4.1 산출 열)이므로 구현·검사는 성분을 따르고 총계는 근사로 단언한다 | D13 §4.1 · D13 별첨A §8.1 | 확정 |
| 2026-08-11 | IMPL-043 | **D13 확정값 전사 범위 대확장** — AI 파라미터 5층(네임드 8인 × 5축) · 팀 프로파일 가산 5팀 · 변동 요소 12항 · 그리드 레벨·시작 보정 · 듀얼 환산 방어 축(브레이킹 3 + 라인 3) · 타이머 4항 · 시간 모델 성분 4항 · 속성 6축 계수 36칸 · P10~P16 0점. TC-C 595 → **711검사** | 독립 레인이 "데이터 337칸 중 240칸 미검출"로 공백 지도를 그려 준 결과의 이행. 스팟 재검증 6종(디아스 공격성 5.0→2.0 · 불카 압박 1.3→1.9 · 타이머 10→17 · 브레이킹 환산 38→3 · P10 0→7 · 완급 비트 1.0→4.0) **전건 CAUGHT** | D13 별첨A §2.1·§2.4·§4.1·§6.1~6.3·§8.1 · D08 §6.2 · IMPL-040 | 확정 (스팟 6종 실측) |
| 2026-08-11 | IMPL-041 | **[기록] 관측 불가 돌연변이 1종** — `_is_resonance_sector`의 `not current_turn_is_duel` 가드를 제거해도 4스위트가 통과한다. 결함이 아니다: `_settle_duel`이 단계 ②를 아예 돌지 않으므로 R7(듀얼 무관여)은 **구조로 보장**되고 플래그 가드는 이중 안전장치다. 관측 가능한 효과가 없는 돌연변이는 검출 실패의 증거가 아니다 | 같은 이유로 정산 ③↔④ '효과 내용' 교환도 두 단계가 가환이라 관측 불가(독립 레인 판단과 일치). **"미검출"을 보고할 때는 그 돌연변이가 관측 가능한 효과를 갖는지 먼저 확인해야 한다** | D08 §3.7 R7 · D14 TC-C11 | 기록 |
| 2026-08-11 | IMPL-036 | **다음 재확인용 경로 메모** — AdMob 플러그인(poingstudios/godot-admob-plugin)의 기본 브랜치는 `master`이고, `Package.swift`·`build.gradle`은 리포 루트가 아니라 **`platforms/ios/`·`platforms/android/` 하위**에 있다 (v5.0.0 monorepo 구조). `main` 브랜치나 루트 경로로 접근하면 404 | IMPL-033 재확인 시 실제로 루트·`main` 경로가 404를 냈고 두 레인이 각각 우회 경로를 찾느라 시간을 썼다. §0.3은 MS-3 이후 매 착수 시점 반복 대상이므로 경로를 기록해 반복 비용을 없앤다 | IMPL-033 · MS2 킥오프 §0.3 | 확정 (참고) |
| 2026-08-11 | IMPL-032 | **V2 코드 발행 키 스캔의 파일명 오탐 교정** — 스트링 키 문법(`{domain}.{feature}`)과 파일명(`progress.json`)이 형태상 겹쳐, 코드가 데이터 파일을 지칭할 때마다 "미등재 키"로 오검출됐다. 확장자 목록(`key_scan_excluded_suffixes`)으로 갈라낸다 — 스트링 키의 마지막 단이 파일 확장자인 경우는 없다 | 세이브 정책 층이 `progress.json`·`suspend.json` 상수를 두자 즉시 3건 오검출. 코드가 파일을 이름으로 지칭하는 일은 앞으로도 계속 생기므로 개별 회피가 아니라 규칙으로 처리 | D12 §4.2 V2 · §8.1 키 문법 | 확정 |
| 2026-08-11 | IMPL-031 | **테스트 목록 단일화** (`tools/validators/run_tests.sh`) — CI YAML이 테스트 파일명을 직접 열거하지 않고 이 스크립트를 호출 | 목록이 CI와 로컬 양쪽에 있으면 "CI에서만 도는 테스트 / 로컬에서만 도는 테스트"가 갈린다. 실제로 신규 스위트 2종(TC-C·TC-P)을 추가하는 시점에 갈릴 수 있었다 | D14 §2.1 TL-1·TL-3 · IMPL-014 | 확정 |
| 2026-08-11 | IMPL-027 | **V4 검출 범위 확대 — 표시 싱크 규칙 신설** (MS-2 인계 §0.2-7 이월분 해소). `.text`·`tooltip_text`·`placeholder_text`에 대입되는 문자열은 **한글 여부와 무관하게 스트링 키여야 한다.** 상수를 경유한 우회도 추적(싱크에 대입된 대문자 상수의 값까지 검사). 오탐 제외 2종: 딕셔너리 첨자 리터럴(`["name_key"]` — 데이터 열 이름) · 공백/이스케이프 전용 리터럴(줄바꿈·구분 공백) | 구 V4는 한글 리터럴만 봐서 `REEL_PLACEHOLDER := "???"`가 통과했다. MS-2는 표시 문자열이 대량 유입되므로 "한글이면 잡는다"는 기준으로는 영문·기호·서식 문자열이 전부 새어 나간다. **부수 교정 3건:** 릴 자리표시 → `ui.debug.reelHidden` · 상태 구분자 → `ui.debug.statusSeparator` · 타이머 서식 → `ui.debug.timerFormat`(어순·소수 자리도 문면이므로 서식 자체를 데이터로). 확대 후 우회 3종(상수 경유·직접 리터럴·서식 문자열) **전건 exit=1 검출** | MS-2 인계 §0.2-7 · D12 §8.1~8.2 · 불변규칙 6 | 확정 (돌연변이 3종 실측) |
| 2026-08-13 | IMPL-070 | **GDAI 에디터 플러그인 격리 — `godot/addons/` 를 `.gitignore` 에 편입하고 `project.godot` 의 autoload·editor_plugins 등록을 커밋 대상에서 제외** | 플러그인이 `GDAIMCPRuntime` autoload를 등록해 **autoload 0개(IMPL-015)가 깨진 상태**로 발견됐다. 커밋하면 ①원격이 `addons/` 없이 pull할 때 등록된 autoload가 파손되고(CLAUDE.md "autoload가 깨지면 원격 헤드리스 작업 전체가 실패") ②네이티브 `.so/.dll` 6종이 리포에 들어가며 ③MCP 런타임이 제품 빌드에 실린다 — 봉인 규칙(불변규칙 5)이 막으려는 외부 관측 경로 그 자체다. **런타임 개입 실측:** 캡처 실행 로그에 `Capture not registered: 'gdaimcp'` 가 찍혀 autoload가 실제로 게임 프로세스에 붙는 것을 확인했다. 플러그인 파일 자체는 로컬에 남겨 사용자가 필요 시 켤 수 있게 두되, **엔진이 실행될 때마다 등록을 되살리므로 커밋 직전 `project.godot` 확인이 필요**하다 | CLAUDE.md 공통 불변 규칙 · IMPL-015 · 불변규칙 5 | 확정 (실측) |
| 2026-08-13 | IMPL-071 | **RACE-01 실화면 구현 — MS-1 디버그 UI 대체** (`godot/ui/race/` — `race_screen.gd/.tscn` · `timer_ring.gd` · `vane_waveform.gd` · `log_feed.gd` · `ui/theme/ui_palette.gd`). 4존 골격 + B0 씬 패널 · 타이머 프레임 링 3구간 · 베인 파형 런타임 렌더 · 로그 피드 4슬롯 · 자원 바. `debug_race.gd/.tscn` 및 `ui.debug.*` 키 21종 제거 | D09 §3 / 별첨A §A-6의 이행. **씬은 엔진이 직접 `pack()`+`ResourceSaver.save()` 로 생성**했다 — 손으로 쓴 `.tscn` 은 에디터가 재정규화하며 디프가 튄다(CLAUDE.md 씬 소유권 조항의 원인). **표시 문자열은 씬에 굳히지 않고 전량 런타임 키 주입**한다(V4·D09 §6.6). **국면 분리 교정:** MS-1 UI는 베인 발화(`vane.*`)를 로그 피드로 흘렸는데 D09 §3.1은 "베인은 릴 존 콜아웃 전속·로그 피드 비유입"을 명문화한다 — `raceLog.*` 만 C존으로 보내도록 갈랐다. **[가안] 4건:** ①기능 컬러 색값(D10 §2.3 면제 조항 대상이나 팔레트 에셋 미유입) ②타이머 링 두께 1/2/3px·점멸 2/5Hz(D09 §3.2는 '가는/중간/굵은'·'저속/고속'의 순서 관계만 확정) ③베인 상태별 A·F·S(D10 §3.3이 산출물로 지정한 '상태·단계 파라미터 표' 미작성 — 정성 서술의 순서 관계만 지킴) ④정지 연출 중 타이머 정지(정본 미규정 — 흐르게 두면 리스핀 사용이 모멘텀 잔여 비율을 구조적으로 깎아 개입 선택에 이중 페널티가 붙는다) ⑤로그 화자 전량 중계 표기(이벤트 스키마에 화자 필드 부재 — D09 §3.3 4종 구분은 발행 층 결선 후) | D09 §3.1~§3.4·§7.1 · D09 별첨A §A-6 · D10 §2.1·§3.3·§5.1·§5.4·§5.7·§6.2 · D13 별첨A §8.1~8.2 | 확정 (눈 검증 8컷 — 봉인·타이머 3구간 실측) |
| 2026-08-13 | IMPL-072 | **[보고] 표현 층 확정 기준값 5종이 D13 대장에 미전사** — `param_confirm_lockout_sec 0.3`(D09 §1.3 확정 오입력 무시 윈도) · `param_log_slot_cap 4`(D09 §3.3 로그 슬롯) · `param_font_size_body 8`·`param_font_size_head 12`(D10 §5.7 폰트 2계열 원도) · `param_scene_panel_height_px 100`(D10 §6.2 씬 컷 원도 높이). D13 §8.1이 D09 값 일부(릴 정지 간격 0.4·타이머 10초)를 "D09 채택"으로 전사한 것과 달리 이 5종은 전사 행이 없다 | **IMPL-035(햅틱 지속 시간 미전사)와 같은 구조**다. 다만 햅틱은 비워도 화면이 서지만 이 5종은 **비우면 화면이 성립하지 않는다** — 그래서 D09/D10 문면값을 데이터 테이블(`core_params.csv`)에 두고 코드 하드코딩을 피하는 형태로 진행하고 창구 문제만 보고한다. 값은 정본이 확정한 것이며 임의 산정이 아니다(D09 §1.1 "본 문서의 수치·단계·시간 값은 전부 확정 기준값"). **총괄 확인 필요: D13 전사 여부** | D09 §1.1·§1.3·§3.3 · D10 §5.7·§6.2 · D13 §8.1 · 불변규칙 2 · IMPL-035 | 보고 (총괄 확인 대기) |
| 2026-08-13 | IMPL-073 | **SEAL-E 검사 구축 — 봉인 규칙의 화면 층** (`godot/tests/test_seal_ui.gd` — 실화면 인스턴스화 · 다수 스핀 · run_tests.sh 등록, 하한 84). 검사 축 3종: ①스핀 커밋 직후 3릴 전량 자리표시 ②순차 공개 중 미공개 릴 자리표시 유지·공개 수 단조 증가 ③**릴 외 UI 표면 전수 무변경** | MS-2 인계 §0.4-5 이월분. 기존 TC-C11은 **엔진의** 공개 표면을 보므로, 엔진이 숨겨도 화면이 먼저 그리는 경로를 잡지 못했다. 표면 판정은 개별 필드를 열거하지 않고 라벨·버튼을 전수 스캔해 **새 표시 요소가 자동으로 검사 대상**이 되게 했다(IMPL-039 방식의 화면 층 승계). **검사 수는 라운드당 고정으로 집계**한다 — 프레임 수에 비례해 세면 검사 수 하한 게이트(IMPL-054)가 흔들린다(실측: 같은 코드가 417↔552로 요동). **돌연변이 실측:** 사본에서 정지 연출 대기(`await`)를 걷어내 즉시 공개로 만들자 `SEAL_UI_TEST_FAIL failures=28` · exit=1 — 릴 조기 공개와 UI 표면 변경 두 축이 모두 발화했다 | 불변규칙 5 · D12 §6.3 · D14 TC-C11·G-6 · IMPL-039·IMPL-055 · 검증 프로토콜 §4 | 확정 (돌연변이 실측) |
| 2026-08-13 | IMPL-074 | **[보고] V4 표시 싱크 검사의 범위 한계 — 지역 변수 경유 미추적.** 싱크 대입을 지역 변수로 한 단계 거치면 리터럴이 검사 대상에서 빠진다(상수는 IMPL-027이 추적하지만 지역 변수는 아니다). 한글은 `_has_hangul` 이 줄 위치와 무관하게 잡으므로 **비한글 표시 문자열에 한정된 구멍** | 이번 구현에서 V4가 6건을 잡았는데 전건이 오탐이었다 — 명명 플레이스홀더 딕셔너리의 **키**(`{"value": …}`)·데이터 열 이름(`get("name_key")`)·서식 지정자(`%d`·`%.1f`)로, 어느 것도 표시 문자열이 아니다. **검증기를 완화하지 않고** 해당 식을 지역 변수로 분리해 통과시켰고(가독성도 개선), 그 과정에서 위 구멍을 발견했다. 규칙 확대는 원격 레인(검증기 소관)에 넘긴다 — 이번 세션이 스스로 만든 우회가 아니라 선재 범위 문제다 | D12 §4.2 V4 · IMPL-027 · 불변규칙 7 | 보고 (원격 레인 이월) |
| 2026-08-13 | IMPL-075 | **UI 아이콘 플레이스홀더 16종 생성 + RACE-01 결선** (`godot/tests/gen_placeholder_icons.gd` → `assets/ui/icons/` — 심볼 6 · 섹터 속성 6 · 재화 2 · 화자 2). 릴 심볼을 텍스트 라벨에서 **32×32 도상**으로 교체, A존에 섹터 속성 아이콘 결선 | 사용자 확정(2026-08-13): 아트는 플레이스홀더 자체 생성. **아트가 아니라 규격의 자리**다 — D10이 확정한 도상 어휘(후류 화살표 / 브레이크 디스크 / 레코드 라인 / 경고 삼각 / 스파크 / 펄스 파형 · 직선로 / 시케인 / 고속 커브 / 노면 경고 / 대치 화살표 / 충전 파형)와 셀 32×32 · 색+도상 이중 부호화 · 실루엣 층위 구분(재화 원형 vs 사각)을 지켜 기하 도형으로 그렸고, 도트 원도가 오면 같은 경로·파일명으로 교체된다(참조부 무변경). **[가안] 2건:** ①심볼 6색(D10 §2.3 기능 컬러 면제 대상이나 팔레트 에셋 미유입 — 판별 실측은 G-2 소관) ②**도상 경로 규약 = 파일명이 테이블 id**(D10 §8.1 에셋 대장의 요소 매핑 열이 아직 없다 — 대장 도입 시 교체). **비공개 릴은 도상 부재로 표현**한다 — 회전 중 심볼을 흘리려면 난수가 필요한데 RNG 6스트림은 게임 로직 전속이고(D12 §6), 표시물이 결과와 상관되면 그 자체가 누출 경로다. SEAL-E도 텍스트가 아니라 **텍스처 유무**로 판정하도록 옮겼다 | D10 §5.1·§5.2·§5.3·§5.5·§8.1·§9 · D09 §3.2·§3.4 · D05 §5.2 · 불변규칙 5 | 확정 (대조 시트 눈 검증) |
| 2026-08-13 | IMPL-076 | **커밋 차단 훅 신설** (`tools/hooks/pre-commit` · 설치 = `git config core.hooksPath tools/hooks`) — `project.godot` 의 도구 플러그인 등록을 커밋 시점에 **차단**한다. 자동 제거가 아니라 거부 | IMPL-070의 구조적 보강. 플러그인은 **엔진을 실행할 때마다** 등록을 되살리므로 사람이 매번 기억하는 방식은 실패한다(이 세션에서 3회 되살아났다). 훅이 커밋 내용을 몰래 고치면 그게 더 위험하므로 거부만 한다. **1차 구현의 결함을 실측으로 잡았다:** 섹션 헤더(`[autoload]`)만 검사했더니, 헤더를 지우고 등록 줄만 남긴 상태에서 그 줄이 **`[application]` 섹션으로 흘러들어 더 나쁜 상태가 됐는데 훅은 통과**했다(sed로 섹션을 지우다 실제로 발생). 등록 줄 자체를 섹션과 무관하게 잡도록 확대하고 재반증 — 양 형태 전건 차단 확인 | IMPL-070 · IMPL-015 · 검증 프로토콜 §4 | 확정 (돌연변이 2형태 실측) |
| 2026-08-13 | IMPL-077 | **MS-2 화면 범위 = D09 대장 23종 (사용자 확정)** — SYS-04(업적)·TUT-01(튜토리얼) 제외. 그 위에 **플로우 골격 5종 구현**: SYS-01 타이틀 · SYS-02 세이브 슬롯 · RACE-03 그랑프리 결산 · RUN-01 간이 정산 · SET-01 투어 결산 + 라우터·세션 층(`ui/flow/`) | 사용자 결정(2026-08-13). 업적은 데이터 테이블 자체가 없고 TUT-01은 D04 텍스트 풀 트랙에 의존한다. **전이 폐쇄성(D09 §2.3)을 구조로 이행했다** — 화면은 대상을 *요청*만 하고(`FlowScreen.go`) 교체는 라우터가 하며, 라우터의 경로 표에 없는 대상은 거부된다. 화면이 스스로 전이하면 플로우맵에 없는 경로가 화면 안에서 생긴다. **세션 주입 순서 결함 1건 실측·교정:** `_ready()` 가 `bind()` 보다 먼저 도는 탓에 화면이 "세션 없음"으로 판단해 자체 세션을 열었고, RACE-01이 독립 세션으로 GP를 돌아 결산 화면이 빈 엔진을 봤다 → 라우터가 `add_child` **이전에** 세션을 넣도록 교정. **[가안] 1건:** SET-01 ⑧의 다음 전이는 D09 §2.3이 HUB-01을 지정하나 개러지 미구현이라 타이틀로 돌린다 | D09 §2.3·§4.2·§5.1 · D09 별첨A §A-1·A-2·A-8·A-9·A-21 · D06 §4.2 · D15 §1.1 | 확정 (관통 주행 14컷 — 투어 결산 도달) |
| 2026-08-13 | IMPL-078 | **[보고] 섀시 컨디션이 GP 간 이월되지 않는다** — `RaceEngine.start_gp()` 가 매 GP `param_chassis_max` 로 리셋한다(실측). 그런데 D07 §1.2가 간이 정산에 **필드 정비**를 두고 D09 §4.3이 그 체증 비용 사전 표시를 필수로 규정한다 — 정비할 대상(이월된 손상)이 없으면 이 기능 전체가 무의미하다 | 로직 층 사안이라 여기서 고치지 않는다(킥오프 §4 "로직·데이터 재작성 금지" · 8880검사로 고정된 영역). RUN-01은 규격대로 3기능 카드와 **비용 사전 표시까지 세우고 실행만 잠갔다** — 화면이 서 있어야 이월이 결선될 때 바로 붙는다. **원격 레인 확인 필요: 섀시 이월이 설계 의도인가(D05 §8), 아니면 MS-1 단순화의 잔존인가** | D05 §8 · D07 §1.2·§3.3 · D09 §4.2·§4.3 · D13 별첨A §3.4 | 보고 (원격 레인 이월) |
| 2026-08-13 | IMPL-079 | **`OutgameState.field_repair_cost_next()` 신설 + G1 화이트리스트 등재** — 다음 회차 정비 비용의 조회 경로(표시 전용·체증 카운터 무변경). 기존 `field_repair_cost()` 는 동작 불변이며 공통 헬퍼로만 분리 | D07 §3.3·D09 §4.3이 "다음 회차 체증 비용 **사전 표시 필수**"를 규정하는데 조회 API가 없었다. 화면이 체증 공식을 다시 쓰면 로직이 헤드리스 테스트가 닿지 않는 곳에 생기므로 코어에 뒀다. **IMPL-060의 G1 가드가 즉시 발화했다** — "공개 메서드 집합 == 커밋된 기대 집합" 화이트리스트가 신규 메서드를 `TC_O_TEST_FAIL` 로 잡았고, 기대 집합에 의도적으로 등재해 통과시켰다. 블랙리스트였다면 조용히 지나갔을 변경이다 | D07 §3.3 · D09 §4.3 · IMPL-060 · D14 TC-O6 | 확정 (게이트 발화 확인) |
| 2026-08-13 | IMPL-080 | **도트 폰트 적용 — Galmuri Mono 11 (사용자 제공)** + 임포트 도트 규격(안티앨리어싱·힌팅·서브픽셀 전부 해제, Godot이 픽셀 폰트를 자동 감지). **[보고] 원도 11px이 D10 §5.7 본문 계열(8/9/10px)과 어긋난다** — 실측: 로그 슬롯 본문 가용 폭 **226px** 대 전각 22자 폭이 8px=176 · 9px=198 · 10px=220 · **11px=242(초과)**. D10 §5.7이 12px 단일 계열을 이 검증으로 탈락시키고 8/9/10을 확정한 계산과 정확히 같은 축이다 | 자수 22자·시각 줄 2는 **불변**이고(D14 §5.1), 움직일 수 있는 것은 폰트 원도와 로그 존 폭(D13 창구)뿐이다. 현재는 11px로 두어 22자가 2줄로 접히며, 이는 D09 §3.3 "최대 2줄" 안이라 규격 위반은 아니지만 D10 §5.7의 한 줄 전제와 어긋난다. **총괄/사용자 확인 필요: Galmuri9(본문 8/9/10 대응) + Galmuri14(대형 12/14/16 대응) 도입 여부.** 라이선스 표기(SIL OFL 계열) 동봉도 배포 전 확인 대상 | D10 §5.7·§2.2 · D09 §3.3·§6.6 · D12 §9.1 · D14 §5.1 | 보고 (확인 대기) |
| 2026-08-13 | IMPL-081 | **G-1·G-2·G-5 실기 게이트를 프로젝트 최후순위로 재배치 (사용자 판정)** — 평가자 5인·MP-L 실기기 조달을 뒤로 미룬다 | 사용자 결정(2026-08-13). **게이트 기준의 완화가 아니라 실행 시점의 재배치**임을 명확히 한다 — D14 §5 공통 원칙(단독 완화 금지)은 판정 기준에 걸리고, 마일스톤 배치는 D15 소관이며 사용자가 결정권자다. **잔여 구속:** D15 §1.1이 G-1·G-2·G-5 통과를 MS-2 완료 조건에 넣었으므로, 게이트 미실행 상태로 MS-2를 종결하려면 **D15 §1.1 완료 조건의 시점 조정을 총괄 경유로 발신**해야 한다. 그때까지 MS-2는 "게이트 외 조건 충족" 상태로 남는다 | D14 §5 · D15 §1.1·§1.2 | 확정 (사용자 판정) |
| 2026-08-13 | IMPL-082 | **폰트 2계열 확정 적용 — Galmuri9(본문 9px) + Galmuri14(대형 14px), 사용자 제공 4종 중** (IMPL-080 보고분 해소). 실측: Galmuri9 전각 22자 = **198px ⊂ 로그 슬롯 226px** — D10 §5.7 검증 ①(로그 한 줄) 성립. 전 폰트 임포트 도트 규격(AA·힌팅·서브픽셀 해제) | D10 §5.7 계열(본문 8/9/10 · 대형 12/14/16)의 각 100% 단에 대응시켰다. **[보고] 검증 ④ 불성립:** 신규 Galmuri 4종은 **비례폭**이라 '반각 = 전각의 정확히 ½' 성질이 없다(실측: 9px에서 반각 i=40 · W=119 — 전각½=99와 불일치. 구 GalmuriMono11만 성질 보유). 이 성질은 130% 번역 팽창 전제(D04 §5.6-7)와 고정폭 라벨 설계의 근거라, **라틴 표기 언어 추가 시점(G-4 트리거) 전 재검토 대상**이다. 한국어 전용인 MS-2에는 차단성 없음. Galmuri11/11-Bold·GalmuriMono11은 미사용 보존(용도 후보: 모노 = 수치 열 정렬). 라이선스(SIL OFL) 고지 파일 동봉은 배포 전 확인 | D10 §5.7 · D04 §5.6-7 · D14 G-4 · IMPL-080 | 확정 (실측) — 검증 ④는 보고 |
| 2026-08-13 | IMPL-083 | **HUB 8종 + COM-01 구현** (`godot/ui/hub/` 8화면 + `ui/com/confirm_dialog.gd` + `hub_screen.gd` 공통 베이스). SET-01 ⑧ → HUB-01 결선(IMPL-077 [가안] 해소) — 타이틀→개러지 귀환 전 플로우 15컷 관통 | 규격 이행 요점: ①공통 바 = 재화 2종 **아이콘+수치**(B-1 — '데이터' 단독 표기 금지의 이행) ②COM-01 초기 포커스 = **취소**(§A-23 오입력 방어) · 비가역 경고행 · 유상·비가역 전속(스킬 해금·시설 구매·스폰서 체결·오버홀 확정) ③튜닝 구매는 COM-01 비대상(재배분 존재 = 가역 — 모달 최소주의) ④기록실 라이벌 카드 = 관계 상태 명칭만, **전이 조건·게이지 절대 비노출**(D09 §4.5) ⑤아카이브 = 게이트 표시 요소 전무 ⑥HUB-08 개러지 앵커 상시 소등(G-M2 물리 분리 — 유일 진입 = SET-02 체인, SET-02 결선 시) ⑦미개방 = 소등+개방 조건 크루명. **잠금 유보 3건:** 정비 베이 실행(IMPL-078 섀시 이월)·튜닝 재배분·크루 영입 앵커(영입 이벤트 층) | D09 §1.4·§4.1~§4.6 · 별첨A §A-11~§A-18·§A-23 · D07 §2·§3·§4·§5 · D06 G-M2 · IMPL-053·060·077 | 확정 (캡처 9컷 + 관통 15컷) |
| 2026-08-13 | IMPL-084 | **RACE-02 듀얼 오버레이 구현** (`ui/race/duel_overlay.gd` — RACE-01 씬 내 오버레이, 라우터 비경유). 대치 표기 · 듀얼 릴 3기 · 부스트 ◆ 스택·투입 · 결과 프레임 내 표기 후 해제 | 규격 이행 요점: ①**화면 전환이 아니다** — D05 §3 상태 머신의 DUEL 삽입·복귀와 1:1이므로 RACE-01이 직접 띄우고 내린다(라우터에 넣으면 상태 머신에 없는 화면 전이가 생긴다) ②**릴 표시 배열 스왑** — 듀얼 중 RACE-01의 릴 참조를 오버레이 릴로 바꿔 공개·은닉·봉인 검사(SEAL-E)가 전부 같은 경로를 지나게 했다. 이중 구현이 없으므로 봉인 규칙이 갈라지지 않는다 ③확률·기대치 표기 0 (R1). **스왑이 만든 결함을 SEAL-E가 실제로 잡았다:** 복귀 시 은닉이 스왑 복원 **전에** 돌아 base 릴에 직전 심볼이 잔존(T1 비공개 실패 15건) → 은닉을 분기 뒤로 이동해 해소 — 검사 신설(IMPL-073)이 첫 실전에서 값을 냈다. **[가안] 3건:** 결과 표기 유지 0.6초(연출 시간 D13 부재) · 타이머 링의 듀얼 프레임 이전은 테두리로 대신(아트 단계) · **네임드 카 넘버 표기 생략**(ai_rivals 데이터에 넘버 열 부재 — 엔진 실측 0. 카 넘버 대장은 D03 소관 설정값이라 임의 기입 금지, 데이터 충전은 원격 레인 몫) | D09 §3.5 · 별첨A §A-7 · D05 §4.3 · IMPL-009·073 · R1 | 확정 (캡처 2컷 + SEAL-E 105검사) |
| 2026-08-13 | IMPL-085 | **SET-02 시즌 결산 구현 + 시즌 체인 결선** (`ui/settle/season_result_screen.gd` · SET-01 분기 → SET-02 → HUB-08(season_chain) → 오버홀 확정 → 다음 시즌 개막 → HUB-01). 필러 이름 매개 결함 1건 교정 | 규격 이행: ①**소프트 실패 문면** — 미달 시 "시즌 마감 — 다음 시즌으로 이어집니다"(사실 서술), "실패/GAME OVER" 계열 0 (D05 §9.5·D06 §6.3) ②에필로그 표기는 달성 시에만(사전 예고 없음 — D08 히든 정합) ③칭호 문면은 획득 구문(이전 함의 금지 — D09 §5.5) ④HUB-08 시즌 체인 중 뒤로 차단 — 확정 전에 새면 G-M2 재진입 불가로 오버홀을 놓친다(취소·재검토 자유는 화면 안에서 보장). **교정:** 듀얼 개시 로그의 필러 이름이 `No.{number} 머신` 그대로 노출 — 코어가 name_key만 넘기고 표기 매개를 안 실었다 → 이벤트에 number 동봉 + 표기 층이 참조 키 번역 시 같은 params로 치환. **V2 오탐 1건:** 접두 비교 리터럴(`"raceLog.duelWin"`)을 키 발행으로 검출 → 실존 키 전량 열거로 전환(우회가 아니라 "의도적 변경만 통과" 구조로 개선) | D09 §5.1 · 별첨A §A-22 · D05 §9.4~§10 · D06 G-M2 · D08 §2.1 | 확정 (캡처 + 관통 15컷 + 게이트 전건) |
| 2026-08-13 | IMPL-086 | **RUN-02 이벤트 노드 + NAR-01 VN + NAR-02 캘린더 비트 구현** (`ui/run/event_node_screen.gd` · `ui/nar/vn_screen.gd` — NAR-02는 NAR-01 내부 오버레이, 신규 슬롯 불신설). 세션에 EventService·NarrativeService 결선 + 세이브 직렬화 편입. 플로우: 새 커리어 → **시즌 오프닝 VN + 캘린더 공개** → RACE-01 / RACE-03 → **이벤트 판정(발생 시만 RUN-02)** → RUN-01 / HUB-05 아카이브 → **재생 모드** → 복귀 | 규격 이행 요점: ①RUN-02 무발생 시 화면 자체 비표출 — 판정을 RACE-03 이탈 시점에 세션이 하고 미발생이면 직행 ②보상 피드백 = 재화 아이콘+증감 문법(D09 §5.2), 연출 L0~L1 한정 ③NAR-01 스킵 상시 노출(절대 규격) · **화면에 전이 관련 표시 0**(형식 A 무관성 — 스킵이 페널티로 오독될 여지 차단) ④스킵해도 캘린더 공개는 건너뛰지 않는다 — 시즌 구조 정보는 서사가 아니라 판단 재료다 [가안] ⑤NAR-02 다이제틱 프레임("리그 발표") · 셔플 설명 텍스트 0 · 개막/최종 앵커 강조 ⑥아카이브 재생 = 동일 화면·종료 시 복귀·전이 재발화 없음(멱등은 코어 소관). **[가안] 3건:** VN 대사 실문안(D04 텍스트 풀 트랙 소관 — 자리 문면 키 1행, 유입 시 라인 목록만 교체) · VN 인스턴스 id 발급 규약(`vn_season_open_s{N}` — 실문안 대장 유입 시 교체) · 아카이브 표제의 슬롯 유형 표기 | D09 §5.3~§5.4 · 별첨A §A-10·§A-19·§A-20 · D08 §2.1~2.2·§7 · D01 G2 · D07 §5.5 | 확정 (관통 19컷 — 이벤트 3회 발생 80% 정합 · 캘린더 오버레이 캡처) |
| 2026-08-14 | IMPL-087 | **SYS-03 옵션 + SYS-05 일시정지 + COM-02 툴팁 구현 — D09 §8.1 대장 23종(사용자 확정 범위) 전량 성립** (`ui/flow/options_store.gd` · `ui/sys/options_screen.gd` · `ui/race/pause_overlay.gd` + 개러지 온보딩 팁 + `gui/timers/tooltip_delay_sec=0.3`) | 규격 이행 요점: ①옵션 15종(+O12) 탭 5 구조(D09 §6.4) · **즉시 반영 — 적용 버튼 없음**(소비부가 사용 시점마다 읽는 구조라 별도 적용 단계가 없다) · 기기별 저장 = `SaveManager.OPTIONS_PATH` 승계(프로필 밖 — D12 §7.1 구성 분리) ②**O4·O5·O6 실효 결선** — 릴 정지 속도(고속 배율·일괄 정지 = 간격 0, 정지 이벤트는 유지) · 타이머 배율/비활성(비활성 = 잔량이 흐르지 않고 링 소등·타임아웃 자연 부재·**모멘텀 조건 불성립**(confirm에 ratio 0 — D05 무개정 성립 구조)) · 수치 표시 토글 ③O5 비활성 고지행 = 사실 서술 상시 병기(§6.2 문면 규격) ④SYS-05 **개입 창 중 가림막 전면 불투명 + 타이머 정지 + 카운트인 중에도 가림막 유지**(카운트다운 동안 보드를 읽으면 우회 성립 — F2 보호) ⑤일시정지 내 옵션 = 오버레이 모드(라우터 전환 시 GP 상태 소실 방지) ⑥COM-02 = 호출 지연 0.3초 + 1회성 온보딩(재화 안내 — 기록은 옵션 파일·옵션 초기화로 재표시). **소비부 미존재 옵션(O1~O3·O7~O12·O13~O15)은 값 체계·저장만** — 각 채널(연출 감쇠·팔레트·오디오 버스·씬 패널) 결선 시점에 실효가 붙는다. **[가안] 3건:** 고속 배율 0.5·볼륨 스텝 10·O10 3단 문면(느림/보통/빠름) — D13 창구 전사 | D09 §3.7·§6.1~§6.4 · 별첨A §A-3·§A-5·§A-24 · D12 §7.1 · D05 §7.2 | 확정 (캡처 2컷 — 가림막·옵션 오버레이 / 게이트 전건) |
| 2026-08-15 | IMPL-088 | **export preset 자격증명 종단 확인 — A안 유지 확정 (IMPL-006·017 이월 종결).** 에디터 익스포트 다이얼로그에서 preset 2종(Windows Desktop·Android)을 실제 생성하고 자격증명 필드에 **카나리 문자열**(실제 자격증명 아님)을 입력해 저장 위치를 실측했다 | 실측 결과: ①커밋 대상 `export_presets.cfg` 본체 — **카나리 0건** ②`res://.godot/export_credentials.cfg` — 카나리 3건 전량(`codesign/password` · `keystore/release_user` · `keystore/release_password`) ③git check-ignore 실증 — `.gitignore:2 (.godot/)` 가 credentials 파일을 차단. **결정적 세부: 자격증명 필드는 본체에 빈 값으로도 존재하지 않는다** — 분리가 "빈 칸 저장"이 아니라 필드 자체가 credentials 파일 전속이므로, 실서명 값을 넣어도 본체에 흔적이 남을 구조가 아니다. C안(템플릿 커밋 + 빌드 시 주입) 후퇴 불요. **부기:** Android 4.7 익스포트 UI에는 debug user/password 항목이 없어(release 계열만 노출) release 쪽으로 확인했다 — 확인 목적(저장 위치)에는 등가. 카나리는 로컬 credentials 파일에만 남으며 커밋되지 않는다 — 실서명 착수 전 에디터에서 비우면 된다. preset 본체는 골격 상태로 커밋(포함/제외 규칙·피처 태그 = 혼입 0 이행 장치 — IMPL-006). Android 권한 전부 false — G-7 허용 목록 결선은 MS-5 광고 착수 시 | IMPL-006 · IMPL-017 · D12 §2.3 · D14 TC-P10·G-7 | **확정 (종단 실측 — 이월 종결)** |
| 2026-08-15 | IMPL-089 | **연출 등급 표현 채널 결선 (§3.4 몫 — IMPL-016·034의 잔여 화면 층 해소).** 트리거 후보 수집(확정 후 이벤트 키 → 트리거 매핑) → 배치 `resolve()` → 채널 실행: L1 = 게이지 섬광 + 약셰이크 / L2·L3 = 강셰이크 + 화면 플래시. **O1(셰이크)·O2(플래시) 감쇠 결선** — 표준 1.0 / 감소 0.5 / 끔 0 의 출력 단계 마스킹 | 규격 이행 요점: ①**옵션이 등급 판정에 관여하는 경로 없음**(D09 §6.3 K8 — 발동·상한 카운터는 코어 그대로, 화면은 출력만 마스킹) ②**트리거는 확정 후(T5) 이벤트에서만 수집** — 릴 정지 전 발화는 그 자체가 결과 상관 신호(봉인) ③셰이크는 **난수 없이 사인 합성** — RNG 스트림은 게임 로직 전속(D12 §6) ④정보 보존: 플래시·셰이크를 꺼도 로그·게이지 채널은 감쇠 대상이 아니다 ⑤타임아웃 자동 확정도 동일 채널(확정은 확정이다). 매핑: 듀얼 결판 3키 → trigger_duel_decision · 찬스 3매치(chanceDuel01) → trigger_chance_three_match · **벽 라이벌 격파 = 듀얼 승리 + 상대가 무대 wall_rival**(무대 1은 부재라 자연 미발동 — 무대 2+ 데이터 유입 시 그대로 결선). **미결선 잔여:** SFX 스팅·햅틱(음원·지속 시간 실물 부재 — IMPL-035) · L3 전용 일러스트 컷인(도트 CG 미유입 + 발동 조건이 무대 1 골격 밖). **[가안] 수치 7종** core_params 전사(감쇠 배율 0.5·셰이크 2/4px·0.25초·플래시 알파 0.35·0.25초·게이지 펄스 0.4초 — D13 §8.1 창구 대상, IMPL-072 계열). **교정 1건:** 듀얼 결과 프레임 표기의 {amount} 미치환 — 이벤트 params 로 번역하도록 수정 (캡처가 잡았다) | D09 §3.6·§6.3 · D12 §5.8·§6 · D08 §8.5 · D13 별첨A §8.1 · IMPL-034·035 | 확정 (L2 실측 캡처 — 플래시·셰이크·상한 소진) |
| 2026-08-15 | IMPL-090 | **섀시 GP 간 이월 결선 — IMPL-078 판정 종결: 매 GP 리셋은 MS-1 단순화 잔존이었다.** D05 §8이 명문으로 확정한다: *"회복: 그랑프리 간 자동 완전 회복은 없다 (런의 긴장 유지)"* — 이월이 설계 의도다. 구현: ①엔진 `chassis_carry_in` 주입 필드(미주입 = 최대치 개시로 기존 계약 불변, 주입 값은 [0,최대치] 절단) ②이월 값의 정본 = `OutgameState.chassis` — 세션이 GP 경계에서 양방향 복사(`begin_gp` 주입 / `close_gp` 회수) ③`begin_tour()` 무상 복원선(70, OV-S4 80 — 하한 동작: 위면 유지) ④**필드 정비 실적용** — 회당 상한 30 절단 + **복원선 절단**(D07 §3.3 명문 "70% 초과 구간의 유일 수단 = 전면 정비") + 회복 여지 0이면 무지불·무카운터 ⑤**전면 정비 완전 회복** + `full_repair_cost()` 조회 신설(표시 전용 — IMPL-079 구조, G1 화이트리스트 등재) ⑥`event_chassis_recover()` 신설 — D06 §3.4 경제 가드(회당 상한 초과 금지) 내장, RUN-02 보상 결선 ⑦**RunSession.serialize()에 outgame 층 편입** — 선재 공백: 재로드 시 재화·이월분이 증발하는 상태였다(인계#6과 합류) ⑧화면 실행 개방: RUN-01 필드 정비·HUB-02 전면 정비 (비활성 판정 = 회복 여지 × 지불 능력) | **[가안] 3건:** 새 커리어 섀시 초기값 = 최대치(D13 항목 부재 — 새 머신은 온전하다) · 이벤트 회복은 복원선 비절단(가드 조항이 회당 상한만 명시 — 최대치 절단만) · 구세이브(chassis 키 부재) 복원 = 최대치(그 세계는 매 GP 최대치 개시였으므로 충실값). **잠금 유지 2건:** RUN-01 덱·소모품 보충(3종 선택 UI = 씬 소관 — 주력 레인 몫) · 소모품 **사용**(T1 인게임 전속 D06 §3.5 R-B — 엔진 사용 API + RACE-01 UI 결선은 별도 트랙, P2·P3는 지속 효과라 엔진 상태 신설 필요). **돌연변이 3종 실측:** 주입 무시(→TC-C·TC-P 발화)·복원선 절단 제거(→TC-O 3건 발화)·직렬화 누락(→TC-P 발화) — 전건 검출. 검사 수 하한 상향: TC-O 252 · TC-C 933 · TC-P 127 | D05 §8 · D06 §3.3·§3.4 · D07 §3.3 · D09 §4.2·§4.3 · D13 별첨A §3.4 · IMPL-078·079·086 | 확정 (돌연변이 3종 실측) |
| 2026-08-15 | IMPL-091 | **[환경] 원격 헤드리스 서버에 git-lfs 부재 — 신규 에셋(아이콘 16종·폰트 5종)이 LFS 포인터 텍스트로 체크아웃되어 SEAL-E가 행(hang)했다.** 릴 아이콘 로드 실패 → 텍스처 영구 null → SEAL-E의 공개 판정(텍스처 유무)이 영원히 불성립 = 36분 무진행 실측. 조치: git-lfs 3.7.1 사용자 공간 설치(`~/.local/bin`) + `git lfs pull` → 스위트 10종 전건 통과 재확인(SEAL-E 98검사·14라운드) | 부수 결함 2건 동시 처치: ①LFS 복원 전 `--headless --import`가 포인터를 이미지로 임포트하며 `.import` 16종을 오염 — 전건 원복 후 재임포트 ②**커밋된 git 훅 5종이 전부 100644(비실행)라 Linux에서 조용히 무시되고 있었다** — pre-commit의 project.godot 오염 차단(IMPL-076)이 원격에서 무력한 상태. 실행 비트(100755)로 교정 커밋. Windows는 실행 비트를 보지 않으므로 주력 레인은 영향 없음. **원격 시작 절차에 `git lfs` 확인이 선행돼야 한다** — CLAUDE.md 공통 규칙에 1행 부기 | CLAUDE.md 공통 불변 규칙 · IMPL-070·076 · D12 §1.3 | 확정 (실측 — 행 재현·해소 확인) |
| 2026-08-15 | IMPL-092 | **네임드 카 넘버 전사 — IMPL-084 [가안] 종결 (총괄 회신 E-2 개방).** D03 결정 로그 #13-③ 확정 8인분을 `ai_rivals.csv` `number` 열로 전사(로렌츠 1·마로 2·디아스 18·볼코바 81·홀로웨이 5·비앙카 51·카이 24·주드 77 — **카이=ai_sherwood는 D03 §5 원문 "카이 셔우드(24)·실버트레일"로 대조 확인**, 이름만으로 매핑하지 않았다). `_build_entrants` 결선 + V1 스키마 `number:int` 등재. 화면(듀얼 대치·로그 배지)은 IMPL-084 구조대로 값 유입만으로 결선된다 | **부수 결함 1건 발견·처치:** 필러 넘버가 `20+i`(21~27) 순차라 **카이 24와 그리드에서 충돌**했다 — 표기 층 오식별 경로. 필러는 21부터 순차하되 실넘버(네임드+플레이어 13) 도달 시 건너뛴다 **[가안 — 필러 넘버 대역은 정본 미규정, 기점 21 승계]**. 검사 2종 신설: D03 전사 대조 8건 + 그리드 유일성 — 돌연변이(충돌 넘버링 복원) 실측 검출(dupes=1). TC-C 하한 933→942 | D03 §5 · D03 결정 로그 #13-③ · 총괄 회신 E-2 · IMPL-084·085 | 확정 (돌연변이 실측) |
| 2026-08-15 | IMPL-093 | **[가안] 정본화 이행 (총괄 회신 C항 후속) + 듀얼 대치 표기 완성.** ①C항 편입 12건의 코드 [가안] 태그 해제 — 근거를 D13 별첨A §8.1(v1.4)로 교체: 타이머 링 두께·점멸(timer_ring) · 베인 4상태 A/F/S(vane_waveform — 코드 값과 편입 값 일치 확인) · 듀얼 결과 표기 0.6초(race_screen). 릴 고속 0.5·볼륨 스텝 10·카운트인 3초·fx 7종은 core_params 기전사라 코드 태그 부재 — 데이터 값 무변경 ②듀얼 0.6초는 리터럴이었어서 **core_params `param_fx_duel_result_hold_sec` 전사 후 데이터 참조로 전환** — 동족 연출 시간(fx_*)과 창구 통일 ③**§A-7 E01 대치 표기 완성** — 네임드 상대를 "No.{넘버} {이름}"으로 표기(정본 문면 "No.1 로렌츠"). IMPL-092 데이터 유입으로 IMPL-084 [가안](표기 생략) 해소. 신규 키 `ui.duel.namedFormat` | 구조 판단 2건: ①위젯 상수(링·베인)는 상수 유지 — 총괄 후속 지시가 "태그 해제·참조 교체"이고, 위젯은 데이터 층 접근이 없는 봉인 잎 노드라 주입 배관 신설은 별도 판단 사안 ②로그 화자 축의 '네임드 카 넘버 배지'(D09 §3.3 결정 #4)는 **화자 필드(이벤트 스키마)·배지 도상과 한 묶음**이라 IMPL-071 화자 [가안]과 함께 이월 — 값(number)은 이벤트에 이미 실려 있어 결선 시 즉시 붙는다. E01 잔여 = 네임드 초상 미니(아트 실물 — 주력 몫) | 총괄 회신 C항·E-2 · D13 별첨A §8.1 v1.4 · D09 §3.5 · 별첨A §A-7 · IMPL-084·087·089·092 | 확정 (게이트 전건) |
| 2026-08-15 | IMPL-094 | **세션 직렬화 왕복 검증 확대 — 이벤트·서사 층 (IMPL-086 편입분의 검사 결선 · 인계 §3-6 종결).** TC-P에 **디스크(SaveManager) 경유** 왕복 검사 신설 — 인메모리 왕복이 아니라 JSON 직렬화가 만드는 타입 변형(int→float 등)까지 검사 범위에 들어온다. 보존 검증 축: 이벤트 쿨다운·판정/발생 카운터 · VN 열람/스킵 기록 · **전이 발화 기록**(유실 시 재로드가 마일스톤 VN을 재발화 — 아카이브 멱등 훼손) · VN 카운터 | **부수 결함 1건 교정:** `RunSession.restore()`가 events·narrative의 복원 실패 반환값을 무시했다 — 손상 payload가 조용히 초기 상태로 리셋되어 백업 복구 경로(TC-P8) 대신 "멀쩡히 로드된 척"이 되는 구조(season·outgame은 전파하는데 두 층만 비대칭). 실패 전파로 교정 + 손상 payload 검사 등재. 돌연변이 실측: 서사 직렬화 누락 주입 → 3건 발화. TC-P 하한 127→136 | D12 §7 · D14 TC-P · IMPL-086·090 · 검증 프로토콜 §4 | 확정 (돌연변이 실측) |
| 2026-08-15 | IMPL-095 | **V4 지역 변수 추적 확대 — IMPL-074 선재 구멍 폐쇄 (인계 §3-3 종결).** 함수 스코프 안에서 문자열 리터럴이 대입된 지역 변수(선언·재대입·`+=` 누적)가 표시 싱크에 닿으면 그 리터럴도 검사한다. 오탐 재발 방지 2종 동봉: ①딕셔너리 **키** 리터럴(`{"value": …}`)과 `.get("name_key", …)` 첫 인자 스트립 — 명명 매개·데이터 열 이름은 표시 문자열이 아니다(IMPL-074 실측 오탐 축, 첨자 스트립의 확장) ②밑줄 포함 순수 snake_case 토큰(`param_charge_hold_cost`)은 테이블 ID 문법(불변규칙 6 — 스트링 키와 별개 체계)으로 면제. 밑줄 없는 단일 낱말은 면제하지 않는다(표시 문자열일 수 있다). **한계 명문:** 싱크 라인 뒤의 재대입은 미추적(실코드에서 루프 역류 필요 — 주석 기록) | **확대가 실코드에서 진성 4건을 즉시 잡았다:** 듀얼 부스트 스택 ◆/◇·튜닝 게이지 ●/○ 가 지역 변수 경유 리터럴이었다 → 스트링 키 4종 신설(`ui.duel.boostFilled/boostEmpty`·`ui.tuningBench.stepFilled/stepEmpty` — `ui.race.costFormat` "◆{cost}" 키화 전례 준수)로 해소. 오탐 검출분 4건은 위 면제 2종의 근거 실측이 됐다(`.get("name_key")`·`param_*` 인자). 돌연변이 실측: 지역 변수 경유 `"???"` 주입 → 검출·원복. V4 검사 수 2267→2350 | D12 §4.2 V4·§8.1 · 불변규칙 6·7 · IMPL-027·074 | 확정 (돌연변이 실측 + 진성 4건 교정) |
| 2026-08-15 | IMPL-096 | **데이터 주도 검증 확대 — 신규 소비부 (인계 §3-5 종결).** 픽스처 스위트에 소비부 검사 추가: ①`field_repair_cost_next` = 현재 × 데이터 체증(픽스처 2.0 — 리터럴 1.5 판별)·카운터 무변경 ②무상 복원선(픽스처 40 — 리터럴 70 판별): begin_tour 하한 + 복원선 도달 후 무동작 ③이벤트 회복 상한 = 데이터 필드 정비 상한(픽스처 7 — 리터럴 30 판별) ④이월 절단 = 데이터 최대치(픽스처 80 — 리터럴 100 판별) ⑤옵션 볼륨 기본값·스텝(픽스처 60·25 — 리터럴 80·10 판별). 픽스처 core_params에 opt 2행 추가·복원선 70→40 변경 | **기기 옵션 파일 보호:** 옵션 기본값 검사는 `user://options.json`이 있으면 기본 경로가 가려지므로 **백업 후 원복**한다 — 주력 머신에서 스위트를 돌려도 기기 설정이 지워지지 않는다. **범위 명문:** fx 파라미터(param_fx_*)·릴 고속·타이머 배율은 소비부가 화면 실행 층(race_screen)이라 로직 스위트 사정권 밖 — 검증 프로토콜 §5 남은 각도에 "실행 층 픽스처 주입" 항목으로 등재. 돌연변이 실측: 복원선 리터럴 70 주입 → 2건 발화. 검사 수 40→55 (하한 동반 상향) | 불변규칙 2 · 검증 프로토콜 §5 · IMPL-079·087·090 | 확정 (돌연변이 실측) |
| 2026-08-15 | IMPL-097 | **TL-2 기능 체크리스트 기계 생성기 구축 (D14 §2.2 이행 — 인계 §3-1 종결).** `godot/tests/gen_tl2_checklist.gd` → `docs/qa/TL2_기능체크리스트.md` (화면 23종 × 5축 — 산출물 커밋·수기 편집 금지·재생성 = 같은 명령). 축별 기계 실측: ①진입/이탈 = ROUTES + `go()` 호출 그래프 역참조(HUB 공통 베이스 뒤로 가기 자동 포함) — **이탈 대상이 경로 표 밖이면 생성 실패** ②표시 요소 = 씬 노드 실측(표시·입력 타입 한정, 오버레이는 호스트 씬 서브트리 분할 — RACE-01 인벤토리에서 DuelOverlay·PauseOverlay 제외) ③옵션 반영 = param_opt_*·param_fx_*·O# 소비 실측 ④T-1 = 사용 키 문면의 '티어' 단독 표기 기계 검사(위반 = 생성 실패) ⑤금지 구역 = 광고 계층 어휘 스캔(검출 = 생성 실패 — D09-2 §8.2 승계) | 화면 목록은 하드코딩하지 않고 **D09 별첨A 절 헤더를 파싱**한다(정본 목록 = 별첨A — D14 §2.2 문면 그대로) · 범위 제외 2종(SYS-04·TUT-01)은 IMPL-077 근거 명기 후 '생성 제외'로 표기. 체크박스 = 실행 단계의 별첨A 명세 눈 대조 몫(D14 §2.2 "실본은 실행 단계 산출물") — 기계 판정 가능 축은 생성 시점 판정 병기, 판정 실패 시 산출물 자체가 나오지 않는다(거짓 초록 차단). 라우터 비경유 5종(RACE-02·SYS-05·NAR-02·COM-01·COM-02)은 소재 매핑 상수로 처리 | D14 §2.2 · D09 별첨A v1.2 · D09-2 §8.2 · IMPL-077 | 확정 (생성 실측 — 23종·기계 FAIL 0) |
| 2026-08-15 | IMPL-098 | **이미지 에셋 제작 사양서 신설** (`docs/assets/이미지에셋_제작사양서_v1_0.md`) — D10 §8 리소스 목록을 제작 발주 단위로 전개: 픽셀 실치수 + 바리에이션 + AI 생성 프롬프트(국·영). 수량·확정 규격은 D10 문면 그대로(총계 불변), 정본 미확정분만 [가안] 12건 신규 제안(사양서 §12 대장) — 핵심: 머신 셀 측면 96×48·추격 64×64 / 씬 컷 제작 원도 408×100(블리드 포함, 실기 릴 존 폭 384 실측 근거) / 9패치·글리프·게이지 원도 / 캐릭터 13인 외형 전부(정본이 의도적으로 미정의 — 신규 창작 영역, 사용자 확정 필요) | 실치수 근거 = 정본 3원 합성: D10 확정값(스탠딩 180×240·심볼 32×32·씬 컷 높이 100·캔버스) + D12 §9(블리드 ±24 기확정·개러지 세로 360×780) + 실기 실측(race_screen.tscn — B존 60%=384px·릴 64×62·게이지 90×6/70×8, IMPL-071 산물). **보고 2건:** ①노스윈드 팀 컬러 4색이 정본 미정(D10 §2.3 슬롯만 예약) — 팔레트 실물 2식 제작이 전 에셋의 선행 과제 ②앱 아이콘·부트 스플래시·스토어 라이브러리 세트가 정본 미계상(사양서 §11) — 총괄 확인 대상 | D10 §2~§8 · D12 §9 · D15 §2.2·§2.6 · IMPL-021·071·075·082 | 확정 (문서 신설) — 보고 2건 IMPL-099 해소 |
| 2026-08-15 | IMPL-099 | **이미지 에셋 제작 사양서 v1.0 → v1.1 — 총괄 검토·[가안] 12건 전량 판정 종결 (IMPL-098 보고 2건 해소).** 원문 대조 전건: D10 §8.2 수량·씬 컷 프레임 58·도상 대장·D03 13인 확정 자산·팀 컬러·카 넘버 8인·D08 별첨A 무대 5종·실기 실측(릴 64×62·게이지 90×6/70×8·씬 패널 100·릴 존 60%=384) — **결함 0.** 교정 5건: ①§10 타이머 링 낡은 [가안] — D13 별첨A v1.4 §8.1 기확정과 시차(작성과 D13 편입이 같은 날) ②§11 계상 판정 — Steam 캡슐 세트·피처 그래픽은 **D15 §2.2-5·§3.1 기계상**(과대 미계상 주장 교정), 진성 미계상 = 앱 아이콘 3계·부트 스플래시만 ③CG-02/05/06 주인공 차폐 토큰 부재(§2.4 "프롬프트 반영 의무" 자기 위반) — 헬멧 토큰 삽입 ④팩토리 에이스 3종 카 넘버(1·18·5) 프롬프트 누락 — 세컨드 차분(2·81·51)과 비대칭 해소 ⑤프리픽스 연대 90년대 후반→중후반(D10 1994~97 정합) | **[가안] 판정:** 총괄 확정 9건(얼굴 레이어 112×112·머신 셀 96×48/64×64·씬 컷 원도 408×100·9패치 3계·글리프 3계·펄스 차지 64×12·시스템 아이콘 구성·키 비주얼·VN 배경 — 산술·정합 전건 성립) + **사용자 확정 3건**(캐릭터 외형 13인 일괄 승인 / **노스윈드 팀 컬러 = 스틸 그레이×아이스 시안** — 제안값 오프화이트에서 변경 채택, 악시온 백×청과 명도 축 분리·주인공 헬멧 오프화이트는 독립 유지 / 플랫폼 부속 계상 승인 + 아이콘 모티프 = **13 데칼 헬멧** — D03 §1.1 브랜드 기호 문면 정합, 16px 단순화 파생 부속). 확정의 층위 = 구현 문서 확정 기준값(구조 불변·값만 조정) — 정본 편입 경로 = D10 차기 개정 시 §8 실치수 부속(비차단). 파일명 v1_1 개칭 | D10 전문 · D03 §1~§3 · D04 §7.1 · D08 별첨A · D15 §2.2·§3.1 · D13 별첨A v1.4 §8.1 · race_screen.tscn · IMPL-093·098 | 확정 (원문 대조 전건) |
| 2026-08-15 | IMPL-103 | **MS-2 완료 선언 총괄 집행 — D15 §1.1 실일자 `2026-08-15` 기입 (마감 발신문 A~C 종결).** ①**게이트 독립 재현 실측** — 발신 스냅샷을 인용 접수하지 않고 주력 머신에서 전 게이트 재실행: `COMPILE_GATE_PASS scripts=78` · `VALIDATORS_PASS warnings=0`(V4 2396·MIX0 3448·ARCH 61647) · `TESTS_PASS suites=10` · **TC-C 942·TC-O 257·TC-P 136 정확 일치** → 완료 조건(TC-C·TC-O + 세이브 TC-P8·P9) 충족을 총괄 자체 실측으로 확인 ②**판번 무증가 판정** — §1.1 본문이 기입란을 "실제 날짜는 실행 시 기입한다(D14 실측 기입란 공란 내장 전례)"로 규정하므로 공란 충전은 개정이 아님. D15 v1.1 유지·§0.8.7 7지점 무접촉 ③**P-2 ㉒ 신설** — D09 별첨A §A-14 예시 문면('첫 투어 우승') 대 D07 §4.3 확정('첫 포디움') 불일치. 구현의 D07 우선 처리(IMPL-101 DF-04) **적법 판정** — 별첨A 해당 문면은 와이어프레임 삽화의 예시 라벨이고 기능 정본은 D07 ④마스터플랜 v2.31 발행·포인터 동기(CLAUDE.md·README) | **총괄 발견 2건:** ①**MS-1 기입란 공란 존치** — 완료 선언 기록 부재(impl_log는 판정 절차만 언급·일자 없음) → 임의 기입 금지, 사용자 확인 사안 등재 ②**총괄 자기 결함 3호 자기 교정** — v2.30이 P-2 ⑱⑲⑳㉑을 H-3 행에만 등재하고 P-2 원장 행(`17건/잔여 12건`)을 미동기. **v2.28 경미④와 동형 재발** → 원장 계수 22건/잔여 17건 교정 + ⑱~㉒ 항목 전건 등재 + §0.8.7 검사에 'H-3↔P-2 양 지점 동기' 지점 추가. 부수 실측: GDAI autoload 주입 상태에서도 헤드리스 게이트 전건 통과 | D15 §1.1 · D14 §5.8 · D07 §4.3 · D09 별첨A §A-14 · 마스터플랜 §0.8.2·§0.8.7·§4.5 · IMPL-097·101·102 | 확정 (게이트 재현 실측) |
| 2026-08-15 | IMPL-104 | **MS-1 완료 선언 집행 (`2026-08-11`) + 그 검토가 적발한 CI 회귀 즉시 교정.** ①**실일자 판정** — 완료 조건 2항의 물리적 성립은 2026-08-10(`e834f6c` 코어 루프 관통 빌드·`546c8a7` CI 래퍼)이나 그 빌드는 **C-2 정산 순서 가드 부재** 상태였고 익일 `25b1d55`(IMPL-019)로 교정됐다 — 불변규칙 3 직결 결함이 남은 날을 완료일로 삼지 않는다. 재검증 3항(`6404693`)·MS-2 착수(`3d675ef`)도 08-11로 **마일스톤 경계 일치**. 판번 무증가(IMPL-103 판정 승계) ②**[진성 결함] CI 회귀 — TL-1 상시 결속 위반:** `validate.yml`이 `actions/checkout@v4`를 `lfs: true` 없이 사용(기본 false). 워크플로 최종 수정 2026-08-11 → **첫 LFS 에셋 유입 2026-08-13(`d732448`)** 이후 CI는 PNG·TTF를 포인터로 체크아웃해 왔다 = **IMPL-091 실측 조건과 동일**(`--import` `.import` 오염 + SEAL-E 텍스처 영구 null 행 36분). 워크플로가 두 단계를 그대로 실행하므로 8/13 이후 빌드 게이트가 사실상 비활성이었을 개연 높음 — 양 레인의 로컬 게이트 실행이 은폐 요인 | **교정:** `lfs: true` + **LFS 포인터 잔존 가드 단계 신설** — `lfs:true`가 조용히 실패해도(쿼터·네트워크·러너 LFS 부재) 행 대신 수초 내 명시적 실패. 총괄 로컬 실측: `LFS_GUARD_PASS files=21`(exit 0) · 포인터 주입 시 파일명 병기 검출(exit 1) — 양방향. **가드 자체 결함 1건 출하 전 자기 적발:** 초안 `grep -q … && echo "$f"`는 Actions의 `bash -e` 하에서 **정상 경로가 비0을 반환**해 명령 치환 대입이 errexit에 걸린다(= 가드가 초록 빌드를 죽인다). `bash -e` 재현으로 exit 1 실측 → `if` 형태 교정 → 양 경로 재실측. **로컬 셸은 errexit이 꺼져 있어 1차 검증을 통과시켰다** — CI 스크립트는 CI의 셸 조건으로 검증해야 한다(주석 명문화). **MS-1 판정 무영향** — MS-1 시점 LFS 추적 실물 0건이라 당시 CI는 실제 성립, 8/13 회귀는 MS-2 기간 별건. **단정 유보 1건:** 주력 머신에 `gh` CLI 부재로 Actions 실행 이력 직접 확인 불가 — 구조·선례 일치 기반 고확신 판정이며 실증은 CLI 가용 시 후속. **교훈:** 게이트를 로컬로만 돌리면 CI 전용 경로(체크아웃·러너 환경)의 회귀는 영구 미발견된다 — 에셋 파이프라인 변경 시 CI 정의도 동시 점검 대상 | D15 §1.1·§1.2 · 불변규칙 3·7 · IMPL-019·091·103 · `.gitattributes` | 확정 (가드 로컬 실측 — 정상·돌연변이 양방향) |
| 2026-08-15 | IMPL-108 | **IMPL-104 CI 회귀 판정의 단정 유보 해소 — Actions 실행 이력 실측 확증 (`gh` CLI 설치 후).** 추정이 아니라 사실이었다. **암전 구간 = 2026-08-13 ~ 08-15 · cancelled 22런 연속, 전건 `6h0m` 타임아웃 킬**(`The job has exceeded the maximum execution time of 6h0m0s`). 그 앞 08-10~08-12는 **success 24건**으로, 경계가 **첫 LFS 에셋 커밋(`d732448`, 08-13)과 정확히 일치**한다. **행 발생 단계도 예측과 일치** — 실패 런 단계 분해에서 `Validators`·`Import cache`는 ✓, **`Compile gate + logic tests`(→ `test_seal_ui.gd`)에서 정지**(`Import cache` ✓는 `\|\| true` 때문이지 정상이어서가 아니다). **교정 효과:** 교정 커밋(`9809fa5`) 런에서 `LFS_GUARD_PASS files=21` → `VALIDATORS_PASS warnings=0` → `COMPILE_GATE_PASS scripts=78` → `TESTS_PASS suites=10`이 **35초** 만에 전건 통과. 후속 2런(IMPL-106·107)도 36초 success | **함의:** TL-1이 **3일간 단 한 번도 판정을 내지 못했다**(불변규칙 7 취지 무력). 그 구간에 머지된 커밋은 전부 로컬 게이트 통과만을 근거로 들어왔다 — 다행히 양 레인이 로컬 게이트를 성실히 돌려 실결함 유입은 확인되지 않았으나, **CI가 초록이어서가 아니라 사람이 대신 돌려서 막힌 것**이다. 소모 러너 시간 ≈ 132시간. **재발 방지 관측점:** 정상 런은 35초다 — 런 시간이 분 단위로 뛰면 그 자체가 신호다. `gh run list`로 결론 분포를 주기 점검하면 '조용한 암전'이 다시 3일 가지 않는다 | IMPL-091·104 · 불변규칙 7 · D15 §1.2(TL-1 상시 결속) · `.github/workflows/validate.yml` | 확정 (Actions 이력 실측 — 22런 전수) |
| 2026-08-16 | IMPL-109 | **MS-3 착수 프롬프트 동기 — 원격 레인 착수 전 stale 3건 교정** (`prompts/MS3_kickoff.md`). ①**결정 로그 범위** IMPL-001~101 → **001~108** + 필독 범위에 총괄 집행분·CI 교정(103·104·108)·에셋 대장(105·106) 편입 ②**마일스톤 문서 상태** "D15 실일자 기입 = 총괄 집행 대기" → **집행 종결**(MS-1 2026-08-11·MS-2 2026-08-15·마스터플랜 v2.31·P-2 ㉒) + 총괄 미처리 1건(UI 폰트 계열 — 원격 무관) 명시 ③**CI 암전 경보 신설** — 원격이 IMPL-091에서 실측한 그 행이 CI에서 22런 재현됐다는 사실과 관측점(정상 런 35초)을 착수 전 필독으로 배치 | **부수 교정 1건 — 게이트 기대치 라벨 오류:** §0-5가 `EVENTS 7019 · SEASON 471`을 '하한'으로 표기했으나 **실측 결과 그 둘은 실측값이고 하한은 6000·450**이었다(`run_tests.sh` 대조). 하한과 실측을 분리 표기하고 **하한의 정본 = `run_tests.sh` 단일**임을 명문화 — 문서 전사값이 어긋나면 스크립트가 맞다. SEAL도 하한 84 대 실측 98로 분리. 오표기 방치 시 "기대치와 다르면 원인부터"라는 §0-5 지시 자체가 오작동한다(여유분을 이상 징후로 오독) | `prompts/MS3_kickoff.md` · `tools/validators/run_tests.sh` · IMPL-102·104·108 | 확정 (검사 수 실측 대조) |
| 2026-08-16 | IMPL-116 | **[총괄 판정] UI 폰트 계열 배정 (IMPL-107 발신) — ② 해당하지 않음·현행 유지·정본 개정 0** (`docs/handoff/UI폰트계열_총괄회신.md`). D10 §5.7 대형·VN 계열의 '헤더'에 HUB 상단 공통 바는 **불포함**. 근거 4건: ①**명명 대조** — D09 정본 3개소(본문 §4.1 표 행·별첨A 공통 상속 문면·와이어프레임)가 일관되게 '**상단 공통 바**'이며 '헤더'로 부른 적이 없다 ②**'헤더' 실용례** — D09 계열에서 헤더 = 화면 내부 섹션 제목("미개방 스킬 티어 헤더"·"달성률 요약 헤더")이지 상시 바가 아니다 ③**계열 성격** — 대형·VN의 나머지 3종(VN 대사창·선택지·결산)이 전부 '집중해 읽는 서사·결산 텍스트'로, 상시 HUD는 이 계열이 아니다 ④본문 계열 적용 대상의 '**라벨**'에 상시 바 수치가 자연스럽게 든다. **귀결: 현행 9px는 정본 위반이 아니었다** — 발신 §E 판정① 처리분 불집행. 안 2(본문 base 9→10)는 O7 3단 소진으로 배제(발신 분석에 동의) | **총괄 발견 — 발신문의 전제가 뒤집혔다:** 발신문은 '폰트 = 조정 레버 / 아이콘 = 확정값' 구도였으나 **정본은 정반대**다. ①D10 §5.2 재화·상태 아이콘에 **크기 규정 부재**(실루엣 층위만 확정) ②D10 §5.1의 32×32는 원문이 "**릴 존 규격 내**"로 심볼에 귀속 ③**D13 별첨A §10 전사가 못박는다** — `D10 §5.1 \| **심볼** 셀 원도 \| 32×32px(표시 96×96) \| 채택`. 즉 **폰트만 D13 전사 확정값이고 재화 아이콘 크기는 정본 무규정**(현행 32px = 사양서 v1.1 §6.1 '전 아이콘 준용' = 구현 문서 층·조정 창구 실기). 시각 균형 조정의 정본 비용은 아이콘 측이 압도적으로 싸다 — 전용 소형 원도(16×16, 잉크 ~13px) 대 본문 9px ≈ **1.5:1**로 대형 계열 안(2.1:1)보다 균형이 낫고 D10·D13 무접촉. **사용자 배제(안 3)의 전제도 달라진다** — 배제 대상은 '32×32 강제 축소(÷2 다운샘플·디테일 절반 소실)'였고 전용 원도는 그 손실이 없다. 아이콘 16종이 전량 플레이스홀더라 **실물 발주 전인 지금이 규격 변경 최저 비용 시점** → 사용자 재확인 대상으로 상신(총괄이 뒤집지 않는다). **문면 교정 1건 이월:** 사양서 §6.1의 '전 아이콘 준용' 귀속 표기를 '심볼 = D10 확정 / 그 외 = 구현 준용'으로 분리 — 값 무변경, 바뀌는 것은 조정 권한 소재. 트리거 = 사양서 차기 개정(아이콘 규격 결정 시 동시 집행) | D10 §5.1·§5.2·§5.7 · D13 별첨A §10 · D09 §4.1·§5.2 · D09 별첨A §A-14 · 불변규칙 2·9 · IMPL-099·107 | 확정 (원문 대조 4건) |
| 2026-08-16 | IMPL-117 | **공통 바 재화 수치 = 대형 계열 결선 — 사용자 폰트 방향 판정에 따른 D10 v1.1 발행 + 구현** (IMPL-116 후속). 사용자가 실기 확인 후 폰트 조정을 채택(권고안 = 재화 전용 소형 원도, **작업 볼륨 증가 수용**). 판정 ②의 귀결상 **무개정 우회가 불가**하므로 정본 개정을 정식 집행: **D10 v1.0 → v1.1** §5.7 적용 대상에 '아웃게임 상단 공통 바의 재화 수치' 편입(결정 #8·판번 7지점 동기·의존 행 6건 현행화·파일명 개칭) + **마스터플랜 v2.32**(§1 D10 행 동기·**P-2 ㉓ 신설** = 하류 6문서 의존 행 'D10 v1.0' 표기). **범위 최소화** — 재화 2종 수치 한정, 시즌·투어·다음 일정은 본문 계열 유지(§A-3 계열 성격 문제의 최소화 조치). **D13 신설·변경 0**(14 = 기확정 단) | **구현이 발신 예상보다 좁게 끝났다:** 발신 §E는 "HUB 8화면 .tscn + event_node_screen + 코드 2개소"를 예상했으나 실제는 **`hub_screen.gd` 1개소**. 공통 바를 이미 이 공통 베이스가 채우므로 `param_font_size_head` 런타임 오버라이드 1회로 **상속 전 화면에 자동 적용**되고, 씬에 14를 굳히지 않아 값의 창구가 D13에 남는다(불변규칙 2 · 전례 `race_screen.gd:99`). RUN-02 보상 행은 공통 바도 '결산'도 아니라(결산 = RACE-03) 범위 제외. **실측 검증(1920×1080 픽셀 측정):** 아이콘 잉크 27px 무변경 · 재화 수치 8 → **12.7px** · **비율 3.4:1 → 2.13:1**(예측 2.1 일치) · **진행 표기 8px 유지 = 범위 한정 실증**. 게이트 `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=10` · HUB 10컷 재캡처 | D10 v1.1 §5.7 결정 #8 · 마스터플랜 v2.32 · D09 §4.1 · 불변규칙 2 · IMPL-107·116 | 확정 (캡처 픽셀 실측 + 게이트 전건) |
| 2026-08-16 | IMPL-118 | **[보고·미조치] `godot/tests/tl5_runner.gd.uid` 미커밋 — 형상 관리 규칙 위반 1건.** CLAUDE.md 공통 불변 규칙이 `*.uid`를 "절대 빼먹지 말 것"으로 명문화하는데, **테스트 20종 중 19종은 `.uid` 커밋 · `tl5_runner.gd`만 누락**(원격 레인 IMPL-114·115 산출물). 주력 머신에서 임포트 시 로컬 생성되어 미추적 파일로 드러났다 | **의도적 미조치 — 원격 레인이 활성 작업 중이기 때문.** 지금 커밋하면 원격이 이미 보유했을 다른 uid와 충돌해 다음 `git pull`이 "untracked working tree file would be overwritten"로 실패한다. **원격 레인이 자기 로컬 `.uid`를 커밋하는 것이 무충돌 경로**다(그쪽엔 이미 있으므로). 실해 위험은 낮다 — 해당 스크립트는 `--script` 경로 실행 전용이라 `uid://` 참조 소비처가 없다. 다만 규칙 위반 상태이므로 원격 레인 몫으로 등재 | CLAUDE.md 공통 불변 규칙(`*.uid`) · IMPL-114·115 | 보고 (원격 레인 조치 대기) |
| 2026-08-15 | IMPL-100 | **RUN-01·HUB-02 고스트 게이지 결선 + 구폰트 잔존 정규화 (원격 회신 §2-1·§2-2 이행 · TL-2 1회전 DF-01~03 처치).** ①공용 위젯 `ui/com/ghost_gauge.gd` — 현재 채움+도달값 고스트 병기, **봉인 잎 노드**(데이터 층 접근 없음 — IMPL-093 구조 판단 승계), 복원선 눈금은 그리지 않음(§A-12 "자동 처리 고지행으로만" 명문) ②코어 `field_repair_preview()` 신설 — **field_repair와 절단 계산을 공유**(사설 `_field_repair_recoverable` 단일 원천)해 표시와 실행이 갈라질 수 없다. 표시 전용(IMPL-079 구조·G1 화이트리스트 등재) ③RUN-01 §A-9 E02 "섀시 {now}% → {after}%"+게이지 / HUB-02 §A-12 총비용(`full_repair_cost`)+완전 회복 고스트 ④초기 골격 5씬(SYS-01·02·RACE-03·SET-01·RUN-01) 구폰트 11/22px → 9/14px 정규화(타이틀만 28=14×2 정수 배율 — D12 §9.1) | **패리티 검사 5건 신설** — 프리뷰=실행 도달값을 상태 걸음마다 대조하되, 40→70 지점은 상한·복원선 절단이 동치(40+30=70)라 못 가르므로 **두 절단이 갈라지는 55→70 지점을 추가**(비공허성 확보). 돌연변이(프리뷰의 복원선 무시) 2건 실측 검출. TC-O 하한 252→257. **[가안] 1건:** 백분율 분모 = 섀시 최대치 — D09 문면("62%")이 분모를 규정하지 않아 게이지 비율과 일치시켰다(T4 최대치 확장 시에도 문면·게이지 일관) | D07 §3.3 · D09 §4.2·§4.3 · 별첨A §A-9·§A-12 · D12 §9.1 · IMPL-079·082·090·093 · 원격 회신 §2 | 확정 (돌연변이 실측 + 캡처 눈 대조) |
| 2026-08-15 | IMPL-101 | **TL-2 기능 테스트 1회전 실행 — D14 §2.2 실본 산출** (`docs/qa/TL2_실행기록_2026-08-15.md` 신설). 화면 23종 전수를 실렌더 캡처 29컷으로 별첨A 명세와 눈 대조 — 관통 11컷(타이틀→개러지) + HUB 10컷 + 오버레이·잔여 8컷(일회성 세션 주입 하네스). 결함 4건 등록·**전건 당회 처치**(DF-01 구폰트 5씬 · DF-02 RUN-01 E02 게이지 부재 · DF-03 HUB-02 총비용·게이지 부재 · DF-04 HUB-04 티어 개방 조건 문면 누락). 잔여 6건은 결함 아님으로 근거 명기 등재(NAR-02 1행 = 무대 2~5 데이터 대기 · HUB-08 후보 추첨 = 원격 로직 트랙 · RUN-01 E03/E04 = 소모품 사용 트랙 묶음 · 초상·팀 카드 = 아트 대기). 부수: capture_hub_shots에 손상 섀시 컷 추가(고스트 게이지는 손상이 있어야 보인다) | DF-04 판단: 조건 문면 정본 = **D07 §4.3**('첫 포디움'/'첫 그랑프리 우승') — 별첨A §A-14 와이어프레임 예시("개방: 첫 투어 우승")가 D07 확정 트리거와 불일치하며, 코어 판정(`skill_tier_open`)도 D07과 일치하므로 기능 정본을 따랐다. **[보고] 별첨A §A-14 예시 문면의 D07 불일치 — 총괄 발신 대기** (문서 수정은 D09 트랙 소관, 구현 측 임의 수정 금지). 봉인·섀시 이월의 표시 층 관통(58→70 복원선 절단 · E11 개시값 58 · 리타이어 분기)을 실측으로 확인 | D14 §2.2·§5.2 (SV/DF 체계) · D09 별첨A 전문 · D07 §4.3 · IMPL-077·090·097·100 | 확정 (실측 1회전) — 보고 1건 총괄 대기 |
| 2026-08-15 | IMPL-102 | **MS-2 마감 (사용자 선언 2026-08-15) — 구현 리포 종결 처리 3건.** ①완료 선언 집행 요청 발신 (`docs/handoff/MS2_마감_총괄발신.md` — §A: D15 §1.1 실일자 기입 요청·충족 근거 스냅샷 / §B: 별첨A §A-14 문면 교정(IMPL-101 DF-04 이월) / §C: 참고 보고) ②**MS-3 착수 프롬프트 발행** (`prompts/MS3_kickoff.md` — D15 §1.1 MS-3 완료 조건을 트랙 11건으로 분해·레인 배정. 양 레인 공용: STEP 0 환경 감지로 자기 몫 선택. 착수 순서 = 원격 T1 무대 2~5부터 — 콘텐츠 완결이 크리티컬 패스) ③CLAUDE.md 현재 마일스톤 행 MS-2 → MS-3 갱신 | 마감 시점 상태: 게이트 전건 (`COMPILE_GATE_PASS scripts=78` · `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=10`) · 화면 23종 + TL-2 1회전 실측 · 총괄 회신 §A 판정("완료 조건 충족 상태 — 완료 선언은 사용자 확인 사안")의 사용자 측 해소. **D15 실일자 기입은 총괄 집행 대기이며 MS-3 구현 착수를 차단하지 않는다** (문서 개정 = 총괄 소관, 구현 진행 = 조건 결속형 — D15 §1.1 구조 자체가 날짜 비의존) | D15 §1.1 · 총괄 회신문 §A · IMPL-077·081·100·101 | 확정 (사용자 선언) |
| 2026-08-11 | IMPL-026 | **V4 비표시 경로 제외 선언** — `v4_exempt_dirs: ["godot/tests"]`. 테스트 하네스의 진단 라벨은 플레이어에게 렌더되지 않으므로 '표시 문자열'이 아니다 | 우회·완화가 아니라 **범위 선언**임을 실측으로 담보: 제외 적용 후 `godot/ui/`·`godot/core/`에 한글 리터럴을 주입해 **양쪽 모두 V4 FAIL로 검출**됨을 확인. 제외 대상은 설정에 명시된 경로뿐이며 코드에 하드코딩하지 않았다 | D12 §8.1 · 불변규칙 6·7 · IMPL-002 | 확정 (실측) |

---

### IMPL-024 부속 — 돌연변이 주입 실측표 (2026-08-11)

MS2 킥오프 §1-4가 요구한 **"통과했는가가 아니라 틀리게 만들면 실패하는가"** 판정. 원본 무접촉(사본 전용 하네스).

| # | 주입한 결함 | 결과 | 검출 지점 |
|---|---|---|---|
| V1 | 홀드 비용 1 → 0 | ✅ CAUGHT | D13 §2.2 대조 · TC-C5 |
| V2 | 부스트 상한 4 → 2 | ✅ CAUGHT (대조 블록 신설 후) | D13 §2.2 대조 |
| V3 | 추월 임계 기저 35 → 30 | ✅ CAUGHT | TC-C6 (필러 48.5·로렌츠 127.5) |
| V4 | 안정성 계수 4.5 → 4.0 | ✅ CAUGHT | TC-C6 |
| V5 | 배틀 존 게이지 ×1.5 → ×1.0 | ✅ CAUGHT | TC-C7 |
| V6 | 해저드 섀시 소모 ×1.15 → ×1.0 | ✅ CAUGHT | TC-C9 |
| L1 | 홀드 턴당 1회 한도 제거 | ✅ CAUGHT | TC-C4 |
| L2 | 턴 단계 T3 누락 | ✅ CAUGHT | TC-C2 (phase_log 대조) |
| L3 | RNG 스트림 **내부 상태** 복원 누락 | ⚠ 최초 MISSED → 교정 후 CAUGHT | TC-C12 |
| L4 | 트러블 무효화 미반영 | ✅ CAUGHT | TC-C4 |
| L5 | 속성 게이지 계수 미적용 | ✅ CAUGHT | TC-C7 |
| — | 구조 데이터 8종(섹터 수·FK·타입·참조·슬롯) | ✅ 전건 CAUGHT | IMPL-023 |

**L3에서 배운 것 (교정 반영):** TC-C12가 GP 개시 직후에 스냅샷을 떴더니 reel 스트림이 **시드 초기 상태**에 있어, `rng.state` 복원을 완전히 삭제해도 재현이 성립했다(시드만으로 같은 값). 릴을 3턴 미리 소비한 뒤 스냅샷을 뜨도록 교정하자 전 시도에서 검출된다. **"재현됐다"는 관찰은 스트림이 중간 상태에 있을 때만 증거 능력이 있다.**

**하네스 자체의 함정 (기록):** 파이썬 패치형 돌연변이가 패턴 불일치로 **조용히 아무것도 바꾸지 않은 채 "미검출"로 보고**된 사례가 1건 있었다(L4 1차). 돌연변이는 주입 성공을 `assert`로 확인해야 하며, 확인 없는 "미검출"은 증거가 아니다.

---

## 미해결 — 총괄 경유 필요

### ~~U-1. D12 §1.1 버전 핀 개정~~ → **종결 (2026-08-10)**

**D12 v1.1 발행 완료** — §1.1 버전 핀 **4.7 계열**(결정 #11) · 재검증 조항 1차 이행 명문 · 라이선스 무변동 확인 · 구조/규칙 무변경(DoD 재심사 불요). `D12_기술설계_v1_0.md` 제거로 "문서당 최신 1개 버전"(마스터플랜 §0.7-1) 준수, `docs/design/` 21파일 유지. 구현 리포지토리 문면도 정본 정합으로 정리 완료.

**잔여 확인 (MS-1 착수 시 — 차단성 없음):** D12 §1.2 타겟 스펙 하한(OpenGL 3.3 / ES 3.0 / Android 8.0 API 26 / iOS 14+)의 4.7 유지 여부 → 상향 시 조정 창구 = **D14 디바이스 매트릭스**(D12 §1.2·마스터플랜 H-6). §7.3 클라우드·§11.1 AdMob SDK의 4.7 호환도 동일 트랙. **마스터플랜 §1 상태표 D12 행 판번 동기(§0.8.7-⑦)는 총괄 소관.**

### ~~U-7. MS-2 주력 레인 발신 대기 통합~~ → **종결 (2026-08-15 — 총괄 처리 완료)**

발신문 전 항목(A~E)이 총괄(사용자 지정 세션)에서 §0.8.2 5단계로 처리됐다. **회신 = `docs/handoff/MS2_총괄회신문.md` · 발행 = D15 v1.1 / D13 v1.4(+별첨A) / 마스터플랜 v2.30.**
요지: A = G-1·G-2·G-5 → **MS-5 완료 조건** 재배치(사용자 배치 확정) / B = 햅틱 전사 집행·로그 슬롯 행 보완, **3건 반려(기전사 실재 — IMPL-072의 5종 중 오입력 윈도·폰트 원도·씬 패널은 별첨A §10에 이미 있었다)** / C = 12건 일괄 승인·D13 편입(기능 컬러 1건 = 에셋 트랙 존치) / D = D13분 3건 집행·D08분 P-2 ⑱ 이월 / E = 카 넘버 **D03 결정 #13-③ 기확정**(데이터 전사 = 원격 레인 몫)·폰트 검증 ④ = P-8 트랙 신설·VN 실문안 = 마일스톤 미결속 확인.
**IMPL-035·072 (보고 대기) → 본 건으로 종결.** MS-2는 게이트 재배치로 완료 조건 충족 상태 — 완료 선언은 사용자 확인 사안.

### ~~U-2. 문면 교정 7건~~ → **처리 완료 (2026-08-15 — D13분 집행·잔여는 P-2 승격)**

| # | 지점 | 처리 |
|---|---|---|
| 1 | D05 §2.3 T6 행 | **P-2 ⑳ 승격** (마스터플랜 v2.30 — D05 차기 개정 시 동시 집행) |
| 2 | 마스터플랜 §1 D13 행 '65항' | **집행 완료** (v2.30 자기 교정 — '상각 210초' 잔존도 동시 교정) |
| 3 | D12 §16 결정 #5 'V1~V7' | **P-2 ⑲ 승격** (D12 차기 개정 시) |
| 4 | D08 §5.3 '시즌 내' 곡선 | **P-2 ⑱ 승격** (D08 차기 개정 시 — P-7 착수가 유력 트리거) |
| 5 | D13 §4.1 표 (15턴 11.6분·파생 행) | **집행 완료** (D13 v1.4 — A1 상각 225초 반영·결정 #14 ①) |
| 6 | D13 별첨A §2.4 실스프레드 | **집행 완료** (v1.4 — 결정 #14 ②) |
| 7 | D13 별첨A §1.3 부속성 보간 | **집행 완료** (v1.4 — 결정 #14 ③) |

### ~~U-3. 미결정 — 사용자 판단 대기~~ → **종결 (2026-08-11)**

- **`godot/assets/`:** 사용자 결정 = **유지 + D10 §8.2 카테고리별 하위 디렉토리**로 디렉토리 계약 편입 (IMPL-021).

### ~~U-6. §2.4 '네임드 (일반)' 인쇄 범위 불일치~~ → **종결 (2026-08-12 사용자 판정: 실제 스프레드로 정정 — IMPL-068)**

§2.4 저항 임계 표는 "네임드 (일반) | 추월 **48.5 ~ 57.5** | 방어 **45 ~ 55**"로 인쇄한다. 그런데 §6.2의 스탯값을 같은 절의 산식에 넣으면 네임드 8인 중 4인이 범위를 벗어난다:

| 산식 | 인쇄 범위 | 실제 스프레드 | 범위 밖 |
|---|---|---|---|
| 추월 = 35 + 안정성×4.5 | 48.5~57.5 | **44.0~57.5** | 디아스·볼코바·주드(안정성 2.0 → 44.0) · 비앙카 압박 시(2.5 → 46.25) |
| 방어 = 30 + 공격성×5 | 45~55 | **40.0~55.0** | 비앙카(공격성 2.0 → 40.0) |

- **선재 불일치다** — 비앙카 압박 훅(IMPL-045)이 만든 것이 아니다. 훅의 46.25는 이미 벌어져 있던 간극에 합류할 뿐이다.
- **영향은 낮다:** 이 범위는 서술적 요약이고 코드가 소비하지 않으며 테스트도 단언하지 않는다 — **틀린 값이 테스트로 굳은 곳은 없다.** 다만 §2.4는 V-1 부등식 설계의 근거 표이고 "필러 48.5 < 개입 기대 66" 같은 판정이 그 위에 서므로, 실제 최저 임계가 44.0이라는 사실은 밸런스 판단에 쓰이는 정보다.
- **처리:** 임의 해석 금지(불변규칙 9) — 보고만 한다. 선택지 ①인쇄 범위를 실제 스프레드로 정정 ②범위가 맞고 §6.2의 안정성·공격성 2.0을 상향 ③범위는 "대표 구간"이라는 서술로 명시하고 산식 산출값을 정본으로 확정.

### ~~U-5. GP 길이 — 성분 산술이 목표 하한을 밑돈다~~ → **종결 (2026-08-12 사용자 판정: 결산·이벤트 225초 — IMPL-067)**

D13 §4.1이 인쇄한 12턴 서킷 총계 **10.0분**은 같은 행의 성분 산술과 맞지 않는다:

```
12턴×21초 + 듀얼 2.5×45초 + 완급 비트 12×1초 + 결산·이벤트 210초
  = 252 + 112.5 + 12 + 210 = 586.5초 = 9.775분
```

- **반올림이 아니다:** 9.775를 소수 1자리로 반올림하면 9.8이다. 차이 0.225분(13.5초)은 어떤 성분 조합으로도 재구성되지 않는다.
- **파생 문제(중요):** GP 목표 길이는 **10~15분**(D05 §2.2 확정 · D13 §4.1 표의 목표 열 동일)이다. 성분 산술 9.775분은 **하한 미달**이며, D13 §4.1 표의 "통과" 판정은 인쇄값 10.0에 의존한다. 즉 "성분값이 정본"이라는 독법을 택하면 12턴 서킷이 목표 구간을 벗어난다.
- **15턴은 정합:** 15턴 총계 11.3분은 듀얼을 턴 비례(3.125회)로 파생하면 11.34분으로 정확히 맞는다. 12턴만 어긋난다.
- **처리:** 임의 해석 금지(불변규칙 9). 구현·검사는 **성분값**을 따르고(성분 4종을 정확히 단언), 총계는 파생 라벨로 구분했다. 허용 오차로 흡수하던 마법수(0.25)는 제거했다 — 그 값이 하필 불일치 폭 0.225를 겨우 덮는 크기여서 문제를 테스트 뒤에 숨기고 있었다(독립 감사 지적).
- **판정 필요:** ①인쇄 총계 10.0이 맞고 성분 중 하나가 오기인가 ②성분이 맞고 12턴 서킷이 목표 하한을 밑도는 것을 수용하는가 ③완급 비트·결산 성분 조정으로 하한을 맞추는가. ②·③은 D13 조정 창구(총괄 경유) 사안이다.

### ~~U-4. 슬롯 진행 보정의 '투어 슬롯' 정의 모호~~ → **종결 (2026-08-12 사용자 판정: 투어 내 GP 슬롯 — IMPL-066)**

D13 별첨A §6.2는 슬롯 진행 보정을 **"투어 슬롯 1~4에 +0.00 / +0.05 / +0.10 / +0.15"**로 4항만 확정한다. 그런데 같은 '투어 슬롯'이라는 말이 정본 안에서 두 가지로 쓰인다:

| 독법 | 근거 | 문제 |
|---|---|---|
| ① **시즌 내 투어 인덱스** (투어 1~5) | D08 §2.3 "투어 슬롯 결속 = 투어 1·투어 5에만 허용 / 중간 슬롯 2~4 금지" · D12 §5.4·V5가 이를 `tour_slot ∈ {1,5}`로 기계화 · D08 §5.3 "슬롯 진행 보정이 **시즌 내** 미세 상승 곡선을 담당" | 시즌은 5투어인데 D13에 값이 4개뿐 — **투어 5의 값이 정본에 없다** (불변규칙 2 = 중단·보고) |
| ② **투어 내 GP 슬롯** (제1~4전) | D08 §4.3 "투어 내 4서킷 슬롯" · 값이 정확히 4개 | 매 투어마다 곡선이 리셋되어 "시즌 내 상승 곡선"(D08 §5.3)과 어긋난다 |

- **MS-2 직접 영향:** 무대 1 = 투어 1. ①이면 보정 +0.00(무영향), ②면 MN-1~4가 0.00/0.05/0.10/0.15를 받아 **AI 기저 강도가 서킷마다 달라진다.**
- **처리:** 임의 해석 금지(불변규칙 9). AI 5층 합성(D12 §5.12)에서 **슬롯 진행 보정 층만 미배선(0)으로 두고** 나머지 4층(팀 프로파일 → 개인 시드 → 무대 보정 → 그리드 레벨)을 구현했다. 판정 후 해당 층만 결선하면 된다.

---

## MS-1 착수 시 실측 확인 사항 → **3항 전건 이행 완료 (2026-08-11)**

| # | 항목 | 근거 | 결과 |
|---|---|---|---|
| 1 | **자격증명 분리 실측** — preset 1개를 만들어 서명 정보가 `export_presets.cfg` 본체에 평문으로 들어가는지 확인. 분리가 안 되면 **C안(템플릿 커밋 + 빌드 시 주입)으로 후퇴** | IMPL-006 | ✅ **A안 유지** — IMPL-017 (종단 확인만 주력 머신 이월) |
| 2 | **D12 §1.2 타겟 스펙 하한**(OpenGL 3.3 / ES 3.0 / Android 8.0 API 26 / iOS 14+)의 4.7 유지 여부 — 상향 시 조정 창구 = D14 디바이스 매트릭스, **중단·보고** | D12 v1.1 §1.1 재검증 조항 잔여분 | ✅ **4항 전건 유지 — 중단 사유 없음** — IMPL-018 |
| 3 | **플러그인 생태 4.7 호환** — D12 §7.3 클라우드 · §11.1 AdMob SDK | 동상 | ⚠ **2건 확인 / 1건(iCloud) 미확인** — IMPL-018 |

### IMPL-018 — D12 §1.1 재검증 조항 잔여분 이행 결과 (2026-08-11)

**결론: D12 §1.2 타겟 스펙 하한 4항 전건 4.7에서 유지. 상향 없음 → 중단·보고 조건 불성립, D14 디바이스 매트릭스 조정 불요.**

| 하한 | 4.7 실측 | 근거 |
|---|---|---|
| 데스크탑 OpenGL 3.3 | 유지 | 공식 시스템 요구사항 문서 4.4~4.7 수치 불변 |
| 모바일 OpenGL ES 3.0 | 유지 | 동상 |
| Android 8.0 (API 26) | 유지 (여유 2단) | 엔진 `minSdk 24`(4.7 브랜치 `platform/android/java/app/config.gradle`) · AdMob 플러그인 `minSdk 24`(`platforms/android/build.gradle`) · GMA Next-Gen SDK 요구 `minSdk 24` — 전건 ≤ 26 |
| **iOS 14+** | **유지 (여유 0)** | 엔진 iOS 배포 타겟 = **14.0** (`platform/ios/detect.py` 4.7 브랜치 `-miphoneos-version-min=14.0` / `-mios-simulator-version-min=14.0` — **4.6과 동일, 상향 없음**) · AdMob 플러그인 SPM `platforms: [.iOS(.v14)]` · 플러그인이 고정한 GMA iOS SDK 13.6.0 자체 요구는 `.iOS(.v13)` |

**오독 방지 (중요):** 공식 문서 시스템 요구사항 표의 iOS 행이 4.5/4.6 **14.1 → 4.7 15.0**으로 오른 것은 **Forward+/Mobile(Vulkan) 경로 전용 수치**다. 본 프로젝트는 **Compatibility 단일 고정**(D12 §1.1)이라 해당 행의 구속을 받지 않으며, 엔진의 실제 빌드 하한은 위 표대로 14.0으로 불변이다. 문서 표에는 Compatibility 렌더러 전용 iOS 수치가 4.4 이래 애초에 존재하지 않으므로, **판단 근거는 문서 표가 아니라 엔진 소스의 배포 타겟**으로 삼는다.

**여유 0 경보 (iOS):** 엔진 14.0 · 광고 플러그인 14 · §1.2 하한 14 — 세 값이 정확히 일치해 완충이 없다. **엔진 또는 AdMob 플러그인 어느 한쪽의 차기 상향이 즉시 §1.2 상향으로 직결**된다(§11.1 "SDK 최소 OS 요구는 §1.2와 착수 시점 동기 — 상향 시 총괄 인지 보고"). D14 MP-L 행(iOS 14 · A11급 · iPhone 8/SE 2세대)이 직접 영향권이므로 **MS-2 이후 매 착수 시점 재확인 대상**으로 승계한다.

**플러그인 생태 (D12 §7.3 · §11.1):**

| 대상 | 4.7 호환 | 비고 |
|---|---|---|
| AdMob (poingstudios/godot-admob-plugin) | ✅ 확인 | 요구 Godot 4.2.0+ · Asset Store 4.0~4.8 범위. **단 v5.0.0은 monorepo·iOS SPM 전환·GMA Next-Gen SDK 채택의 구조 대개편 메이저 버전** — 실착수(MS-5 광고) 시 마이그레이션 검토 필요 |
| Play Games Services Saved Games (Android 클라우드) | ✅ 확인 | 최소 Godot 4.3 · 4.3~4.8 커버 · 유지보수 활발 |
| iCloud (공식 `godotengine/godot-ios-plugins`) | ⚠ **미확인** | "Fix build with Godot 4.4"(2025-01) · "…4.5 and 4.6"(2026-01) 커밋은 확인되나 **4.7 대응 커밋 미확인**. 매 마이너 대응 패턴상 반영 가능성 높으나 현시점 단정 불가 — **iOS 클라우드 세이브 실착수 시점(MS-5) 재확인 필수** |

**차단성 판정:** 전건 MS-1 산출물에 영향 없음(모바일 레이어·광고·클라우드 = MS-1 범위 외, 어댑터 인터페이스 골격만 존재). iCloud 미확인 건은 **MS-5 착수 게이트로 이월**한다.
