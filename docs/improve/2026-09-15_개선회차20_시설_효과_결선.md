# 개선 회차 20 — 시설 3종 효과 결선 (2026-09-15)

> 세션 문서 (개선 모드). 설계 정본 D01~D16 은 참고 자료이며 사용자 요청이 우선한다. 단일 세션 · 주력 GUI 머신(Windows · GDAI).
> **상태: 구현 완료 · 게이트 녹색 · 사용자 확인 대기.** §4 검사·게이트 · §5 상태.

## 0. 배경 — 매뉴얼 9절 3항

시설 4종 중 `sponsor_slot_plus`(G4)만 코드에서 읽히고 `archive_deep_tab`·`recall_playback`·`deck_preset_slot` 세 문자열은 **어디에서도 소비되지 않았다.** 45·55·65 DP 를 내도 아무것도 열리지 않았다.

## 1. 정본 근거 (조사 결과)

| 시설 | 정본 | 확정 규격 |
|---|---|---|
| G1 텔레메트리 아카이브 | D07 §2.2·§6.1·§6.2 · D09 §4.5 / 별첨A §A-15 | 기록실 **라이벌 파일 탭의 하위 탭**으로 심화 통계 — "형식 B의 주요 카운터 수치 자체(듀얼 전적 등)는 G1 개방 시 열람 가능 — **수치는 보이되 임계는 보이지 않는다**" |
| G2 크루 라운지 | D07 §2.2·§6.3 · D02 §8 · D09 §4.5 / 별첨A §A-15 | **이벤트 체인 연속 재생(몰아보기)** · "목록 상단 부가 모드 버튼" · 기본 열람은 무상·상시라 자원 게이트 뒤에 두지 않는다 |
| G3 세컨드 워크벤치 | D07 §2.2·§4.1 · D09 §4.4 / 별첨A §A-14 | 덱 구성 **2세트** 저장·즉시 전환 · "상단: 프리셋 A/B 전환(G3 개방 시)" · 화면 비전환(인라인) |

**정본 침묵 1건** — G2 의 "크루 코멘터리"는 문안·데이터 스키마가 D04 에도 D12 에도 없다. 사용자 결정으로 **연속 재생까지만** 만든다(문면 신설 없음 · "등"이라는 열린 문면이라 조항 이행은 성립).

## 2. 사용자 결정 (객관식 · 2026-09-15)

| Q | **결정** |
|---|---|
| G2 범위 | **연속 재생만** — 코멘터리 문안은 신설하지 않는다 |
| 작업 단위 | **시설 3종을 한 회차로** |

## 3. 구현

| 파일 | 변경 |
|---|---|
| `godot/core/state/outgame_state.gd` | `facility_effect_open(effect)` — 표의 `effect` 열이 열쇠이고 코드는 시설 id 목록을 갖지 않는다 · 덱 프리셋 2세트(`deck_presets`·`active_deck_preset`·`deck_preset_count()`·`switch_deck_preset()`) 신설·직렬화. **`deck` 의 의미는 바꾸지 않았다** — 살아 있는 덱은 그대로이고 프리셋은 쉬는 칸이다 |
| `godot/ui/flow/run_session.gd` | `archive_chain_payload()` — 아카이브 목록을 막 VN 사슬과 **같은 형태**(`next`/`next_payload`)로 엮는다. 뒤에서부터 감고, 되찾지 못한 항목은 사슬에서 빠진다 |
| `godot/ui/hub/records_screen.gd` | G1 하위 탭(기본/심화 통계) — 심화 행 = 관계 카운터 **수치** + 선착 기록. 임계·잔여는 싣지 않는다. 선착은 표의 `beat_rival` 마일스톤으로 본다(계수기 신설 없음) · G2 연속 재생 버튼을 아카이브 목록 **상단**에 |
| `godot/ui/hub/strategy_screen.gd` | G3 프리셋 A/B 행을 표제 아래에 · 전환은 그 자리에서 재구성(`_rebuild()`) · 활성 칸은 **색으로** 표시(소등하면 잠긴 것으로 읽힌다) |
| `godot/data/strings/strings.csv` | 문면 8행 |
| `godot/tests/test_tc_o.gd` | 축 `_facility_effects_window` · G1 화이트리스트 3행 |
| `godot/tests/test_ui_screens.gd` | `PAYLOAD_MAKERS` +1 (`archive_chain_payload`) |

## 4. 검사·게이트

| 단계 | 결과 |
|---|---|
| 컴파일 4파일 | exit 0 |
| TC-O (공유 트리) | **464** PASS (447 → +17) |
| UISCR (공유 트리) | 1078 PASS |
| 실렌더 (프로브 삭제 완료) | 기록실 기본/심화 하위 탭 · 아카이브 연속 재생 버튼 · 전략실 프리셋 A/B 전환(활성 칸 청록) |
| 워크트리 검증기 | **VALIDATORS_PASS warnings=0** |
| 워크트리 테스트 | 1차 주행 19/20 (TC-C 2389 · TC-O 464 · G4W **3201** · GLYPH **3888** · UISCR 1078 전부 PASS — 신설 문면 8행이 라벨 폭·글리프 검사를 지났다) · AUDIO_WIRING 13건 붉음(콜드 캐시 첫 주행 · 7회째) → **단독 재실행 PASS 107** → 20/20 |

## 5. 상태

- 구현 완료 · **사용자 확인 대기**.
- **다음 = 매뉴얼 9절 4항(그리드 레벨).** 정본은 D08 §5.3 "라이벌 전체 파라미터 1단 상승"이고 D13 별첨A §6.2 가 값을 채운 것은 **페이스 +0.4 · 듀얼 임계 +3 두 축뿐**이다. 나머지 3축(공격성·안정성·러시)과 시작 보정은 값이 없어 불변규칙 2 에 걸린다 — 착수 전 확인이 필요하다. 부수로 D08 §5.3 의 "에필로그 이후 레벨 고정"이 코드에 없다(`season_state.gd` 챔피언 시 무조건 +1).
- 실기에서 볼 항목(참고): G1 구매 후 기록실 라이벌 파일에 하위 탭이 서는가 · G2 구매 후 아카이브 상단 연속 재생이 사슬로 이어지는가 · G3 구매 후 전략실에서 A/B 를 오가며 덱이 갈리는가.
