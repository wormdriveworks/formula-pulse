# MS-3 Steam EA — 구현 착수 프롬프트 (양 레인 공용)

> 이 파일 전체를 AI 에이전트(Claude Code 등)의 첫 지시로 사용한다.
> **MS-2 버티컬 슬라이스 마감(2026-08-15) 직후 발행.** 어느 머신에서 열든 같은 파일을 쓴다 —
> CLAUDE.md STEP 0 환경 감지 결과에 따라 §3의 자기 레인 몫만 집는다.

---

MS-3 **Steam EA 출시**의 구현 리포 몫을 진행합니다.

## 목표 (D15 §1.1 — MS-3 완료 조건, 원문 그대로)

> **콘텐츠 완결(5무대 20서킷·4막·에필로그) + TL-5 1차(모델 유효성 ±15%) 통과 +
> G-4 실행 1차(EA 언어분: 한·영·일) + G-6·G-7 통과 + 스토어 준비물(D15 §2.2~§2.5)·
> 상표 1군 점검(§5) 완료**

핵심 통찰: **크리티컬 패스는 콘텐츠 완결이다.** 무대 2~5는 데이터 유입만으로 결선되는 구조가
이미 실측 확인돼 있고(원격 회신 §1-7 — 캘린더·화면·연출 채널 코드 무변경), 게이트 4종(TL-5·G-4·
G-6·G-7)은 콘텐츠·번역이 서야 실행 가능하다. 따라서 **원격 레인의 무대 2~5부터 착수한다.**

## 0. 시작 절차 (레인 공통 — 순서 고정)

1. `git pull` (두 머신이 같은 브랜치를 오간다 — 반드시 최신화 후 시작)
2. **git lfs 확인** (원격 필수 — 에셋이 수백 바이트 포인터면 `git lfs pull`부터. IMPL-091: 미설치 시 SEAL-E 행)
3. 훅 확인: `git config core.hooksPath tools/hooks` (project.godot 오염 차단 — IMPL-070·076·091)
4. 캐시 재생성: `godot --headless --import` (원격) / `_console.exe --headless --import` (주력)
5. **게이트 재확인 — 기대치와 다르면 작업 시작 전에 원인부터:**
   `COMPILE_GATE_PASS scripts=78` · `VALIDATORS_PASS warnings=0` · `TESTS_PASS suites=10`
   - **검사 수 하한의 정본 = `tools/validators/run_tests.sh`** (여기 한 곳. 문서에 옮겨 적은 값이
     어긋나면 스크립트가 맞다) — TC-C 942 · TC-P 136 · TC-O 257 · EVENTS 6000 · SEASON 450 ·
     NARRATIVE 68 · DATA 55 · SEAL 84.
   - **현행 실측 (2026-08-16 주력 재현):** TC-C 942 · TC-P 136 · TC-O 257 · **EVENTS 7019** ·
     **SEASON 471** · NARRATIVE 68 · DATA 55 · **SEAL 98**(rounds=14). 하한보다 위인 항목은
     여유분이며, 검사를 의도적으로 줄일 때만 하한을 함께 내린다.
   *(구 판에서 EVENTS 7019·SEASON 471을 '하한'으로 표기했으나 이는 실측값이었다 — 라벨 교정)*
6. (주력만) 커밋 전 project.godot의 GDAI autoload 재등록 제거 — 훅이 차단하지만 지우는 건 사람 몫

## 1. 인계 상태 — MS-2 마감 시점에 서 있는 것

- **코어·화면 전량 성립:** 엔진(T1~T6·정산 8단계)·시즌·아웃게임·이벤트·서사·연출 채널·세이브 +
  **D09 화면 23종**(25종 − SYS-04·TUT-01, IMPL-077). TL-2 기능 테스트 1회전 실측 완료
  (`docs/qa/TL2_실행기록_2026-08-15.md` — 잔여 6건의 트랙 배정이 §3의 근거다).
- **결정 로그 = `docs/decisions/impl_log.md` IMPL-001~108.** 새 세션은 최소한 IMPL-090~108을
  읽고 시작하라 — 섀시 이월·카 넘버·V4 확대·직렬화·TL-2 생성기·고스트 게이지·TL-2 1회전
  (090~101) + **총괄 집행분·CI 교정 (103·104·108)** + 에셋 배치 대장·아이콘 오진 철회 (105·106).
- **⚠ CI가 3일간 죽어 있었다 — 반드시 알고 시작하라 (IMPL-104·108).** `actions/checkout@v4`가
  `lfs: true` 없이 돌아 2026-08-13(첫 LFS 에셋 유입)부터 08-15까지 **22런 연속 6시간 타임아웃**
  (원격이 IMPL-091에서 실측한 그 SEAL-E 행이 CI에서 재현된 것). 교정 완료 — `lfs: true` +
  **LFS 포인터 잔존 가드**(포인터가 남으면 행 대신 수초 내 `LFS_GUARD_FAIL`). **관측점: 정상 런은
  35초다.** 런 시간이 분 단위로 뛰면 그 자체가 암전 신호이므로 `gh run list`로 결론 분포를 본다.
- **검증 절차 = `docs/decisions/verification_protocol.md`** — 새 검사·게이트를 붙이기 전 필독.
- **레인 규율 = `docs/handoff/MS2_주력완료_원격인계.md` §2** (화면 계약·세션 주입 순서·V4 싱크
  규율·도상 경로 규약·씬 텍스트 금지 — 원격이 화면 층을 밟기 전에 반드시 읽는다).
- 아트: 전량 플레이스홀더. **제작 사양서 v1.1 확정(IMPL-099) — 발주 가능.** 실물 유입 시
  동일 경로·파일명 교체 규약.
- 마일스톤 문서 상태: **총괄 집행 종결 (2026-08-15 — `docs/handoff/MS2_마감_총괄회신.md`).**
  D15 §1.1 실일자 기입 완료 — **MS-1 = 2026-08-11 · MS-2 = 2026-08-15**(게이트 총괄 재현 실측 경유,
  판번 무증가). 마스터플랜 = **v2.31**. P-2 ㉒ 신설(D09 별첨A §A-14 예시 문면 — 비차단).
- **총괄 미처리 1건 (원격 무관·주력 몫):** `docs/handoff/UI폰트계열_총괄발신.md` — HUB 공통 바
  폰트 계열 배정 판정 대기. 발신문 자체가 차단성 없음을 명시(표현 층 국소).

## 2. 완료 조건 분해 — 트랙 배정

| # | 트랙 | 소관 | 정본 |
|---|---|---|---|
| T1 | 무대 2~5 콘텐츠 (서킷 16종·라이벌 배치·벽 라이벌·캘린더 풀) | **원격** | D08 + 별첨A |
| T2 | 소모품 사용 경로 (엔진 API·P2/P3 지속 효과·R5 반입) | **원격** → 주력 협업 | D06 §3.5 |
| T3 | HUB-08 오버홀 후보 추첨 (시즌 결산 층 — 추첨 스트림) | **원격** | D07 §7 · D12 §6 |
| T4 | 업적 데이터 + SYS-04 화면 / TUT-01 튜토리얼 | 원격(데이터) + 주력(씬) | D09 §8.1 · D04 |
| T5 | RUN-01 E03 소모품·E04 덱 인라인 카드 (T2 결선 후) | **주력** | 별첨A §A-9 |
| T6 | 아트 실물 교체·사운드 결선(SFX 68식·햅틱)·L3 컷인·로그 화자 배지 | **주력** (실물 유입 후) | D10 · D11 · IMPL-035·093 |
| T7 | 4막·에필로그·VN 실문안 | **사용자 발주** (D04 트랙) → 원격 편입 | D04 |
| T8 | 영어·일본어 스트링 → G-4 실행 1차 | **사용자/외부** → 게이트 실행 | D15 §4 · D14 §5.4 |
| T9 | TL-5 1차(모델 유효성 ±15%) + D14 §8.3 미모델링 4항 수동 실기 | 원격(러너) + 주력(실기) | D14 §8 |
| T10 | G-6(봉인 실기 3방법)·G-7(Steam 제출물) — **MS-3 직전** | 주력 | D14 §5.6·§5.7 |
| T11 | 스토어 준비물·상표 1군 점검 | **사용자/총괄** | D15 §2.2~§2.5·§5 |

**착수 순서:** 원격 = T1 → T2 → T3 (T1이 최장 크리티컬 패스 — TL-5·밸런스 실측의 전제).
주력 = 아트 유입 대기 중 T2 협업(RACE-01 사용 UI)과 T5, 이후 T6. T7·T8·T11은 외부 입력 —
유입 즉시 해당 레인이 편입한다.

## 3. 레인별 첫 작업

### 원격 헤드리스 레인 (T1 착수)
1. D08 별첨A에서 무대 2~5 서킷 16종을 전사 — `circuit_*.json` 구조는 무대 1(mn1~mn4) 전례.
   수치의 유일 창구 = D13(+별첨A). **없는 값 발견 = 중단·보고** (불변규칙 2).
2. `season_calendar.json` shuffle_pool 충전 → NAR-02 캘린더 5행·시즌 2+ 셔플이 자동 결선
   (코드 무변경 — 기확인). 무대별 `wall_rival` 유입 → IMPL-089 벽 라이벌 격파 연출 자동 결선.
3. 검증: 신규 데이터 전량에 V1 스키마·픽스처 판별 검사 확장 (검증 프로토콜 §4 — 돌연변이 실측 포함).
4. 완료 시 회신 문서 관례 유지 (`docs/handoff/` — 무엇이 결선됐고 주력에 무엇이 넘어가는지).

### 주력 GUI 머신 (대기 없이 할 수 있는 것)
1. T2 협업 준비: RACE-01 E13 소모품 슬롯의 사용 UI 규격 확인 (별첨A §A-4 E13 — "T1에만 활성").
2. 아트 발주 상태 추적 — 유입분부터 동일 경로 교체·캡처 눈 대조 (TL-2 실행기록 전례).
3. G-6·G-7은 MS-3 직전 실행 — 미리 당기지 않는다 (D15 §1.2 결속).

## 4. 규칙 리마인드 (전문 = CLAUDE.md — 위반 발견 시 중단·보고)

- 수치는 D13 창구 전속 — 코드 임의 기입 금지. 없으면 중단·보고.
- 봉인(불변규칙 5): 릴 정지 연출 전 결과 상관 신호 전 경로 비노출 — SEAL-E가 지킨다.
- 씬(.tscn) 소유권 = 주력. 원격은 로직·데이터·테스트.
- V7은 경고 전용 — 차단형으로 만들지 않는다.
- 설계 문서 간 충돌 발견 = 임의 해석 금지, 중단·보고 (전례: IMPL-101 DF-04 — D07 우선 판단도
  보고와 함께였다).
- 결정·[가안]은 전부 impl_log에 — 날짜·ID·근거·영향 문서.

## 5. 완료 조건 (이 프롬프트의 범위)

MS-3 완료 조건 전체가 아니라 **구현 리포 몫의 성립**이 이 프롬프트의 범위다: T1~T6 결선 +
T7~T8 유입분 편입 + T9~T10 실행 지원. 스토어·상표(T11)는 리포 밖 트랙이다.
각 트랙 완료 시 게이트 전건(§0-5 기대치 이상) + impl_log 등재 + 커밋·푸시가 완료의 정의다.
